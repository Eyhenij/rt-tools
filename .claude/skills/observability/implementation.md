# observability — что здесь своё

Имена и привязки этого дерева при правиле `SKILL.md` рядом.

Правило говорит приёмом и называет пути, общие для деревьев мастерской, — их переписывать
здесь не надо. Сюда идёт только то, чего пакет знать не может: как названы вещи именно в этом
репозитории, и в каком его файле каждая статья правила исполняется.

Пока в файле стоит `<!-- заполняет проект -->`, правило считается неразвёрнутым: `agent-kit
sync --check` отказывает, а агент читает указание, у которого здесь нет адресата.

## Как это называется здесь

Только то, что расходится с правилом: ключ задач, адрес борды, префикс компонентов, имя
владеющей сущности, валюта хранения, набор областей коммита. Строку, которая просто повторяет
правило, сюда не переносят — она устареет отдельно от него.

| В правиле         | Здесь                                       |
| ----------------- | ------------------------------------------- |
| <понятие правила> | <имя в этом коде> <!-- заполняет проект --> |

## Где это лежит

| Что        | Где                                |
| ---------- | ---------------------------------- |
| <механизм> | `<путь>` <!-- заполняет проект --> |

## Где исполняются статьи

Первая колонка — статья дословно, как она написана в разделе «Как закон применяется здесь»
(жирная часть пункта). Статья без строки и строка без статьи — расхождение: правило обещает то,
чего в дереве нет, либо в дереве стоит то, о чём правило молчит.

| Статья                                                                                   | Где исполняется                             |
| ---------------------------------------------------------------------------------------- | ------------------------------------------- |
| Номер обращения заводится один раз на запрос и стоит в каждой строке лога о нём.         | `<путь>:<символ>` <!-- заполняет проект --> |
| Номер обращения уходит вызывающему заголовком ответа и в подробностях отказа.            | `<путь>:<символ>` <!-- заполняет проект --> |
| Номер, пришедший снаружи, чистится и укорачивается, а пустой заводится заново.           | `<путь>:<символ>` <!-- заполняет проект --> |
| Наружу уходит код и общий текст ошибки, подробности остаются в логах.                    | `<путь>:<символ>` <!-- заполняет проект --> |
| Отказ по вводу и правам пишется отдельно от поломки и без стека.                         | `<путь>:<символ>` <!-- заполняет проект --> |
| Причина отказа разбирается в одном месте.                                                | `<путь>:<символ>` <!-- заполняет проект --> |
| Поля строки лога вычищаются всегда, а не по решению того, кто пишет.                     | `<путь>:<символ>` <!-- заполняет проект --> |
| Приложение при старте пишет, с чем поднялось.                                            | `<путь>:<символ>` <!-- заполняет проект --> |
| В сводке старта стоят имена возможностей и их состояние, но не значения переменных.      | `<путь>:<символ>` <!-- заполняет проект --> |
| Возможность, включаемая парой ключей, знает третье состояние.                            | `<путь>:<символ>` <!-- заполняет проект --> |
| Порог уровня решает, появится ли строка лога в выводе, но не в хранилище отказов.        | `<путь>:<символ>` <!-- заполняет проект --> |
| Отобранная строка лога сохраняется в хранилище отказом.                                  | `<путь>:<символ>` <!-- заполняет проект --> |
| Логгер отдаёт отказ приёмнику, а хранилища не видит.                                     | `<путь>:<символ>` <!-- заполняет проект --> |
| Строки лога клиента хранилища, домена отказов и домена оповещений в приёмник не идут.    | `<путь>:<символ>` <!-- заполняет проект --> |
| Обращение к чужой службе идёт с явным пределом ожидания.                                 | `<путь>:<символ>` <!-- заполняет проект --> |
| Отправка наружу заводит свою строку до обращения, а исход дописывается в неё после.      | `<путь>:<символ>` <!-- заполняет проект --> |
| Запись отказа не задерживает ответ и не роняет запрос.                                   | `<путь>:<символ>` <!-- заполняет проект --> |
| Контекст запроса снимается в момент вызова логгера, а не в момент записи.                | `<путь>:<символ>` <!-- заполняет проект --> |
| Записанное владелец читает своим разделом, закрытым отдельным правом.                    | `<путь>:<символ>` <!-- заполняет проект --> |
| Хранилище не растёт без предела: раз в сутки лишнее и старое удаляются.                  | `<путь>:<символ>` <!-- заполняет проект --> |
| Предел хранилища назван числом строк, а не байтами.                                      | `<путь>:<символ>` <!-- заполняет проект --> |
| О новом отказе владелец узнаёт сам — строкой журнала событий и письмом.                  | `<путь>:<символ>` <!-- заполняет проект --> |
| В письме нет ничего, что закрыто правом на экран отказов.                                | `<путь>:<символ>` <!-- заполняет проект --> |
| Частоту отказов владелец читает кривой над лентой: столбик — ведёрко выбранного периода. | `<путь>:<символ>` <!-- заполняет проект --> |
| Тревогу поднимает рост поломок, а не всех отказов.                                       | `<путь>:<символ>` <!-- заполняет проект --> |
| Всплеск считается по последнему закрытому ведёрку, текущее правилу не отдаётся.          | `<путь>:<символ>` <!-- заполняет проект --> |
| Частоту разбирает такт расписания, а не запрос экрана.                                   | `<путь>:<символ>` <!-- заполняет проект --> |

## Что ещё стоит знать при чтении кода

- <Что видно только изнутри этого дерева: почему приём выглядит здесь именно так.>

## Чем это проверяется

- <Команда, которая ловит нарушение, — и что именно она ловит.>
