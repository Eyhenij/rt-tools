# task-flow — что здесь своё

Имена и привязки этого дерева при правиле `SKILL.md` рядом.

Правило говорит приёмом и называет пути, общие для деревьев мастерской, — их переписывать
здесь не надо. Сюда идёт только то, чего пакет знать не может: как названы вещи именно в этом
репозитории, и в каком его файле каждая статья правила исполняется.

Пока в файле стоит `<!-- заполняет проект -->`, правило считается неразвёрнутым: `agent-kit
sync --check` отказывает, а агент читает указание, у которого здесь нет адресата.

## Как это называется здесь

Только то, что расходится с правилом: ключ задач, адрес борды, префикс компонентов, имя
владеющей сущности, валюта хранения, набор областей коммита. Строку, которая просто повторяет
правило, сюда не переносят — она устареет отдельно от него.

| В правиле         | Здесь                                       |
| ----------------- | ------------------------------------------- |
| <понятие правила> | <имя в этом коде> <!-- заполняет проект --> |

## Где это лежит

| Что        | Где                                |
| ---------- | ---------------------------------- |
| <механизм> | `<путь>` <!-- заполняет проект --> |

## Где исполняются статьи

Первая колонка — статья дословно, как она написана в разделе «Как закон применяется здесь»
(жирная часть пункта). Статья без строки и строка без статьи — расхождение: правило обещает то,
чего в дереве нет, либо в дереве стоит то, о чём правило молчит.

| Статья            | Где исполняется                             |
| ----------------- | ------------------------------------------- |
| Гард судит объявленный переход, а не наличие файлов. | `<путь>:<символ>` <!-- заполняет проект --> |
| Отказ по состоянию называет обязательное действие того состояния, которое объявлено. | `<путь>:<символ>` <!-- заполняет проект --> |
| Именем состояния считается только слово из перечня. | `<путь>:<символ>` <!-- заполняет проект --> |
| Два требования — два гарда, и снять одно можно, не теряя второго. | `<путь>:<символ>` <!-- заполняет проект --> |
| Влитая договорённость ветку не запирает. | `<путь>:<символ>` <!-- заполняет проект --> |
| Договорённость требуется по путям правки, а не по оценке задачи. | `<путь>:<символ>` <!-- заполняет проект --> |
| Что требует слова владельца, берётся списком, а не оценкой на месте. | `<путь>:<символ>` <!-- заполняет проект --> |
| Папка задачи заводится под любую работу, без исключений. | `<путь>:<символ>` <!-- заполняет проект --> |
| Папка задачи едет в ветку коммитом, а не живёт в одном рабочем дереве. | `<путь>:<символ>` <!-- заполняет проект --> |
| Папка задачи разбирается последним коммитом до открытия PR, а не после одобрения. | `<путь>:<символ>` <!-- заполняет проект --> |
| Открыв PR, исполнитель называет владельцу три вещи: номер, чего ждёт и что сделает следом. | `<путь>:<символ>` <!-- заполняет проект --> |
| Просьба о слиянии — отдельный ход, и раньше зелёного прогона её не бывает. | `<путь>:<символ>` <!-- заполняет проект --> |
| PR открывается черновиком, а не в конце работы. | `<путь>:<символ>` <!-- заполняет проект --> |
| Гард замысла — нижняя граница требования, а не его предел. | `<путь>:<символ>` <!-- заполняет проект --> |
| Взятая задача ходом не кончается. | `<путь>:<символ>` <!-- заполняет проект --> |
| Ожидание одной части этапа остановкой этапа не бывает. | `<путь>:<символ>` <!-- заполняет проект --> |
| Сделанное отмечается только в ходе работы. | `<путь>:<символ>` <!-- заполняет проект --> |
| Слово для нового понятия ищется в словаре дерева. | `<путь>:<символ>` <!-- заполняет проект --> |
| Папка задачи заводится черновиком и получает номер командой. | `<путь>:<символ>` <!-- заполняет проект --> |
| Брошенный разбор виден. | `<путь>:<символ>` <!-- заполняет проект --> |
| Эпик не закрывается по признаку, подтверждённому только чтением. | `<путь>:<символ>` <!-- заполняет проект --> |
| Задачи эпика заводятся все разом, тем же ходом, что и сам эпик. | `<путь>:<символ>` <!-- заполняет проект --> |
| Замысел эпика лежит там, где его найдут без сети и после мержа. | `<путь>:<символ>` <!-- заполняет проект --> |
| Сборка по образцу начинается с чтения самого образца, а не пересказа о нём. | `<путь>:<символ>` <!-- заполняет проект --> |
| Путь к образцу лежит вне дерева и приходит в заход хуком запуска сессии. | `<путь>:<символ>` <!-- заполняет проект --> |
| Закрытая работа разбирается правилами, и это шаг закрытия, а не отдельная просьба. | `<путь>:<символ>` <!-- заполняет проект --> |
| Разбор закрытой работы уходит в фон, а исполнитель берёт следующую задачу. | `<путь>:<символ>` <!-- заполняет проект --> |
| Находки разбора ждут владельца, а в пакет уезжает только сводка наблюдений. | `<путь>:<символ>` <!-- заполняет проект --> |
| Дешёвый шаг закрытия идёт раньше дорогого, а прогон — после вливания. | `<путь>:<символ>` <!-- заполняет проект --> |
| Договорённость вливается в спек домена одним из последних коммитов ветки, до открытия PR. | `<путь>:<символ>` <!-- заполняет проект --> |
| Папка закрытой задачи разбирается, а не переносится целиком. | `<путь>:<символ>` <!-- заполняет проект --> |
| Открытие PR и снятие черновика отбиваются, пока ветка везёт папку своей задачи. | `<путь>:<символ>` <!-- заполняет проект --> |
| Ветка, снёсшая папку, обязана прибавить запись в архив. | `<путь>:<символ>` <!-- заполняет проект --> |
| Обход — строка `Task-folder-skip: <причина>` в PR или в самой команде. | `<путь>:<символ>` <!-- заполняет проект --> |

## Что ещё стоит знать при чтении кода

- <Что видно только изнутри этого дерева: почему приём выглядит здесь именно так.>

## Чем это проверяется

- <Команда, которая ловит нарушение, — и что именно она ловит.>
