<!-- rt-kit v0.26.0 · templates/findings.md · 4214037b72f4 · правится надстройкой, не здесь -->
# Findings of the epic <what it is about>

<They accumulate here, next to the epic plan: the task folder dies with the merge, and the owner
reads the findings at once, when the epic is over. The file name is the plan name with the tail
`-findings`, and it lies next to the plan itself.

A section per task, a line per finding. Each names an address — the rules layer, the names of
this tree or an override on top of the laid-out: without an address, what is general settles in
its own tree and stays unknown to everyone else.

The length is looked at before the first line is written: a task section takes dozens of lines,
and it crosses the document length limit silently. Does not fit — the next part is started with
the name of the task that started it: `<plan name>-findings-<KEY>-<number>.md`. A part has no
ordinal number — every working tree sees its own, and two trees of one epic start the same file.>

## <KEY>-<number> — <what the task did>

- **<The finding in one sentence — what is wrong, not what to do.>** <What confirms it: a number,
  a refusal, a file. What it cost.> **Address:** <rules layer | names of this tree — <what
  exactly> | override on top of the laid-out>.

## Left unresolved

<A finding with no edit yet. Named here, it moves to the closed ones together with the edit that
closed it — instead of staying listed as live.>

- **<finding>** — <why it is not closed: waits for the owner's decision, becomes a task of its own, is disputed>.
