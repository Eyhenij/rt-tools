/**
 * Типы груза — либа вне доменной сетки: у неё нет ни слоёв, ни своего предмета, она называет
 * форму того, что едет между деревом и приёмником.
 *
 * Список зависимостей пуст и останется пустым: те же типы берёт отправляющая сторона, и
 * первая же зависимость уехала бы вместе с ними в каждое дерево, поставившее пакет правил.
 */
export const messageBusCommonBoundaries = [{ sourceTag: 'scope:message-bus-common', onlyDependOnLibsWithTags: [] }];
