/**
 * Публикуемые пакеты `projects/*`. Границами они не сужены: направление между ними держат их
 * собственные `package.json` — у пакета утилит зависимостей нет вовсе, основание зовёт только
 * утилиты, оба кита — основание и утилиты. Метки проектов при этом пусты, и сузить их значило
 * бы объявить второй ответ на тот же вопрос, расходящийся с первым при первой же публикации.
 *
 * Правило стоит последним во всём своде: первое подходящее выигрывает, и звёздочка выше по
 * тексту накрыла бы лесенку приёмника целиком.
 */
export const packagesBoundaries = [{ sourceTag: '*', onlyDependOnLibsWithTags: ['*'] }];
