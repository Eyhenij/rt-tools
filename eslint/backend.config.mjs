/**
 * Что снимается с серверной стороны.
 *
 * Правила каркаса фронта на приёмник не действуют — он на NestJS, — но линтеру об этом сказать
 * нечем: метку внедрения оба каркаса зовут одинаково, `@Injectable()`. Отсюда список здесь, а не
 * в корневом конфиге: цель линта у каждого проекта своя и зовётся из его каталога, поэтому путь
 * от корня дерева не совпал бы там ни с одним файлом, а `**\/*.ts` в корневом конфиге накрыл бы
 * заодно и оба кита.
 *
 * Подключается конфигом каждого серверного проекта последним: в плоском конфиге выигрывает
 * последнее совпавшее правило.
 */
export const backendConfig = [
    {
        files: ['**/*.ts'],
        rules: {
            // `inject()` — это функция каркаса фронта; в приёмнике её нет вовсе, и правка,
            // которую предлагает правило, не компилируется.
            '@angular-eslint/prefer-inject': 'off',
        },
    },
];
