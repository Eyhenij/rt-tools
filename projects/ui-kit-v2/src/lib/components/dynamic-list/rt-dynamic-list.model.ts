/**
 * Модель `<rt-dynamic-list>`: один корневой неймспейс с префиксом `I`.
 */
export namespace IRtDynamicList {
    /**
     * Отчего пусто. Причины две, и человеку они говорят разное: в разделе записей нет вовсе или
     * их не оставил отбор. Из второго человеку показывают дорогу назад.
     */
    export type EmptyReason = 'section' | 'filter';
}
