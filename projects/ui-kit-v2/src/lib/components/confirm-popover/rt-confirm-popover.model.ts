/**
 * Модель confirm-popover: один корневой неймспейс с префиксом `I`.
 */
export namespace IRtConfirmPopover {
    /** Тон подтверждающей кнопки. Подмножество `IButton.Theme`. */
    export type Tone = 'danger' | 'warning' | 'primary';
}
