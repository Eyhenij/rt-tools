/**
 * Имена общего слоя раскладки демонстрационного экрана.
 *
 * Блок страницы объявлен здесь, а не строкой в каждом экране: правила под него живут одним
 * файлом стилей, и разошедшееся имя оставило бы экран без отступов, кегля заголовка и
 * промежутков — молча, потому что класса без правила в этом случае нет вовсе.
 */

/** Блок страницы раздела: отступы, шапка, заголовок и зона содержимого. */
export const APP_PAGE_BLOCK: string = 'app-page';
