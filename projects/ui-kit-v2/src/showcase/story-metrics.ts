/**
 * Размеры демонстрационных ячеек.
 *
 * Поля ввода занимают всю ширину родителя, поэтому в ячейке, растущей по содержимому, они
 * схлопываются и показывают не размер, а его отсутствие. Ширина задаётся снаружи и одна на
 * все матрицы полей: разойдись она — соседние страницы показывали бы один и тот же контрол
 * разной ширины, и разница читалась бы как свойство компонента.
 *
 * Обвязка витрины: `tsconfig.lib.json` исключает `src/showcase/**`, в пакет не уезжает.
 */

/** Ширина ячейки с полем ввода — примерно колонка формы. */
export const STORY_FIELD_WIDTH: string = '15rem';

/** Ширина ячейки с полем, которому нужна вся ширина формы: многострочным и составными. */
export const STORY_FIELD_WIDTH_WIDE: string = '22rem';
