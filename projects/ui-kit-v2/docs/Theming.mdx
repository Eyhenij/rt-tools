import { Meta, Source, Title } from '@storybook/addon-docs/blocks';

<Meta title="Foundation/Design Tokens/Theming" />

export const pane = {
    border: '1px solid var(--rt-color-border-default)',
    borderRadius: 10,
    padding: 18,
    display: 'grid',
    gap: 10,
    marginBlock: 12,
};
export const row = { display: 'flex', gap: 12, alignItems: 'center', flexWrap: 'wrap' };
export const swatch = (token) => ({
    width: 90,
    height: 44,
    borderRadius: 6,
    background: `var(${token})`,
    border: '1px solid var(--rt-color-border-default)',
});
export const cap = { fontFamily: 'var(--rt-font-family-mono)', fontSize: 11, opacity: 0.7 };
export const warn = {
    background: 'color-mix(in srgb, var(--rt-color-state-warning) 8%, transparent)',
    border: '1px solid color-mix(in srgb, var(--rt-color-state-warning) 30%, transparent)',
    borderRadius: 8,
    padding: '14px 18px',
    fontSize: 13,
    marginBlock: 12,
};

<Title>Тема</Title>

Тема — признак на корневом элементе. Тёмная переопределяет **только назначения**; шкалы
остаются теми же, и ни один компонент о теме не знает.

<Source
    language="html"
    code={`<html data-theme="dark">   <!-- либо class="rt-theme-dark" -->`}
/>

Оба признака равнозначны — `_theme-dark.scss` объявляет их одним правилом:
`:root[data-theme='dark'], html.rt-theme-dark`.

---

## Кто ставит признак

`ThemeService` — он же держит выбор между перезагрузками.

<Source
    language="typescript"
    code={`const theme: ThemeService = inject(ThemeService);

theme.current(); // Signal<'light' | 'dark'>
theme.isDark(); // Signal<boolean>
theme.toggle();
theme.setTheme('dark');`}
/>

Выбор пишется под ключ устройства, а не пользователя: последняя выставленная тема переживает
вход, выход и перезагрузку. Под серверным рендером признак не ставится вовсе — разметка
приезжает светлой и переключается после гидратации.

Готовый переключатель — `rt-theme-toggle`; своего состояния он не держит и ходит в тот же
сервис.

---

## Что видно на этой странице

Переключатель темы в панели сверху пишет `data-theme` на `<html>` — тот же признак, что ставит
`ThemeService`. Прямоугольники ниже перерисуются вместе со страницей.

<div style={pane}>
    <div style={row}>
        <div>
            <div style={swatch('--rt-color-bg-page')} />
            <span style={cap}>bg-page</span>
        </div>
        <div>
            <div style={swatch('--rt-color-bg-surface')} />
            <span style={cap}>bg-surface</span>
        </div>
        <div>
            <div style={swatch('--rt-color-bg-surface-subtle')} />
            <span style={cap}>bg-surface-subtle</span>
        </div>
        <div>
            <div style={swatch('--rt-color-action-primary')} />
            <span style={cap}>action-primary</span>
        </div>
        <div>
            <div style={swatch('--rt-color-state-danger')} />
            <span style={cap}>state-danger</span>
        </div>
    </div>
    <div>
        <span style={{ color: 'var(--rt-color-text-primary)' }}>Основной текст</span>
        {' · '}
        <span style={{ color: 'var(--rt-color-text-muted)' }}>приглушённый</span>
        {' · '}
        <span style={{ color: 'var(--rt-color-text-link)' }}>ссылка</span>
    </div>
</div>

---

## Почему графит на `:root`

Диалоги, панели и меню рисуются наложением на уровне `body` — вне разметки того экрана, из
которого их открыли. Держи тёмные назначения на классе оболочки, наложения не унаследовали бы
палитру и остались бы светлыми поверх тёмной страницы.

---

## Перекраска под свою марку

Кит не форкается: марка приходит объявлением одиннадцати ступеней бренда после `tokens.css`.
Больше от приложения ничего не требуется — действие, ссылка, подложка, кольцо фокуса, пузыри
переписки и их прозрачные оттенки берут цвет отсюда сами, в обеих темах.

Объявления на корне страницы в слой каскада не уходят намеренно: перекраска держится порядком —
объявленное после `tokens.css` побеждает. Правила кита при этом лежат в слое `rt-kit`, поэтому
любое правило приложения перебивает их и без счёта специфичности.

```css
@import '@rt-tools/ui-kit-v2/styles/tokens.css';

:root {
    --rt-brand-50: #f2f7ee;
    --rt-brand-100: #d9e8cd;
    --rt-brand-200: #bdd8a9;
    --rt-brand-300: #9ec581;
    --rt-brand-400: #7bb054;
    --rt-brand-500: #4f9022;
    --rt-brand-600: #427a1c;
    --rt-brand-700: #366317;
    --rt-brand-800: #294c11;
    --rt-brand-900: #1d360b;
    --rt-brand-950: #122106;
}
```

Ступень 500 — сам цвет марки; наведение и нажатие задаёт приложение своими ступенями, а не
получает вычисленными: точный оттенок наведения выводом из одного цвета не задать. Вычисляются
только прозрачные оттенки — у них нет своего цвета, есть доля бренда.

Толщина кольца фокуса тоже настраивается — `--rt-focus-ring-width`, `--rt-focus-ring-offset` и
`--rt-focus-ring-color` объявлены отдельно, а составное значение собирается из них.

<div style={warn}>
    <strong>Синего в ките больше нет.</strong> Шкала <code>--rt-blue-*</code> снята: цвет марки жил в ней ступенью, и перекрасить его, не
    сдвинув заодно пузыри переписки и подложку шапки, было нельзя.
</div>

---

## Чего делать не надо

<div style={warn}>
    <strong>Не берите шкалу в стилях экрана.</strong> <code>var(--rt-neutral-0)</code> останется белым и в тёмной теме: переопределять его
    некому. Возьмите назначение — <code>var(--rt-color-bg-surface)</code>.
</div>

<div style={warn}>
    <strong>Не заводите свой признак темы.</strong> Компонент кита слушает только <code>data-theme</code> и <code>rt-theme-dark</code>; на
    своём признаке он останется светлым, а экран вокруг него потемнеет.
</div>

---

## Ручки потребителя

Одиннадцать свойств кит **намеренно не объявляет**: их значение приходит от приложения, а до
него работает запасное. Всё, чего в этой таблице нет, кит объявляет сам — и переопределять его
запасным значением на месте не нужно.

Перечень сверяется машиной: `pnpm run check:tokens-graph` читает
`tools/tokens-handles.json` и требует, чтобы каждое имя оттуда стояло и здесь. Молча эти два
перечня разойтись не могут.

| Свойство                        | Что задаёт                             | Пока не задано                 |
| ------------------------------- | -------------------------------------- | ------------------------------ |
| `--rt-aside-width`              | ширина боковой панели                  | ступень `--rt-aside-width-md`  |
| `--rt-dialog-width`             | ширина диалога                         | ступень `--rt-dialog-width-md` |
| `--rt-header-height`            | высота шапки приложения                | `0px` — кит её не рисует       |
| `--rt-header-content-max-width` | кап ширины содержимого шапки           | `100%`                         |
| `--rt-logo-wordmark`            | начертание логотипа, светлая тема      | `none`                         |
| `--rt-logo-wordmark-dark`       | то же для тёмной темы                  | светлое начертание             |
| `--rt-logo-tagline`             | подпись под логотипом, светлая тема    | `none`                         |
| `--rt-logo-tagline-dark`        | то же для тёмной темы                  | светлая подпись                |
| `--rt-workspace-aside-size`     | ширина боковой колонки рабочей области | `360px`                        |
| `--rt-workspace-list-size`      | ширина списка рабочей области          | `320px`                        |
| `--rt-page-header-bg`           | подложка шапки страницы                | подложка панели навигации      |

<div style={warn}>
    <strong>Ручка — обещание, а не забытое объявление.</strong> Имя, которого нет ни в этой таблице, ни в слоях оформления, — мёртвая
    ссылка: правило работает, значение не применяется, и увидеть это можно только глазами.
</div>
