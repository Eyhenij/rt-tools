export * from './is-html-element-guard.util';

/**
 * @description `isNil` lives in `@rt-tools/utils`, which carries no framework dependency.
 * Re-exported here so existing imports from `@rt-tools/core` keep resolving.
 */
export { isNil } from '@rt-tools/utils';
