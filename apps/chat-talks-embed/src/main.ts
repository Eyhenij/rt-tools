/**
 * Точка входа скрипта установки: он объявляет элемент админки и на этом кончается.
 *
 * Сам он ничего не рисует и никуда не встаёт: где стоять разделу, решает админка потребителя — она
 * ставит тег. Второе объявление того же имени браузер отвергает ошибкой, поэтому имя проверяется до
 * объявления: два скрипта на одной странице — это не отказ страницы, а один лишний скрипт.
 */
import { TalksEmbedElement, TALKS_TAG } from './lib/talks-embed.element';

if (!customElements.get(TALKS_TAG)) {
    customElements.define(TALKS_TAG, TalksEmbedElement);
}
