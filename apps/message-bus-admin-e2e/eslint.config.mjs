import baseConfig from '../../eslint.config.mjs';

/**
 * Правила сквозного набора.
 *
 * Свои поверх общих нужны в одном месте: стенд написан обычным JavaScript и запускается прямо
 * узлом — прогонщик спек транспилирует только то, что грузит сам, а подъём стенда он зовёт
 * командой. Общий конфиг таких файлов не разбирает вовсе, и без этой строки они остались бы
 * непроверенными.
 */
export default [
    ...baseConfig,
    {
        files: ['**/*.mjs'],
        languageOptions: {
            ecmaVersion: 'latest',
            sourceType: 'module',
        },
    },
];
