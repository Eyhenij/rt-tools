/**
 * Имена стенда сквозного набора: порты, хранилище, пара входа и деревья, чей груз он показывает.
 *
 * Названы в одном месте потому, что их спрашивают трое: подъём стенда, засев хранилища и сами
 * спеки. Разойдясь, они не отказывают — спека просто ищет на экране дерево, которого засев не
 * заводил, и падение читается как дефект экрана.
 *
 * Порты взяты не те, на которых владелец держит стенд для замера глазами (3000 и 4200): набор
 * поднимается поверх работающей машины и не имеет права занять её порт или ответить вместо неё.
 *
 * Хранилище — своя база в том же контейнере, что и рабочая. Набор чистит её перед каждым
 * прогоном целиком, и общая с рабочей база означала бы, что прогон стирает данные, на которых
 * владелец только что мерил.
 */

/** Порт приёмника стенда. */
export const API_PORT = 3310;

/** Порт админки стенда: её отдаёт своя раздача статики, она же проксирует `/api` приёмнику. */
export const ADMIN_PORT = 4310;

export const API_ORIGIN = `http://localhost:${API_PORT}`;
export const ADMIN_ORIGIN = `http://localhost:${ADMIN_PORT}`;

/** Адрес хранилища стенда. Имя базы своё, пользователь и порт — те же, что у рабочей. */
export const STAND_DATABASE_URL = 'postgresql://message_bus:message_bus@localhost:55432/message_bus_e2e';

/** Адрес того же хранилища для служебных команд: заведение базы стенда идёт не изнутри неё. */
export const SERVER_DATABASE_URL = 'postgresql://message_bus:message_bus@localhost:55432/message_bus';

/** Имя базы стенда. Названо отдельно: `CREATE DATABASE` не принимает адреса. */
export const STAND_DATABASE = 'message_bus_e2e';

/**
 * Пара входа стенда.
 *
 * Тайной она не является и не притворяется: запись живёт только в базе стенда, которую набор
 * заводит и вычищает сам, а сам стенд слушает локалхост. Пара названа здесь, а не в окружении,
 * потому что тест, погашенный отсутствием переменной, значится пропущенным, а прогон при этом
 * выглядит успешным.
 */
export const ACCOUNT = Object.freeze({ name: 'Набор', password: 'nabor-e2e-2026' });

/**
 * Деревья стенда.
 *
 * Три штуки и с разной ролью: у первого груз всех трёх родов и его больше страницы, у второго —
 * меньше, третье не присылало ничего. Третье нужно ровно затем, чтобы отбор по нему давал пустой
 * список при непустом разделе: пустота по отбору и пустота вовсе — разные состояния экрана, и
 * различить их нечем, пока в отборе нет дерева без груза.
 */
export const TREES = Object.freeze([
    Object.freeze({ slug: 'stand-one', name: 'Стенд первый' }),
    Object.freeze({ slug: 'stand-two', name: 'Стенд второй' }),
    Object.freeze({ slug: 'stand-three', name: 'Стенд третий' }),
]);

/** Адреса разделов админки. Те же, что объявляют маршруты разделов. */
export const SECTIONS = Object.freeze({
    postmortems: '/postmortems',
    proposals: '/proposals',
    summaries: '/summaries',
});
