# message-bus-admin-accounts-api

Слой пуст. Своего обращения к приёмнику у раздела людей нет: список читает общая основа стора по
адресу, который называет слой моделей, а правок над записями раздел не делает ни одной — заводит,
отключает и меняет пароль команда строки запуска на узле приёмника.

Либа остаётся, потому что слой у домена объявлен: молчание про слой проверка слоёв читает как
недоделанный домен.
