// Слой пуст: своего обращения к приёмнику у раздела нет — список читает общая основа стора, а
// правок над записями у раздела не бывает вовсе. Барель остаётся, потому что слой у домена
// объявлен: пустой слой дефектом не считается, а вот домен без слоя неотличим от недоделанного.
export {};
