// Слой пуст: своего вида у раздела нет — строка списка показывается готовыми ячейками таблицы
// кита, а панели подробностей у приглашения не бывает вовсе. Барель остаётся, потому что слой у
// домена объявлен: пустой слой дефектом не считается, а вот домен без слоя неотличим от
// недоделанного.
export {};
