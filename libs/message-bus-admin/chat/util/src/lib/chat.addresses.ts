/**
 * Адрес раздела чата в админке.
 *
 * Стоит одним местом, потому что его читают двое: маршрут раздела и пункт меню. Написанный
 * заново в каждом из них путь расходится с другим молча — пункт ведёт в пустоту.
 *
 * Адреса операций приёмника лежат в общей либе: их объявляет та сторона, которая ими отвечает.
 */

/** Путь раздела в админке. Он же стоит у пункта меню. */
export const CHAT_ROUTE: string = 'chat';
