import { messageBusCommon } from './message-bus-common';

describe('messageBusCommon', () => {
    it('should work', () => {
        expect(messageBusCommon()).toEqual('message-bus-common');
    });
});
