// Клиент хранилища. Вывод генератора живёт в этом слое, потому что `util` — единственный слой,
// который видят все остальные: ниже его класть нельзя, иначе доступ к типам схемы пришлось бы
// объявлять отклонением от лесенки в каждом домене.
//
// Сам клиент нужен единственному потребителю — службе хранилища из слоя `data-access`.
export { Prisma, PrismaClient } from './generated/prisma/client';
