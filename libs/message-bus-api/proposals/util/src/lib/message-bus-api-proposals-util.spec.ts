import { messageBusApiProposalsUtil } from './message-bus-api-proposals-util';

describe('messageBusApiProposalsUtil', () => {
    it('should work', () => {
        expect(messageBusApiProposalsUtil()).toEqual('message-bus-api-proposals-util');
    });
});
