import { messageBusApiProposalsFeature } from './message-bus-api-proposals-feature';

describe('messageBusApiProposalsFeature', () => {
    it('should work', () => {
        expect(messageBusApiProposalsFeature()).toEqual('message-bus-api-proposals-feature');
    });
});
