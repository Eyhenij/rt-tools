import { messageBusApiObservationsFeature } from './message-bus-api-observations-feature';

describe('messageBusApiObservationsFeature', () => {
    it('should work', () => {
        expect(messageBusApiObservationsFeature()).toEqual('message-bus-api-observations-feature');
    });
});
