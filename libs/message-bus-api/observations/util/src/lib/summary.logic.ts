/**
 * Форма сводки: что в ней обязано быть, чтобы приём состоялся.
 *
 * Судится наличие полей, а не их содержимое. Счётчики и снимок надстроек приёмник хранит как
 * приехали: разбирает их тот, кто читает, а приёмник, знающий поля наизусть, отказывал бы на
 * каждой правке отправляющей стороны.
 */
/**
 * Обязательные поля сводки: голова груза, счётчики отрезка, снимок надстроек и невыбранное.
 *
 * Версии схемы строк наблюдения здесь нет намеренно: их в одной сводке встречается несколько, и
 * приёмник их не судит вовсе — это часть содержимого, а не форма запроса.
 */
export const SUMMARY_FIELDS: readonly string[] = [
    'schema',
    'tree',
    'days',
    'sessions',
    'loads',
    'denials',
    'kinds',
    'guards',
    'unused',
    'unpicked',
    'overrides',
    'versions',
    'total',
];
