import { messageBusApiObservationsUtil } from './message-bus-api-observations-util';

describe('messageBusApiObservationsUtil', () => {
    it('should work', () => {
        expect(messageBusApiObservationsUtil()).toEqual('message-bus-api-observations-util');
    });
});
