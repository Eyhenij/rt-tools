# message-bus-api-trees-data-access

Запросы к деревьям и их токенам: дерево годного токена по хешу.

Зовёт проверка токена в слое `feature`. Отозванный токен из выборки выпадает, но в хранилище
остаётся — груз, приехавший по нему, читается по-прежнему.
