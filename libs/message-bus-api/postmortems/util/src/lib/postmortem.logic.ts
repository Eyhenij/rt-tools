/**
 * Форма разбора происшествия.
 *
 * Разбор приезжает текстом целиком: приёмник закрытый и свой, а шапка без механизма промаха
 * ничего не объясняет — правило из разбора выводится по механизму.
 *
 * Опознаётся разбор именем своего файла на дереве: заголовок правится вместе с текстом, а имя
 * файла правку переживает. Цена этого допущения названа в договорённости `Q-19` — переименованный
 * на дереве разбор приедет вторым экземпляром.
 */

/** Обязательные поля одной записи. */
export const POSTMORTEM_ITEM_FIELDS: readonly string[] = ['file', 'text'];
