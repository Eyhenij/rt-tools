import { messageBusApiPostmortemsUtil } from './message-bus-api-postmortems-util';

describe('messageBusApiPostmortemsUtil', () => {
    it('should work', () => {
        expect(messageBusApiPostmortemsUtil()).toEqual('message-bus-api-postmortems-util');
    });
});
