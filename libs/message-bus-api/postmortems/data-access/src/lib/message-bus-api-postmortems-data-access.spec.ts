import { messageBusApiPostmortemsDataAccess } from './message-bus-api-postmortems-data-access';

describe('messageBusApiPostmortemsDataAccess', () => {
    it('should work', () => {
        expect(messageBusApiPostmortemsDataAccess()).toEqual('message-bus-api-postmortems-data-access');
    });
});
