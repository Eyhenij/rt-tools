import { messageBusApiPostmortemsFeature } from './message-bus-api-postmortems-feature';

describe('messageBusApiPostmortemsFeature', () => {
    it('should work', () => {
        expect(messageBusApiPostmortemsFeature()).toEqual('message-bus-api-postmortems-feature');
    });
});
