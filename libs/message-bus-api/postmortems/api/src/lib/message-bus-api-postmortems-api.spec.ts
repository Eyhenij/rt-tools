import { messageBusApiPostmortemsApi } from './message-bus-api-postmortems-api';

describe('messageBusApiPostmortemsApi', () => {
    it('should work', () => {
        expect(messageBusApiPostmortemsApi()).toEqual('message-bus-api-postmortems-api');
    });
});
