// Слой пуст: наружу домен наблюдаемости не ходит — приёмник принимает, а не рассылает, и
// выхода к чужой службе класть сюда нечего. Барель остаётся, потому что слой у домена
// объявлен: пустой слой дефектом не считается, а домен без слоя неотличим от недоделанного.
export {};
