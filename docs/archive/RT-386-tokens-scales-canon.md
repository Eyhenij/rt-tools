# RT-386 — шкалы сведены в канон, а назначения перестали держать литералы

Задача #386, PR #412, слит 13 августа 2026. Прежний адрес — `docs/tasks/RT-386-tokens-scales-canon/`.
Запись собрана из разбора просьбы и хода работы при закрытии задачи; после выкатки не
правится.

## Просьба владельца

> /Users/eyhenij/WebstormProjects/rt-tools/.claude/handoff/RT-272-tokens-action-roles.md

Передача прошлого захода, отданная одной строкой. Читается как «работа RT-272 закрыта, бери
следующее»: сама передача называет следующим шагом позицию 7 линии
`docs/plans/ui-kit-v2-design-tokens.md` — задачу RT-386.

## Что уже есть в дереве

Разведка командами по текущему дереву, до первого вопроса.

| Что обещает позиция 7   | Что на диске сейчас                                                                                                         |
| ----------------------- | --------------------------------------------------------------------------------------------------------------------------- |
| литералы из назначений  | `_semantic.scss` — 12 пиксельных литералов: `--rt-space-2xs: 2px`, `--rt-text-base: 16px`, габариты 28/42/250/240/24/12/4px |
| свод шкалы нейтрального | `_primitives.scss` — 21 ступень, 9 из них off-grid: `150 250 280 350 420 450 510 520 550`                                   |
| off-grid отступы сняты  | `--rt-space-1-5`, `-2-5`, `-3` помечены `@deprecated` и берутся свободно; `--rt-space-7` (28px) помечен не был              |
| общая шкала высот       | кнопка `30/40/50/60/70` (sm/md/lg/xl/2xl), поле `36/45/52` (sm/md/lg) — в одной строке расходятся на 6/5/2px                |
| хвосты RT-272           | 5 принятых литералов цвета в `tools/tokens-styles-allowlist.json`; `--rt-info-500: #2563eb` против марки `#155dfc`          |

Задача на борде уже заведена — issue #386, открыта, тело описывает все четыре пункта. Передача
утверждала обратное («задача под неё ещё не заведена»); рамка переданного текста проверена
командой здесь и не подтвердилась.

## Что уже сказано в правилах

Поиск по `docs/constitution`, `.claude/skills`, `docs/specs` словами темы — высота, шкала,
ступень, нейтраль. Найденное закрыло пять из заготовленных вопросов, и они владельцу не
задавались:

| Что искали                    | Где ответ                                                                                               |
| ----------------------------- | ------------------------------------------------------------------------------------------------------- |
| пять принятых литералов цвета | `docs/tasks/RT-272-tokens-action-roles/progress.md` — «приняты списком, чинит их свод палитры RT-386»   |
| шкала размеров под габариты   | тело issue #386 — «для габаритов, которым нет места в существующих шкалах, завести шкалу размеров»      |
| роль info против марки        | линия и передача RT-272 относят её к этой работе                                                        |
| правится ли правило           | `.claude/skills/styling-bem/SKILL.md:119` — «свою шкалу высоты контролов кит заводит отдельной работой» |
| признак закрытия и образец    | тело issue #386 (пять признаков); образец приёма — RT-385, ряд считается в OKLCH ровным шагом           |

Договорённость о продукте на эту работу уже написана и живёт в
`docs/specs/ui-kit-v2/proposed/design-tokens/`: она описывает всю линию, а не одну позицию.
Правила `SC-UKV-41` (шкала остаётся шкалой) и `SC-UKV-48` (контролы совпадают по высоте) —
ровно про эту задачу. Открытым в ней помечен один вопрос — `Q-4`, и он прямо отложен до этой
работы.

## Вопросы и ответы

**Общая шкала высот: какой ряд становится общим? Спек требует только совпадения (SC-UKV-48),
сам ряд не назван нигде. Сегодня кнопка 30/40/50/60/70, поле 36/45/52.**
Считается заново, кратно 8: `32/40/48/56/64`. Двигаются обе семьи — кнопка на 2px в
sm/lg/xl/2xl, поле на 4/5/4px; md остаётся 40 и кнопку в самом ходовом размере не двигает.

**Свод шкалы нейтрального — это `Q-4` спека, единственный вопрос, ответ на который отложен до
этой работы. Канонические двенадцать ступеней сами держат два семейства: 200 серый `#e6e6e6`
рядом с 300 синеватым `#cad5e2`, 900 серый `#1f1f1f` рядом с 950 синеватым `#122341`.**
Ряд пересчитывается ровным в OKLCH: 12 ступеней `0 50 100 200 300 400 500 600 700 800 900 950`,
один тон, одна цветность, ΔL ровным шагом. Девять off-grid ступеней снимаются, употребления
переезжают на ближайшую.

**Делится ли работа? Задача везёт четыре пункта плюс три хвоста RT-272. Два из них двигают вид
широко (высоты и нейтраль) и откатываются порознь.**
Одна задача, как в линии: позиции 5–7 правят одни и те же три файла оформления, и деление дало
бы две пересъёмки эталонов вместо одной.

## Решения

- **Ряд высот считается заново, кратно восьми** — 32/40/48/56/64. Довод владельца принят
  рекомендованным: шаг ложится на сетку отступов, а md остаётся 40 и не двигает самый ходовой
  размер кнопки. Тот же приём линия уже приняла для бренда и ступеней 600/700 шкал статуса.
  Отвергнуто: объявить общим ряд кнопки (шаг 10 не кратен шагу отступов, а поле высотой 30px с
  текстом 16px на грубом указателе тесновато) и ряд полей (сам неровный — шаг 9, потом 7).

- **Ряд нейтрального пересчитывается ровным в OKLCH** — иначе выбор из шкалы остаётся выбором
  вслепую: два семейства, серое и синеватое, стоят в одном ряду соседними ступенями. Это
  четвёртое названное вслух исключение вида, и оно широкое: нейтраль берёт почти каждый
  компонент. Отвергнуто: сблизить снятые ступени с соседними, не трогая канон (дешевле, но два
  семейства остаются), и поднять все девять в назначения (кадры не двигаются вовсе, но девять
  назначений под одно место каждое — тот же беспорядок этажом выше).

- **Работа идёт одной веткой** — линия прямо говорит, что позиции 5–7 правят одни и те же три
  файла оформления и параллелить их нельзя. Отвергнуто: вынести высоты в свою задачу — деление
  дало бы два круга разбора ~450 кадров глазами и отодвинуло позицию 8.

- **Конвейер ролей не зовётся.** Договорённость о продукте на эту работу написана линией
  раньше — `docs/specs/ui-kit-v2/proposed/design-tokens/`, — и её правила `SC-UKV-41` и
  `SC-UKV-48` описывают ровно эту задачу. Писать её заново нечем и незачем; вместо конвейера
  договорённость дописывается по ходу теми статьями, которые эта работа делает верными.

## Что осталось невыясненным

- **Куда именно едет каждая из девяти снятых ступеней нейтрального.** Не задавали: это работа
  замера, а не решения — ближайшая каноническая ступень считается, а не выбирается. Место,
  где ближайшей не окажется, возвращается владельцу отдельным вопросом.
- **Сводится ли роль info с маркой или разводится намеренно.** Не задавали: сегодня разница
  `#2563eb` против `#155dfc` неразличима на глаз, и любой из двух исходов работу не блокирует.
  Решение принимается на этапе, где роль встаёт в пересчитанный ряд, и записывается в ход
  работы.
- **Точные значения пересчитанного ряда нейтрального.** Считаются на этапе 3 и до счёта
  неизвестны никому; владельцу они уйдут вместе с разбором разошедшихся кадров.

## Решения по ходу

- **Три габарита округляются к ряду, и вид двигается** — 28→24 у высоты пункта навигации по
  слову владельца, 42→40 у стороны аватара и 250→240 у ширины колонки панели, где ближайшая
  ступень одна. Ступень под 28px не заводится: спек прямо запрещает дописывать ступень под
  случай (`SC-UKV-41`), а пометка рядом ничего не отбивает. Владелец выбрал округление вниз —
  высота пункта совпадает с высотой пункта развёрнутой панели, тоже 24px; внутренний отступ
  пункта при этом уезжает на 4px, а не на 8px, и это уже решение этапа 2. Затронутые этапы
  замысла: 1, 2.

- **Отступы округляются к ближайшей ступени, при равенстве — вверх** — слово владельца: 6→8,
  10→8, 12→16, 28→32. Одно правило на все 58 мест; разное направление у разных ступеней
  оставило бы расхождение кадра необъяснимым одним доводом. Затронутый этап замысла: 2.

- **Предпросмотр вопроса про высоту пункта навигации обещал уплотнение, которого не будет.**
  `--rt-nav-item-height` стоит в вёрстке `min-height`, а не `height`, и порог этот не
  связывающий: при подписи 14px и внутреннем отступе 8px пункт занимает около 33px и до 28px не
  доставал и раньше. Выбранное владельцем 24 понижает порог, которого вёрстка не касается, а
  фактическую высоту пункта задаёт округление отступа вверх — тем же плюс четыре, что и в
  отвергнутом варианте. Довод «совпадает с пунктом развёрнутой панели» при этом остаётся верным
  для самого порога. Замером подтверждается на этапе 8. Затронутые этапы замысла: 1, 2, 8.

- **Ряд нейтрального идёт ровным шагом на всю длину** — слово владельца после того, как замер
  показал цену: светлые ступени 50, 100 и 200 стояли вплотную (0.985 / 0.976 / 0.925), то есть
  три почти белых фона, и ровный шаг их неизбежно разносит. Тон 264.4° и цветность 0.0234 взяты
  у прежней ступени 600 — её владелец принял точь-в-точь в RT-272; в самом светлом конце
  цветность ужата охватом sRGB, как у бренда на 600 и 700. Затронутый этап замысла: 3.

- **Ступень 510 уехала на 600, а не на ближайшую по светлоте 500** — она и была заведена как
  «самый светлый серый, берущий порог на белом», а новая 500 даёт 4.08:1, то есть порога не
  берёт. Критерий здесь порог, а не близость; 600 даёт 5.68:1. Затронутый этап замысла: 3.

- **Роль info разведена с маркой** — слово владельца. Довод спека: «палитра держит одну роль», а
  уведомление «к сведению» — статус наравне с успехом и опасностью; у потребителя с зелёной
  маркой оно иначе стало бы неотличимо от успешного. Тон уведён к 250°, а не дальше: за этой
  границей охват sRGB режет цветность вдвое и синий уходит в бирюзу. Затронутый этап замысла: 5.

- **Шесть прозрачных оттенков переехали на счёт от ступени** — четыре подложки статуса тёмной
  темы и два оттенка шкалы повторяли коды цвета своих ступеней тройками каналов. Гейт литералов
  их не видел: его набор — стили компонентов, а слой оформления в него не входит. В границы взяты
  потому, что это ровно тот дефект, который задача чинит сводом палитры, и следующая правка
  ступени развела бы их с источником молча. Затронутый этап замысла: 5.

- **Дорожка группы кнопок взяла приглушённую поверхность, а не ближайшую по светлоте ступень** —
  прежний `#eef0f4` стоял почти на уровне белого, а отличаться дорожка обязана от активного
  сегмента, который белым и покрашен. Тот же критерий, что у ступени 510: не близость, а роль.
  Затронутый этап замысла: 5.

- **Приглушённый текст переведён на ступень 600** — слово владельца. Пересчёт ряда увёл пять его
  пар вниз (3.79 → 3.08:1): фон приглушённой поверхности потемнел, а сам текст остался на 500.
  Роль «самая светлая ступень, берущая порог» после пересчёта перешла к шестисотой — та же
  замена, что у ступени 510. Шесть пар вышли выше порога и сняты из списка принятого. Затронутый
  этап замысла: 6.

- **`--rt-focus-ring-offset: 0px` остаётся литералом** — безразмерный ноль внутри `calc` с
  длиной невалиден, и браузер отбрасывает объявление кольца целиком; причина стоит
  комментарием в самой строке. Признак этапа 1 читается «ни одного литерала, кроме этого».
  Затронутый этап замысла: 1.

- **Эталоны пересняты всем каталогом, а не поимённо** — расхождения разобраны до пересъёмки
  числом: у всех 371 кадра главный переход цвета один из трёх названных, а прочие переходы
  ложатся на те же девять изменений вида. Поимённая пересъёмка здесь ничего не стерегла бы:
  она берёт файлы историй, а разошлись 92 набора из 155 — перечисление вышло бы длиннее
  каталога. И она же не достаёт кадры пороговой ширины: их прогон в разбор не отдаёт вовсе,
  поэтому копия эталонов снята до пересъёмки и сверена с новой отдельно. Затронутый этап
  замысла: 8.

- **Конвейер ролей не зовётся** — договорённость о продукте на эту работу написана линией раньше
  и описывает её правилами `SC-UKV-41` и `SC-UKV-48`; писать её заново нечем. Вместо конвейера
  договорённость дописывается этапом 7 теми статьями, которые работа делает верными. Затронутый
  этап замысла: 7.

## Заходы

### 2026-08-13

- Передача RT-272 прочитана; её утверждение «задача под позицию 7 ещё не заведена» проверено
  командой и не подтвердилось — issue #386 открыт, тело описывает все четыре пункта.
- Разведка замерами: 12 пиксельных литералов в `_semantic.scss`, 21 ступень нейтрального из них
  9 off-grid, четыре off-grid отступа (три с пометкой, `--rt-space-7` без неё), два несовпадающих
  ряда высот — кнопка 30/40/50/60/70 против поля 36/45/52.
- Поиск по законам, правилам и спекам закрыл пять заготовленных вопросов из восьми — владельцу
  ушли три, и все три оказались теми, что документация помечает открытыми.
- Ответы владельца: ряд высот `32/40/48/56/64` кратно восьми; ряд нейтрального пересчитывается
  ровным в OKLCH; работа идёт одной веткой.
- Этап 1. Шкала `--rt-size-*` — семнадцать ступеней, множитель тот же, что у отступов (ступень N
  равна N × 4px). Литералов в `_semantic.scss` было 12, стало 1.
- Восемь литералов легли на ступень без сдвига вида: толщина кольца фокуса на
  `--rt-border-width-thick`, `--rt-space-2xs` на ступень `0-5`, `--rt-text-base` на `--rt-text-md`,
  ширина значения счётчика и высота пункта панели на `--rt-size-6`, ширина попапа профиля на
  `--rt-size-60`, размеры каретки и маркера на `--rt-size-3` и `--rt-size-1`.
- Три габарита на ряд не легли и ушли владельцу вопросом до правки, а не замером после.
- Гейты графа, литералов и тёмной темы зелёные, `lint:styles` чистый.
- Этап 2. Сняты четыре ступени отступов — 6, 10, 12 и 28 пикселей; 57 строк в 19 файлах
  переведены на соседние. Объявлено токенов было 451, стало 447.
- Снятых имён не осталось нигде: ни в стилях, ни в коде, ни в витрине. Страница «Scales» читает
  шкалу отступов по приставке (`<Rows prefix="--rt-space-" />`), поэтому правки не потребовала.
- Гейты графа и литералов зелёные, `lint:styles` возвращает ноль.
- **Этап 3. Замер вскрыл больше, чем обещали и линия, и спек: у нейтрали ступень 900 была темнее
  ступени 950 — ряд шёл назад.** Тот же род дефекта, что RT-385 нашла у шкалы успеха. Кроме
  того, цветность прыгала от 0 до 0.061 (два семейства, серое и синеватое, соседними ступенями),
  а шаг светлоты гулял от 0.009 до 0.173.
- Первая попытка взять лестницу светлоты у бренда один в один провалилась: у бренда светлый
  конец сжат (50 = 0.9693), потому что ступень 500 у него — сам цвет марки. Нейтрали нужен свой
  диапазон, 0.985…0.20.
- Ряд посчитан: 12 ступеней, тон 264.4°, цветность 0.0234, шаг светлоты 0.0785. Объявлено
  токенов было 447, стало 438.
- Девять снятых ступеней разошлись по пятнадцати употреблениям, все в трёх файлах слоя
  оформления и все в навигации: 150→50, 250→200, 280→200, 350→300, 420→400, 510→600, 520→600.
  Ступени 450 и 550 не употреблялись вовсе и просто сняты.
- Гейт тёмной темы зелёный: ни одна пара, стоявшая выше порога, пересчётом вниз не ушла.
- Сборка `@rt-tools/ui-kit-v2` проходит, `lint:styles` возвращает ноль.
- **Этап 4. Рядов высот оказалось три, а не два.** Тело задачи и линия знали про кнопку
  (30/40/50/60/70) и поле (36/45/52); иконочная кнопка шла своим третьим — 28/36/44/52/60.
  Все три сведены на `32/40/48/56/64`.
- Шкала `--rt-control-height-*` получила пять ступеней вместо трёх. Кнопка и иконочная кнопка
  снялись со своих чисел, семейство полей уже ссылалось на шкалу и переехало вместе с ней.
- Высоты числом в стилях кита остались только у не-контролов: ячейка таблицы, шапка страницы,
  шаг степпера, полоски и значки. Правило литералов их не судит, и задача про них не говорит.
- Гейты графа и литералов зелёные, `lint:styles` ноль, спеки второго кита зелёные.
- **Этап 5. `tools/tokens-styles-allowlist.json` пуст, литералов в наборе ноль** — впервые за
  линию. Оверлей боковой панели взял `--rt-color-bg-overlay` (45% → 50%), тень окна уехала
  ступенью `--rt-shadow-dialog`, подложка окна — оттенком `--rt-overlay-navy-70`, считаемым от
  ступени обвязки, дорожка спиннера — `--rt-overlay-white-25` (30% → 25%), дорожка группы кнопок
  — приглушённой поверхностью.
- Заодно снята закомментированная строка в группе кнопок — мёртвый код, переживший переезд.
- **Найдено сверх списка: шесть прозрачных оттенков повторяли коды цвета своих ступеней** —
  `rgb(5 150 105 / 12%)` это `--rt-success-500`, `rgb(37 99 235 / 12%)` это `--rt-info-500`, и
  так все шесть. Переведены на `color-mix` от ступени.
- **Тон info и тон марки совпадали буквально — 262.9° обе.** Две палитры отличались только
  цветностью, а ступень 700 совпадала до кода цвета. Ряд info пересчитан на тоне 250°: ступени
  600 и 700 считаны от 500 шагом линейки бренда, все четыре берут порог на белом (4.92 / 6.60 /
  8.85:1).
- Гейты графа, литералов и тёмной темы зелёные; `lint:styles` ноль; спеки и сборка проходят.
- **Этап 6. Долг по устаревшим отношениям закрыт: у считалки есть ключ `--measure`,** он печатает
  все пары с отношениями, и причины переписаны по нему. Обновлено 16 записей, потом ещё 4.
- Пять пар с приглушённым текстом ушли вниз пересчётом (3.79 → 3.08:1). Перевод текста на ступень
  600 поднял их: шесть пар вышли выше порога и сняты из списка. Пар ниже порога было 51, стало 45.
- Гейт поймал снятие сам — «значится в списке, но в стилях этого больше нет»: список только
  убывает, и починка снимает и место, и строку.
- **Найдено сверх задачи: подпись неактивного сегмента группы кнопок в тёмной теме даёт 3.53:1.**
  Дорожка там красится ступенью напрямую, а не назначением, и пары в перечне нет — завести её
  нечем: перечень прямо не берёт имена, которых слой не объявляет. Дыра наследственная (до работы
  было 3.68:1), правкой усугублена на 0.15. Относится к RT-394.
- Полнота перечня по ролям проверена: у всех шести ролей есть и пара подписи на подложке, и пара
  цвета роли на поверхности. У отключённости второй нет, и это верно — пятого члена у неё нет.
- **Этап 7.** Витрина: страница шкал получила разделы габаритов и высот контролов, абзац про
  отступы переписан — половинных ступеней в шкале больше нет. Таблица родов в обзоре назвала две
  новые приставки. Описание иконочной кнопки перестало повторять ряд высот числами.
- Страницы цветов, назначений и тем правки не потребовали: они читают шкалы по приставке имени.
- Договорённость: `Q-4` закрыт ответом владельца и переехал в закрытые; правило про прозрачный
  оттенок расширено с бренда на любую ступень; названных вслух изменений вида стало девять
  вместо трёх; привязки `SC-UKV-41` и `SC-UKV-48` уточнены под сделанное.
- **`SC-UKV-41` назван непокрытым честно.** Он обещает проверку, которая отбивает литерал в
  слое назначений, а набор гейта литералов — стили компонентов. Литералы сняты, но вернуться им
  ничто не мешает; шесть прозрачных оттенков нашлись чтением, а не прогоном. То же дописано
  надстройкой в правило оформления, вместе со снятием устаревшей оговорки про размер числом.
- Раскладка потребовала пересборки пакета без кэша: `nx build` отдавал кэш, и `agent-kit:sync`
  отказывал «ресурсы правлены позже собранного» на чужом файле.
- `agent-kit:check` сходится с пакетом. `check:specs` и `check:docs` красные тем же наследством,
  что и до работы: по спеку оформления расхождений нет ни одного.
- **Этап 8. `SC-UKV-48` подтверждён замером в обеих темах со снятым переходом:** кнопка,
  иконочная кнопка, поле и числовое поле дают 32, 40 и 48 на `sm`, `md` и `lg`; кнопка и
  иконочная кнопка берут ещё 56 и 64. Ступени шкалы в обеих темах одни и те же, разошлись только
  цвета — значит, тема действительно переключилась, а не осталась светлой.
- Высота пункта навигации замерена: 30px у пункта с одной подписью и 40px у пункта со значком.
  Порог `min-height` стоит на 24px и не связывает ни тот, ни другой — обещанного уплотнения нет,
  как и говорило решение по ходу, только число оказалось 30, а не ожидавшиеся 33.
- Разошлось 371 кадр из 445 в 92 наборах. Главный переход цвета у всех 371 — один из трёх, и все
  три названы: подложка страницы `#fafafa → #f8faff` (366 кадров), тёмная поверхность
  `#1f1f1f → #232934` (3), полупрозрачная чернота поверх той же подложки `#7d7d7d → #7c7d7f` (2).
- Остальные переходы разобраны по всем 371 кадру и ложатся на ряд нейтрального
  (`#f6f7f9 → #d8e0f0`, `#e6e6e6 → #bfc7d6`), на приглушённый текст (`#7e7e7e → #606775`), на
  разведённый info (`#2563eb → #0173c6`, `#eff6ff → #eef6ff`) и на дорожку группы кнопок
  (`#eef0f4 → #d8e0f0`). Ни одного перехода мимо девяти названных.
- Геометрия сдвинулась у 63 кадров, и каждый сдвиг считается: иконочная кнопка +4 на ступень,
  поле `md` −5 (45 → 40) у фильтра, отступы округлились у календаря, переписки и панели.
- Глазами просмотрены восемь семей — иконочная кнопка, календарь, уведомление, группа кнопок,
  таблица, поле, спиннер, панель действий; в каждой видно ровно то, что обещал разбор чисел.
- **Ловушка кадров пороговой ширины подтвердилась:** прогон назвал 69 эталонов «не имеющими
  истории», а истории у всех 69 живые. Пересъёмка обновила 440 кадров при 371 разошедшемся —
  разница ровно эти 69.
- Повторный прогон зелёный: 445 кадров, осиротевших нет. `check:all` зелёный.
- Главная влита. Конфликт один — шапка разложенного правила оформления: взята сторона главной,
  пакет пересобран без кэша, `agent-kit:sync` разложил правило заново, и надстройка слилась
  всеми шестью разделами. `agent-kit:check` сходится с пакетом 0.5.3.
- Главная принесла в код только пакет правил: `git diff` от точки расхождения по `projects/`
  называет три файла, все под `agent-kit`. Кадры витрины после слияния перепроверены — зелёные.
- `check:docs` позеленел сам: путь, которого не хватало, главная поправила. `check:dupes` падает
  на отсутствующем `node_modules/@rt-tools/utils` — то же самое на главной, состояние машины.
- PR #412 открыт от машинной учётной записи, ревьювер владелец, задача переведена в разбор.
  Сверка очереди называет те же два наследственных расхождения — PR #404 и папку RT-272.
