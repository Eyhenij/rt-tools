# Привязка — локальное значение шаблона

- **Директива объявляет имя, а показом не распоряжается** — `let.directive.ts:constructor`
- **Представление создаётся один раз, а новое значение приезжает в него** — `let.directive.ts:constructor`
- **Имя доступно и как значение по умолчанию, и под именем директивы** — `let.directive.ts:IRtLetContext`
- **Тип значения доезжает до содержимого** — `let.directive.ts:ngTemplateContextGuard`
- **Смена значения перерисовывает содержимое, даже когда оно объявлено перерисовкой по требованию** — `let.directive.ts:constructor`

Пути от корня пакета: `projects/core/src/lib/directives/`.

- **Сценарий** — Тест
- **`SC-CR-01`** — `let.directive.spec.ts`
- **`SC-CR-02`** — `let.directive.spec.ts`
- **`SC-CR-03`** — `let.directive.spec.ts`
- **`SC-CR-04`** — `let.directive.spec.ts`
