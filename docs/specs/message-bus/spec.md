# Приёмник груза

**Статус:** действует · **Ревизия:** 2026-08-15 · **Префикс сценариев:** `SC-MB`
**Зависимости:** `agent-kit` (что дерево о себе знает и что уезжает наружу)
**Законы:** `verifiability`, `code-structure`, `lib-imports`, `delivery`, `observability`,
`frontend-application`, `reuse-first`, `lists`, `entity-editing`, `entity-models`, `navigation`
**Процедуры:** нет — операции объявлены контроллерами приёмника

## Зачем

Пакет правил стоит в нескольких деревьях, и то, чем в нём пользуются, известно только каждому
дереву про себя. Договорённость `docs/specs/agent-kit/proposed/feedback-loop/` описала, что
дерево о себе знает и что из этого уезжает наружу; куда именно уезжает — она не описывает
вовсе, и отправке обращаться некуда.

Открытая очередь работ приёмником быть не может: груз говорит о рабочих привычках команды —
чем пользуются, обо что спотыкаются, сколько раз признавали промах. Отсюда закрытая своя
служба.

Эта договорённость называет приёмник: что он принимает, чем отличает одно дерево от другого,
что делает с повторной отправкой и где обязан отказать вместо молчания.

Она же называет вторую половину службы — чтение принятого. Прочитать груз запросом к базе с
самого узла может только тот, у кого есть доступ к узлу, а служба уезжает в интернет: знающая
одни лишь токены деревьев, она отдавала бы принятое всякому, кто дошёл до её адреса, и не
отвечала бы на вопрос, кто читал. Отсюда вход человека и админка: чем человек представляется
приёмнику и что он видит после входа. Разделов три — по роду груза.

## Терминология

| Термин             | Что это                                                                                                |
| ------------------ | ------------------------------------------------------------------------------------------------------ |
| Приёмник           | Закрытая служба, принимающая груз запросом. Своя, а не чужая очередь работ                             |
| Груз               | То, что уезжает прогоном отправки: сводка со снимком надстроек, предложения, разборы происшествий      |
| Род груза          | Одно из трёх: сводка, предложение, разбор происшествия. У каждого своя операция приёма                 |
| Дерево             | Запись о репозитории, от которого приезжает груз. Деревья различаются признаком, а не адресом          |
| Токен дерева       | То, чем дерево представляется приёмнику. Приёмник держит только хеш; сам токен печатается один раз     |
| Отзыв токена       | Пометка о том, что токен больше не принимается. Токен не удаляется: по нему читается прежний груз      |
| Запись месяца      | Свод одного дерева за один календарный месяц. Одна на пару «дерево — месяц»                            |
| Прогон             | Одна отправка с дерева: до трёх запросов подряд, по одному на род груза                                |
| Снимок надстроек   | Состояние дерева на момент прогона: какие разделы каких ресурсов пакета оно правит и чего не разложило |
| Версия схемы груза | Номер формата запроса приёма. Меняется, когда меняется состав полей самого груза                       |
| Версия схемы строк | Номер формата строки наблюдения на дереве. Их в одной сводке встречается несколько                     |

Вторая половина словаря — про чтение принятого:

| Термин              | Что это                                                                                |
| ------------------- | -------------------------------------------------------------------------------------- |
| Админка             | Приложение, которым человек читает принятый груз. Правок груза в ней нет вовсе         |
| Учётная запись      | Имя и пароль одного человека. Заводится командой строки запуска                        |
| Вход                | Состояние, в котором приёмник знает, кто спрашивает. Живёт сроком и обрывается выходом |
| Срок входа          | Время, после которого вход перестаёт приниматься и человек представляется заново       |
| Раздел              | Экран админки со своим адресом. В раздел ведёт пункт меню                              |
| Список              | Страница раздела: строка на запись, тулбар над ней и пагинация под ней                 |
| Панель подробностей | Панель с одной записью целиком, выезжающая по нажатию на строку                        |
| Отбор               | Условие, сужающее список. Стоит в тулбаре и виден на экране                            |
| Страница списка     | Отрезок списка, приезжающий одним запросом. Номер страницы и размер называет запрос    |
| Выборка             | Страница, размер, сортировка и отбор вместе. Живёт в адресе раздела                    |

### Как это называется в интерфейсе

Поверхность приёмника — операции запроса и команды строки запуска; интерфейс есть у админки.

| В договорённости          | В строке запуска и на экране                             |
| ------------------------- | -------------------------------------------------------- |
| заведение дерева          | `message-bus tree:add <имя> <признак>`                   |
| выдача нового токена      | `message-bus tree:token <имя>`                           |
| отзыв токена дерева       | `message-bus tree:revoke <имя>`                          |
| список деревьев           | `message-bus tree:list`                                  |
| заведение учётной записи  | `message-bus account:add <имя>`                          |
| смена пароля              | `message-bus account:passwd <имя>`                       |
| отключение учётной записи | `message-bus account:disable <имя>`                      |
| список учётных записей    | `message-bus account:list`                               |
| вход                      | экран «Вход»: имя и пароль                               |
| выход                     | пункт «Выйти» в шапке                                    |
| разделы                   | «Разборы происшествий», «Предложения», «Сводки деревьев» |
| панель подробностей       | панель, выезжающая по нажатию на строку                  |

## Правила

**Форма груза.**

- **Форма груза объявлена одним местом, и приёмник её не переобъявляет.** Типы едут из общей
  либы, куда их кладёт эта работа, а пакет-отправитель берёт их оттуда же: своя копия чужого
  типа расходится с оригиналом молча, а компилируется из них только одна.
- **Груз каждого рода принимается своей операцией.** Сводка, предложение и разбор устроены
  по-разному, и общая операция «принять что-нибудь» перекладывала бы разбор формы на приёмник.
- **Приёмник не разбирает содержимого сводки.** Он проверяет обязательные поля своего рода, а
  счётчики и снимок надстроек хранит как приехали: разбирает их тот, кто читает, а приёмник,
  знающий поля наизусть, отказывал бы на каждой правке пакета.
- **Версия схемы груза обязательна, а версии схемы строк наблюдения — часть содержимого.**
  Первая говорит, чем разбирать запрос, и без неё приём отказывает; вторые приёмник не судит
  вовсе.
- **Груз незнакомой версии схемы принимается и помечается ею.** Отказ терял бы отрезок целиком,
  а дерево о новой редакции пакета узнаёт не сразу и не всегда.
- **Груз тяжелее предела не принимается.** Предел объявлен настройкой приёмника: без него
  запрос произвольного размера кладёт службу на самом малом узле. Вес приехавшего называется
  тогда, когда запрос объявил его сам, — обрезанное тело своего веса не знает.

**Дерево и его токен.**

- **Дерево представляется токеном, а приёмник держит только его хеш.** Утёкшая выкатка или
  снятый дамп базы доступа не дают: по хешу токен не восстанавливается.
- **Токен печатается один раз, при выдаче.** Второй раз показать его неоткуда; потерянный не
  восстанавливается, а замещается новым.
- **У дерева годен один токен: новый замещает прежний, помечая его отозванным.** Иначе список
  деревьев говорит о состоянии токена в единственном числе, а токенов у дерева несколько.
- **Отозванный токен перестаёт приниматься сразу.** Дерево узнаёт об этом отказом отправки, а
  не молчанием: молчаливый приём выброшенного груза выглядит работающей отправкой.
- **Отозванный токен не удаляется.** Груз, приехавший по нему, остаётся читаемым: удалив
  токен, приёмник потерял бы и то, кем присланы прежние записи.
- **Признак дерева называется при заведении, а не берётся из первого груза.** Дерево считает
  его у себя от адреса своего репозитория; приёмник, узнавший признак из груза, принял бы за
  своё то, что прислали.
- **Признак дерева в грузе сверяется с деревом токена.** Иначе дерево дописало бы чужую запись
  месяца, назвавшись в грузе соседом.
- **Заведение и отзыв токена дерева операцией запроса не делаются.** Токен дерева зовёт только
  приём груза; выдавать и отзывать токены — дело команд, которые ходят к хранилищу напрямую.

**Запись месяца.**

- **Запись месяца одна на пару «дерево — месяц».** Нашлась — обновляется, не нашлась —
  заводится. Новая запись на каждый прогон рассыпала бы месячную картину дерева по прогонам.
- **Сводка последнего прогона замещает прежнюю целиком.** Счётчики, снимок надстроек и
  невыбранное берутся из последнего прогона: отрезок сводки короче месяца, окна прогонов
  перекрываются, и сложение завышало бы числа молча, а повтор после обрыва связи — удваивал.
- **Предложения и разборы происшествий копятся, а не замещаются.** Сводка отвечает на вопрос
  «как дела сейчас», эти два рода — «что случилось за месяц».
- **Запись месяца помнит время последнего прогона.** Без него список деревьев не отвечает на
  вопрос, отчитывается ли дерево вообще, а молчащее дерево неотличимо от исправного.
- **Месяц берётся по часам приёмника, во всемирном времени.** Деревья стоят в разных поясах, и
  граница месяца от отправителя кладёт две записи на один месяц; местное время узла переезд
  узла не переживает.
- **Наблюдения прошлого месяца, приехавшие первым прогоном нового, ложатся в новый.** Отрезок
  сводки — несколько последних дней, и первого числа он в основном о прошлом месяце. Резать
  его приёмник не может: он видит числа, а не строки, из которых те собраны.
- **Одновременный приезд двух прогонов одного дерева разрешает хранилище, а не проверка
  чтением.** Проигравший гонку перечитывает запись и обновляет её, а не отказывает: отказ на
  ожидаемом случае терял бы прогон.
- **Пара «дерево — месяц» уникальна.** Ограничение хранилища, а не проверка в коде: два прогона
  из одного дерева приезжают одновременно, и проверка чтением их не разведёт.

**Что делает приёмник с недоехавшим.**

- **Груз, не прошедший проверку формы, отбивается целиком в пределах своей операции.**
  Межзапросной сделки не бывает: роды едут тремя запросами, и «принято два из трёх» — законное
  состояние, о котором отправитель говорит владельцу сам.
- **Все записи одной операции ложатся вместе или не ложатся вовсе.** Пять предложений, из
  которых упало третье, оставили бы запись месяца в состоянии, которого не было ни до, ни
  после.
- **Предложения и разборы, приехавшие раньше сводки, заводят запись месяца сами.** Порядок
  запросов прогона приёмник не назначает, а отказ «сводки ещё не было» превратил бы порядок в
  скрытое требование.
- **Отказ приёмника называет причину дереву, а не подробности своего устройства.** Дерево
  печатает эту причину владельцу, и «внутренняя ошибка» в ней означает потерянный прогон.
- **Каждый отказ записывается в журнал приёмника с родом груза и признаком дерева.** Ни токена,
  ни текста груза там нет: журнал читают, чтобы понять, что сломалось, а не чтобы прочитать
  чужое.
- **Приёмник ничего не отдаёт без токена дерева.** Открытых операций у него нет вовсе, кроме
  пробы живости.
- **Проба живости отвечает только тогда, когда хранилище отвечает тоже.** Служба считается
  поднятой, когда она выполнила задание, а не когда сообщила о готовности.
- **Проба живости не называет ни редакции, ни состава — ни в теле, ни в заголовках ответа.**
  Всё, кроме ответа «поднята», — подсказка тому, кто ищет вход.

**Разборы происшествий и предложения.**

- **Разбор происшествия приезжает текстом целиком.** Приёмник закрытый и свой; шапка без
  механизма промаха ничего не объясняет, а правило из разбора выводится по механизму.
- **Проверка на адрес дерева разбор происшествия не накрывает.** Разбор по устройству называет
  файлы дерева, где промах случился; накрытая проверка отбивала бы каждую отправку этого рода.
  Требование стоит к отправителю: проверка живёт у него.
- **Разбор опознаётся именем своего файла на дереве.** Заголовок правится вместе с текстом, а
  имя файла переживает правку — по нему повторно приехавший разбор обновляет прежний.
- **Повторно приехавший разбор обновляет прежний, а не заводит второй.** Он правится на дереве
  после того, как уехал, и второй экземпляр читался бы как второе происшествие.
- **Разбор, исчезнувший на дереве, у приёмника остаётся.** Приёмник принимает, а не следит:
  удаление по молчанию отправителя стёрло бы записи первого же дерева, переставшего слать.
- **Предложение опознаётся признаком — хешем своего текста, а не текстом в ограничении.** Тексты
  предложений идут в килобайтах, а уникальность по такому полю упирается в предел размера строки
  индекса: вставка отказала бы хранилищем на первом длинном предложении. Признак считает
  приёмник.
- **Признак предложения уникален в пределах дерева, а не записи месяца.** Дерево шлёт файл
  предложений целиком, и повтор приезжает в любом месяце: граница месяца от него не защищает, а
  по двум записям не видно, что это одно и то же.
- **Уже лежавшее пропускается, а не отбивает запрос.** Повтор здесь — правило, а не промах
  отправителя: отправитель узнаёт своё уехавшее по отметке в файле, а файл живёт в рабочем
  дереве и теряется при переключении ветки.
- **Ответ приёма на предложения называет, сколько записей легло и сколько уже лежало.** Иначе
  отправитель печатает «уехало» и в том случае, когда нового не уехало ничего, а человек читает
  это как принятое предложение.
- **Предложение остаётся при своей записи месяца.** Уникальность держит дерево, а связь — нет:
  по записи месяца видно, в каком месяце предложение приехало впервые.
- **Предложение хранит ресурс, к которому относится.** Ради счёта «сколько деревьев правят это
  место» эпик и заведён, а без ресурса он не берётся ниоткуда.

**Вход и учётная запись.**

- **Груза без входа не отдаёт ни одна операция.** Отдачу самого приложения вход не сторожит:
  без груза оно пусто, а закрытая статика требует проксировщика, которого у работы нет.
- **Человек представляется именем учётной записи и паролем.** Второго способа входа нет: почты у
  службы нет вовсе, а общий на всех токен не отвечает на вопрос, кто читал, и его смена выбивает
  сразу всех.
- **Браузер несёт вход кукой, недоступной скриптам, и посылает её только своему адресу.** Значение
  входа, лежащее там, куда дотягивается скрипт страницы, утекает вместе с любой чужой строкой на
  ней; кука с запретом чтения скриптом и с запретом отправки по чужому переходу не утекает и не
  подделывается чужой страницей.
- **Приёмник держит только хеш пароля.** Снятый дамп базы доступа не даёт: по хешу пароль не
  восстанавливается.
- **Отказ входа не называет, что именно не сошлось, и отвечает за то же время.** Разные ответы на
  неизвестное имя и на неверный пароль перебирают имена учётных записей за того, кто их подбирает;
  разное время ответа отвечает на тот же вопрос молча, поэтому неизвестное имя сверяется с
  заглушкой хеша, а не отвергается сразу.
- **Пароль не попадает ни в журнал, ни в ответ, ни в адрес.** Журнал читают, чтобы понять, что
  сломалось; пароль, попавший в строку журнала, живёт там столько же, сколько сам журнал.
- **Неудачная попытка входа записывается в журнал с именем учётной записи.** Иначе подбор пароля
  неотличим от тишины, а первым признаком становится чужое чтение груза.
- **Неудачные попытки подряд удлиняют ответ.** Служба стоит в интернете, и перебор, отвечающий с
  той же скоростью, ограничен только сетью. Учётная запись при этом не запирается: запертую
  запись отпирать некому — админки учёток нет.
- **Вход живёт сроком и по его истечении перестаёт приниматься.** Человек представляется заново;
  вечный вход переживает и потерянную машину, и ушедшего человека.
- **Входов у одной записи бывает несколько, и выход обрывает тот, которым пришли.** Два браузера
  — два входа; выход, обрывающий все, выбивает человека там, где он ничего не делал.
- **Токен дерева админки не открывает, а вход человека не открывает приёма груза.** Два способа
  представиться живут порознь: иначе утёкший с дерева токен читает весь груз всех деревьев.
- **Каждая операция объявляет свой способ доступа явно.** Умолчание «открыто, пока не закрыли»
  открывает наружу всякую новую операцию, о которой автор не подумал; умолчание здесь обратное, и
  открытость называется в самой операции.
- **Учётная запись заводится, меняет пароль и отключается командой строки запуска.** Заведение из
  веба — это ещё экран, право на него и вопрос, кем заводится первая запись; команды у приёмника
  уже есть, а утёкший пароль иначе закрывать нечем вовсе.
- **Имя учётной записи занято одним человеком, и в нём не различается регистр.** Команда
  отказывает на занятом имени вместо того, чтобы завести вторую запись: `Иван` рядом с `иван`
  означает, что вход перестал отвечать, кто именно вошёл.
- **Отключённая запись входа не заводит, а её прежние входы перестают приниматься.** Отключение,
  оставляющее живой вход, значит «нельзя войти снова», а не «доступ закрыт».
- **Служба говорит при старте, что учётных записей нет ни одной.** Свежий узел иначе выглядит
  поломкой входа: любая пара отбивается тем же отказом, и отличить «ты ошибся» от «заводить
  некого» нечем.

**Что админка показывает.**

- **Админка читает груз и не правит его.** Приёмник принимает, админка читает: правка принятого
  расходилась бы с деревом-источником молча, а следующий прогон её затирал.
- **Пункт меню заводится вместе со своим экраном.** Пункт, ведущий в никуда, читается как
  поломка, а не как обещание будущего раздела.
- **Раздел открывается своим адресом — прямой ссылкой и после перезагрузки.** Раздел, живущий
  вкладкой внутри чужого экрана, ссылкой не передаётся и перезагрузку не переживает.
- **Выборка живёт в адресе раздела.** Страница, размер, сортировка и отбор стоят в адресе: иначе
  перезагрузка на второй странице отобранного списка возвращает на первую, а ссылка на увиденное
  не передаётся.
- **Человек, отправленный с адреса раздела на вход, после входа попадает туда, куда шёл.** Иначе
  ссылка на раздел работает только у того, кто уже вошёл.
- **Подписи экранов берутся из словаря приложения, а не пишутся в разметке.** Оттуда же берутся
  подписи, которые рисует кит: иначе рядом с русскими заголовками встаёт английское умолчание
  кита, и видно это только на собранном экране.

**Списки груза.**

- **Каждый раздел собран одним и тем же списочным экраном.** Заголовок, тулбар, таблица и
  переключатель страниц у трёх разделов одни; переписанные заново, они расходятся молча.
- **Каждая запись показана отдельной строкой, а каждое её свойство — своим столбцом.**
- **Нажатие на строку открывает панель подробностей.** Требование владельца: собранное иначе не
  принимается. Действий над записью в строке нет — админка ничего не правит.
- **Панель показывает запись целиком, а строка списка — нет.** Разбор приезжает текстом целиком, и
  страница, несущая тексты всех своих строк, растёт весом без предела.
- **Текст, приехавший с дерева, показывается текстом, а не исполняется как разметка.** Приёмник
  содержимого груза не разбирает, и дерево с токеном иначе получало бы исполнение своего текста в
  браузере того, кто вошёл.
- **Список приезжает страницами, а не целиком.** Число записей растёт с каждым деревом, и
  «прочитать всё» упирается в вес ответа раньше, чем в терпение читающего.
- **Размер страницы назван умолчанием и ограничен сверху.** Иначе правило о страницах обходится
  одним запросом со страницей в сто тысяч строк — тот же неограниченный вес ответа, только через
  параметр.
- **Страница за пределом списка отвечает пустой страницей и общим числом, а не отказом.** Список,
  укоротившийся между двумя запросами, — обычное дело; отказ на это читается как поломка.
- **Порядок по умолчанию — свежие сверху, и он назван на экране.** У равных значений порядок
  разводится вторым ключом: иначе одна и та же запись видна на двух страницах подряд, а соседняя
  не видна ни на одной.
- **Человек меняет порядок заголовком столбца и видит, какой порядок применён.**
- **Состав и порядок столбцов выбирает человек, и выбор сохраняется.** Ключ, под которым он
  сохранён, — свой у каждого раздела.
- **Отбор по дереву называет деревья именами, а не признаками.** Признак — хеш адреса
  репозитория, и по нему человек не узнаёт своё дерево; отбирается при этом признак.
- **Отбор переживает переход на другую страницу списка.** Сброшенный отбор замечают не сразу и
  читают чужие строки как свои.
- **Ответ, догнавший свой список после следующего запроса, не показывается.** Иначе строки
  прежнего отбора встают под новым — и это не отличить от груза, приехавшего только что.
- **Пока список читается, на месте строк видно, что идёт чтение.** Пустую область не отличить от
  списка, в котором ничего нет.
- **Пустой список объясняет, почему он пуст.** Отбор, не давший ни строки, и дерево, не
  приславшее ни одной записи, — разные ответы, и второй означает исправную службу.
- **Не прочитавшийся список говорит почему, и попытка повторяется одним действием.** Пустой экран
  после отказа службы читается как «записей нет» и уводит от настоящей причины.
- **Закрытая панель возвращает список в том же состоянии.** Страница, отбор и сортировка после
  закрытия те же: сброс на первую страницу превращает чтение десяти разборов подряд в десять
  поисков.
- **Записи, которой нет, панель не рисует пустой.** Ссылка на запись переживает её появление и
  исчезновение, и пустая панель читается как поломка чтения.
- **Вошедший видит груз всех деревьев.** Прав внутри админки нет: учётная запись принадлежит
  службе, а не дереву, и отбор по дереву сужает показанное, а не доступ.
- **Время показывается в поясе того, кто смотрит, а хранится во всемирном.** Пояс, выбранный
  молча, сдвигает порядок «свежие сверху» на границе суток.

**Отказ, видимый человеку.**

- **Отказ службы называет человеку номер обращения, и тот же номер стоит в журнале.** Иначе о
  поломке рассказывают словами «не работает», и найти её в журнале нечем.
- **Ожидание ответа ограничено сроком.** Запрос, висящий без предела, выглядит так же, как
  работающая служба, и человек ждёт вместо того, чтобы повторить.
- **Имя учётной записи уникально по приведённому виду.** Ограничение хранилища, а не проверка
  чтением: две команды заведения, запущенные подряд, проверку чтением не разведут.
- **Пароль в хранилище лежит только хешем.** Ни колонки под сам пароль, ни его копии в журнале
  нет ни на одном пути.
- **Админка читает те же записи, что кладёт приём.** Второй копии под чтение нет: расходящаяся
  копия показывала бы вчерашний груз как сегодняшний.

## Что не входит

- **Заведение учётных записей из интерфейса и экран учёток.** Записи заводит команда строки
  запуска; человека без доступа к узлу завести нечем.
- **Восстановление пароля почтой.** Почты у службы нет вовсе.
- **Роли и права внутри админки.** Слово владельца: вошедший видит всё.
- **Требования к самому паролю.** Записи заводит тот, у кого есть доступ к узлу.
- **Выкатка, сертификат, обратный проксировщик и арендованный узел.** Приёмник поднимается
  контейнерами на машине владельца, и токен ходит по открытому HTTP.
- **Свод по нескольким деревьям и графики.** Слово владельца: показываются списки, а свод
  назначается тогда, когда деревьев станет три.
- **Правка принятого груза.** Приёмник принимает, админка читает; правок нет ни у той, ни у
  другой стороны.
- **Признак прогона и защита от повторной отправки.** Замещение сводки делает повтор
  безвредным, а предложения и разборы отбираются по признаку и имени.
- **Оповещения о приехавшем грузе.** Ни почты, ни оповещений: груз читают, когда за ним
  приходят.
- **Чистка старых записей по сроку.** Приёмник свой и малый; срок хранения назначается тогда,
  когда объём станет виден.
- **Вторая тема оформления.** Кит умеет обе; вторая заводится тогда, когда её попросят.

## Контракт

Поверхность — операции HTTP и восемь команд строки запуска. Дерево представляется заголовком
`X-Tree-Token`, человек — входом; одно вместо другого не принимается. Без токена любая операция
приёма, кроме пробы живости, отказывает; без входа не отдаёт груза ни одна операция чтения.

| Операция                     | Что делает                                                            |
| ---------------------------- | --------------------------------------------------------------------- |
| POST /api/intake/summary     | заводит или обновляет запись месяца сводкой последнего прогона        |
| POST /api/intake/proposals   | кладёт к записи месяца предложения, которых у дерева ещё не было      |
| POST /api/intake/postmortems | заводит или обновляет разборы происшествий дерева по именам их файлов |
| GET /api/health              | отвечает, что служба поднята и хранилище отвечает                     |
| POST /api/auth/login         | принимает имя и пароль, заводит вход                                  |
| POST /api/auth/logout        | обрывает тот вход, которым пришли                                     |
| GET /api/auth/session        | говорит, кто вошёл и жив ли вход                                      |
| GET /api/trees               | отдаёт деревья именами и признаками — для отбора                      |
| GET /api/postmortems         | страница разборов без текста целиком                                  |
| GET /api/postmortems/:id     | один разбор с текстом целиком                                         |
| GET /api/proposals           | страница предложений                                                  |
| GET /api/proposals/:id       | одно предложение целиком                                              |
| GET /api/summaries           | страница записей месяца без сводки целиком                            |
| GET /api/summaries/:id       | запись месяца со сводкой целиком                                      |

Страница списка запрашивается номером, размером, порядком и отбором по дереву. Ответ несёт
строки и общее число записей: без него пагинация не знает, сколько страниц.

Обязательные поля по родам груза:

| Род груза   | Что обязано быть в запросе                                                  |
| ----------- | --------------------------------------------------------------------------- |
| сводка      | версия схемы груза, признак дерева, счётчики, снимок надстроек, невыбранное |
| предложения | версия схемы груза, признак дерева, список: текст, адрес, ресурс            |
| разборы     | версия схемы груза, признак дерева, список: имя файла, текст целиком        |

Что внутри счётчиков и снимка — приёмник не судит.

### Коды отказов

Не применимо: приёмник отвечает кодом ответа HTTP, а не именованными кодами домена. Где приём
обязан отказать вместо молчания:

| Что случилось                            | Код   | Что говорит                                                 |
| ---------------------------------------- | ----- | ----------------------------------------------------------- |
| токена дерева в запросе нет              | `401` | что операция требует токен дерева                           |
| токен не найден или отозван              | `401` | что токен не принят; какой именно — не называется           |
| версия схемы груза не названа            | `400` | что версия обязательна                                      |
| обязательного поля своего рода нет       | `400` | какого поля не хватает и у какого рода                      |
| признак дерева не сошёлся с токеном      | `400` | что признак в грузе принадлежит другому дереву              |
| груз тяжелее предела                     | `413` | предел, а вес — когда запрос объявил его сам                |
| имени или пароля в запросе нет           | `400` | какого поля не хватает                                      |
| номер или размер страницы не число       | `400` | какой параметр не разобран и каковы его границы             |
| имя или пароль не сошлись                | `401` | что пара не принята; что именно не сошлось — не говорит     |
| входа нет, он просрочен или запись снята | `401` | что операция требует входа                                  |
| операции админки предъявлен токен дерева | `401` | то же, что и без входа                                      |
| запрошенной записи нет                   | `404` | что записи нет                                              |
| хранилище недоступно                     | `503` | что не принят груз или не прочитано, и номер обращения      |
| остальное                                | `500` | что груз не принят; устройство приёмника не пересказывается |

Принято — `201` у заведённой записи месяца и `200` у обновлённой; в обоих случаях приёмник
называет месяц и дерево, чтобы дерево напечатало это владельцу. У предложений он называет ещё
два числа — сколько записей легло и сколько приехало повторно:

| Поле ответа | Что значит                                                   |
| ----------- | ------------------------------------------------------------ |
| added       | сколько предложений легло записями этим запросом             |
| known       | сколько приехало повторно: их признак у этого дерева уже был |

## Данные

Хранилище — Postgres, схема правится миграциями. Колонок и индексов договорённость не
называет: они в схеме, здесь — правило, которое ограничение выражает.

| Сущность            | Что в ней                                                                           |
| ------------------- | ----------------------------------------------------------------------------------- |
| Дерево              | признак, читаемое имя, дата заведения                                               |
| Токен дерева        | хеш, дата выдачи, пометка отзыва с датой; дерево у токена одно                      |
| Запись месяца       | дерево, месяц, счётчики, снимок надстроек, невыбранное, версия схемы, время прогона |
| Предложение         | запись месяца, дерево, текст, признак, адрес, ресурс, дата приезда                  |
| Разбор происшествия | дерево, имя файла, текст целиком, дата приезда, дата обновления                     |
| Учётная запись      | имя, хеш пароля, признак отключения, дата заведения, время последнего входа         |
| Вход                | учётная запись, время заведения, время истечения, пометка о выходе                  |

- **Пара «дерево — месяц» уникальна.** Ограничение хранилища, а не проверка в коде: два прогона
  из одного дерева приезжают одновременно, и проверка чтением их не разведёт.
- **Пара «дерево — имя файла разбора» уникальна.** По ней повторно приехавший разбор обновляет
  прежний.
- **Пара «дерево — признак предложения» уникальна.** Ею держится отбор уже приехавшего, и держит
  её хранилище: два прогона одного дерева приезжают одновременно, и прочитанное первым устареет
  раньше, чем он допишет своё.
- **Дерево у предложения стоит своей колонкой, а не читается через запись месяца.** Ограничение
  хранилища читает колонки одной строки, и уникальность по полю связанной записи не объявляется.
- **Снимок надстроек и счётчики хранятся как приехали.** Приёмник их не разбирает, и колонки
  под каждое поле разъезжались бы с пакетом при первой же его правке.

## Экраны и состояния

У приёмника экранов нет; экраны есть у админки.

| Экран                | Состояния                                                                         |
| -------------------- | --------------------------------------------------------------------------------- |
| Вход                 | пустая форма · отправка · отказ пары · отказ службы                               |
| Разборы происшествий | чтение · страница со строками · пусто · пусто по отбору · отказ чтения с повтором |
| Предложения          | те же пять состояний                                                              |
| Сводки деревьев      | те же пять состояний                                                              |
| Панель подробностей  | чтение · запись целиком · записи нет · отказ чтения                               |

Таблицу состояний не сверяет ничто: состояние, в которое код не умеет приходить, читается здесь
описанием работающего. Подтверждаются состояния сценариями и замером в браузере.

## Сквозные требования

### Локали

Язык один — русский. Приёмник читают владелец и исполнитель, а подписи админки лежат в словаре
приложения, а не в разметке. Оттуда же берутся подписи кита: у него свой словарь на восемь
языков, и без этого пагинация и пустое состояние выходят по-английски рядом с русскими
заголовками. Второй язык дешевле добавить потом.

### SEO

Не применимо: служба закрытая, всё, кроме экрана входа, стоит за входом, и поисковикам не
показывается ничего.

### Мобильная раскладка

Строка списка на узком экране показывается карточкой — это вход таблицы кита, а не своя
разметка. Экран, собранный мимо неё, узкую раскладку теряет молча.

### Мультиобъектность

Деревьев много, и каждое видит только свой груз: операция приёма работает от токена, а не от
переданного признака дерева. Признак, названный в грузе, с деревом токена сверяется, и
расхождение отбивает приём.

Вошедший человек при этом видит груз всех деревьев: учётная запись принадлежит службе, а не
дереву, и отбор по дереву сужает показанное, а не доступ.

## Решения

- **Приёмник — закрытая своя служба.** Решение владельца. Очередь работ репозитория пакета
  открыта всему свету. Отвергнуто: приватный репозиторий вместо службы — роль, разбиравшая
  нужность, предлагала именно это, владелец довод прочитал и решение подтвердил.
- **Три ветки, а не одна.** Решение владельца. Откатывается порознь: приёмник без админки
  осмыслен, админка без приёмника неисполнима. У каждой ветки своя задача в очереди работ, и
  заводится она перед своей веткой. Отвергнуто: одна ветка на всё; и две ветки.
- **Приём всех трёх родов груза в первой ветке.** Роды отличаются формой записи, но не тем, как
  приезжают. Отвергнуто: только сводка в первой ветке — тогда веток становится четыре.
- **Слои семьи бэкенда — `api`, `data-access`, `feature`, `util`.** Решение владельца, списано с
  дерева-образца дословно; закрывает `Q-2` эпика. Тег и алиас выведены из пути либы, как
  того требует сверка раскладки. Отвергнуто: те же слои со своими именами тегов.
- **Спеки новых приложений и либ гоняет Vitest.** Решение владельца. Пакеты в `projects/`
  остаются на Jest, граница проходит по каталогу. Отвергнуто: Jest везде.
- **Разбор происшествия уезжает текстом целиком.** Решение владельца; закрывает `Q-14`
  договорённости пакета. При вливании переписываются две статьи спека отправителя: о том, что
  проверка на адрес дерева накрывает весь груз, и о выдаче токена из админки. Отвергнуто: одна
  шапка; и текст с проверкой на адрес дерева.
- **Сводка последнего прогона замещает прежнюю.** Решение владельца. Окна прогонов
  перекрываются, и сложение завышало бы числа молча. Отвергнуто: сложение счётчиков; и хранение
  прогонов порознь — оно отменяет одну запись на пару «дерево — месяц».
- **Закон о наблюдаемости раскладывается этой веткой, правило слоя API остаётся снятым.**
  Решение владельца. Журнал отказов нужен с первого живого прогона; правило слоя API требует
  процедур Connect, которых здесь не будет.
- **Первый токен выдаёт команда самого приложения.** Решение владельца. Она же останется после
  админки — для первой учётной записи. Отвергнуто: начальный засев Prisma.
- **Признак дерева называется доводом команды заведения — допущение исполнителя.** Довод: иначе
  приёмник узнаёт признак из груза и принимает за своё присланное. Отменяется одной правкой.
- **Разбор опознаётся именем файла — допущение исполнителя.** Заголовок правится вместе с
  текстом, имя файла переживает правку. Открыто как `Q-19`.
- **Признак предложения — хеш его текста, а область уникальности — дерево.** Довод исполнителя:
  уникальность по полю в килобайты упирается в предел размера строки индекса, а граница месяца
  от повтора не защищает — он приезжает в любом месяце. Закрывает `Q-16`. Отвергнуто:
  уникальность по паре «дерево — текст»; признак, назначаемый отправителем — его пришлось бы
  хранить в файле предложения, то есть там же, где живёт отметка, теряющаяся при переключении
  ветки.
- **Ответ приёма расширяется, а не заводится вторым.** Отправитель уже читает этот ответ, и
  второй запрос ради счёта повторов добавил бы прогону ещё одно место, где он рвётся.
- **Вход паролем, а учётные записи заводит команда строки запуска.** Служба уезжает в интернет —
  вход нужен; заведение из веба — это экран, право и вопрос о первой записи. Отвергнуто: заведение
  записей из интерфейса и экран учёток. Цена: человека без доступа к узлу завести нечем.
- **Три списочных экрана, а не один.** Слово владельца на расхождение, принесённое разбором:
  разборы, предложения и сводки одной работой. Отвергнуто: один экран с окупаемостью каркаса на
  нём. Цена: ветка длиннее, и откат уносит все три.
- **Вид списочного экрана — требование приёмки.** Слово владельца: таблица с тулбаром и
  пагинацией, нажатие на строку открывает панель подробностей. Собранное иначе не принимается.
- **Сквозной набор сценариев заводится этой работой.** Слово владельца на расхождение: правило
  `testing` требует такого набора для сценария, чьё «Тогда» называет человека и то, что он видит,
  а в дереве его нет вовсе. Отвергнуто: закрыть обещание юнитами и замерами. Цена: стенд из
  прод-сборки, засев базы и прогон одним воркером — работа заметного размера.
- **Раскладываются пять законов: о списке записей, о правке сущности, о моделях сущностей, о
  навигации и о доступе.** Слово владельца: четыре названных им и закон о доступе сверх того —
  работа заводит вход, а закон под него стоял снятым. Цена: у правил правки записи и навигации
  компаньоны выйдут из строк «не исполняется», и их придётся обслуживать на каждой сверке спеков.
- **Вход несёт кука, недоступная скриптам.** Админка и приём отдаются с одного имени, поэтому
  второго источника у куки нет; чужой переход её не посылает. Отвергнуто: заголовок с токеном —
  он требует держать значение там, куда дотягивается скрипт страницы.
- **Отдача самого приложения входом не сторожится.** Закрытая статика — это проксировщик, а он в
  отдельной задаче; без груза приложение пусто. Цена: адрес админки виден всякому.
- **Текст груза показывается текстом.** Дерево с токеном иначе получает исполнение своей разметки
  в браузере вошедшего. Отвергнуто: показ размеченным текстом с вычисткой — вычистка стоит
  дороже, а читаемость от неё выигрывает мало.
- **Выборка живёт в адресе.** Иначе правило о перезагрузке нарушается буквой. Цена: разбор и
  сборка параметров адреса на каждом разделе.
- **Порядок по умолчанию — свежие сверху, второй ключ — идентификатор записи.** Без второго ключа
  записи с равным временем перескакивают между страницами.
- **Отказ чтения занимает место списка, а не подаётся тостом.** Расхождение с правилом списочного
  экрана, названное расхождением: тост уходит сам, и человеку, вернувшемуся к экрану, повторять
  нечем. Причина, номер обращения и повтор стоят на месте таблицы.
- **Число попыток входа не ограничивается, а ответ удлиняется — допущение исполнителя.** Запертую
  запись отпирать некому: админки учёток нет. Отменяется одной правкой.
- **Язык один и тема одна — допущение исполнителя.** В границах работы владелец их не назвал.
  Отменяется одной правкой, пока подписи лежат в словаре.
- **Команды смены пароля и отключения записи заводятся вместе с заведением — допущение
  исполнителя.** Иначе утёкший пароль закрывать нечем вовсе, а стоят они строкой рядом с уже
  написанными командами деревьев.

## Открытые вопросы

- `Q-17` — сколько груз хранится. Срок назначается тогда, когда объём станет виден; до тех пор
  не удаляется ничего.
- `Q-18` — читается ли в админке груз дерева, чей токен отозван. Отказ приёма решён; чтение
  прежнего груза работа админки не решила — она его показывает.
- `Q-19` — чем опознаётся разбор происшествия. Работа идёт допущением «именем файла».
  Переименованный на дереве разбор приедет вторым экземпляром, и это цена допущения.
- `Q-20` — все ли разборы уезжают каждым прогоном или только новые. Отбор назначает
  отправитель, и до его правки приёмник принимает сколько прислали; при росте числа разборов
  груз упрётся в предел веса.
- `Q-21` — сколько живёт вход и продлевается ли он работой человека. Работа идёт допущением
  «двенадцать часов, продления нет»; число называет владелец, и меняется оно настройкой.
- `Q-22` — что делать с предложением, текст которого правили после отправки. Приезжает новой
  записью; сшивать их нечем, пока у предложения нет имени, переживающего правку текста.
- `Q-25` — какие свойства записи стоят столбцами у каждого из трёх разделов. Работа идёт
  допущением: у разборов — дерево, имя файла, время приезда и время обновления; у предложений —
  дерево, ресурс, адрес, время приезда; у сводок — дерево, месяц, число заходов, время прогона.

## История изменений

- 2026-08-14 — заведено разбором просьбы владельца о второй задаче эпика
  `docs/plans/agent-kit-observations.md`.
- 2026-08-14 — приведено к состязательному разбору: контракт назвал операции, заголовок токена
  и обязательные поля; правила назвали судьбу записи месяца при повторном прогоне, перевыпуск
  токена, порядок операций, гонку, журнал отказов и опознание разбора.
- 2026-08-14 — влито в спек домена: приёмник принимает груз трёх родов, команды деревьев
  исполняются, живой прогон с дерева пакета прошёл. Каждое правило привязано к коду в
  `implementation.md` рядом.
- 2026-08-15 — влита договорённость о признаке предложения: предложение опознаётся хешем своего
  текста, уникальность держит пара «дерево — признак», а ответ приёма называет, сколько записей
  легло и сколько уже лежало. Закрыт `Q-16`, заведён вопрос о правленом после отправки тексте.
- 2026-08-15 — влита договорённость о входе и чтении принятого груза: вход человека паролем,
  четыре команды учётных записей, три раздела админки со списком, отбором по дереву и панелью
  подробностей. Номер вопроса о правленом тексте предложения при вливании стал `Q-22`: `Q-21`
  занял вопрос о сроке входа, на который ссылается код.
