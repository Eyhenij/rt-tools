# The binding — the store gives no emptiness

- **A selector gives back the declared type even when the field is not in the state** — `base-async-store.service.ts:loading`
- **The spare answer is chosen by the emptiness of the value** — `base-async-store.service.ts:requestStatus`
- **The taking apart of a refusal judges the emptiness, not the truthfulness** — `base-async-store.service.ts:handleError`
- **An absence of a value in the common state of a list is named by one way** — `state-base.interface.ts:List`
- **The initial state declares exactly the fields that stand in the type** — `base-initial-state.const.ts:ASYNC`

The paths are from the root of the package: `projects/store/src/lib/`.

- **Scenario** — Test
- **`SC-ST-01`** — `base-async-store.service.spec.ts`
- **`SC-ST-02`** — `base-async-store.service.spec.ts`
- **`SC-ST-03`** — `base-async-store.service.spec.ts`
- **`SC-ST-04`** — `base-async-store.service.spec.ts`
- **`SC-ST-05`** — `base-async-store.service.spec.ts`
