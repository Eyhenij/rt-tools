# Задержка показа спиннера

**Статус:** предложено · **Ревизия:** 19 августа 2026 · **Префикс сценариев:** `SC-UK`
**Зависимости:** нет
**Законы:** `frontend-application`, `reuse-first`
**Процедуры:** нет

Договорённость о продукте, написанная до кода. Вливается в спек домена последним коммитом PR — с
прежними номерами сценариев.

## Зачем

Спиннер появляется в тот же миг, когда началось ожидание. Ответ, пришедший за сотню-другую
миллисекунд, показывает человеку вспышку: спиннер мигнул и исчез, а взгляд успел за него
зацепиться. Вспышка читается как поломка — экран дёрнулся, хотя всё в порядке.

## Терминология

| Термин          | Что это                                                               |
| --------------- | --------------------------------------------------------------------- |
| Задержка показа | Время от вставки спиннера до того, как он становится виден            |
| Вспышка         | Показ спиннера на срок, который человек воспринимает как рывок экрана |

### Как это называется в интерфейсе

| В договорённости | На экране                                   |
| ---------------- | ------------------------------------------- |
| Задержка показа  | вход `delay` в миллисекундах                |
| Вспышка          | мигнувший и сразу пропавший кружок ожидания |

## Правила

- **Спиннер ждёт задержку и только потом становится виден.** Ожидание, кончившееся раньше
  задержки, спиннера не показывает вовсе: показывать нечего — ответ уже пришёл.
- **Умолчание задержки — ноль.** Спиннер, вставленный без задержки, ведёт себя как прежде:
  правка не меняет ни одного из мест, где он уже стоит.
- **Задержка отсчитывается от вставки спиннера, а не от первой перерисовки.** Отсчёт, начатый
  позже, растягивает ожидание на неизвестное человеку время.
- **Снятый до срока спиннер счётчик за собой убирает.** Отложенный показ, доживший до уже
  снятого компонента, рисовал бы кружок поверх готового экрана.
- **Задержка живёт в самом спиннере.** Его вставляют пять мест кита и любое приложение; своя
  задержка у каждого означала бы пять разных ответов на один вопрос.

## Что не входит

- Наименьшее время показа: спиннер, показавшийся на миг и пропавший, — своя работа, и заводится
  она отдельной задачей.
- Спиннер второго кита: он свой, и правка его входов сюда не тащится.
- Скелетоны и заглушки: они отвечают на другой вопрос — чем занять место, а не когда показать
  ожидание.

## Контракт

Не применимо: поверхность домена — входы компонентов кита, процедур он не обслуживает.

### Коды отказов

Не применимо: отказов у показа нет.

## Данные

Своих записей хранилища у кита нет.

## Экраны и состояния

| Состояние                       | Что видно           |
| ------------------------------- | ------------------- |
| задержка не названа             | спиннер сразу       |
| задержка названа, срок не вышел | ничего              |
| задержка названа, срок вышел    | спиннер             |
| ожидание кончилось раньше срока | ничего за всё время |

## Сквозные требования

### Локали

Не применимо: подписей у спиннера нет.

### SEO

Не применимо.

### Мобильная раскладка

Не применимо: задержка от ширины экрана не зависит.

### Мультиобъектность

Не применимо.

## Решения

- **Задержка — вход спиннера, а не обёртка вокруг него.** Довод: обёртка требует правки каждого
  места вставки, а спиннер уже стоит в пяти местах кита. Отвергнуто: директива отложенного
  показа рядом.
- **Умолчание — ноль, а не «разумные триста».** Довод: правка не должна менять поведение мест,
  где спиннер уже стоит; срок выбирает тот, кто знает, чего ждут. Отвергнуто: умолчание в
  триста миллисекунд — оно молча растянуло бы каждое ожидание кита.

## Открытые вопросы

- `Q-2` — нужно ли спиннеру наименьшее время показа вдобавок к задержке. Работа идёт с
  допущением, что нет: задержка снимает вспышку сама, а второе время добавляет свой рывок в
  конце.

## История изменений

- 19 августа 2026 — заведена.
