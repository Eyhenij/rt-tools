# Граница состояния не читается концом хода

**Статус:** предложено · **Ревизия:** 2026-08-21 · **Префикс сценариев:** `SC-AK`
**Зависимости:** нет
**Законы:** `work-conduct`, `verifiability`
**Процедуры:** нет

## Зачем

Работа ведётся состояниями: у состояния есть вход, обязательное действие и выход. Ход при этом
кончается четырьмя способами, и граница состояния среди них не названа. Читается она ровно
наоборот: исполнитель доводит обязательное действие до конца, дочитывает раздел своего состояния
до последнего приёма, следующего движения там не находит — и отдаёт ход отчётом о сделанном.

Так уже кончились два хода: один на записанном замысле, второй на закрытом разборе просьбы —
`docs/postmortems/2026-08-21-turn-ended-at-the-written-plan.md` и
`docs/postmortems/2026-08-21-turn-ended-at-the-closed-grill.md`. Оба раза дыру нашёл владелец,
а не проверка: у состояния был ведущий раздел, раздел был на месте, и всё выглядело описанным.

Правило ведения работы перечисляет, что ходом не кончается, — коммит, прочитанную
договорённость, зелёную проверку, — но перехода между состояниями среди перечисленного нет.
Ни один раздел состояния не говорит, что делается сразу за его обязательным действием. Сверка
состояний сравнивает имена разделов с перечнем и внутрь раздела не смотрит.

Договорённость кладёт в каждый раздел строку следующего движения, дописывает статью в правило и
в закон и заводит проверку, которая эти строки читает.

## Терминология

| Термин                     | Что это                                                               |
| -------------------------- | --------------------------------------------------------------------- |
| Раздел состояния           | заголовок вида «Состояние `имя`» в ведущем паттерне и текст под ним   |
| Следующее движение         | то, что делается сразу за обязательным действием этого раздела        |
| Строка следующего движения | строка раздела, которая это движение называет                         |
| Зачин строки               | часть строки, общая для всех разделов; по ней строку находит проверка |
| Хвост строки               | часть после зачина: движение именно этого раздела, своими словами     |

### Как это называется в интерфейсе

Интерфейса нет. Строку исполнитель читает в паттерне, расхождение — в выводе проверки.

## Правила

- **Раздел состояния называет следующее движение.** Раздел, обрывающийся на последнем приёме,
  читается как конец работы: за ним не написано ничего, и ход кончается отчётом.
- **Строка стоит в каждом разделе состояния, а не по одной на состояние.** Состояние, у которого
  разделов несколько, ведёт исполнителя по ним порознь, и дочитывает он тот раздел, в котором
  стоит: следующее движение у разведки и у разбора с владельцем разное, хотя состояние одно.
- **Формулировка у каждого раздела своя.** Дословно одинаковая строка читается как шаблон и
  перестаёт замечаться на третьем разделе — а замечать её нужно ровно в ту минуту, когда
  обязательное действие сделано и ход просится наружу.
- **Строку находит зачин, общий для всех разделов.** Отличить рассказ о следующем движении от
  рассказа о чём угодно машине нечем, и без общего признака проверка читала бы прозу.
- **Хвост у каждой строки свой, и две дословно совпавшие строки — расхождение.** Общий зачин
  приглашает списать соседнюю строку целиком; названное движение — то единственное, ради чего
  строка стоит в разделе.
- **Правило ведения работы называет переход между состояниями среди того, чем ход не
  кончается.** Разделы говорят о своём движении поштучно, и общего утверждения из них не
  собрать: исполнитель, стоящий в одном состоянии, читает один раздел.
- **Карта хода говорит то же самое теми же словами по смыслу.** Она приходит в контекст на
  каждом запуске и перечисляет выходы наравне с правилом; промолчав о переходе, она расходится с
  правилом там, где её читают чаще всего.
- **Закон о ведении работы называет это статьёй.** Что должно быть верно, говорит закон; какими
  словами и в каком разделе это стоит — правило и паттерны при нём.
- **Проверка заводится своя, со своей командой и своей строкой в наборе гейта пуша.** Сверка
  состояний сравнивает два множества имён; чтение текста внутри раздела — другой предмет, и
  сложенные в одну команду они краснеют одной строкой на две разные поломки.
- **Отказ называет состояние, ведущий паттерн и заголовок раздела.** Расхождение, названное
  числом разделов без имён, чинится перечитыванием всех разделов подряд.
- **Проверка пропускает дерево, где правила ведения работы нет.** Сверять нечего — нулевой код.
  Пустой перечень состояний при этом отказом считается: он означает не «нечего сверять», а
  «перечень сломан».
- **Раздел про состояние вне перечня эта проверка не судит.** Его ловит сверка состояний, и
  вторая проверка о том же дала бы две красные строки об одном промахе.

## Что не входит

- Страж выходов хода: владелец запретил его править этой работой.
- Расширение сверки состояний — она остаётся при своём предмете.
- Новые состояния работы и новые выходы хода: перечни те же, что были.
- Тексты отказа самого стража и хука входа в заход.

## Контракт

Поверхность — команда проверки: нулевой код, когда у каждого раздела состояния есть своя строка
следующего движения, и ненулевой с перечнем расхождений, когда нет.

### Коды отказов

Не применимо: проверка отвечает кодом возврата и перечнем расхождений, а не именованными кодами.

## Данные

Своих данных нет. Проверка читает перечень состояний в правиле ведения работы и разделы
состояний в паттернах, которые их ведут.

## Экраны и состояния

Не применимо: экранов нет.

## Сквозные требования

### Локали

Тексты строк и отказов — на языке дерева.

### SEO

Не применимо.

### Мобильная раскладка

Не применимо.

### Мультиобъектность

Не применимо: проверка судит одно дерево, в котором её запустили.

## Решения

- **Зачин общий, хвост свой.** Владелец отверг единую дословную строку, а проверять прозу без
  общего признака нечем. Зачин даёт проверке зацепку, хвост — тот смысл, ради которого строка
  написана; повтор хвоста проверка ловит наравне с его отсутствием.
- **Строка в каждом разделе, а не одна на состояние.** Разделов больше, чем состояний, и
  исполнитель дочитывает не состояние, а раздел.
- **Своя команда, а не расширение сверки состояний** — так решил владелец.
- **Закон правится этой же веткой** — владелец разрешил прямо.

## Открытые вопросы

Нет.

## История изменений

- 2026-08-21 — договорённость записана по разбору просьбы задачи RT-979, пятой в эпике RT-974.
