# Заведение дерева токеном из админки

**Статус:** предложено · **Ревизия:** 19 августа 2026 · **Префикс сценариев:** `SC-AK`
**Зависимости:** приём груза
**Законы:** `frontend-application`, `delivery`
**Процедуры:** нет

Договорённость о продукте, написанная до кода. Вливается в спек домена последним коммитом PR — с
прежними номерами сценариев.

## Зачем

Токен у дерева появляется одним путём: команда обменивает одноразовый код приглашения на токен.
Владелец приёма теперь выдаёт токен прямо в админке — и вставить его дереву нечем. Единственная
дорога к тому же результату идёт через ssh на узел приёма, а это не путь для человека.

## Терминология

| Термин         | Что это                                                                  |
| -------------- | ------------------------------------------------------------------------ |
| Выданный токен | Токен дерева, который человек получил в админке приёма и держит на руках |
| Обмен по коду  | Прежний путь: команда несёт одноразовый код и получает токен в ответ     |

### Как это называется в интерфейсе

| В договорённости | На экране               |
| ---------------- | ----------------------- |
| Выданный токен   | довод `--token <токен>` |
| Обмен по коду    | довод `--code <код>`    |

## Правила

- **Дерево заводится двумя путями, и человек выбирает доводом.** Код приглашения обменивается на
  токен, выданный токен кладётся как есть. Один путь на всех означал бы, что выданный в админке
  токен применить нечем.
- **Выданный токен в сеть не уходит.** Обменивать его не на что, и приём о нём не спрашивают: он
  же его и выдал. Обращение ради подтверждения добавляло бы отказ там, где всё верно.
- **Два довода вместе отбиваются.** Это два пути к одному и тому же, и молчаливый выбор одного
  из них скрыл бы от человека, что второй он назвал зря.
- **Ни одного довода — отказ называет оба пути.** Отказ, называющий только прежний путь, второй
  для человека не существует.
- **Токен ложится на диск одинаково, каким бы путём ни пришёл.** Тот же файл, те же права
  «читает и пишет только владелец файла». Разные права у двух путей означали бы, что
  безопасность зависит от того, как заводили дерево.
- **Лежащий токен не перезаписывается молча ни у одного из путей.** Заведённое дерево потеряло бы
  связь со своим прежним грузом; намеренная перезапись объявляется отдельным доводом.
- **Проверки, общие обоим путям, стоят до развилки.** Лежащий токен и незаполненный ключ файла
  судятся раньше, чем выбирается путь: поставленные после, они защищали бы только один из них.
- **Адрес приёма и запрет открытого пути требуются только обмену по коду.** Токен, который никуда
  не уходит, по открытому пути не читается, и требовать TLS у пути без сети — значит отбивать
  заведение там, где угрозы нет.

## Что не входит

- Прямая выдача токена в админке приёма — сторона приёмника, своя работа.
- Снос признака дерева и его сверки с токеном.
- Самостоятельная регистрация потребителя — отдельный эпик.

## Контракт

Не применимо: процедур домен не обслуживает, поверхность — доводы строки запуска.

### Коды отказов

| Код | Когда                                                                        |
| --- | ---------------------------------------------------------------------------- |
| `1` | оба довода вместе, ни одного, незаполненный ключ файла токена, лежащий токен |

## Данные

Файл токена, названный ключом настройки. Своих записей хранилища у домена нет.

## Экраны и состояния

Не применимо: экранов у пакета нет, поверхность — строка запуска.

## Сквозные требования

### Локали

Не применимо: пакет говорит на одном языке.

### SEO

Не применимо.

### Мобильная раскладка

Не применимо.

### Мультиобъектность

Не применимо.

## Решения

- **Выданный токен приходит доводом, а не файлом или переменной окружения** — человек держит его
  на руках сразу после выдачи, и лишний шаг «положи в файл» вернул бы ту же проблему.
  Отвергнуто: чтение из переменной окружения — токен остаётся в истории оболочки у всех.
- **Подтверждения токена у приёма нет** — приём его и выдал. Отвергнуто: пробное обращение —
  оно требует адреса приёма и сети там, где ни того ни другого не нужно.

## Открытые вопросы

- `Q-1` — печатать ли имя дерева после заведения выданным токеном. Имени команда не знает: оно
  приезжает только ответом на обмен по коду. Работа идёт с допущением, что имя не печатается, а
  связь проверяется отправкой вхолостую.

## История изменений

- 19 августа 2026 — заведена.
