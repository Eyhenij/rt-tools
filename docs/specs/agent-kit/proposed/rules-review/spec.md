# Ревью текстов слоя правил

**Статус:** предложено · **Ревизия:** 2026-08-16 · **Префикс сценариев:** `SC-AK`
**Зависимости:** нет
**Законы:** `project-documentation`, `verifiability`
**Процедуры:** нет

## Зачем

Пакет везёт тексты, по которым работает агент в каждом дереве, куда его поставили. Сегодня эти
тексты не судит никто: раскладка кладёт файлы, не читая их, целостность сверяет ссылки шапок, а
набор текстов стережёт границу адресности — домены, порты и пути чужого дерева. Что́ в текстах
написано, машине не видно вовсе, а два правила, говорящие об одном разное, читаются как два
верных указания: исполнитель берёт то, которое загрузилось раньше.

Проверки, которые пакет раскладывает в дерево, судят дерево — спеки против тестов, повторы, слои
либ, пути в документах. Ни одна не судит текстов самого пакета, хотя ошибка в них уезжает ко
всем потребителям разом и находит её тот, кто пошёл по правилу и сделал не то.

Договорённость заводит слой ревью из двух половин. Машинная считает то, что считается: у правила
есть паттерн, у ресурса — обязательные разделы его рода, названное прозой имя соседа существует.
Роль читает семью текстов целиком и ищет расхождение смысла, которого машине не видно. Плюс ход
двух самых длинных правил перестаёт собираться чтением прозы: он изображён графом в самом
правиле.

## Терминология

| Термин              | Что это                                                                                      |
| ------------------- | -------------------------------------------------------------------------------------------- |
| Семья ресурсов      | закон, все правила под ним и все паттерны при этих правилах                                  |
| Расхождение         | находка машинной половины: то, что считается и повторяется от запуска к запуску              |
| Находка             | находка роли: два текста, говорящих об одном разное, либо случай, которого не назвал ни один |
| Невыбранный вид     | редакция ресурса под значение оси, которого это дерево не выбрало                            |
| Ход правила         | порядок шагов, который правило предписывает, вместе с развилками между ними                  |
| Граф хода           | изображение хода правила в тексте самого правила                                             |
| Ветвление хода      | развилка, после которой шаги идут по-разному в зависимости от условия                        |
| Обязательный раздел | заголовок, без которого ресурс этого рода считается описанным наполовину                     |

### Как это называется в интерфейсе

Не применимо: у пакета нет экранов. Расхождения читает исполнитель и прогон, находки — владелец.

## Правила

- **Тексты пакета судятся тем же набором требований, что и копии, разложенные в дерево.** Сейчас
  обязательные разделы, привязка утверждений и наличие паттерна проверяются только у разложенного:
  промах в источнике доезжает до потребителя и краснеет у него, а не там, где его правят.
- **Невыбранный вид судится наравне с выбранным.** Дерево раскладывает одну редакцию правила, и
  остальные не читает никто — ни раскладка, ни проверки дерева. Три редакции правила о поставке
  расходятся между собой молча, и узнаёт об этом первое дерево, выбравшее другой хостинг.
- **Ресурс без обязательного раздела своего рода — расхождение.** Набор разделов объявлен на род:
  у закона — статьи, у правила — раздел о применении закона здесь и перечень паттернов, у
  паттерна — раздел о том, когда его брать. Отсутствие раздела значит, что ресурс описан
  наполовину, а не что в нём нечего сказать.
- **Правило без паттерна — расхождение.** Связь снизу вверх пакет уже сверяет: у паттерна есть
  правило, у правила — закон. Обратная сторона не сверялась ни разу, и правило без готового кода
  исполняется пересказом.
- **Имя соседнего ресурса, названное прозой, проверяется наравне со ссылкой шапки.** Правила
  ссылаются друг на друга именами в тексте — «как заводится сам скил», «устройство слоёв». Снятый
  или переименованный ресурс оставляет такую фразу верной на вид и ведущей в пустоту, а шапки при
  этом целы.
- **Запреты текстов действуют и внутри блока кода.** Узлы графа — тот же текст ресурса, и путь
  чужого дерева в узле остаётся путём чужого дерева. Проверка, читающая только прозу, пропустит
  его целиком.
- **Машинная половина краснеет только на считаемом.** Ответ роли не повторяется от запуска к
  запуску, и поставленный в гейт он отбивал бы работу по-разному на одном и том же дереве.
- **Проверка текстов пакета стоит в наборе, который гоняется перед пушем.** Проверку, которую
  надо помнить, до второго месяца не доводят.
- **Ревью читает семью целиком, а не файл по одному.** Противоречие живёт между двумя текстами:
  правило требует то, что паттерн при соседнем правиле запрещает. Файл, прочитанный в одиночку,
  выглядит непротиворечивым всегда.
- **Роль возвращает находки и ничего не правит.** Правка текста правила действует на все будущие
  сессии всех деревьев, и вносит её человек, а не тот, кто её предложил.
- **Находка называет два места дословно и то, чем они расходятся.** Пересказ своими словами
  проверить нечем: владелец не отличит настоящий дефект от прочтения роли.
- **Пробел ищется чтением, а не счётом привязок.** Статья закона, под которой нет ни одного
  правила, считается машиной только по проставленному руками якорю: их пришлось бы завести на
  каждую из 241 статьи, и через полгода их перестанут проставлять — проверка останется зелёной
  при пустой полке. На уровне целого закона она уже сегодня находит ноль: правило есть у каждого
  из семнадцати.
- **Ревью зовётся двумя способами: командой вручную и машинной половиной в гейте.** Обе половины
  зовутся без сговора: команда не ждёт гейта, гейт не ждёт команды.
- **Граф изображает ход правила и лежит в тексте самого правила.** Карта связей между ресурсами
  уже считается по шапкам и читателю ничего нового не говорит. Граф, живущий отдельным файлом или
  внутри отчёта, расходится с прозой так же, но виден он при этом одному исполнителю и один раз.
- **Граф заводится правилу, чей ход ветвится.** Правило, у которого ход линеен, графа не
  получает: три коробки со стрелкой не сообщают ничего сверх заголовка, а платится за них местом
  в окне каждой сессии каждого дерева — двадцать шесть графов дают к корпусу правил пять-семь
  процентов, и находит в них что-то одно правило из десяти.
- **Граф правится тем же изменением, что и проза, которую он изображает.** Разойдясь, они
  оставляют обе стороны читаемыми как действующие, и первым это замечает тот, кто пошёл по графу.

## Что не входит

- **Правка найденного машиной.** Ревью называет расхождение и место; текст правит человек. Слой,
  который сам переписывает правила, меняет их во всех будущих сессиях без чьего-либо ведома.
- **Ревью текстов дерева.** Судятся тексты пакета. Надстройки, компаньоны и скилы дерева
  проверяются тем, что пакет в дерево раскладывает.
- **Графы во всех правилах.** Названы два: правило ведения работы и правило поставки. Остальные
  двадцать два короче ста восьмидесяти строк, и ветвления, ради которого рисуют, в них нет.
- **Отказ сборки на смысловой находке.** Находка роли — довод для человека, а не приговор ветке.
- **Хук на правке файла ресурса.** Работа в пакете — это и есть правка текстов ресурсов, и хук
  сработал бы на каждом движении по прямому назначению. Теряется немедленность напоминания:
  считаемое ловит гейт, смысловое — команда.
- **Сплошной прогон по всему пакету как признак закрытия.** Одиннадцать тысяч строк не помещаются
  в окно, и сплошной проход вернёт крошку вместо дорогих находок.
- **Сведение находок с записями предложений.** Находка ревью — не предложение правки с адресом:
  адрес у неё один, и это пакет.

## Контракт

Две части. Машинная — проверка в наборе пакета: читает каталог ресурсов, печатает расхождения,
отдаёт ненулевой код при непустом списке. Смысловая — команда: зовёт роль по названной семье,
печатает находки, кода не меняет.

| Вход                        | Откуда                                                    |
| --------------------------- | --------------------------------------------------------- |
| каталог ресурсов пакета     | набор, который пакет везёт, вместе со всеми видами        |
| набор обязательных разделов | объявлен на род ресурса                                   |
| имя семьи                   | довод команды: имя закона; без довода команда его требует |

### Коды отказов

| Что случилось                                       | Что печатается                                     |
| --------------------------------------------------- | -------------------------------------------------- |
| у ресурса нет обязательного раздела своего рода     | имя ресурса, его род и недостающий заголовок       |
| у правила нет ни одного паттерна                    | имя правила и подсказка завести паттерн            |
| имя соседа, названное прозой, каталогу не отвечает  | имя ресурса, названное имя и строка, где оно стоит |
| в блоке кода ресурса стоит адрес конкретного дерева | имя ресурса, номер строки и найденный адрес        |
| команда позвана без имени семьи                     | требование назвать семью и перечень имён законов   |

## Данные

Своего хранилища ни у одной из половин нет. Машинная судит тексты набора, роль — те же тексты
чтением. Находки живут в ответе команды.

## Экраны и состояния

Не применимо: у пакета нет экранов.

## Сквозные требования

### Локали

Не применимо: язык слоя правил один.

### SEO

Не применимо.

### Мобильная раскладка

Не применимо.

### Мультиобъектность

Не применимо: набор ресурсов у пакета один.

## Решения

- **Слой двусоставный.** Противоречие «правило A требует того, что правило B запрещает» не
  ловится ни поиском по словам, ни разбором шапок; пробел «у правила нет паттерна» считается
  точно и дёшево. Отвергнуто: только машина — смысловые расхождения остались бы ненайденными;
  только роль — результат не повторяется от запуска к запуску, и в гейт его не поставишь.
- **Способов запуска два, а не три.** Третьим был хук на правке файла ресурса; в пакете правка
  текстов ресурсов и есть работа, и хук шумел бы на каждой. Отвергнут в пользу строки в правиле о
  заведении скила.
- **Пробел на уровне статьи отдан роли.** Машине для него нужен якорь на каждую статью, и
  проставляются они руками; проверка, чья зелень держится ручной разметкой, зеленеет и на пустой
  полке.
- **Граф ложится в текст правила.** Отвергнуто: граф только в отчёте ревью — его видит один
  исполнитель и один раз; отдельный файл рядом с правилом — тот же риск расхождения с прозой плюс
  лишний файл в раскладке.
- **Графов два, а не двадцать шесть.** Медиана длины правила — около ста двадцати строк, и
  ветвления там нет. Налог за сплошной вариант платится в каждой сессии каждого дерева, а находка
  ожидается в одном правиле из десяти.
- **Признак закрытия — два-три семейства, а не весь пакет.** Полнота здесь не цель, а её
  видимость: ревью считается состоявшимся, когда владелец подтвердил найденное дефектами и часть
  из них починена.

## Открытые вопросы

- **Q-1** — обязателен ли у правила полный набор из шести разделов или только те три, что названы
  правилом. Решение владельца: расширение набора краснеет разом на всех ресурсах пакета.
  Заведён 2026-08-16.
- **Q-2** — текст статей, которые получает закон о документации проекта. Закон без ведома владельца
  не правится, формулировки приносятся ему отдельно. Заведён 2026-08-16.
- **Q-3** — раскладывается ли ревью в дерево потребителя. Машинная половина судит тексты пакета, а
  в чужом дереве лежат только разложенные копии и надстройки над ними; судить их — другое
  требование, и его никто не называл. Заведён 2026-08-16.
- **Q-4** — что делает исполнитель с находкой прямо в ходе работы: правит тут же, заводит задачу
  или несёт владельцу. Заведён 2026-08-16.
- **Q-5** — идёт ли роль одна или веером из нескольких ролей со сведением ответов. Заведён
  2026-08-16.

## История изменений

- 2026-08-16 — заведено по разбору просьбы владельца о слое ревью текстов пакета.
