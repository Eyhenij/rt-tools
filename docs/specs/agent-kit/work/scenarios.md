# Сценарии — ведение работы командами

Идентификатор ставится в начало заголовка теста через тире. Пока сценарий не покрыт, он несёт
пометку «Не покрыто» с причиной. Префикс общий на домен, и номера при переезде в поддомен не
пересчитывались: номер связывает сценарий с заголовком теста.

### SC-AK-12 — слияние отбивается, пока ветка несёт папку своей задачи

Дано ветка под задачу, и папка её задачи закоммичена в ветку
Когда идёт команда слияния PR
Тогда гард отказывает, называет лежащую папку и то, чем отказ снимается

### SC-AK-13 — разобранная папка слиянию не мешает

Дано ветка под задачу, и папки её задачи в ветке больше нет, а в каталоге архива прибыл файл
Когда идёт команда слияния PR
Тогда гард пропускает

### SC-AK-14 — судится содержимое ветки, а не рабочее дерево

Дано папка задачи снесена в рабочем дереве, но снос не закоммичен
Когда идёт команда слияния PR
Тогда гард отказывает: в главную ветку папка уедет

### SC-AK-15 — снос без записи в архив не проходит

Дано папка задачи снесена в ветке целиком, и в каталоге архива ветка ничего не прибавила
Когда идёт команда слияния PR
Тогда гард отказывает и называет отсутствие записи в архиве

### SC-AK-17 — обход из текста команды действует без сети

Дано папка задачи лежит в ветке, а в тексте команды слияния стоит строка обхода с причиной
Когда идёт команда слияния PR
Тогда гард пропускает, не спрашивая очередь работ

### SC-AK-18 — обход без причины обходом не считается

Дано папка задачи лежит в ветке, а строка обхода стоит без причины
Когда идёт команда слияния PR
Тогда гард отказывает

### SC-AK-19 — папка старой формы имени находится вложенным каталогом

Дано ветка формы с косой, и папка её задачи лежит вложенным каталогом
Когда идёт команда слияния PR
Тогда гард отказывает и называет вложенный путь целиком

### SC-AK-20 — сверка очереди видит папку закрытой задачи во вложенном каталоге

Дано в каталоге задач лежит вложенная папка старой формы, и задача за ней закрыта
Когда идёт сверка очереди работ
Тогда папка названа расхождением, а образец и черновики разбора — нет

### SC-AK-21 — дерево без каталога задач требования не получает

Дано профиль дерева каталога задач не называет
Когда идёт команда слияния PR
Тогда гард пропускает молча

### SC-AK-59 — незакоммиченная правка останавливает закрытие захода

Дано в рабочем дереве есть незакоммиченная правка
Когда исполнитель зовёт закрытие захода
Тогда ни одна ветка не снимается, дерево остаётся на текущей ветке, а исполнителю называются
незакоммиченные файлы

Не покрыто: команду исполняет агент, и прогоном она не проверяется — проверка идёт вызовом в
дереве.

### SC-AK-60 — работа по правилу с влитым PR возвращает дерево на главную ветку

Дано работа ведётся в ветке под номером задачи, её PR влит, рабочее дерево чистое
Когда исполнитель зовёт закрытие захода
Тогда дерево переходит на главную ветку и подтягивает её из удалённого

Не покрыто: команду исполняет агент, и прогоном она не проверяется — проверка идёт вызовом в
дереве.

### SC-AK-61 — работа вне правила оставляет дерево в своей ветке

Дано работа идёт в ветке, за которой задачи нет, рабочее дерево чистое
Когда исполнитель зовёт закрытие захода
Тогда главная ветка вливается в текущую, а дерево остаётся на ней

Не покрыто: команду исполняет агент, и прогоном она не проверяется — проверка идёт вызовом в
дереве.

### SC-AK-62 — снимаются влитые ветки, невлитая остаётся названной

Дано среди локальных веток есть влитая в главную и есть ветка с коммитами, которых в главной нет
Когда исполнитель зовёт закрытие захода
Тогда влитая снимается, невлитая остаётся, и её имя вместе с числом несвёденных коммитов
называется исполнителю

Не покрыто: команду исполняет агент, и прогоном она не проверяется — проверка идёт вызовом в
дереве.

### SC-AK-63 — мёртвые отслеживания снимаются тем же вызовом

Дано в дереве есть ссылка на удалённую ветку, которой в удалённом больше нет
Когда исполнитель зовёт закрытие захода
Тогда ссылка снимается вместе с остальной уборкой

Не покрыто: команду исполняет агент, и прогоном она не проверяется — проверка идёт вызовом в
дереве.

### SC-AK-64 — передача пишется последней и лежит вне дерева

Дано закрытие захода дошло до записи передачи
Когда команда её пишет
Тогда файл ложится в каталог передачи из профиля дерева, одним файлом на ветку, в историю не
попадает, а путь к нему называется исполнителю последней строкой

Не покрыто: команду исполняет агент, и прогоном она не проверяется — проверка идёт вызовом в
дереве.

### SC-AK-152 — заведение кончается ответом очереди работ

Дано задача заведена и стоит в очереди работ с колонкой и исполнителем
Когда команда заведения складывает свой вывод
Тогда она называет колонку и исполнителя и кончается нулевым кодом

Покрыто: `projects/agent-kit/tests/checks-config.test.sh`.

### SC-AK-153 — задача вне очереди работ кончает команду отказом

Дано задача заведена, а в очереди работ её нет
Когда команда заведения складывает свой вывод
Тогда она называет отсутствие отдельной строкой и кончается ненулевым кодом

Покрыто: `projects/agent-kit/tests/checks-config.test.sh`.

### SC-AK-154 — задача без исполнителя названа отдельно

Дано задача стоит в очереди работ, но исполнителя у неё нет
Когда команда заведения складывает свой вывод
Тогда она называет это отдельной строкой и кончается ненулевым кодом

Покрыто: `projects/agent-kit/tests/checks-config.test.sh`.

### SC-AK-155 — неспрошенная очередь подтверждением не считается

Дано очередь работ спросить не удалось
Когда команда заведения складывает свой вывод
Тогда она называет, что состояние очереди неизвестно, и кончается ненулевым кодом

Покрыто: `projects/agent-kit/tests/checks-config.test.sh`.

### SC-AK-194 — упоминание обхода посреди строки обходом не считается

Дано команда слияния несёт слова об обходе, но строку он не начинает
Когда идёт вызов слияния при лежащей папке задачи
Тогда гард отбивает: текст, называющий обход, обходом не является

Покрыто: `projects/agent-kit/tests/git-guards.test.sh`.

### SC-AK-195 — подстановка вместо причины обходом не считается

Дано строка обхода стоит верно, а причина у неё записана подстановкой в угловых скобках
Когда идёт вызов слияния при лежащей папке задачи
Тогда гард отбивает: подстановка причиной не бывает

Покрыто: `projects/agent-kit/tests/git-guards.test.sh`.

### SC-AK-277 — вершина открытого PR без прогона названа расхождением

Дано открытый PR, на вершине которого не завёлся ни один прогон, а лежит она дольше отпущенного
Когда идёт сверка очереди работ
Тогда она называет вершину, её возраст и то, чем событие возвращается, и кончается ненулевым
кодом

Покрыто: `projects/agent-kit/tests/checks-board.test.sh`.

### SC-AK-278 — свежая вершина без прогона не судится

Дано открытый PR, вершина которого запушена минуту назад, и прогона за ней ещё нет
Когда идёт сверка очереди работ
Тогда она молчит: между пушем и прогоном проходит время

Покрыто: `projects/agent-kit/tests/checks-board.test.sh`.

### SC-AK-279 — прогон на вершине снимает вопрос

Дано открытый PR, на вершине которого прогон есть
Когда идёт сверка очереди работ
Тогда она молчит о нём, каким бы ни был его цвет

Покрыто: `projects/agent-kit/tests/checks-board.test.sh`.

### SC-AK-280 — дерево без файла конвейера прогонов не спрашивает

Дано дерево, у которого названного файла конвейера нет, и открытый PR без прогона на вершине
Когда идёт сверка очереди работ
Тогда она говорит, что прогоны не спрашивались, и расхождением это не считает

Покрыто: `projects/agent-kit/tests/checks-board.test.sh`.

### SC-AK-281 — готовое, оставленное черновиком, названо расхождением

Дано открытый PR-черновик, прогон на вершине которого завершён успехом
Когда идёт сверка очереди работ
Тогда она называет вершину и то, чем черновик снимается, и кончается ненулевым кодом

Покрыто: `projects/agent-kit/tests/checks-board.test.sh`.

### SC-AK-282 — черновик при незелёном прогоне не судится

Дано открытый PR-черновик, прогон на вершине которого идёт или упал
Когда идёт сверка очереди работ
Тогда она о черновике молчит: работа ещё не готова

Покрыто: `projects/agent-kit/tests/checks-board.test.sh`.

### SC-AK-367 — задача из первой колонки поставку отбивает

Дано дерево назвало первую колонку очереди работ, и задача стоит в ней
Когда открывается PR
Тогда открытие отбито, названы колонка задачи и команда перевода

Покрыто: `projects/agent-kit/tests/git-guards-readiness.test.sh`.

### SC-AK-368 — задача, взятая в работу, колонкой не отбивается

Дано задача стоит не в первой колонке
Когда открывается PR
Тогда колонка отказа не даёт

Покрыто: `projects/agent-kit/tests/git-guards-readiness.test.sh`.

### SC-AK-369 — дерево, не назвавшее первой колонки, проверки не получает

Дано имя первой колонки деревом не объявлено, а задача стоит в первой колонке
Когда открывается PR
Тогда колонка не судится вовсе

Покрыто: `projects/agent-kit/tests/git-guards-readiness.test.sh`.

### SC-AK-374 — черновик у PR без разбора не снимается

Дано у PR нет ни запрошенного ревьювера, ни оставленного отзыва
Когда снимается черновик — любым из клиентов хостинга
Тогда снятие отбито, названы номер PR и нехватка разбора; чтение того же PR снятием не считается

Покрыто: `projects/agent-kit/tests/git-guards-readiness.test.sh`.

### SC-AK-375 — PR с разбором черновик снимает

Дано у PR запрошен ревьювер либо оставлен отзыв не автором
Когда снимается черновик
Тогда гард пропускает вызов молча

Покрыто: `projects/agent-kit/tests/git-guards-readiness.test.sh`.

### SC-AK-376 — без ответа о PR черновик снимается

Дано помощник очереди работ молчит либо PR с таким номером ему неизвестен
Когда снимается черновик
Тогда гард пропускает вызов молча

Покрыто: `projects/agent-kit/tests/git-guards-readiness.test.sh`.

### SC-AK-377 — состояние PR называет его разбор

Дано у PR запрошен ревьювер и оставлен отзыв
Когда спрашивается состояние PR
Тогда PR найден, разбор у него есть, и в списке стоят оба — и запрошенный, и оставивший отзыв

Покрыто: `projects/agent-kit/tests/checks-board.test.sh`.

### SC-AK-378 — разбор автора разбором не считается

Дано единственный, кто назван при PR, — его же автор
Когда спрашивается состояние PR
Тогда разбора у PR нет, хотя из списка названных автор не пропадает; неизвестный номер
отвечает отсутствием

Покрыто: `projects/agent-kit/tests/checks-board.test.sh`.

### SC-AK-379 — молчание хостинга состоянием PR не считается

Дано хостинг не ответил либо отказал во входе
Когда спрашивается состояние PR
Тогда ответ назван молчанием сети, и приговора о разборе в нём нет

Покрыто: `projects/agent-kit/tests/checks-board.test.sh`.

### SC-AK-380 — состояние PR берётся у помощника очереди работ

Дано помощник очереди работ на месте и отвечает
Когда профиль спрашивают о состоянии PR
Тогда он отдаёт ответ помощника; без помощника — молчит

Покрыто: `projects/agent-kit/tests/defaults.test.sh`.

### SC-AK-381 — ответ с меткой молчания сети наружу не выходит

Дано помощник ответил меткой «сети не было»
Когда профиль спрашивают о состоянии PR
Тогда он молчит, и ответ наружу не выходит

Покрыто: `projects/agent-kit/tests/defaults.test.sh`.

### SC-AK-382 — без узла состояние PR не спрашивается

Дано узла, которым запускается помощник, на машине нет
Когда профиль спрашивают о состоянии PR
Тогда он молчит, и ответ наружу не выходит

Покрыто: `projects/agent-kit/tests/defaults.test.sh`.

### SC-AK-383 — черновик без ссылки на PR судится наравне с названным

Дано у PR текущей ветки нет разбора
Когда черновик снимается вызовом без ссылки на PR — любым из клиентов хостинга
Тогда снятие отбито тем же отказом, что и у вызова со ссылкой

Покрыто: `projects/agent-kit/tests/git-guards-readiness.test.sh`.

### SC-AK-384 — PR, названный адресом или именем ветки, судится наравне с номером

Дано PR без разбора назван адресом либо именем ветки
Когда снимается черновик
Тогда снятие отбито, и в отказе стоит номер из ответа очереди работ; номера в ответе нет —
в отказ идёт то, чем PR назвали в команде

Покрыто: `projects/agent-kit/tests/git-guards-readiness.test.sh`.

### SC-AK-385 — возврат PR в черновик проходит

Дано у PR нет разбора
Когда PR возвращают в черновик — со ссылкой на него или без неё
Тогда гард пропускает вызов молча

Покрыто: `projects/agent-kit/tests/git-guards-readiness.test.sh`.

### SC-AK-390 — колонка спрашивается на открытии PR, а не на заведении ветки

Дано задача стоит в первой колонке очереди работ
Когда по ней заводится ветка, а затем открывается PR
Тогда заведение ветки проходит, а открытие PR отбито — и отбито именно колонкой

Покрыто: `projects/agent-kit/tests/git-guards-readiness.test.sh`.

### SC-AK-391 — номер PR берётся из ответа хостинга

Дано PR назван адресом
Когда спрашивается состояние PR
Тогда PR найден, и номер его приходит из ответа хостинга; вызов без ссылки идёт к клиенту
вовсе без довода

Покрыто: `projects/agent-kit/tests/checks-board.test.sh`.

### SC-AK-392 — PR находится и по имени ветки

Дано PR назван именем ветки либо адресом
Когда спрашивается состояние PR
Тогда названное уходит клиенту хостинга доводом, и PR по нему находится

Покрыто: `projects/agent-kit/tests/checks-board.test.sh`.

### SC-AK-423 — конфликтующий PR черновик не снимает

Конфликт приезжает в отданный PR чужим слиянием: кнопка слияния у него заблокирована хостингом,
а снятый черновик зовёт её нажать.

Дано PR открыт черновиком, разбор у него есть, а с главной веткой он конфликтует
Когда снимается черновик
Тогда снятие отбито, и отказ называет номер PR вместе с конфликтом

Покрыто: `projects/agent-kit/tests/git-guards-readiness.test.sh`.

### SC-AK-424 — молчание о сливаемости снятие черновика не задерживает

Дано помощник очереди работ о сливаемости не говорит вовсе
Когда снимается черновик у PR с разбором
Тогда гард пропускает вызов молча: поля нет — требования нет, иначе правка пакета отбивала бы
снятие у каждого дерева, чей помощник ещё не поправлен

Покрыто: `projects/agent-kit/tests/git-guards-readiness.test.sh`.

### SC-AK-425 — конфликтующий открытый PR назван расхождением

Дано открытый PR конфликтует с главной веткой
Когда идёт сверка очереди работ
Тогда сверка называет его расхождением и говорит, с чем конфликт: гард судит один ход, а
конфликт приезжает в отданный PR чужим слиянием, без действия автора

Покрыто: `projects/agent-kit/tests/checks-board.test.sh`.

### SC-AK-426 — непосчитанная и сливаемая заявки молчат

Дано у PR сливаемость ещё не посчитана либо посчитана и конфликта нет
Когда идёт сверка очереди работ
Тогда строки о нём нет: хостинг считает сливаемость заново после каждой правки главной ветки, и
строка на непосчитанное краснела бы на каждой свежей вершине

Покрыто: `projects/agent-kit/tests/checks-board.test.sh`.

### SC-AK-526 — открытие заявки отбивается, пока ветка несёт папку своей задачи

Дано ветка под задачу, и папка её задачи закоммичена в ветку
Когда идёт команда открытия заявки
Тогда гард отказывает, называет лежащую папку и то, чем отказ снимается: уборка стоит до
открытия заявки, потому что слияние нажимает человек на хостинге и туда гард не достаёт

Покрыто: `projects/agent-kit/tests/git-guards.test.sh`.

### SC-AK-527 — обход из текста команды действует и на открытии заявки

Дано папка задачи лежит в ветке, а в тексте команды открытия стоит строка обхода с причиной
Когда идёт команда открытия заявки
Тогда гард папку не называет

Покрыто: `projects/agent-kit/tests/git-guards.test.sh`.

### SC-AK-528 — снос без записи в архив не проходит на открытии заявки

Дано папка задачи снесена в ветке целиком, и в каталоге архива ветка ничего не прибавила
Когда идёт команда открытия заявки
Тогда гард отказывает и называет отсутствие записи в архиве

Покрыто: `projects/agent-kit/tests/git-guards.test.sh`.

### SC-AK-531 — отставший прод назван расхождением

Дано последняя успешная выкатка сделана не с вершины главной ветки
Когда идёт сверка очереди работ
Тогда сверка называет отставание числом коммитов и коммит последней выкатки: слияние прод не
двигает, и прогон главной ветки о нём не говорит ничего

Покрыто: `projects/agent-kit/tests/checks-board.test.sh`.

### SC-AK-532 — дерево без названного потока выкатки сверки прода не получает

Дано в настройке дерева рабочий поток выкатки не назван
Когда идёт сверка очереди работ
Тогда прод не сверяется, и сверка говорит об этом вслух: молчание читалось бы как сошедшийся прод

Покрыто: `projects/agent-kit/tests/checks-board.test.sh`.
