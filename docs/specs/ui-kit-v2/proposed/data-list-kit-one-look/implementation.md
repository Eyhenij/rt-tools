# Привязка — семья таблицы в виде первого кита

Кода ещё нет, поэтому привязок вида `файл:символ` здесь нет: договорённость написана до кода.
Здесь — предвиденные места, чтобы при вливании было видно, куда смотреть.

## Где предвидится исполнение

| Правило                                     | Где предвидится                                                                                   |
| ------------------------------------------- | ------------------------------------------------------------------------------------------------- |
| содержимое половины темы не выходит за край | обёртка тем витрины `projects/ui-kit-v2/src/showcase/story-themes.component.scss` и тулбар списка |
| панель колонок в виде первого кита          | `projects/ui-kit-v2/src/lib/components/data-list/settings/`                                       |
| значки сравниваются в материальном наборе   | набор `projects/ui-kit-v2/src/assets/icons-material`                                              |
| парные кадры                                | `tools/kit-shot-pairs.json`, проверка `tools/check-kit-shot-pairs.mjs`                            |
