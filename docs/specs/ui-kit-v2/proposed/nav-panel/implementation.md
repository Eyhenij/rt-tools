# Привязка — панель раздела: закрепление и поиск

Привязки проставляются по мере того, как этапы замысла закрываются: до правки кода места у
утверждений нет. Пути от корня пакета — `projects/ui-kit-v2/src/lib/components/page-header/`.

- **Правило** — Где исполняется
- **Мода панели приходит входом, а нажатие переключателя уходит выходом.** — ещё не написано
- **Умолчание моды — всплывающая.** — ещё не написано
- **Закреплённая панель стоит в потоке разметки, а не поверх страницы.** — ещё не написано
- **Закреплённая мода показывает панель раздела, чей адрес открыт.** — ещё не написано
- **Активного раздела нет — закреплённой панели нет вовсе.** — ещё не написано
- **Обе моды рисуются одним шаблоном.** — ещё не написано
- **Запрос поиска отбирает пункты по подстроке подписи, без учёта регистра.** — ещё не написано
- **Пустой запрос возвращает набор целиком.** — ещё не написано
- **Отбор считается вне шаблона.** — ещё не написано
- **Запрос поиска живёт, пока панель открыта, и предпочтением не является.** — ещё не написано
- **На узком экране переключателя не видно.** — ещё не написано

- **Сценарий** — Тест
- **`SC-UKV-96`** — ещё не написан
- **`SC-UKV-97`** — ещё не написан
- **`SC-UKV-98`** — ещё не написан
- **`SC-UKV-99`** — ещё не написан
- **`SC-UKV-100`** — ещё не написан
- **`SC-UKV-101`** — ещё не написан
- **`SC-UKV-102`** — ещё не написан
- **`SC-UKV-103`** — ещё не написан
- **`SC-UKV-104`** — ещё не написан
- **`SC-UKV-105`** — ещё не написан
- **`SC-UKV-106`** — ещё не написан
