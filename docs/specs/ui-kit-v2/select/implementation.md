# What it is carried out by — the consumer's own trigger of a choice from a list

The first column is the rule of the spec next to it verbatim. The second is where it is carried out
in the tree; the scenario it is checked by is named there too, and what exactly every scenario is
covered by is said in `scenarios.md`.

A rule without a line and a line without a rule is a divergence: the spec promises what is not in
the tree, or the tree holds what the spec is silent about.

- **The behaviour of the trigger stays with the kit, and the consumer gives only what is drawn inside.** — **Not carried out yet.** The input is written by the task RT-2152, stage 3; scenario `SC-UKV-159`
- **A consumer who declares no trigger sees what they saw before.** — **Not carried out yet.** The same task, stage 3; scenario `SC-UKV-157`
- **The consumer's markup receives three values and no more: whether the list is open, what is chosen, whether the choice is switched off.** — **Not carried out yet.** The same task, stage 3; scenarios `SC-UKV-160`, `SC-UKV-161`, `SC-UKV-162`
- **One input serves both families.** — **Not carried out yet.** The same task, stage 3; scenario `SC-UKV-163`
- **The consumer's markup goes inside the button, not instead of it.** — **Not carried out yet.** The same task, stage 3; scenario `SC-UKV-158`
