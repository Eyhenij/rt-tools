# Замысел

Задача: #644. Ветка: `RT-644-anchor-row-in-table`.

**Поведение:** не меняется — правится текст паттерна, кода в правке нет.

После написания этот файл не правится. Пересмотр этапа идёт в `progress.md` решением по ходу.

## След задачи

| Что      | Где                                                      |
| -------- | -------------------------------------------------------- |
| Законы   | `docs/constitution/project-documentation.md`             |
| Правила  | `.claude/skills/spec-driven/`                            |
| Паттерны | `projects/agent-kit/assets/patterns/spec-driven-rule.md` |

## Что считается сделанным

- Паттерн называет промах: строка привязки, дописанная в конец компаньона, в таблицу не
  попадает, и сверка спеков на это молчит.
- Сказано и то, чем промах чинится: место строки то же, что у её утверждения в правиле, и
  порядок обеих сторон держится одинаковым.

## Этапы

### 1. Промах записан в паттерн

- **Что делается:** пункт дописывается в «Частые промахи», после промаха о переформулированном
  утверждении.
- **Признак готовности:** паттерн говорит, куда строка встаёт и почему конец файла не годится.
- **Чем проверяется:** `pnpm run agent-kit:check`, `node tools/check-doc-paths.mjs`.

### 2. Запись приёма закрывается ссылкой на задачу

- **Что делается:** #596 закрывается с указанием номера задачи.
- **Признак готовности:** записи нет в открытых.
- **Чем проверяется:** `gh issue list --label agent-kit-feedback --state open`.

## Чего эта работа не делает

- Не учит сверку спеков видеть строку привязки вне таблицы: отличить её от прозы нечем.
