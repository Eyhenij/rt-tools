# Plan

**Task:** RT-2005 · **Branch:** RT-2005-helpers-declare
**Spec:** `docs/specs/agent-kit/hooks/spec.md`
**Behaviour:** unchanged — правка идёт в заголовках помощников и кода приложения не касается

Первая работа эпика RT-2004. Ветка стоит на ветке эпика.

## Task footprint

| Что       | Где                                            |
| --------- | ---------------------------------------------- |
| Помощники | `projects/agent-kit/assets/hooks/` — 13 файлов |
| Пробы     | `projects/agent-kit/tests/hooks-kind.test.sh`  |
| Спека     | `docs/specs/agent-kit/hooks/`                  |

## What counts as done

- Первые строки каждого из тринадцати помощников говорят, что он не страж и кто его зовёт.
- Список молчащих убран из набора проб, и проба зелёная без него.
- Ни один помощник не получил строки объявления события: они не стражи.

## Stages

### 1. Помощники говорят о себе

- **Steps:**
    1. тринадцать заголовков дописаны: не страж, события не объявляет, кто зовёт
    2. список молчащих убран из набора проб
    3. пробы прогнаны без списка
- **What is done:** заголовки дописаны, список снят.
- **Readiness sign:** проба о помощниках зелёная без списка исключений.
- **Verified by:** `bash projects/agent-kit/tests/hooks-kind.test.sh` — без провалов.

## What this work does not do

- Не трогает поведение помощников: правка только в заголовках.
- Не закрывает два других списка — это задачи RT-2006 и RT-2007.
