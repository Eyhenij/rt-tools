# Ход работы

## Где стоим

Перезаписывается каждым заходом, а не дописывается.

- **Этап:** замысел написан, этап 1 не начат
- **Сделано:** разведка целиком; эпик RT-806 объявлен карточкой и замыслом, три задачи разбора
  долга заведены — RT-807, RT-808, RT-809; замысел этой задачи записан
- **Следующий шаг:** набор дерева зовёт умолчание пакета
- **Незакоммиченное:** нет — ветка запушена
- **Ждём владельца:** нет
- **PR:** не открыт намеренно — правки кода в ветке ещё нет, а `Closes #793` в теле был бы
  неправдой: задача не решена. Открывается тем ходом, которым начнётся этап 1

**Папка этой задачи не разбирается.** Владелец сказал разобрать папки и пометить их
завершёнными; здесь это неисполнимо: работа не сделана, этап 1 не начат, и снятый замысел
отобрал бы у следующего захода то, по чему он её продолжит, — а гард правки кода отбил бы саму
правку. Разбирается папка последним коммитом того PR, который эту задачу закроет.

## Решения по ходу

- **`plan.md` не кладётся образцом впрок** — пустой замысел на диске неотличим от замысла без
  этапов, а следующий заход читает папку первым делом и ей доверяет. Написан одним движением,
  когда ответы пришли.
- **Набор дерева зовёт умолчание, а не перечисляет проверки строками** — довод: перечисленные
  строками, они закрывают сегодняшнюю дыру и оставляют завтрашнюю. Проверка, которую пакет
  заведёт следующей редакцией, до дерева снова не доедет, и промах будет тот же самый и так же
  молчаливый. Вопрос был задан владельцу дважды и остался без ответа; цена решения названа в
  теле PR. Затронутый этап замысла: 1.
- **Накопленное принимается списком, а не разбирается здесь** — довод: владелец сказал «заведи
  эпик на разбор старых ошибок», то есть разбор идёт своим порядком. Пока проверки вне гейта,
  долг растёт быстрее, чем его разбирают.

## Заходы

### 17 августа 2026 года

- Взята по слову владельца, пока прогоны трёх готовых работ стояли в очереди
- Разведка: набор гейта объявлен надстройкой заново и умолчание пакета не зовёт; полнота гейта
  судится по именам шагов конвейера, а недостающих проверок в конвейере нет вовсе — поэтому
  потеря молчаливая с обеих сторон
- Замер состояния всех шести: три зелёные, три красные (11, 48 и 42 расхождения). Красное —
  накопленный долг: ни одна из трёх не гоняется ни в наборе, ни на прогоне
- Первая постановка вопроса о наборе владельцу была непонятна; переспрошено простыми словами
- Владелец спросил, откуда взялись расхождения, и ответ показал, что вопрос стоял неверно:
  гейт их не проверял никогда, и «вдруг покраснело» не случалось — числа сняты ручным запуском
- По слову владельца заведён эпик RT-806 и три задачи разбора долга: RT-807, RT-808, RT-809
