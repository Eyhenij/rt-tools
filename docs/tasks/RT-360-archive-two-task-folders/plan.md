# Замысел

Задача: #360. Ветка: `RT-360-archive-two-task-folders`.

**Задача:** RT-360 · **Ветка:** RT-360-archive-two-task-folders
**Поведение:** не меняется — работа трогает только тексты работ, кода и правил не касается.

После написания этот файл не правится. Пересмотр этапа идёт в `progress.md` решением по ходу.

## След задачи

| Что     | Где                                              |
| ------- | ------------------------------------------------ |
| Спеки   | не задеты                                        |
| Законы  | `docs/constitution/work-conduct.md`              |
| Правила | `.claude/skills/task-flow/`                      |
| Код     | не задет — только `docs/tasks/`, `docs/archive/` |

## Что считается сделанным

- В `docs/archive/` лежат две записи — `RT-351-publish-tail-cleanup.md` и
  `RT-358-glossary-window-handoff.md`: слова владельца и решения по ходу с причинами.
- Папок `docs/tasks/RT-351-publish-tail-cleanup/` и
  `docs/tasks/RT-358-glossary-window-handoff/` в дереве нет.
- Папка этой задачи разобрана тем же отчётом.
- `npm run check:board` выходит нулём, `npm run check:docs` новых расхождений не даёт.

## Этапы

### 1. Две записи в архив

- **Что делается:** из каждой папки берутся слова владельца и решения по ходу с причинами;
  запись собирается одним файлом по форме `RT-<номер>-<slug ветки>.md`, замыслы удаляются.
- **Признак готовности:** обе записи на месте, папок нет.
- **Чем проверяется:** `npm run check:board`, `npm run check:docs`.

### 2. Отчёт и разбор своей папки

- **Что делается:** отчёт от машинной учётной записи; папка этой задачи разбирается последним
  коммитом до слияния.
- **Признак готовности:** отчёт открыт, сверка очереди выходит нулём.
- **Чем проверяется:** `npm run check:board`.

## Чего эта работа не делает

- Не чинит дыру, через которую папки уезжают: слияние через веб не видит гард поставки. Это
  задача #338.
- Не решает судьбу ветки `RT-334-archive-closed-task-folders` с застрявшим переписанным
  архивом: там другая работа, и решение за владельцем.
- Не правит текстов уже уехавших записей архива: они описывают день переезда.
