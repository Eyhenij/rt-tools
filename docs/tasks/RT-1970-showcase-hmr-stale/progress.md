# Ход работ

## Где стоим

Переписывается каждым заходом, не дописывается.

- **Состояние:** `этапы-кончились`
- **Этап:** 3 из 3 — закрыт
- **Сделано:** починка написана, набор проб 21 из 21, обход историй по живой витрине зелёный —
  546 показов и 79 страниц обзора, пустых показов и ошибок рисования нет.
- **Следующий шаг:** набор проверок перед отправкой, разбор папки задачи, открытие заявки.
- **Не закоммичено:** ход работ.
- **Ждём владельца:** ничего по этой задаче.
- **Заявка:** не открыта.

## Решения по ходу

- **Название задачи замером не подтвердилось.** Карточка говорит «протухает после переключения
  ветки». Пять циклов переключения под поднятой витриной оставили указатель с 200 и нулём строк
  `Unable to index`. Затронут этап плана: 2.
- **Замер «подъём после kill -9» засчитан один раз, а не два.** Второй подъём упёрся в занятый
  порт и встал на вопросе витрины «поднять на 6008?», а 200 в тот заход ответила ещё живая первая.
  Затронут этап плана: 2.
- **Цена поломки больше, чем сказано в карточке.** Карточка говорит про страницу истории, замер
  показал: не отдаётся указатель целиком — `/index.json` отвечает 500. Затронут этап плана: 1.

## Заходы

### 2026-09-14

- Снята висевшая витрина прошлой сессии: процесс жил 1 ч 52 мин и не слушал ни одного порта.
- Поднята свежая витрина на 6007, `npm run check:window` зелёный.
- Переключение ветки под ней дало поломку: `Unable to index` и `/index.json` 500.
- Витрина, поднятая заново на тех же файлах, даёт 200 и 625 показов.
- Второй цикл переключения поломки не дал — поломка приходит не с каждым переключением.
- Пять циклов переключения ветки под поднятой витриной: `index.json=200`, строк `Unable to index`
  ноль в каждом. Итог — сломано 0 из 5.
- Два подъёма после `kill -9` по всем процессам витрины: `index.json=200`, ошибок ноль.
- Прочитан разбор витрины в `storybook/dist/core-server/index.js`: ошибка разбора файла историй
  кладётся в кэш записью `type: "error"`, `getIndex` после этого падает целиком, а запись
  снимается только событием наблюдателя по этому файлу. Отсюда и вечное состояние: события
  больше нет — указателя больше нет.
- Порча файла историй на поднятой витрине: `/index.json` — 500. Возврат файла: 200. Обе пробы —
  на витрине, поднятой начисто, с ожиданием 12 секунд после каждой правки.
- Шесть показов с карточками промерены на обрезку текста: `molecules-files-filecard--name` —
  шесть узлов с непомещающимся текстом, и все шесть обрезаны элипсисом; в остальных пяти
  непомещающегося текста нет вовсе.
- Починка написана по наблюдаемому признаку, а не по причине: указатель, отданный отказом,
  виден всегда, а кто испортил файл — нет.
- Обход историй после починки: 546 показов, 79 страниц обзора, ни одного пустого показа и ни
  одной ошибки рисования, код выхода 0.
