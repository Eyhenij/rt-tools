# Ход работы

## Где стоим

- **Состояние:** `этапы-кончились`
- **Этап:** все три отмечены
- **Сделано:** этап 1 — гард экзамена целиком; этап 2 — гард второго драйвера судит содержимое
  запускаемого файла; этап 3 — номер заголовка вынимается формой, гард утверждения не судит
  чужое слово. Инцидент заведён в приёме и переведён в «in_work»
- **Следующий шаг:** влить главную ветку, привести тексты, прогнать набор, разобрать папку
- **Незакоммиченное:** правки гардов, наборы проб, сценарии в спеках, статья правила и привязка
- **Ждём владельца:** нет
- **PR:** ещё не открыт

## Решения по ходу

- **2026-08-28. Молчание помощника профиля этой работой не чинится — оно уже влито.** Первая
  половина пункта 2 отчёта («печатать раз за сессию, что профиль не назван») стоит в главной
  ветке коммитом `3faa4966f`, вторая («сводка называет случай, когда механизм пуст») — коммитом
  `e28849248`: `doctor` перечисляет местные значения, которых ждут взятые хуки, а помощник
  объявляет своё строкой `# Местное значение:`. Отчёт написан по редакции 0.16.1 и об этом знать
  не мог. Этап 2 поэтому свёлся к пункту 3 — запуску драйвера из кода.
- **2026-08-28. Гард второго драйвера судит содержимое запускаемого файла, а не только строку
  команды.** Альтернатива — признать дыру в разделе «Чего из закона здесь нет» — отвергнута:
  этой самой дверью профиль обошли дважды за один заход, и признание оставило бы её открытой.
  Ложный отбой поиска по дереву снят тем, что код в доводе судится только вместе с именем
  интерпретатора и его флагом кода.
- **2026-08-28. У гарда обходных путей не было ни одной пробы.** Заведён набор
  `tests/browser-guard-drivers.test.sh` — и на новую дверь, и на старые: правка, сместившая
  границу образца, иначе не краснела бы нигде.

## Заходы

### 2026-08-28

- Инцидент из дерева-потребителя: пять дефектов редакции 0.16.1, первый запирает дерево целиком.
- Этап 1: помощник `hooks/write-targets.sh` заведён, `rule-source-guard.sh` переведён на него
  (13 ok), `exam-guard.sh` получил вывод настроек из-под гарда, судейство записи файла оболочкой,
  расширенное чтение вердикта и новый текст отказа — `tests/exam-guard.test.sh` 32 ok.
- Этап 2: `browser-guard-no-other-drivers.sh` судит содержимое запускаемого файла и код в доводе
  — `tests/browser-guard-drivers.test.sh` 16 ok; на снятой правке набор краснеет шестью
  проверками, проверено копией гарда через `HOOKS_OVERRIDE`.
- Этап 3: `git-guard-delivery.sh` вынимает номер из той части заголовка, которую признала сама
  форма — набор `git-guards.test.sh` 93 ok, на старом коде две проверки краснеют;
  `claim-guard.sh` не судит кавычки, строки цитирования, код и условное наклонение, отказ первым
  выходом называет снятие слова — `claim-guard.test.sh` 30 ok.
- Груз отправлен: `propose` увёз сводку, предложение и 62 разбора; разбор происшествия этой
  задачи виден в приёме чтением груза и переведён в «in_work».
- `bash projects/agent-kit/tests/run.sh` — ВСЕ НАБОРЫ ЗЕЛЁНЫЕ; `npm run check:specs` расхождений
  не называет; `node tools/check-file-size.mjs` — длиннее предела 0.
