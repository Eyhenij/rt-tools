# Plan

**Task:** RT-1915 · **Branch:** RT-1915-cargo-label-task
**Behaviour:** unchanged — правка живёт в слое правил; приложения она не касается.

## Task footprint

| What  | Where                                                            |
| ----- | ---------------------------------------------------------------- |
| Specs | `docs/specs/agent-kit/board/`                                    |
| Laws  | `docs/constitution/delivery.md`                                  |
| Rules | `.claude/skills/git-workflow/`                                   |
| Code  | `projects/agent-kit/assets/checks/`, `projects/agent-kit/tests/` |

## What counts as done

- Карточка с меткой груза и заголовком `[<КЛЮЧ>-<номер>]` судится сверкой как задача: по
  исполнителю, по доске, по связи с эпиком и по совпадению заголовков.
- Запись груза без такого заголовка по-прежнему из задачной половины убирается и в счёт груза
  идёт.
- Ложной строки «задачи нет среди открытых» о заявке на такую задачу больше нет.

## Stages

### 1. Задача узнаётся по заголовку, а не по отсутствию метки

- **What is done:** отбор `open` перестаёт убирать помеченную карточку с заголовком задачи; счёт
  груза считает то же, что убрано; статья спека со строкой компаньона и сценарий; пробы набора
  сверки.
- **Readiness sign:** новые пробы краснеют на прежнем отборе.
- **Verified by:** `bash projects/agent-kit/tests/checks-board.test.sh` — строка «0 провалов».

### 2. Дерево видит правку

- **What is done:** разложенные копии обновляются, сверка очереди работ прогоняется на живой
  очереди.
- **Readiness sign:** ложной строки о заявке `#1914` нет, счёт груза не вырос.
- **Verified by:** `npm run check:board` — в выводе нет строки «is not among the open ones».

## What this work does not do

- Не снимает метку груза ни с одной карточки: метка говорит, откуда работа выросла, и это её
  дело.
- Не меняет разбор груза и его отметки: правится только отбор сверки.
