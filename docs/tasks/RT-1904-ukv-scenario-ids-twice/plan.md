# Замысел

**Task:** RT-1904 · **Branch:** RT-1904-ukv-scenario-ids-twice
**Behaviour:** unchanged — двигаются номера сценариев в договорённости; кода приложения работа не касается

После написания этот файл не правится. Пересмотр этапа идёт в `progress.md` решением по ходу.

## След задачи

| Что            | Где                                              |
| -------------- | ------------------------------------------------ |
| Договорённость | `docs/specs/ui-kit-v2/proposed/material-preset/` |
| Спек           | `docs/specs/ui-kit-v2/form-dictionary/`          |
| Проверка       | `tools/check-specs.mjs`                          |

## Что считается сделанным

- Четырнадцать номеров, выданных дважды, разведены: договорённость о наборе оформления несёт
  свободные номера, словарь формы панели остаётся при своих.
- Ни один заголовок теста не тронут: сценарии договорённости не покрыты ни одним из них.
- `npm run check:specs` даёт код 0.

## Этапы

### 1. Номера договорённости о наборе оформления сдвинуты в свободный хвост

- **Что делается:** сценарии `SC-UKV-99`…`SC-UKV-112` договорённости становятся
  `SC-UKV-115`…`SC-UKV-128` — в перечне сценариев и в строках компаньона рядом. У словаря формы
  панели номера остаются: за ними стоят тесты, за номерами договорённости — ничего.
- **Признак готовности:** ни один номер не выдан дважды, свободный хвост продолжается с прежнего
  места.
- **Чем проверяется:** `npm run check:specs` — выход `0`.

## Чего эта работа не делает

- Не трогает ни строки правил и проверок: столкновение вышло из порядка веток эпика, а не из
  проверки. Отдельная находка о том, что ответ о свободном номере стареет, лежит у владельца
  разбором закрытой работы.
- Не правит содержание ни одного сценария: двигается только номер.
