# Ход работы

## Где стоим

- **Состояние:** `этапы-кончились`
- **Этап:** 1 из 1 сделан — спиннер ждёт перед показом
- **Сделано:** договорённость написана, вход задержки заведён, показ отложен до срока, спека
  рядом закрывает четыре сценария
- **Следующий шаг:** отдача ждёт слова владельца — ветка держится локально
- **Незакоммиченное:** нет
- **Ждём владельца:** нет
- **PR:** не открывается — владелец назначил держать ветки локально

## Решения по ходу

- **2026-08-19. Ветка остаётся локальной, отчёт не открывается.** Владелец сказал прямо: «Не пуш
  ветки, храни их только локально».
- **2026-08-19. Задержка живёт в самом спиннере, а не у того, кто его вставляет.** Спиннер
  вставляют пять мест кита и любое приложение; задержка у каждого из них своя означала бы пять
  разных ответов на один вопрос. Альтернатива — утилита отложенного признака рядом — отвергнута:
  она требует правки каждого места вставки.

## Заходы

### 2026-08-19

- Разведка: у спиннера три входа (диаметр, коробка, фон), собственного признака показа нет —
  его рисуют снаружи через `@if`. Спеки и истории рядом нет. Утилиты отложенного показа в ките
  нет ни одной; ближайшее — задержка поиска второго кита, но она о потоке строки.

- **2026-08-19. Отсчёт перенесён из конструктора в крючок вставки.** Спека показала, что в
  конструкторе вход держит своё умолчание: задержка, названная потребителем, туда ещё не дошла,
  и спиннер показывался сразу. Затронутый этап: 1.
- Этап 1 сделан: `pnpm exec nx test @rt-tools/ui-kit --testFile=src/lib/ui-kit/spinner/spinner.component.spec.ts` — 4 из 4 зелёных.
- Полный набор кита: 35 тестов зелёных, линт, типы и сборка прошли.
- Снимки не гонялись: истории у спиннера нет вовсе, а при умолчании задержки разметка кадра та
  же — показ включается до первой отрисовки.
