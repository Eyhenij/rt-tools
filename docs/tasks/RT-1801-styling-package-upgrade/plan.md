# Замысел

**Задача:** RT-1801 · **Ветка:** RT-1801-styling-package-upgrade
**Поведение:** не меняется — правятся правило и паттерн пакета, кода приложения работа не касается

## След задачи

| Что     | Где                                                                       |
| ------- | ------------------------------------------------------------------------- |
| Спеки   | нет: правило и паттерн, спека домена под них нет                          |
| Законы  | `docs/constitution/delivery.md`                                           |
| Правила | `.claude/skills/dependencies/`, `.claude/skills/dependencies-upgrade/`    |
| Код     | `projects/agent-kit/assets/rules/`, `projects/agent-kit/assets/patterns/` |

## Что считается сделанным

- Правило зависимостей несёт статью о подъёме пакета оформления отдельной задачей; компаньон
  дерева — строку привязки к ней.
- Паттерн подъёма несёт шаг о поиске снятых свойств командой между установкой снимка и сборкой.
- Раскладка сходится, сверка спеков — код 0.

## Этапы

### 1. Статья и шаг

- **Что делается:** статья в конец раздела «Как закон применяется здесь» правила; шаг третьим в
  «Порядке» паттерна; строка привязки в компаньоне; раскладка.
- **Признак готовности:** `pnpm run agent-kit:check` сходится; `node tools/check-specs.mjs` —
  код 0; `node tools/check-prose-style.mjs` по правленым файлам без находок.
- **Чем проверяется:** те же три команды.

## Чего эта работа не делает

- Проверки дерева на снятые свойства не заводит: дерево пакетов оформления не читает.
