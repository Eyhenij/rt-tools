# Замысел

**Задача:** RT-1076 · **Ветка:** RT-1076-task-from-text-needs-proof
**Поведение:** не меняется — работа правит тексты пакета правил, кода приложения не трогает;
так сказал владелец, поручив брать задачи с приёмника по одной

После написания этот файл не правится. Пересмотр этапа идёт в `progress.md` решением по ходу.

## След задачи

| Что     | Где                                                                      |
| ------- | ------------------------------------------------------------------------ |
| Спеки   | не задеваются: обе правки держатся чтением                               |
| Законы  | не задеваются                                                            |
| Правила | `.claude/skills/task-flow-start/`, `.claude/skills/git-workflow-commit/` |
| Код     | не трогается                                                             |

## Что считается сделанным

- Паттерн заведения задачи говорит: задача, заведённая по тексту документа, называет в теле
  проверку в дереве — файл и символ либо вывод команды.
- Паттерн начала работы говорит: разведка кончается воспроизведённым симптомом, а
  невоспроизведённый закрывает задачу как отпавшую.
- Раскладка сходится, длина текстов в пределе, набор обвязки зелёный.

## Этапы

### 1. Две строки в паттернах

- **Что делается:** правка паттерна заведения задачи и шага разведки в паттерне начала работы
- **Признак готовности:** обе строки стоят, раскладка сходится
- **Чем проверяется:** `pnpm run agent-kit:check` — разложенное сходится с пакетом

### 2. Закрытие

- **Что делается:** набор обвязки, разбор папки задачи последним коммитом
- **Признак готовности:** папка разобрана, набор зелёный; заявка ждёт утра — владелец просил
  ветки наружу не отправлять
- **Чем проверяется:** `pnpm run agent-kit:check` — разложенное сходится с пакетом

## Чего эта работа не делает

- Не заводит гарда: тело задачи заводится командой, а вывод команды в нём от цитаты документа
  машине не отличить.
