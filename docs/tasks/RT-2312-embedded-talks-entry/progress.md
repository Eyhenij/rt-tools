# Progress

## Where we stand

- **State:** `этап-идёт`
- **Stage:** 3 of 4 — Операции чтения под признаком страницы
- **Done:** операция обмена подписи на признак заведена отдельным контроллером встраиваемой страницы,
  отказы на все случаи описания, десять проверок рядом
- **Next step:** пустить чтение списка, чтение сообщений, ответ и закрытие разговора под признаком
  страницы, сузив каждую до сайта признака
- **Uncommitted:** нет
- **Waiting for the owner:** нет
- **PR:** ещё не открыт; предыдущая задача эпика — PR 2320, ждёт кнопки владельца

## Steps

- `[x]` 1.1 Дописать в `chat-hook.logic.ts` счёт подписи входа: ключ сайта и минута
- `[x]` 1.2 Написать разбор признака страницы: что в нём лежит, когда он истёк
- `[x]` 1.3 Покрыть обе проверками рядом с файлом
- `[x]` 2.1 Завести операцию в чтении чата: принимает ключ, минуту и подпись, отвечает признаком
- `[x]` 2.2 Отказы: нет подписи, не сходится, минута вне допуска, неизвестный или выключенный ключ
- `[x]` 2.3 Покрыть проверками контроллера
- `[>]` 3.1 Пустить чтение списка, чтение сообщений, ответ и закрытие разговора под признаком
- `[ ]` 3.2 Сузить каждую до сайта признака; чужая переписка отвечает как ненайденная
- `[ ]` 3.3 Просроченный признак отвечает своим отказом
- `[ ]` 4.1 Привести компаньон описания: выполненные правила получают адреса в коде
- `[ ]` 4.2 Разобрать папку задачи последним коммитом, добавив запись в `docs/archive/`
- `[ ]` 4.3 Набор перед отправкой, отправка, PR стопкой в ветку RT-2311, рецензент, номер владельцу

## Decisions along the way

- Операции встраиваемой страницы стоят отдельным контроллером `chat-embedded.controller.ts`, а не в
  чтении оператора, как называл след задачи: то закрыто входом человека, эта дверь — подписью
  потребителя, и два способа представиться в одном файле читались бы как одна поверхность.
- Заведены три кода отказа: `ChatEntryRejected` на нет подписи, чужую подпись, неизвестный ключ и
  выключенную площадку — один на все, чтобы перебирающий ничего о ключах не узнал; `ChatEntryStale`
  на минуту вне допуска; `ChatEntryExpired` для просроченного признака — его бросит этап 3.
- Адрес страницы сверяется со списком площадки, только когда он назван: заголовок ставит браузер, и
  страница его не подделает, а обращение без заголовка пришло с сервера потребителя — за него
  отвечает подпись.

- Вторую тайну сайту не заводим: подпись входа считается тайной `hookSecret`, которая у сайта уже
  есть и уже лежит у потребителя на сервере. Компаньон описания скажет, что «секрет сайта» — это она.

## Handover of the session

Put together by a hook before the compaction of the context (auto).

**Working tree:** /Users/eyhenij/WebstormProjects/rt-tools
**Branch:** RT-2312-embedded-talks-entry

### Where we stand at the minute of the compaction

- **State:** `этап-идёт`
- **Stage:** 2 of 4 — Операция обмена подписи на признак
- **Next step:** завести операцию обмена подписи на признак в
- **PR:** ещё не открыт; предыдущая задача эпика — PR 2320, ждёт кнопки владельца

The progress in full — `docs/tasks/RT-2312-embedded-talks-entry/progress.md`; the plan lies next to it.

### Uncommitted

```
none
```

### Commits over the main branch

```
56f274143 feat(rt:message-bus): подпись входа встраиваемой страницы и признак страницы
5d2a082dd docs(rt:message-bus): папка задачи RT-2312 заведена, замысел записан
d34ec4d01 docs(rt:message-bus): встраиваемая страница переписок описана до кода
a98bc3f3f Merge remote-tracking branch 'origin/main' into RT-2309-embedded-talks-page
3fc332460 Merge remote-tracking branch 'origin/main' into RT-2309-embedded-talks-page
0a1d6b234 docs(rt:agent-kit): записи о прошлом старше срока убраны и в ветке эпика
40e0fb850 docs(rt:agent-kit): строка копии называет эпик встраиваемой страницы
8bf726d3a docs(rt:message-bus): план эпика встраиваемой страницы переписок записан
```

Written by a hook before the compaction of the context. Everything standing here is checked
against the tree: a handover retells what was written and describes the minute it was put together.
