# Замысел

**Задача:** RT-1687 · **Ветка:** RT-1687-merge-and-pr-articles
**Поведение:** не меняется — правится слой правил

## След задачи

| Что      | Где                                                        |
| -------- | ---------------------------------------------------------- |
| Правила  | `projects/agent-kit/assets/rules/git-workflow.github.md`   |
| Паттерны | `projects/agent-kit/assets/patterns/git-workflow-merge.md` |

## Что считается сделанным

- Строка таблицы о собранном из описания файле стоит после строки о коде.
- Статья о конфликте в исполняемом разложенном файле стоит перед списком команд проверки.
- Статья о базе заявки стоит за статьёй о вершине открытой заявки без прогона.
- Раскладка положила правки в дерево, сверка зелёная.

## Этапы

### 1. Правки источников

- **Что делается:** три вставки в два ресурса.
- **Чем проверяется:** `pnpm run agent-kit:check`.

### 2. Раскладка и проверки

- **Что делается:** `pnpm run agent-kit:sync`, тесты пакета, проверки документации.
- **Чем проверяется:** тесты `@rt-tools/agent-kit` зелёные, `check-doc-paths` и `check-specs` чисты.
