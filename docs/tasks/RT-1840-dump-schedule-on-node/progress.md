# Ход работы

## Где стоим

Перезаписывается каждым заходом, а не дописывается.

- **Состояние:** `этапы-кончились`
- **Этап:** 3 из 3 — тексты
- **Сделано:** этап 1 — предел `KEEP_DUMPS` в `save`, восемь выгрузок на местной копии
  оставляют семь файлов; этап 2 — `deploy/dump.crontab`, шаг выкатки сливает строку в
  расписание, дважды прогнан на подмене: своя строка одна, чужая на месте; защита каталога
  дампов и журнала от `rsync --delete`; этап 3 — спек узла, привязки, SC-MB-286, описание прода
- **Следующий шаг:** набор гейта, разбор папки, черновик заявки за #1841
- **Незакоммиченное:** чужая правка манифеста дерева — в коммит не идёт
- **Ждём владельца:** после слияния и выкатки — `ssh message-bus 'crontab -l | grep dump.sh'`
- **PR:** ещё не открыт

## Решения по ходу

- **Каталог дампов и журнал защищены от `--delete` при переносе состава.** Найдено на копии:
  rsync с `--delete` сносит на узле всё, чего нет в дереве, — защищён был только `.env.prod`, и
  первая же выкатка после ночной выгрузки уносила бы все дампы. Затронутый этап замысла: 2.

## Заходы

### 2026-09-06

- Заведена вслед за RT-1839: на узле не было ни задания расписания, ни каталога дампов.
