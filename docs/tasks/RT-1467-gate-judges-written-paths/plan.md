# Замысел

**Задача:** RT-1467 · **Ветка:** RT-1467-gate-judges-written-paths
**Поведение:** не меняется — работа правит обвязку слоя правил; кода приложения не касается

После написания этот файл не правится. Пересмотр этапа идёт в `progress.md` решением по ходу.

## След задачи

| Что       | Где                                             |
| --------- | ----------------------------------------------- |
| Умолчание | `projects/agent-kit/assets/defaults/project.sh` |
| Проба     | `projects/agent-kit/tests/defaults.test.sh`     |
| Сценарий  | `docs/specs/agent-kit/checks/scenarios.md`      |

## Что считается сделанным

- Кусок команды, пишущий только перенаправлением или `tee`, отдаёт целью записи её аргумент, а
  не всякое похожее на путь слово из своего текста.
- Прочие виды записи разбираются по-прежнему.
- Сценарий с номером стоит в спеке, проба его закрывает, набор проб пакета зелёный.

## Этапы

### 1. Цель записи вместо всех слов куска

- **Что делается:** в `rt_shell_paths_default` кусок с одним лишь перенаправлением отдаёт цели
  перенаправления и `tee`.
- **Признак готовности:** `bash -n` молчит, старые сценарии не краснеют.
- **Чем проверяется:** `bash projects/agent-kit/tests/defaults.test.sh`.

### 2. Сценарий и проба

- **Что делается:** сценарий в спеке проверок, пробы на оба случая — путь в тексте записи не
  берётся, путь правки по месту берётся.
- **Признак готовности:** проба падает без правки и проходит с ней.
- **Чем проверяется:** `npm run check:specs`, набор проб пакета.

## Чего эта работа не делает

- Не трогает вырезание тела документа: оно уже сделано и работает.
