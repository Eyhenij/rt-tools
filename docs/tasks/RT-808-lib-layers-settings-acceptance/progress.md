<!-- rt-kit v0.8.3 · samples/tasks/_template/progress.md · baf326afb38f · правится надстройкой, не здесь -->
# Ход работы

## Где стоим

Перезаписывается каждым заходом, а не дописывается.

- **Этап:** 4 из 4 — тексты приведены к сделанному, работа отдана на разбор
- **Сделано:** этапы 1–3 закрыты — тег назван настройкой, все три рода приняты внесённым
  нарушением, тексты приведены к сделанному
- **Следующий шаг:** пуш ветки и открытие PR черновиком — по слову владельца
- **Незакоммиченное:** нет
- **Ждём владельца:** слова на пуш и открытие PR по этой задаче; по задаче RT-807 — вливания
  PR #814
- **PR:** ещё не открыт

## Решения по ходу

- **Задача не закрывается как разобранная, хотя проверка зелёная** — довод: `noDependencyTags`
  в дереве пуст, и род расхождения не судится вовсе, а либа с прибитым пустым списком
  зависимостей в дереве есть. Затронутый этап замысла: 1.
- **В `noDependencyTags` попал один тег, а не все шесть с пустым списком** — довод: настройка
  называет теги, чей список обязан остаться пустым, а не те, у которых он пуст сегодня. У
  `scope:message-bus-common` причина записана в его же границах: те же типы берёт отправляющая
  сторона. Пять тегов слоя утилит приёмника пусты сегодня и законно получат зависимость завтра.
  Затронутый этап замысла: 1.
- **Приставка принята подменой настройки, а не переименованием либы** — довод: род расхождения
  уже принят внесённым нарушением в RT-793, а здесь проверялось, что зелень держит настройка.
  Подмена `libPrefix` на чужое слово краснеет 43 строками, то есть всеми либами доменной сетки.
  Затронутый этап замысла: 3.

## Заходы

### 17 августа 2026

- Прочитаны законы поставки и ведения работы, правила `lib-layers`, `task-flow`, `git-workflow`,
  `doc-style`, `spec-driven`, `typescript-conventions` и паттерны начала работы и коммита. Ответ
  на вопрос о вливании PR #814 нашёлся в законе, владельцу он не задавался.
- Состояние RT-807 сверено с передачей: PR #814 открыт, `MERGEABLE / CLEAN`, зелёный прогон на
  коммите `0c73c96f`, на вершине проверок ноль, восемь файлов сверху — все `.md`.
- Разобрано, что осталось от RT-808: из трёх настроек RT-793 внесённым нарушением принята одна.
  `noDependencyTags` пуст, и `checkNoDependencyLibs` на пустом списке не делает ничего.
- Приёмка внесённым нарушением, по строке на случай: непустой список зависимостей у тега —
  «его список непуст»; снятое описание тега — «тег не описан ни в одном файле границ»; подмена
  `libPrefix` — 43 строки о приставке; снятие `public-api.ts` из `barrelFiles` — 2 строки о
  реэкспорте в бареле; реэкспорт чужого символа в бареле либы — своя строка. После снятия всех
  нарушений `git diff` показывает только настройку.
- Зелёные: `pnpm run agent-kit:check`, `node tools/check-specs.mjs`,
  `node tools/check-doc-paths.mjs` (294 документа), `node tools/check-lib-layers.mjs`.
  Не гонялось: гейт пуша целиком — он идёт на пуше, а пуш ждёт слова владельца.
