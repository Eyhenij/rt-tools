# Замысел

**Поведение:** не меняется — правится текст паттерна, кода приложения работа не трогает.

## Этап 1. Событие ввода подделкой значения не заменяется

Раздел между «Узкий экран» и ловушками инструмента: прокрутка присвоением события не даёт, отсюда
две проверки, и ни одна не заменяет другую.

## Этап 2. Долгий прогон живёт в странице

Раздел сразу за ловушками инструмента: обход кладётся в переменную страницы, вызовы спрашивают о
готовности, собранное печатается срезами.

## Этап 3. Раскладка и проверки

`agent-kit:sync`, `agent-kit:check`, проверки адресов и слога.
