# Состояние

**Задача:** RT-1712 · **Ветка:** RT-1712-measure-events-and-long-runs

## Где стоим

- **Состояние:** `этапы-кончились`

## Этапы

- [x] 1. Событие ввода подделкой значения не заменяется
- [x] 2. Долгий прогон живёт в странице
- [x] 3. Раскладка и проверки

## Замечания хода

- Ветка стоит каскадом от `RT-1711-stand-tenant-header`; в удалённое дерево не отправляется.

## Что сделано

- Раздел «Событие ввода подделкой значения не заменяется»: прокрутка присвоением события не даёт,
  отсюда две проверки — механика настоящим колесом, раскладка под признаком — признаком руками.
  Назван и признак того, что попал именно сюда.
- Раздел «Долгий прогон живёт в странице, а не в вызове»: обход кладётся в переменную страницы,
  следующие вызовы спрашивают о готовности, собранное печатается срезами — ответ инструмента
  режется молча.
- Заодно починен слог одной строки.
- Раскладка сходится, адреса и слог чисты.
