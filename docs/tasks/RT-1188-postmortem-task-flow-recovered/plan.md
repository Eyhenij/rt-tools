# Замысел

**Задача:** RT-1188 · **Ветка:** RT-1188-postmortem-task-flow-recovered
**Поведение:** не меняется — довозится написанный документ, кода приложения правка не касается

После написания этот файл не правится. Пересмотр этапа идёт в `progress.md` решением по ходу.

## След задачи

| Что     | Где                                                                |
| ------- | ------------------------------------------------------------------ |
| Спеки   | не задеты                                                          |
| Законы  | `docs/constitution/work-conduct.md` — разбор происшествия под ним  |
| Правила | `.claude/skills/task-flow/`, `.claude/skills/git-workflow-commit/` |
| Код     | не задет                                                           |

Разбор несёт три предложения слою правил по ресурсам `patterns/git-workflow-commit.md` и
`rules/task-flow.md`. Их разбор — работа приёма груза, не этой задачи: сюда они попадают только
как содержимое файла.

## Что считается сделанным

- `docs/postmortems/2026-08-19-task-created-before-task-flow.md` лежит в главной ветке, и его
  содержимое сходится побайтно с тем, что было написано 19 августа.
- Папки задачи в ветке нет, запись о работе есть в `docs/archive/`.

## Этапы

### 1. Разбор довезён коммитом

- **Что делается:** файл, спасённый из снятой рабочей копии, кладётся в ветку задачи.
- **Признак готовности:** коммит несёт ровно один файл, и это разбор.
- **Чем проверяется:** `git show --stat HEAD` — один файл, 72 строки прибавления, путь
  `docs/postmortems/`.

### 2. Папка задачи разобрана

- **Что делается:** разбор просьбы уезжает в описание прошлого, замысел и ход работы снимаются.
- **Признак готовности:** в ветке нет `docs/tasks/RT-1188-…/`, в `docs/archive/` есть запись.
- **Чем проверяется:** `git status --short` — чисто; `ls docs/tasks/` — папки задачи нет.

## Чего эта работа не делает

- **Не правит содержание разбора** — он описывает ход 19 августа, и правка сделала бы его
  описанием другого хода.
- **Не разбирает его предложения слою правил** — это идёт приёмом груза своим порядком.
- **Не ищет, не потерялось ли так же что-то ещё** — рабочих копий больше нет, искать негде.
