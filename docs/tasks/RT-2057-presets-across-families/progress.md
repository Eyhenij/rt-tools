# Progress

## Where we stand

- **State:** `этап-идёт`
- **Stage:** 2 of 3 — обёртка по волнам
- **Done:** этап 1 закрыт счётом: набор касается 70 семейств из 84, остальные названы поимённо
- **Next step:** обернуть матрицы затронутых семейств парой половин, волна за волной
- **Uncommitted:** нет
- **Waiting for the owner:** нет
- **PR:** ещё не открыт

## Decisions along the way

- **Сначала счёт, потом обёртка** — у части семейств набор ничего не меняет, и пара там даёт
  вдвое более широкий кадр без единого нового сведения. Affected stage of the plan: 1.
- **Считаем по вычисленным значениям узлов кита, а не по кадру** — кадр по корню показа меняется
  у всех 431 показов подряд: набор переписывает и фон самой обвязки показа. Узлы кита отделены от
  узлов обвязки по имени класса. Affected stage of the plan: 1.
- **У трёх семейств группы кнопок счёт даёт ноль, и это след их собственной пары** — половины у
  них уже стоят, и признак набора, поставленный на корень, ничего в них не двигает. Их зеро — не
  свойство набора. Affected stage of the plan: 1.

## Sessions

### 2026-09-12

- Заведены ветка от ветки эпика и папка задачи; колонка передвинута в работу
- В порядок эпика добавлена строка 6.11: находка про панель подробностей рабочего стола,
  задача 2074
- В решения эпика записано слово владельца: перенос первого кита начинается с разговора с ним
- Первый счёт по кадрам отброшен: набор меняет кадр у всех 431 показов, потому что переписывает и
  фон обвязки показа
- Счёт по вычисленным значениям узлов кита: показов с корнем 431, набор меняет узлы у 208,
  семейств 84, затронуто 70, у них 360 показов
- Не затронуто 14 семейств: полоса прокрутки, логотип, скелетон и его обёртка, область текста,
  переключатель, разница значений, список подробностей, колокольчик, просмотрщик фото, текстовый
  редактор — и три семейства группы кнопок, у которых пара уже стоит
