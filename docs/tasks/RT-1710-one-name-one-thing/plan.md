# Замысел

**Поведение:** не меняется — правится текст правила, кода приложения работа не трогает.

## Этап 1. Статья о едином словаре имён

В правило `styling-bem`, рядом со статьёй об общем слое раскладки — статья: одна вещь называется
одним именем во всех местах, где встречается; место задаётся видом блока, а не вторым набором имён.

## Этап 2. Адрес статьи, раскладка и проверки

Строка в компаньоне, `agent-kit:sync`, `agent-kit:check`, проверки адресов и слога.
