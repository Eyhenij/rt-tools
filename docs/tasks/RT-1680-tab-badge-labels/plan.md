# Замысел

**Задача:** RT-1680 · **Ветка:** RT-1680-tab-badge-labels
**Спек:** `projects/ui-kit-v2/src/lib/components/tabs/CONTEXT.md`
**Поведение:** меняется

После написания этот файл не правится. Пересмотр этапа идёт в `progress.md` решением по ходу.

## След задачи

| Что     | Где                                                                               |
| ------- | --------------------------------------------------------------------------------- |
| Спеки   | `projects/ui-kit-v2/src/lib/components/tabs/CONTEXT.md`                           |
| Законы  | `docs/constitution/verifiability.md`, `docs/constitution/frontend-application.md` |
| Правила | `.claude/skills/ui-component-tests/`, `.claude/skills/rt-tools-storybook/`        |
| Код     | `projects/ui-kit-v2/src/lib/components/tabs/`                                     |

## Что считается сделанным

- У `[rtTab]` два новых необязательных входа: `badgeTitle` — подсказка при наведении на метку,
  `badgeAriaLabel` — подпись метки для чтения с экрана. Незаданные — разметка прежняя.
- Спека компонента утверждает оба входа и прежнее поведение без них.
- `CONTEXT.md` и история витрины называют новые входы.

## Этапы

### 1. Входы и разметка

- **Что делается:** два сигнальных входа в директиве; метка получает `[rtTooltip]` и
  `[attr.aria-label]` от них, пустое значение ничего не ставит.
- **Признак готовности:** спека компонента зелёная с тремя новыми случаями: подсказка у метки,
  подпись у метки, без входов у метки нет ни подсказки, ни подписи.
- **Чем проверяется:** `pnpm exec nx test @rt-tools/ui-kit-v2 --testFile=src/lib/components/tabs/rt-tabs.component.spec.ts` — 0 провалов.

### 2. Контекст и витрина

- **Что делается:** строки входов в `CONTEXT.md`; контролы и поля обёртки истории.
- **Признак готовности:** история собирается, контролы на месте.
- **Чем проверяется:** `pnpm exec nx affected -t lint typecheck test build` — зелёный.

## Чего эта работа не делает

- Не меняет вид метки и не трогает подсказку неверной вкладки.
- Не заводит спек домена для вкладок: у китов договорённость живёт в `CONTEXT.md`.
