# Ход работы

## Где стоим

- **Состояние:** `этапы-кончились`
- **Этап:** 1 из 1 сделан — кнопка знает о пустом значении
- **Сделано:** договорённость о продукте написана, признак пустоты заведён на готовой утилите,
  условие показа кнопки читает его, спека рядом закрывает четыре сценария
- **Следующий шаг:** отдача ждёт слова владельца — ветка держится локально
- **Незакоммиченное:** нет
- **Ждём владельца:** нет
- **PR:** не открывается — владелец назначил держать ветки локально

## Решения по ходу

- **2026-08-19. Ветка остаётся локальной, отчёт не открывается.** Владелец сказал прямо: «Не пуш
  ветки, храни их только локально». Работа доводится до зелёных проверок и коммита, отдача
  откладывается до его слова.

## Заходы

### 2026-08-19

- Разведка: кнопка копирования — голая разметка в шаблоне базовой ячейки, условие показа одно
  (`column()?.copyable`), значение ячейки считает `cellValue`, спеки рядом нет вовсе. Готовая
  утилита пустоты — `isEmpty` в `@rt-tools/utils`, в первом ките ещё не звалась.

- Этап 1 сделан: `pnpm exec nx test @rt-tools/ui-kit --testFile=src/lib/ui-kit/table/components/table-base-cell/table-base-cell.component.spec.ts` — 4 из 4 зелёных.
- Линт, типы и сборка кита прошли; `npm run check:specs` видит домен первого кита с четырьмя
  покрытыми сценариями, `npm run check:docs` — 361 документ без расхождений.
- Пустых значений в данных историй таблицы нет, поэтому эталоны снимков правка не двигает.
