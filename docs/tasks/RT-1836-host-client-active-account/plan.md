# Замысел

**Задача:** RT-1836 · **Ветка:** RT-1836-host-client-active-account
**Поведение:** не меняется — правка текста правила поставки и его спека, кода приложения не
задевает.

После написания этот файл не правится. Пересмотр этапа идёт в `progress.md` решением по ходу.

## След задачи

| Что       | Где                                                                               |
| --------- | --------------------------------------------------------------------------------- |
| Правило   | `projects/agent-kit/assets/rules/git-workflow.github.md`                          |
| Ловушки   | `projects/agent-kit/assets/pitfalls/git-workflow.github.md`                       |
| Спек      | `docs/specs/agent-kit/delivery-identity/`                                         |
| Проба     | `projects/agent-kit/tests/git-guards-identity.test.sh`                            |
| Компаньон | `.claude/skills/git-workflow/implementation.md`                                   |
| Копии     | `.claude/skills/git-workflow/SKILL.md`, `.claude/skills/git-workflow/pitfalls.md` |

## Что считается сделанным

- Правило поставки говорит, что активная запись клиента хостинга выбирается на машину, а не на
  дерево, и машинная запись подставляется на вызов, а активной не делается.
- Правило спека с привязкой к тексту правила, сценарий SC-AK-892 с пробой на текст статьи.
- Компаньон этого дерева называет, чем спросить активную запись клиента.

## Этапы

### 1. Тексты

- **Что делается:** статья правила и ловушка при нём, правило спека с привязкой, сценарий,
  строка компаньона дерева; проба SC-AK-892; раскладка копий.
- **Признак готовности:** проба находит статью в правиле; сверка спеков и раскладка зелёные.
- **Чем проверяется:** `bash projects/agent-kit/tests/git-guards-identity.test.sh` — «0
  провалов»; `node tools/check-specs.mjs` — код 0; `pnpm run agent-kit:check` — «разложенное
  сходится».

### 2. Отдача

- **Что делается:** папка задачи разбирается, заявка открывается черновиком чередой за
  RT-1834.
- **Признак готовности:** заявка открыта, прогон на вершине идёт.
- **Чем проверяется:** `gh pr view` — номер и состояние.

## Чего эта работа не делает

- Не заводит гарда на вход в клиент хостинга: состояние машины гардам дерева не видно.
- Не правит редакции правила для других хостингов: статьи о личности вызова там нет.
- Не правит компаньон правила у дерева-потребителя: он у него свой.
