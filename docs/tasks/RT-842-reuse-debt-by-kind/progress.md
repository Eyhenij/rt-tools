<!-- rt-kit v0.8.3 · samples/tasks/_template/progress.md · baf326afb38f · правится надстройкой, не здесь -->

# Ход работы

## Где стоим

Перезаписывается каждым заходом, а не дописывается.

- **Этап:** 4 из 5 — настоящие обходы снимаются кодом; два из четырёх сняты, два остались
- **Сделано:** долг единообразия с 198 сведён до 2. Принято с причиной 194 строки двух родов —
  обёртки историй витрины с обвязкой показа и компоненты, у которых шаблона нет вовсе. Снято
  кодом: шаблон значка уехал в свой файл, три отступления таблицы второго кита помечены
  маркером `native-ok` с причиной. Заведена первая спека каталога BEM ядра — два случая,
  зелёные.
- **Следующий шаг:** разобрать оставшиеся две строки — `@Input() rtMod` в директивах блока и
  элемента ядра. Перевод на сигнальный вход этим заходом откачен: спека, написанная под него,
  падала четырьмя случаями из шести, **и ровно так же она падает на исходных директивах** —
  значит неверна спека, а не правка, и разбирать надо `parseMods`/`setMods` в
  `projects/core/src/lib/bem/bem.utils.ts`. Пока это не разобрано, вывода «перевод ломает
  модификаторы» делать нельзя: он был сделан этим заходом и оказался поспешным.
- **Незакоммиченное:** список принятого, маркеры в таблице второго кита, шаблон значка, спека
  директив BEM, папка этой задачи
- **Ждём владельца:** нет
- **PR:** ещё не открыт

## Решения по ходу

- **Обёртки историй витрины приняты, а не переписаны** — правило показа кита прямо говорит:
  история целит в обёртку, обёртка держит демонстрационное состояние и в пакет не едет.
  Затронутый этап замысла: 2.
- **Компоненты с пустым шаблоном приняты** — `template: ''` у трёх компонентов и одна
  интерполяция у подсказки: своей вёрстки за ними нет, блок рисуется стилями на хосте, а
  признак читает само наличие ключа. Затронутый этап замысла: 3.
- **Отступления таблицы второго кита помечены маркером, а не переписаны** — сигнальный
  `viewChild()` резолвится после первой отрисовки строк, а колонка действий должна попасть в
  реестр `CdkTable` до неё; обоснование уже стояло в коде, не хватало только маркера.
  Затронутый этап замысла: 4.
- **Спека директив BEM сокращена до двух зелёных случаев** — четыре случая про модификаторы
  красные и на исходном коде тоже; коммитить красное нельзя, а разбор их — работа следующего
  захода. Затронутый этап замысла: 4.

## Заходы

### 18 августа 2026 года

- Числа долга сняты этим же днём: обходов готового 211 в 198 признаках, принято 0
- Раскладка по родам: 186 обёрток историй витрины, 3 обвязки показа, 4 компонента без шаблона,
  4 настоящих обхода
- Стало: 194 принято, долг 2
- Гард единообразия отбил правку, потому что в тексте команды оболочки стояла снимаемая строка
  с декоратором входа; правка легла инструментом правки файла, где судится содержимое
- Гард пуша отбил `git stash push`, приняв слово в имени подкоманды за пуш
