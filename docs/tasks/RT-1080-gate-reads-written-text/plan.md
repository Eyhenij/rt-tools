# Замысел

**Задача:** RT-1080 · **Ветка:** RT-1080-gate-reads-written-text
**Поведение:** не меняется — работа правит тексты пакета правил, кода приложения не трогает;
так сказал владелец, поручив брать задачи с приёмника по одной

После написания этот файл не правится. Пересмотр этапа идёт в `progress.md` решением по ходу.

## След задачи

| Что     | Где                                     |
| ------- | --------------------------------------- |
| Спеки   | не задеваются: граница держится чтением |
| Законы  | не задеваются                           |
| Правила | `.claude/skills/agent-kit/`             |
| Код     | не трогается                            |

## Что считается сделанным

- Скил пакета называет границу гейта: пути берутся у пишущего куска целиком, и путь, названный
  в записываемом тексте, требует правила наравне с целью записи.
- Там же назван приём обхода: запись и текст с путями разводятся на два вызова.
- Раскладка сходится, длина текста в пределе, набор обвязки зелёный.

## Этапы

### 1. Ловушка о границе гейта

- **Что делается:** пункт о путях в записываемом тексте и приём обхода
- **Признак готовности:** пункт стоит, раскладка сходится
- **Чем проверяется:** `pnpm run agent-kit:check` — разложенное сходится с пакетом

### 2. Закрытие

- **Что делается:** набор обвязки, разбор папки задачи последним коммитом
- **Признак готовности:** папка разобрана, набор зелёный; заявка ждёт утра — владелец просил
  ветки наружу не отправлять
- **Чем проверяется:** `pnpm run agent-kit:check` — разложенное сходится с пакетом

## Чего эта работа не делает

- Не правит разбор команды: путь записи в теле интерпретатора стоит там же, где пути данных, и
  надёжного признака между ними нет.
