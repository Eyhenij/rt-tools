# Plan

**Task:** RT-2263 · **Branch:** RT-2263-rules-findings-rt-2208
**Behaviour:** unchanged — слово владельца: «Делай все три пункта»; работа правит слой правил, кода
приложения не касается

Разбор по правилам после каждой закрытой работы эпика RT-2208 нашёл 16 мест, где правило молчит о
том, на чём работа споткнулась, или расходится с соседним файлом. Семь из них по RT-2212 лежат в
`docs/plans/receiver-texts-keys-findings.md`, девять по RT-2213 и RT-2214 перечислены в теле задачи
#2263, и готовые формулировки к ним написаны.

## Task footprint

| What     | Where                                                                                                                         |
| -------- | ----------------------------------------------------------------------------------------------------------------------------- |
| Rules    | `projects/agent-kit/assets/rules/` — testing, typescript-conventions, task-flow                                               |
| Patterns | `projects/agent-kit/assets/patterns/` — doc-style-human, git-workflow-*, status-report-table, testing-e2e, spec-driven-domain |
| Hooks    | `projects/agent-kit/assets/hooks/` — проверка слога, проверка выдачи                                                          |
| Checks   | `projects/agent-kit/assets/checks/` — таблица задач эпика                                                                     |
| Tree     | `.claude/rt-kit/gate-map.sh` — карта правил дерева; `.claude/skills/*/implementation.md`                                      |

## What counts as done

- Каждая из 16 находок стала статьёй правила, пунктом образца, строкой соседнего файла или веткой
  карты правил — либо названа в разделе о том, чего работа не делает, и заведена задачей.
- Правка каждого хука и проверки пакета закрыта случаем в наборе пакета.
- Разложенное в дереве совпадает с пакетом, длина текстов и описаний в пределе.

## Stages

### 1. Девять находок RT-2213 и RT-2214

- **What is done:** статьи в `testing` (две), `typescript-conventions`, `task-flow`; пункты в
  образцах `doc-style-human`, `git-workflow-pr-body`, `git-workflow-merge`, `status-report-table`;
  ветка карты правил дерева на проверки и случаи в `tools/`. Строки соседних файлов — там, где
  статье есть что назвать в коде.
- **Readiness sign:** длина всех текстов в пределе, описания правил в пределе, привязки статей
  сходятся с обеих сторон; карта правил на `tools/check-admin-texts.mjs` называет `testing`.
- **Verified by:** `node tools/check-file-size.mjs` — «longer than the limit 0»;
  `npm run check:descriptions` — «all within the limit of 300 characters»;
  `node tools/check-specs.mjs` — расхождений нет.

### 2. Семь находок RT-2212

- **What is done:** статья `observability` о месте, где в ответ добавляется поле, и статья
  `testing` о проверке такого поля по всему набору; два пункта образца `testing-e2e` — исчезновение
  узла перед следующим действием и всплывающее окно, перехватывающее соседнее нажатие; пункт
  образца `spec-driven-domain` о таблице под заголовком договора; ветка карты правил на файлы, где
  зовут `effect`, `computed` или `toSignal`.
- **Readiness sign:** те же три проверки зелёные; карта правил на файл с вызовом `computed` вне
  компонента называет `angular-patterns`.
- **Verified by:** `node tools/check-file-size.mjs` и `node tools/check-specs.mjs` — расхождений
  нет; `bash .claude/rt-kit/gate-map.sh` через вызов `skill_for edit` — печатает имя правила.

### 3. Правки хуков и проверки пакета

- **What is done:** проверка слога судит тела heredoc, а не весь текст команды; проверка выдачи
  ищет `--title` раньше `-t`; таблица задач эпика на ветке самого эпика берёт его карточку по
  метке. Каждая правка закрыта случаем в наборе пакета.
- **Readiness sign:** все наборы пакета зелёные, и в них есть новые случаи: правка текста со старой
  редакцией внутри команды проходит, `-t lint` в теле не становится заголовком, таблица на ветке
  эпика печатает состав.
- **Verified by:** `npm run agent-kit:hooks` — «ВСЕ НАБОРЫ ЗЕЛЁНЫЕ».

### 4. Издание в дерево

- **What is done:** пакет собран, тексты разложены в дерево, набор перед отправкой зелёный.
- **Readiness sign:** раскладка не расходится с пакетом ни одним файлом.
- **Verified by:** `npm run agent-kit:check` — расхождений нет.

## What this work does not do

- Не издаёт пакет в npm: издание — отдельное решение владельца и отдельный ручной запуск.
- Не чинит проверку перед отправкой, которую снимает приставка перед командой: это задача #2264.
- Не отправляет находки в приём: это работа эпика #2195.
- Не делит правило `testing`, хотя блок о проверках дерева в нём вырос: разделение задевает карту
  правил и наборы, и оно стоит отдельной задачи.
