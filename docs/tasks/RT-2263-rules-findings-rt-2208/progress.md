# Progress

## Where we stand

- **State:** `этап-идёт`
- **Stage:** 4 из 4 — раскладка и набор перед отправкой
- **Done:** этапы 1, 2 и 3 закрыты — тексты, карта правил, две правки хуков и одна правки проверки
- **Next step:** набор перед отправкой, затем PR
- **Uncommitted:** нет
- **Waiting for the owner:** нет
- **PR:** не открыт

## Decisions along the way

- **Находка о счёте-мере не записана в правило о ходе работ** — обе его половины стоят в сотне
  знаков от предела длины: 23 870 и 23 985 при пределе 24 000. Статья туда не влезает даже сжатой,
  перенос довода в холодную часть переполняет её. Заведена задача #2265. Задет этап: 1.

- **Формулировки девяти находок восстановлены из записи прошлого захода** — разбор вернул их в том
  же ходу, где окно сессии сменилось; текст сохранён у исполнителя целиком. Задет этап: 1.

- **Правка заголовка PR откачена** — хук поставки 497 строк и его файл сценариев 498 при пределе
  500: ни правка, ни сценарий к ней не влезают. Заведена задача #2266, готовый код и случай набора
  лежат в её теле. Задет этап: 3.
- **Из источника пакета снята шапка раскладки** — `checks/epic-table.github.mjs` нёс её один во
  всём пакете, и проверка места правки запрещала править сам источник: она читала шапку и посылала
  править источник, то есть тот же файл. Задет этап: 3.

## Sessions

### 2026-09-18

- Заведены задачи #2263 и #2264, удалена лишняя ветка на хосте.
- Ветка взята от главной после того, как владелец влил эпик #2208 — `453f540ea`.
- Этап 1: две статьи в `testing`, одна в `typescript-conventions`, четыре пункта образцов, ветка
  карты правил дерева на проверки в `tools/`; три строки привязок. Зелёные: длина файлов, описания,
  привязки статей, адреса документов.
- Этап 2: статья в `observability` и статья в `testing` о поле ответа; два случая в образце
  `testing-e2e`; пункт образца `spec-driven-domain`; ветка карты правил по тексту файла на
  `effect`, `computed` и `toSignal`. Зелёные: длина, привязки статей.
- Этап 3: проверка слога берёт текст из тел heredoc и не судит документ вне корня дерева; общий
  разбор целей записи не читает тела heredoc; таблица задач эпика на ветке самого эпика берёт его
  карточку по метке. Четыре случая в наборе слога, два в разборе целей, три в таблице эпика.
  Зелёные: все наборы пакета, длина файлов, привязки статей, раскладка сходится с пакетом.
