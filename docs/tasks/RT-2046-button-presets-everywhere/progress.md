# Progress

## Where we stand

- **State:** `этап-идёт`
- **Stage:** 3 of 3 — тексты и набор перед push
- **Done:** пара половин на всех историях группы кнопок; 23 эталона сняты после осмотра кадров
- **Next step:** завести задачи на остальные семейства и на обрез кадра, прогнать набор
- **Uncommitted:** нет
- **Waiting for the owner:** нет
- **PR:** ещё не открыт

## Decisions along the way

- **Показ пары берётся готовый** — половины уже умеют вставать рядом или одна под другой по
  наполнению. Affected stage of the plan: 1.
- **Половины переносятся по ширине содержимого, а не по порогу в rem** — колонки сетки резали
  матрицу кнопки по третьему столбцу, и обрез был одинаков у обеих половин, отчего читался как
  задуманный. Affected stage of the plan: 1.
- **Восемь кадров переписки пересняты в этой ветке** — они разошлись от слияния основы, а не от
  правки кнопок; ветка отвечает целиком за то, что несёт. Affected stage of the plan: 2.

## Sessions

### 2026-09-10

- Папка задачи разнесена в свою ветку: заведена она была из ветки соседней задачи и висела там
- Записаны разбор и замысел на три этапа
- Пара половин обёрнута вокруг каждой матрицы кнопки, эталоны кнопок сняты после осмотра
- Разобрано, отчего красна основа: восемь кадров переписки разошлись с эталонами на одну-две точки
  по ширине после слияния двух волн — каждая была зелёной поодиночке
- Четыре «осиротевших» эталона на ширине 1441 оказались следствием тех же падений: прогон не
  доходил до второй ширины, и кадр не попадал в перепись. Удалять их было нечего
- Кадры осмотрены попарно — ожидаемый и полученный, — после чего эталоны переписки и поля ввода
  сообщения сняты заново: 529 историй, 551 снимок, расхождений нет
- Правило показа получило три статьи о паре наборов, привязки легли в компаньон
- `pnpm run check:affected` — код выхода 0, 41 проект, 91 задача
- Владелец: на истории `Themes` не было материальных кнопок. Пара наборов теперь оборачивает и её:
  четыре вида вместо двух. Четыре сочетания прибиты в глобальном стиле витрины — половина тем
  лежит глубже половины набора, и прибитое на предке до неё не доходило
- Обход всех историй витрины: 529 историй, материальный набор показывают 75
- Пара наборов заведена всей группе кнопок, а не одному компоненту: значковая кнопка, ссылка на
  загрузку, кнопка с выбором и группа переключателей — 23 показа
- Матрица «размер кнопки × размер значка» осталась без пары, и это записано в разметке: она уже
  занимает всю ширину страницы, поля половины уводят содержимое за край, а кадр по корню режет
  его молча — одинаково у обеих половин
- Попытка вылечить это обвязкой откатана: раздвигание окна по выносу содержимого ломает истории,
  которые сами назвали свою ширину, — кадр порога 768 снимался при окне 1233
- Обход витрины: 502 показа матриц, пару наборов показывают 98 после этой работы
