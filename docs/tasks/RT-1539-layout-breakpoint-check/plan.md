# Замысел

**Задача:** RT-1539 · **Ветка:** RT-1539-layout-breakpoint-check
**Поведение:** не меняется — правятся тексты слоя правил: статья закона и раздел паттерна.
Кода приложения работа не задевает.

После написания этот файл не правится. Пересмотр этапа идёт в `progress.md` решением по ходу.

## След задачи

| Что      | Где                                                                  |
| -------- | -------------------------------------------------------------------- |
| Законы   | `projects/agent-kit/assets/laws/verifiability.md`                    |
| Паттерны | `projects/agent-kit/assets/patterns/browser-verification-measure.md` |
| Спеки    | `docs/specs/agent-kit/`                                              |

## Что считается сделанным

- Закон о проверяемости несёт статью о том, что правка вида проверяется на каждом пороге
  раскладки, а не на одной ширине.
- Паттерн замера несёт готовый приём снять все пороги одним заходом и три признака, по которым
  читается ответ.
- Обе записи груза, вызвавшие эту правку, отмечены в приёме после слияния.

## Этапы

### 1. Статья закона о порогах раскладки

- **Что делается:** в конец раздела «Статьи» закона о проверяемости дописывается статья о
  проверке на каждом пороге.
- **Признак готовности:** статья стоит в законе, предел веса выдержан.
- **Чем проверяется:** `node tools/check-file-size.mjs` — файл закона не назван.

### 2. Раздел паттерна о замере по всем порогам разом

- **Что делается:** в паттерн замера дописывается раздел с готовым приёмом: пачка ширин за
  заход, три признака ответа, оговорка о полосе прокрутки.
- **Признак готовности:** раздел стоит в паттерне, предел веса выдержан.
- **Чем проверяется:** `node tools/check-file-size.mjs` — файл паттерна не назван.

### 3. Раскладка и сверка

- **Что делается:** пакет собирается, ресурсы раскладываются в дерево.
- **Признак готовности:** раскладка совпадает с источниками.
- **Чем проверяется:** `pnpm run agent-kit:check` — расхождений нет.

## Чего эта работа не делает

- Не заводит проверки на исполнение статьи: признака «смотрел ли на порогах» у машины нет.
- Не трогает предложение о чтении локальных веток перед заведением ветки — оно идёт своей
  задачей.
