# Plan

**Task:** RT-2006 · **Branch:** RT-2006-failopen-declared
**Spec:** `docs/specs/agent-kit/hooks/spec.md`
**Behaviour:** unchanged — правка идёт в заголовках стражей и кода приложения не касается

Вторая работа эпика RT-2004. Ветка стоит на RT-2005: та правит тот же набор проб.

## Task footprint

| Что    | Где                                           |
| ------ | --------------------------------------------- |
| Стражи | `projects/agent-kit/assets/hooks/` — 4 файла  |
| Пробы  | `projects/agent-kit/tests/hooks-kind.test.sh` |
| Спека  | `docs/specs/agent-kit/hooks/`                 |

## What counts as done

- Заголовок каждого из четырёх стражей объявляет отказ в пользу работы и называет, что именно
  пропускается.
- Список молчащих убран из набора проб, и проба зелёная без него.
- Поведение стражей не тронуто: правка только в словах.

## Stages

### 1. Стражи объявляют отказ в пользу работы

- **Steps:**
    1. четыре заголовка дописаны: что пропускается и почему
    2. список молчащих убран из набора проб
    3. пробы прогнаны без списка
- **What is done:** заголовки дописаны, список снят.
- **Readiness sign:** проба об отказе в пользу работы зелёная без списка исключений.
- **Verified by:** `bash projects/agent-kit/tests/hooks-kind.test.sh` — без провалов.

## What this work does not do

- Не правит поведение стражей: все четыре и так пропускают при ошибке, нигде это не записано.
- Не закрывает список стражей без проб — это задача RT-2007.
