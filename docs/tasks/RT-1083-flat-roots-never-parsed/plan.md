# Замысел

**Задача:** RT-1083 · **Ветка:** RT-1083-flat-roots-never-parsed
**Поведение:** не меняется — работа правит проверки пакета правил, кода приложения не трогает;
так сказал владелец, поручив брать задачи с приёмника по одной

После написания этот файл не правится. Пересмотр этапа идёт в `progress.md` решением по ходу.

## След задачи

| Что     | Где                                 |
| ------- | ----------------------------------- |
| Спеки   | `docs/specs/agent-kit/checks/`      |
| Законы  | не задеваются                       |
| Правила | не задеваются                       |
| Код     | `projects/agent-kit/assets/checks/` |

## Что считается сделанным

- Сторона плоских корней перечислена в разборе списка известного и приходит в сбор плоских либ
  разобранной, с причиной и номером задачи у каждой записи.
- Отказ разбора показывает форму записи: ключ, а при нём причина и номер задачи.
- Сценарии закрывают обе правки, и спек проверок их называет.
- Набор обвязки зелёный, раскладка сходится.

## Этапы

### 1. Сторона плоских корней

- **Что делается:** сторона перечисляется в разборе, сбор берёт её разобранной
- **Признак готовности:** непустая сторона доходит до сбора плоских либ
- **Чем проверяется:** `bash projects/agent-kit/tests/checks-lib-layers.test.sh` — сценарии зелёные

### 2. Форма записи в отказе

- **Что делается:** строка образца в тексте отказа разбора
- **Признак готовности:** отказ называет форму, а не только сторону
- **Чем проверяется:** `bash projects/agent-kit/tests/checks-config.test.sh` — сценарии зелёные

### 3. Закрытие

- **Что делается:** сценарии в спек, набор обвязки, разбор папки задачи последним коммитом
- **Признак готовности:** папка разобрана, набор зелёный; заявка ждёт утра — владелец просил
  ветки наружу не отправлять
- **Чем проверяется:** `bash projects/agent-kit/tests/run.sh` — строка о зелёных наборах

## Чего эта работа не делает

- Не отбивает сторону, которой нет в перечне разбора: список общий на несколько проверок, и
  лишняя сторона там законна.
