# Замысел

Задача: #700. Ветка: `RT-700-task-folder-left-behind`.

**Задача:** RT-700 · **Ветка:** RT-700-task-folder-left-behind
**Поведение:** не меняется — разбирается папка текстов, кода работа не трогает.

После написания этот файл не правится. Пересмотр этапа идёт в `progress.md` решением по ходу.

## След задачи

| Что               | Где                                                    |
| ----------------- | ------------------------------------------------------ |
| Чужая папка       | `docs/tasks/RT-676-cargo-package-in-image/`            |
| Своя папка        | `docs/tasks/RT-700-task-folder-left-behind/`           |
| Описание прошлого | `docs/archive/` и его указатель                        |
| Правило           | `task-flow`, паттерн `task-flow-close`                 |
| Сверка            | `npm run check:board` — она и назвала оставшуюся папку |

## Что считается сделанным

- Сверка очереди работ не называет ни одной папки закрытой задачи.
- В описании прошлого стоит запись о работе над образом: что решили и куда ушло каждое решение.
- Своя папка снята тем же коммитом, что и чужая, и после неё в архиве есть своя запись.

## Этапы

### 1. Чужая папка разобрана

- **Что делается:** решения по ходу и разбор просьбы задачи #676 уезжают в описание прошлого
  одной записью, замысел удаляется, строка встаёт в указатель архива.
- **Признак готовности:** `docs/tasks/RT-676-cargo-package-in-image/` нет.
- **Чем проверяется:** `npm run check:board`, `npm run check:docs`.

### 2. Своя папка разобрана

- **Что делается:** то же со своей папкой — последним коммитом ветки, после того как проверки
  прошли целиком.
- **Признак готовности:** в `docs/tasks/` остаются только README и образец.
- **Чем проверяется:** те же две сверки.
