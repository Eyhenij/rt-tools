# Ход работы

## Где стоим

- **Этап:** 4 сделан — экран приглашений собран, раздел заведён целиком
- **Сделано:** договорённость с двенадцатью сценариями и разбором безопасности; этап 1 —
  модель `TreeInvite`, миграция `20260817121454_tree_invite`, чистые функции кода, хеша, срока
  и состояния, запросы приглашений, три команды владельца, `SC-MB-128` закрыт; этап 2 —
  публичная операция `POST /api/intake/enroll`, ограничитель частоты (чистая функция решения и
  счётчик в памяти домена доступа), погашение и выпуск токена одной сделкой с условной правкой,
  одинаковый отказ на четыре негодных состояния, `SC-MB-117`…`SC-MB-125` закрыты. Спеки: 42 у
  либы разбора и печати, 34 у либы операций и команд, 6 у ограничителя — все зелёные; этап 3 —
  команда пакета `agent-kit enroll`: отбивка адреса без TLS у себя, запись токена правами
  владельца файла, отказ на уже лежащий токен и довод `--force`, `SC-MB-126` и `SC-MB-127`
  закрыты. Спек пакета и его описание получили строку о команде. Непокрытых сценариев у
  договорённости не осталось ни одного
  закрыты. Спек пакета и его описание получили строку о команде; этап 4 — операции админки
  `GET /api/invites` и `DELETE /api/invites/:name`, закрытые входом человека, общая модель строки
  списка; чтение переведено на страницу приёмами трёх соседних списков; раздел «Приглашения»
  заведён шестью либами, пункт меню, маршрут, стор с отзывом, экран приёмами правила списков и
  тостер в оболочке. Спеки: 44 у домена деревьев приёмника, 9 у моделей раздела, 9 у экрана —
  все зелёные; сборка админки зелёная. Непокрытых сценариев у договорённости не осталось ни
  одного, договорённость приведена к тому, чем экран вышел
- **Следующий шаг:** проверка экрана в браузере на своей машине, затем гейт пуша и коммит
- **Незакоммиченное:** весь этап 4 — шесть либ раздела, границы, алиасы, меню, маршруты,
  словарь, тостер в оболочке, страничное чтение приглашений на приёме и правка договорённости
- **Ждём владельца:** нет
- **PR:** #802, черновик

## Решения по ходу

- **17 августа 2026. Приглашение вместо общего секрета установки и вместо очереди обращений.**
  Секрет один на все деревья: утёкший у одного открывает заведение всем. Очередь обращений
  открывает приём мусору — послать обращение сможет кто угодно. Затронутый этап замысла: нет.
- **17 августа 2026. Срок годности самого токена дерева в задачу не входит.** Решение владельца
  при разборе. Затронутый этап замысла: нет.
- **17 августа 2026. «Одно годное приглашение на имя» держит колонка, гаснущая в пусто.**
  Частичный уникальный индекс схема клиента не объявляет, а положенный сырым запросом пропадал
  бы при следующей сверке схемы. Имя дублируется колонкой `activeName`: она уникальна, а при
  погашении и отзыве гаснет в пусто. Затронутый этап замысла: нет.
- **17 августа 2026. Приглашение годно двое суток.** Договорённость назвала «ограниченное
  время», не назвав числа. Затронутый этап замысла: нет.
- **17 августа 2026. Выдача приглашения заведённому дереву отбивается.** Дереву, которое уже
  есть, токен выдаёт своя команда, а имя занято им же. Затронутый этап замысла: нет.
- **17 августа 2026. Счётчик ограничителя живёт в памяти приёма, а не в хранилище.** Предел
  стоит затем, чтобы таблица не росла со скоростью отправителя; класть ради него запись в ту же
  таблицу значило бы решать задачу ею же. Приём поднят одним процессом — второго, у которого был
  бы свой счётчик, нет ни на проде, ни на стенде. Перезапуск сбрасывает окно, и это принято.
  Затронутый этап замысла: нет.
- **17 августа 2026. Ключ клиента — адрес из заголовка дороги, а неопознанный получает общий
  ключ.** Ответ на `Q-MB-INV-1` договорённости: своего ключа у обращения нет, а доверять
  заголовку можно ровно потому, что дорога своя. Обращения без адреса иначе обходили бы предел
  все разом. Затронутый этап замысла: нет.
- **17 августа 2026. Домену деревьев открыт слой служб доступа.** Счётчик один на приложение —
  ключ клиента у всех публичных операций, заводящих запись, общий; заведи домен деревьев свой,
  второй ответ на вопрос «кто это» разошёлся бы с первым. Строка дописана в границы с причиной.
  Затронутый этап замысла: нет.
- **17 августа 2026. Момент обращения приезжает последним доводом операции.** Каркас отдачи его
  не заполняет — метки у довода нет, — и в бою работает умолчание, а спека проверяет годность и
  окно вызовом. Первая редакция читала часы внутри, и спека на просроченное приглашение зеленела
  или краснела в зависимости от времени суток. Затронутый этап замысла: нет.

- **17 августа 2026. Открытый адрес приёма отбивает сам пакет, а исключение — только локальная
  машина.** Токен уходит единственным ответом на обращение, и по открытому пути его прочитает
  всякий, кто стоит между сторонами; на локальной машине сети между ними нет вовсе. Проверка
  стоит до сети: отбитое обращение до приёма не доходит. Затронутый этап замысла: нет.

- **17 августа 2026. Приглашения получают свой пункт меню, а не вкладку раздела «Деревья».**
  Решение владельца при разборе. Договорённость обещала вкладку, но получать её некому: раздела
  «Деревья» в админке нет вовсе — деревья живут выпадашкой отбора, — а вкладок нет ни у одного
  раздела. Завести раздел «Деревья» значило бы сперва завести список деревьев, которого приём не
  отдаёт: у него есть только выбор для отбора. Договорённость правится строкой — она ещё не
  влита. Затронутый этап замысла: 4, объём тот же.

- **17 августа 2026. Чтение приглашений на приёме переведено на страницу.** Решение владельца
  при разборе. Приглашений единицы, и голый массив был честнее, но страницу, порядок, повтор и
  отказ трём соседним спискам даёт одна общая основа: список, отвечающий не её формой, читался
  бы в обход неё, и раздел вышел бы первым в дереве, собранным мимо общего пути. Правка стоила
  контроллера и его спек. Затронутый этап замысла: 4, объём тот же.

- **17 августа 2026. Отзыв спрашивает согласия окном, а не отзывает по нажатию.** Решение
  владельца при разборе. Действие необратимо: отозванное приглашение не возвращается, дереву
  нужно новое. Вопрос называет последствие, а не спрашивает «вы уверены», и подтверждение везёт
  сам пункт меню кита. Затронутый этап замысла: нет.

- **17 августа 2026. Тостер поставлен в оболочку админки.** Отзыв — первое действие админки,
  которому есть о чём сказать; до него сторы говорили только отказом чтения, а он занимает место
  списка. Шина кита в дереве была, а слушателя у неё не стояло ни одного: без тостера удавшийся
  отзыв и его отказ выглядели бы одинаково — молча. Затронутый этап замысла: нет.

- **17 августа 2026. Слой вида у раздела заведён пустым.** Своего вида разделу не нужно —
  строку показывают готовые ячейки таблицы кита, а панели подробностей у приглашения нет. Но
  домен без слоя проверка раскладки читает как недоделанный, и барель с причиной дешевле строки
  в списке известного. Затронутый этап замысла: нет.

## Заходы

### 17 августа 2026

Разведка показала: токен выдаёт только команда строки запуска на приёме, признак дерева
считается на самом дереве и приёму не известен, а публичных операций, заводящих запись, у приёма
нет ни одной — значит ограничитель частоты заводится вместе с первой такой.

Доэтапное, не этой работой: договорённость о слотах списочной страницы осталась невлитой, а
папка закрытой задачи уехала в главную ветку. Закрыто задачей RT-803, влитой в главную ветку.

Этапы 1 и 2 сделаны целиком. Миграция написана разницей на одноразовом контейнере и накатана на
него же; сверка со схемой ответила «No difference detected». Своей базы работа не трогала.

Найдено по ходу: в докере кончилось место — постгрес отвечал `No space left on device`, и это
выглядело поломкой стенда. Убрано тем же поимённым отбором, каким убирает конвейер: висячие
образы и кэш сборщика старше двух часов. База стенда после этого помнила проваленную миграцию —
снесена, она пересоздаётся сама.
