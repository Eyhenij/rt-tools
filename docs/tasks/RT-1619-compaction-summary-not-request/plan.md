# Замысел

**Задача:** RT-1619 · **Ветка:** RT-1619-compaction-summary-not-request
**Поведение:** не меняется — правка гардов слоя правил, кода приложения работа не задевает

После написания этот файл не правится. Пересмотр этапа идёт в `progress.md` решением по ходу.

## След задачи

| Что   | Где                                               |
| ----- | ------------------------------------------------- |
| Спеки | `docs/specs/agent-kit/turn-guards/`               |
| Код   | `projects/agent-kit/assets/hooks/`                |
| Пробы | `projects/agent-kit/tests/proposal-guard.test.sh` |

## Что считается сделанным

- Сводка сжатия репликой владельца не считается ни одним гардом пакета.
- Настоящая реплика владельца после сводки судится по-прежнему.
- Спек называет сводку термином и статьёй, сценарий покрыт пробой.

## Этапы

### 1. Признак ввода отсеивает сводку

- **Что делается:** во всех копиях признака ввода добавляется отсев записи со своим признаком
  сводки; пробы показывают оба исхода
- **Признак готовности:** пробы гарда предложений зелены, весь набор пакета зелёный
- **Чем проверяется:** `bash projects/agent-kit/tests/run.sh` — все наборы зелёные

### 2. Статья и сценарий в спеке

- **Что делается:** термин сводки, статья о ней и сценарий ложатся в поддомен о гардах хода
- **Признак готовности:** сверка спеков молчит, у статьи есть привязка и сценарий
- **Чем проверяется:** `npm run check:specs` — расхождений нет

## Чего эта работа не делает

- Девять копий признака в один помощник не сводит: своя работа со своей ценой.
- Двух других работ эпика не трогает — RT-1617 и RT-1618.
