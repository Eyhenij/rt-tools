# Ход работы

## Где стоим

Перезаписывается каждым заходом, а не дописывается.

- **Состояние:** `этап-идёт`
- **Этап:** 5 из 5 — Прогон на живом грузе
- **Сделано:** этапы 1–4 закрыты — команда чтения, сквозной круг на местном приёмнике,
  переписанное правило с компаньоном и паттерном, спека с девятью сценариями и их тестами
- **Следующий шаг:** владелец заводит учётную запись службы на проде одной командой, после чего
  заход читает настоящий груз и переводит разобранное
- **Незакоммиченное:** нет
- **Ждём владельца:** заведения учётной записи службы на проде — ssh туда исполнителю отбит
- **PR:** ещё не открыт

## Решения по ходу

- **Спека чтения заведена своим поддоменом `cargo-read`, а не дописана в `cargo-mark`** —
  поддомена отметки в главной ветке ещё нет, он приедет заявкой RT-903, и дописанная туда спека
  дала бы конфликт при слиянии. Предмет при этом разный: там записи двигают, здесь читают.
  Затронутый этап замысла: 4.
- **Шапка раскладки снята с копий образца папки задачи** — `cp -r docs/tasks/_template` приносит
  файлы с шапкой `rt-kit`, и гард места правки отбивает первую же правку разбора просьбы. Копия
  образца образцом не является: шапка снята тремя строками. Затронутый этап замысла: нет —
  находка уходит к замыслу эпика о слое правил.

## Заходы

### 2026-08-24

- Разведка приёмника: чтение закрыто `@SessionOperation()` во всех трёх контроллерах, запись
  открыта токеном в четырёх операциях; ключ отметки — `sha256` текста записи, и он считается на
  стороне дерева
- Задача RT-1046 заведена, на борде в колонке работы, исполнитель — учётная запись машинной работы
- Гард места правки отбил правку папки задачи: копии образца несут шапку раскладки
- Написана `tools/cargo-pull.mjs` (345 строк): вход парой службы, страница списка, дочитывание
  текстов, ключ отметки рядом с записью
- Сквозной круг на местном приёмнике сошёлся: груз отправлен, прочитан, отмечен ключом из
  чтения, повторное чтение показало `in_work`
- Отказы до сети проверены: незнакомый род, нет пары, пара не принята — все с кодом 1
- Прод отвечает на вход `401 — пара не принята`: связь есть, учётной записи там ещё нет
- Правило `cargo-triage` переписано: статей стало 17, строки исполнения сверены с ними попарно
- Заведена спека `docs/specs/agent-kit/cargo-read/` — 11 утверждений, 9 сценариев SC-AK-549…557
- `projects/agent-kit/tests/cargo-pull.test.sh` — 19 проверок, все зелёные; весь набор пакета зелёный
