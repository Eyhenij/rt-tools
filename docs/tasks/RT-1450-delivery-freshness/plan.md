# Замысел

**Задача:** RT-1450 · **Ветка:** RT-1450-delivery-freshness
**Поведение:** не меняется — работа правит слой правил; кода приложения не касается

После написания этот файл не правится. Пересмотр этапа идёт в `progress.md` решением по ходу.

## След задачи

| Что     | Где                                                            |
| ------- | -------------------------------------------------------------- |
| Правило | `projects/agent-kit/assets/rules/git-workflow.github.md`       |
| Паттерн | `projects/agent-kit/assets/patterns/git-workflow-freshness.md` |
| Спутник | `.claude/skills/git-workflow/implementation.md`                |

## Что считается сделанным

- Правило говорит четыре вещи: заявки перечитываются в трёх местах; отставание отличается от
  спора и спрашивается у хранилища; догнанная и неотправленная ветка работой не считается;
  волна веток проверяется пробным слиянием.
- Паттерн `git-workflow-freshness` лежит при правиле и назван в его разделе «Паттерны».
- Привязки статей стоят в спутнике, длина в пределе, раскладка сходится.

## Этапы

### 1. Четыре статьи в правило

- **Что делается:** три статьи о свежести заявок — после статьи о конфликтующей заявке;
  статья о пробном слиянии волны — рядом со статьёй о влитой главной.
- **Признак готовности:** статьи стоят, длина в пределе.
- **Чем проверяется:** `node tools/check-file-size.mjs`, `npm run check:specs`.

### 2. Паттерн свежести

- **Что делается:** новый паттерн с шапкой при правиле поставки и строка в разделе «Паттерны».
- **Признак готовности:** паттерн разложен, сверка связности молчит.
- **Чем проверяется:** `pnpm run agent-kit:check`, `npm run check:specs`.

## Чего эта работа не делает

- Не заводит гарда на перечитывание заявок: гард судит один ход и о соседних заявках не знает.
