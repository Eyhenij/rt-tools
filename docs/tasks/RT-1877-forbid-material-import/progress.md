# Progress

## Where we stand

Rewritten by every session, not appended to.

- **State:** `этапы-кончились`
- **Stage:** 1, 2 и 3 закрыты
- **Done:** запрет написан и проверен с двух сторон; обе двери прогона закрыты, прогон кита зелёный
  — 122 набора, 1525 проверок; смежные зависимости оказались чисты и правки не потребовали
- **Next step:** разобрать папку задачи и открыть заявку черновиком
- **Uncommitted:** папка задачи и правки настроек
- **Waiting for the owner:** нет
- **PR:** ещё не открыта

## Decisions along the way

- **Этап 3 закрыт замером, а не правкой** — Material в смежных зависимостях второго кита не стоял
  вовсе. Карточка называла это требованием к итогу, а не находкой; итог сошёлся без изменений.
  Affected stage of the plan: 3.
- **Проверять запрет пошли на файле свойств оформления, а не на компоненте** — компонентный файл
  потянул бы за собой правило устройства компонента, к работе не относящееся, а проверка запрета от
  выбора файла не зависит: запрет объявлен на все файлы кита. Affected stage of the plan: 1.

## Sessions

### 2026-09-08

- Все утверждения карточки проверены замером и сошлись: материала в ките ноль, обе приоткрытые
  двери на месте по одной строке, образец закрытого пакета рядом работает так, как назван.
- Запрет проверен с двух сторон: на файле с временно вписанным импортом линтер отказывает и
  печатает причину; на нём же без импорта — ноль отказов.
- Обе двери сняты, прогон кита зелёный: 122 набора, 1525 проверок, 15,5 секунды.
- Прогон шёл жестом напрямую, а не через сборщик задач: во второй рабочей копии сборщик не строит
  граф проектов. Набор проверок при этом тот же — настройка прогона кита одна.
