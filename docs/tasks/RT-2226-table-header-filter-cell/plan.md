# Plan

**Task:** RT-2226 · **Branch:** RT-2226-table-header-filter-cell
**Spec:** `docs/specs/ui-kit-v2/table-filter/spec.md`
**Behaviour:** changes — у таблицы второго кита появляется отбор в шапке столбца, которого в ней
нет. Домена у самой таблицы кита в дереве нет вовсе, а домен динамического списка прямо говорит,
что отбор в шапке принадлежит семейству таблицы и есть работа своя. Так что заводится узкий
поддомен `table-filter` — как уже заведены `scrollbar`, `overflowing-text` и `number-grouping`.
Договорённость пишется прямо в него, черновика не заводится: так сделали RT-1879 и RT-1880.

## Task footprint

| What  | Where                                                                                                       |
| ----- | ----------------------------------------------------------------------------------------------------------- |
| Specs | `docs/specs/ui-kit-v2/table-filter/` — заводится здесь; рядом читается `docs/specs/ui-kit-v2/dynamic-list/` |
| Rules | `.claude/skills/reuse-first/`, `.claude/skills/rt-tools-storybook/`, `.claude/skills/ui-component-tests/`   |
| Code  | `projects/ui-kit-v2/src/lib/components/table/` — ячейка отбора рядом с шапкой сортировки                    |
| Tests | пробы рядом с кодом и истории витрины в папке семейства                                                     |
| Docs  | `CONTEXT.md` и `Overview.mdx` таблицы — таблица отбора в их таблицах входов                                 |

**Номера сценариев начинаются с SC-UKV-214.** На этой ветке последний выданный меньше, но 214
занят работой RT-1880: она стоит правкой #2225 и в ветку эпика не влита. Взять его второй раз —
значит оставить два сценария под одним номером после слияния.

## What counts as done

- В шапке столбца таблицы второго кита виден отбор, и выбранное меняет строки.
- Четыре вида отбора работают: текст, число, выбор из списка, дата.
- Собрано из готовых частей второго кита: `rt-filter-control`, `rt-select`, `rt-input`,
  `rt-input-number`, `rt-date-picker`, `rt-menu`. `@angular/material` не появляется ни строкой.
- Каждое обещание отбора стоит статьёй с привязкой в своём поддомене.
- Кадры витрины показывают оба состояния шапки — отбор пуст и отбор выбран — и оба набора
  оформления.
- Страница-обзор таблицы называет отбор и его входы.

## Stages

### 1. Описание отбора своим поддоменом

- **What is done:** заводится `docs/specs/ui-kit-v2/table-filter/` со статьями об отборе: чем он
  открывается, какие четыре вида бывают, что уходит наружу при выборе, что делает очистка, где
  живут его стили. Сценарии с SC-UKV-214 и привязка у каждой статьи.
- **Readiness sign:** аудит описаний не называет ни одного нового сценария без теста и ни одной
  новой статьи без адреса.
- **Verified by:** `npm run check:specs` — сегодня код выхода 0 при 6 доменах и 1693 сценариях;
  после этапа число сценариев растёт на дописанные.

### 2. Ячейка отбора на готовых частях второго кита

- **What is done:** ячейка отбора пишется рядом с шапкой сортировки. Вид отбора выбирает, какую
  готовую часть позвать; оператор сравнения и «можно выбрать несколько» приходят настройкой
  столбца. Наружу уходит выбранное, а решает, что с ним делать, тот, кто держит список. Стили — в
  слой `rt-kit.components`, между шкалой и местом стоит своё свойство.
- **Readiness sign:** в папке таблицы второго кита ноль упоминаний `@angular/material`, типы и оба
  линтера чисты, проверки токенов и слоя каскада расхождений не называют.
- **Verified by:** `pnpm exec nx run @rt-tools/ui-kit-v2:typecheck`, `pnpm exec nx lint
@rt-tools/ui-kit-v2`, `npm run check:tokens-styles`, `npm run check:cascade-layer`,
  `npm run check:tokens-graph` — сегодня все зелёные, и такими остаются.

### 3. Пробы отбора

- **What is done:** тест на каждый сценарий описания: четыре вида, выбор и очистка, что уходит
  наружу, что отбор не трогает строки сам.
- **Readiness sign:** каждый новый сценарий назван заголовком теста, прогон зелёный.
- **Verified by:** `pnpm exec nx test @rt-tools/ui-kit-v2` — сегодня 1565 из 1565; после этапа
  число растёт на написанные.

### 4. Истории витрины и эталонные кадры

- **What is done:** истории шапки с отбором: оба состояния — пуст и выбран, — четыре вида отбора,
  оба набора оформления и обе темы. На кадры смотрят глазами до того, как снять эталон.
- **Readiness sign:** два подъёма ворот подряд зелёные, новые кадры не пустые, счёт семейств
  витрины сходится.
- **Verified by:** `node tools/visual-gate.mjs ui-kit-v2` — сегодня 589 кадров; после этапа число
  растёт на снятые. `node tools/check-kit-coverage.mjs` — сегодня 124 компонента и директивы, 94
  предмет истории.

### 5. Страница-обзор и заметка семейства

- **What is done:** отбор и его входы вносятся в `Overview.mdx` и `CONTEXT.md` таблицы: как столбец
  объявляет отбор, какие виды бывают и что приходит наружу.
- **Readiness sign:** аудит входов кита сходится, связи витрины не ведут в пустоту.
- **Verified by:** `node tools/verify-ui-kit-v2-docs.cjs` — сегодня «Overview pages matched: 76, no
  divergences»; `node tools/check-showcase-links.mjs` — ноль связей, ведущих в пустоту.
