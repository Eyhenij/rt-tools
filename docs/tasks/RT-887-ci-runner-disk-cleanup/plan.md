# Замысел

**Задача:** RT-887 · **Ветка:** RT-887-ci-runner-disk-cleanup
**Поведение:** не меняется — правка идёт в конвейер и в описание прошлого; кода приложения работа
не задевает.

После написания этот файл не правится. Пересмотр этапа идёт в `progress.md` решением по ходу.

## След задачи

| Что     | Где                                                                   |
| ------- | --------------------------------------------------------------------- |
| Законы  | `docs/constitution/delivery.md`, `docs/constitution/verifiability.md` |
| Правила | `.claude/skills/git-workflow-docker/`, `.claude/skills/git-workflow/` |
| Код     | `.github/workflows/ci.yml`                                            |
| Разборы | `docs/postmortems/`                                                   |

## Что считается сделанным

- Шаг сквозного набора не начинается, пока база стенда не отозвалась здоровой, а не дождавшись —
  отказывает, печатая её лог и занятое место.
- У кэша сборщика есть предел размера, и он снимается независимо от возраста.
- Оба происшествия этого дня разобраны записями в `docs/postmortems/`.
- Правило о работе с образами знает, что порога по возрасту одного мало.

## Этапы

1. Разбор происшествий записан.
2. Конвейер: отказ по неподнявшейся базе и предел размера кэша.
3. Правило `git-workflow-docker` приведено к сделанному.
4. Проверки дерева и прогон конвейера на этой ветке.
