# Progress

## Where we stand

Rewritten by every session, not appended to.

- **State:** `этап-идёт`
- **Stage:** 3 of 4 — Переснять эталоны образом и снять обход
- **Done:** третий этап закрыт: обход снят, дорога одна — набор идёт только браузером образа,
  образ поднимает сам набор, доводы браузера переехали туда же. Два запуска подряд 123 из 123
- **Next step:** четвёртый этап — витрина второго кита через тот же образ
- **Uncommitted:** настройки набора, цель, подъём образа, описание набора, спутник правила
- **Waiting for the owner:** нет
- **PR:** not open yet

## Decisions along the way

- **Ветка отведена от RT-2170** — обход, который эта работа снимает, лежит там и в главную ещё не
  влит. Affected stage of the plan: 3.

## Sessions

### 16 сентября 2026

- Задача заведена после разбора RT-2170: причина расхождения — две машины, а не настройка браузера
- Образ `mcr.microsoft.com/playwright:v1.62.1-noble` — та же версия, что у набора; браузер отдаётся
  по входу командой `playwright run-server`
- Проба одного экрана по два раза: машина `9095f1ad6fba` дважды, образ `d690082a623f` дважды.
  Обе стороны держатся, и растр образа свой — ровно то, ради чего перенос и делается
- Вход `RT_SHOT_BROWSER` в настройках набора: назван — браузер приезжает из образа, не назван —
  всё как прежде. Стенд остаётся на машине, из образа она зовётся своим именем
- Признак входа помечен `secure`, и браузер шлёт такую только по защищённому соединению.
  Исключение у него одно — `localhost`; из образа машина зовётся иначе, признак не уходил вовсе,
  и весь набор отвечал отказом. Стенд снимает пометку: он стоит на своей машине
- Семь спек звали стенд полным адресом — из образа такой адрес ведёт в пустоту. Переведены на
  относительный: он сам берётся от адреса страницы
- Прогон через образ: 115 из 123. Семь падений были про адреса и закрылись правкой
- Эталоны пересняты образом: 122 из 123
- Замер ширины отбора по дереву падает по делу. Содержимому нужно 180,53 пункта, кнопка 179,03,
  допуск в тексте один пункт — не хватает полутора. На машине тот же замер проходит: её
  начертания у́же. Значит кнопка мерится не по самой длинной опции, и под другими начертаниями
  подпись обрезается. Это дефект админки, а не обвязки
- Ширина кнопки отбора заведена задачей RT-2175 и стоит на доске
- Обход снят: `ignoreSnapshots` ушёл, и дорога осталась одна — без адреса браузера образа набор
  отказывается запускаться вовсе и называет команду. Две дороги давали кадр, которому нельзя
  верить: снятый браузером машины, он не совпадает ни с одной другой
- Образ поднимает сам набор — `stand/shot-browser.mjs`; цель `e2e` зовёт прогонщик через него.
  Версия образа берётся у прогонщика, а не пишется числом: разные издания не понимают друг друга
- Доводы браузеру переехали в подъём образа. Соединение их не передаёт — проба показала, что
  браузер, поднятый готовой командой образа, доводов от соединения не получает вовсе
- Язык браузера доводом не задаётся: в образе стоит `LC_ALL=C.UTF-8`, и она сильнее. Браузер
  отвечал `en-US@posix` при названном `--lang=ru-RU`, а кадр раздела «Использование» нёс
  `08/01/2026` вместо `01.08.2026` — то самое, что чинила RT-2129, внутри зелёного кадра.
  С `LC_ALL=ru_RU.UTF-8` порядок русский
- Пересъёмка исправленным образом тронула два кадра из 123: «Использование» — поле периода, 4870
  точек, коробка x 234–796, y 160–199; тёмный вход — 94 точки, это доводы про растр, которых у
  прошлого подъёма не было вовсе
- Начертание в образе и на машине — не Montserrat: админка называет его, но ни файла, ни ссылки
  на него в сборке нет, и обе стороны рисуют запасным. Кадры от этого не врут — тем же запасным
  видит его и человек без Montserrat, — но сам пропуск стоит задачи
- Два запуска подряд: 123 из 123 и 122 из 123 — тот самый замер ширины прошёл в одном и упал в
  другом. Непостоянство, а не дефект вёрстки, как было записано в RT-2175
- Причина найдена пробой, а не чтением: открытый список поворачивает шеврон, и прямоугольник на
  экране у повёрнутого квадрата шире его самого. Сразу после открытия шеврон выходит 17,51 при
  настоящих 16; проверка прибавляла эти полтора пункта к тому, что должна вместить кнопка, и
  требовала от неё больше нужного. Поймал поворот — красный, не поймал — зелёный
- Измерение шеврона переведено на то, сколько места он занимает в строке: поворот на это не
  влияет. Шесть повторов подряд зелёные, числа сходятся ровно — 179,03 против 179,03
- RT-2175 закрыта как ошибка разбора: кнопка в порядке, неверно мерил тест. Правка вошла сюда —
  измерение живёт в этом наборе, и оно в следе этой задачи
- Второе непостоянство нашлось следом: кадр панели столбцов раз в несколько запусков нёс
  подсказку «Столбец закреплён». Указатель оставался там, где его бросило последнее нажатие, и
  подсказка всплывала по задержке — успела или нет, решал случай. Кадр теперь снимается с
  указателем в углу; четыре повтора подряд зелёные
- Подъём образа переехал в `tools/shot-browser.mjs`: витрины снимают кадры тем же браузером, а
  их проверка живёт в `tools/`, не при наборе экранов
- Уведённый указатель показал, что в эталонах сидело наведение: шесть кадров изменились
  пересъёмкой — подсвеченная строка под указателем и всплывшая подсказка были записаны в них как
  часть экрана
- Кадру попапа профиля указатель нужен на месте: кит открывает этот попап наведением и от увода
  закрывает — это его замысел, а не дефект. Оговорка названа при самом кадре
- Третий этап закрыт: два запуска подряд 123 из 123 через цель набора

## Handover of the session

Put together by a hook before the compaction of the context (auto).

**Working tree:** /Users/sviatoslavkhutornoy/WebstormProjects/rt-tools
**Branch:** RT-2174-frames-taken-in-container

### Where we stand at the minute of the compaction

- **State:** `этап-идёт`
- **Stage:** 3 of 4 — Переснять эталоны образом и снять обход
- **Next step:** завести задачу на узкую кнопку отбора по дереву и вернуться к снятию обхода
- **PR:** not open yet

The progress in full — `docs/tasks/RT-2174-frames-taken-in-container/progress.md`; the plan lies next to it.

### Uncommitted

```
none
```

### Commits over the main branch

```
80104f498 test(rt:message-bus): эталоны экранов пересняты образом
14e1d8dfb feat(rt:message-bus): набор экранов умеет брать браузер из образа
003083d88 docs(rt:message-bus): первый этап закрыт — образ держит растр знаков
dc2c32e7a docs(rt:message-bus): папка задачи про съёмку кадров в образе заведена, замысел записан
3f9aed59e fix(rt:message-bus): кадры экранов и витрин судятся в одном месте — в конвейере
275f14229 docs(rt:message-bus): в записи архива названо, почему работа не годится как есть
fd4dd4ef0 chore(rt:agent-kit): 19 записей архива убраны по вышедшему сроку
6eddd1fb7 docs(rt:message-bus): папка задачи про красные кадры разобрана, запись в архиве
8e27227c4 docs(rt:ui-kit-v2): пересъёмка эталона подтверждается вторым поднятием
bfb9588c1 test(rt:message-bus): 27 эталонов вернулись к виду, который машина воспроизводит
3fa2675cf docs(rt:message-bus): первый этап закрыт — закреплять в обвязке нечего
8902a01d8 docs(rt:message-bus): в состоянии работы записано, где садится расхождение кадра
e9eaf14a3 docs(rt:message-bus): в состоянии работы записано, что главная краснеет на себе самой
e7d83084d docs(rt:message-bus): в состоянии работы записаны измерения первого этапа
0b2bbcc7e docs(rt:message-bus): папка задачи про красные кадры главной заведена, замысел записан
```

Written by a hook before the compaction of the context. Everything standing here is checked
against the tree: a handover retells what was written and describes the minute it was put together.
