# Ход работы

## Где стоим

- **Этап:** все шесть сделаны; идёт закрытие работы — шаги 9 и 10 позади, отчёт открывается
- **Сделано:** этапы 1–6; главная ветка влита; договорённость о приёмнике влита в спек домена
  `message-bus`; тексты домена и линия работ приведены к сделанному
- **Следующий шаг:** разбор папки задачи, сверка очереди работ, отчёт с ведома владельца
- **Незакоммиченное:** вливание договорённости и правка текстов
- **Ждём владельца:** пуш ветки и открытие отчёта
- **Отчёт:** ещё не открыт

## Решения по ходу

- **2026-08-14. `Q-2` линии работ закрыт разведкой, а не вопросом.** Слои семьи бэкенда у
  дерева-образца прочитаны обходом каталогов: `api`, `data-access`, `feature`, `util`. Владелец
  подтвердил дословное списывание. Затронутый этап: 1.
- **2026-08-14. Vitest у новых проектов, Jest у пакетов.** Решение владельца; граница проходит
  по каталогу. Компаньон правила `testing` перестаёт быть верным и правится этапом 6.
  Затронутые этапы: 1 и 6.
- **2026-08-14. Вторая задача линии режется на три ветки.** Решение владельца: каркас приёмника,
  учётные записи и вход, админка. Эта ветка — первая.
- **2026-08-14. Конвейер `plan` по этой линии не зовётся.** Он однажды развернул решение
  владельца в противоположное. Договорённость написана исполнителем, разбирает `spec-critic`.
- **2026-08-14. Счётчики записи месяца замещаются последним прогоном.** Решение владельца.
  Окна прогонов перекрываются, и сложение завышало бы числа молча, а повтор после обрыва
  удваивал бы их. Предложения и разборы по-прежнему копятся. Затронутые этапы: 3 и 5.
- **2026-08-14. Закон о наблюдаемости снимается с отписки, правило слоя API остаётся снятым.**
  Решение владельца: журнал отказов нужен сразу, иначе первый же отказ на живом прогоне
  неразбираем; правило слоя API требует процедур Connect, которых здесь не будет.
  Затронутые этапы: 2 и 6.
- **2026-08-14. Тег либы и её алиас выводятся из пути, а не из имени домена.** Сверка раскладки
  требует `scope:` плюс путь от `libs/`: у образца путь начинается с `libs/api`, здесь — с
  `libs/message-bus-api`, поэтому теги вышли длиннее образцовых. Решение владельца о дословных
  слоях этим не задето: слои те же четыре. Затронутый этап: 1.
- **2026-08-14. Домен деревьев заведён пятым.** Дерево с токеном — предмет, а не механика:
  правило `lib-layers` требует заводить домен под предмет, и командам деревьев этапа 4 своё
  место нужно всё равно. Слои те же четыре; `api` пуст — наружу домен не ходит. Затронутые
  этапы: 3 и 4.
- **2026-08-14. Проверка токена закрыта по умолчанию.** Ставится на всё приложение сразу,
  открытая операция объявляет себя меткой. При обратном порядке забытая строка открывала бы
  новую операцию приёма наружу молча. Договорённость это и обещает: открытых операций у
  приёмника нет вовсе, кроме пробы живости. Затронутый этап: 3.
- **2026-08-14. Записью месяца владеет домен наблюдений.** Сводка и есть её тело; предложения и
  разборы, приехавшие раньше сводки, заводят запись через него — ребро выписано строкой границ с
  причиной. Второй upsert той же пары «дерево — месяц» разошёлся бы с первым при первой правке.
  Затронутый этап: 3.
- **2026-08-14. Разбор отказов один на приложение.** Недоступное хранилище, перечень родов и
  предел веса разбираются в одном месте, а не в каждой операции: три операции решали бы порознь,
  что считать поломкой хранилища. Там же журнал отказа — род груза и признак дерева. Затронутый
  этап: 3.
- **2026-08-14. Колонка сводки стала необязательной.** Запись месяца, заведённую предложениями,
  пустой объект описывал бы неотличимо от сводки с нулевыми счётчиками, а такая сводка законна.
  Миграция `20260814082145_month_record_summary_optional`. Затронутый этап: 3.
- **2026-08-14. Отправка в пакет заводится целиком, все три рода.** Решение владельца по
  противоречию в замысле: этап 5 требует настоящей отправки пакета, а раздел «Чего эта работа не
  делает» запрещает править отправляющую сторону. Четвёртая задача линии этим съезжает в эту
  ветку частично. Затронутый этап: 5.
- **2026-08-14. Форма груза объявляется отправляющей стороной.** Пакет уходит в реестр, и импорт
  внутренней либы уехал бы в описания его типов и сломал бы установку. Объявление живёт в
  `projects/agent-kit/src/lib/cargo.ts`, приёмник берёт его подпутём `@rt-tools/agent-kit/cargo`,
  реэкспорта нет: потребитель импортирует символ у источника. Затронутый этап: 5.
- **2026-08-14. Снимок надстроек, невыбранное и признак дерева считаются в момент отправки.**
  Спек отправителя того и требует: снимок — состояние, а не событие. Строка наблюдения и гард
  происшествия при этом не трогаются — они остаются первой задачей линии. Затронутый этап: 5.
- **2026-08-14. Признак дерева — хеш от приведённого адреса репозитория.** Адрес по нему не
  восстанавливается, а у двух рабочих копий одного репозитория он один; формы адреса приводятся
  к одному виду до хеша. Затронутый этап: 5.
- **2026-08-14. Счётчики сводки отбираются по именам ресурсов пакета.** Живой прогон отбило
  проверкой на адрес дерева: в наблюдениях лежат имена собственных правил дерева, и одно из них
  содержит имя дерева. Пакету они не значат ничего, а число событий остаётся полным. Затронутый
  этап: 5.
- **2026-08-14. Отправка предложений в очередь работ снята.** Груз уезжает в закрытый приём —
  правило договорённости отправителя: сводка говорит о рабочих привычках команды, и в открытой
  очереди это выложено всему свету. `SC-AK-81` переписан с адреса репозитория на адрес приёма.
  Затронутый этап: 5.
- **2026-08-14. Точка входа у приёмника одна на службу и на команды.** С доводом `main.js`
  исполняет команду деревьев и выходит, без доводов — слушает порт. Вторая сборка означала бы
  второй образ, который расходится с первым молча, а образ у приёмника один и на прод, и на
  ручной прогон. Затронутый этап: 4.
- **2026-08-14. Команды деревьев подключены своим модулем, а не тем, что везёт проверку
  токена.** Проверка ставится на всё приложение, а команды каркаса отдачи не касаются вовсе:
  один модуль на двоих привёл бы глобальную проверку туда, где нет ни запросов, ни того, что
  она защищает. Затронутый этап: 4.
- **2026-08-14. Список деревьев собирается тремя плоскими запросами, а не одним со вложенной
  выборкой.** Их читает и двойник хранилища в спеке, а вложенную выборку он повторял бы формой
  вызова, а не поведением. Затронутый этап: 4.
- **2026-08-14. Типы каркаса отдачи в приёмник не берутся.** Своих он не несёт, а лежащие в
  дереве описывают его прошлую редакцию — сборка на них падала. Операции объявляют узкий тип
  ответа сами: код ответа — часть контракта приёма. Затронутый этап: 3.

- **2026-08-14. Договорённость отправителя этой веткой не вливается.** Её сценариев шестнадцать,
  покрыты одиннадцать: строка наблюдения и гард происшествия — первая задача линии работ, и она
  не начата. Вливание идёт последней задачей линии, а до тех пор спек домена `agent-kit` делится
  на поддомены: он и так вдвое длиннее предела. Затронут шаг 9 закрытия.
- **2026-08-14. Номера сценариев договорённости отправителя сдвинуты.** Главная ветка влила
  договорённость о каскаде законов и заняла `SC-AK-119` … `SC-AK-134` своими тестами, а
  невлитая договорённость держала тот же отрезок. Влитые номера не трогаются — на них ссылаются
  чужие тесты, — поэтому сдвинулись невлитые: `SC-AK-152` … `SC-AK-167`. Затронут мерж главной.
- **2026-08-14. Спек приёмника приведён к форме, которую читает сверка спеков.** Она заточена на
  процедуры Connect: правила ищет плоским списком без подзаголовков, а таблицу контракта с путями
  в обратных кавычках сверяет с декораторами процедур, которых у приёмника нет. Подзаголовки
  правил стали жирными строками, пути операций — без кавычек, коды отказов открыты строкой «Не
  применимо» с доводом. Сама проверка везётся пакетом и правится не здесь. Затронут шаг 9.

- **2026-08-14. Задача заведена заново под номером 573, ветка и папка переименованы.** Задачи, под
  номером которой шла работа, в очереди работ не оказалось вовсе: команда заведения напечатала
  номер, а записи не завела. Прежний номер не восстанавливается — он никому не принадлежит, и
  шапка замысла правится вместе с именем ветки. Затронуты шаги 11 и 12 закрытия.
- **2026-08-14. Задача и отчёт ведутся учётной записью владельца, а не машинной.** Решение
  владельца по факту: машинная учётная запись помечена хостингом, и всё, что она создаёт, видно
  только ей самой — ни задачи, ни отчёта прочитать нельзя даже владельцу репозитория. Обращение о
  снятии пометки отправлено; до ответа очередь работ ведётся владельцем. Коммиты по-прежнему
  подписываются машинной записью: история от пометки не зависит. Затронуты шаги 11 и 12.

## Заходы

### 2026-08-14

- Главная ветка подтянута (13 коммитов), влитые `RT-496-feedback-loop` и
  `RT-431-domain-layer-out` сняты.
- Разведка до первого вопроса: линия работ, договорённость пакета, раскладка своего дерева и
  раскладка дерева-образца обходом каталогов на два уровня.
- Шесть обязательных вопросов заданы и отвечены, ответы дословно в `grill.md`.
- Договорённость написана: девятнадцать правил, семь границ, восемь кодов отказа, пять
  сущностей хранилища, четыре открытых вопроса, двадцать сценариев `SC-MB`.
- Задача #498 заведена, папка переименована из черновика, ветка снята, колонка — «В работе».
- Раскладка: приложение NestJS, двенадцать либ семьи бэкенда (три домена по четыре слоя),
  общая либа типов груза, границы слоёв в конфиге линтера, Vitest у новых проектов при Jest у
  пакетов. `nx run-many -t lint test build` — двадцать проектов зелёные.
- Обвязку генератора пришлось править: настройка сборщика приехала в CommonJS, а дерево —
  модульное; ограничение замечаний линтера уехало неизвестным флагом в плагиновую цель.
- **Состязательный разбор договорённости дал семнадцать находок.** Тяжёлые: проводного
  контракта груза нет ни у одной из сторон (ни имён операций, ни заголовка с токеном, ни
  списка обязательных полей); «дописывается» допускает два прочтения, и `SC-MB-2` их не
  различает; сценарий отправителя `SC-AK-131` обещает ровно обратное правилу приёмника;
  статья отправителя о проверке на адрес дерева отбивала бы каждую отправку разбора; приёмнику
  неоткуда узнать признак дерева до первого прогона; перевыпуска токена нет ни одной командой;
  «отбивается целиком» несовместимо с тремя операциями. Правки не внесены — следующий шаг.
- `npm run check:layers` включён настройкой и краснеет на пяти строках: каталога границ
  `eslint/boundaries/domains` в дереве нет, и четыре реэкспорта в бареле старых пакетов —
  долг, который проверка вскрыла включением, а не эта работа.
- Договорённость переписана по находкам: контракт назвал четыре операции, заголовок
  `X-Tree-Token` и обязательные поля трёх родов; заведены разделы правил о форме груза, дереве
  и токене, записи месяца, недоехавшем грузе и двух родах, которые копятся. Сценариев стало
  тридцать два.
- Спек отправителя сведён с приёмником: `SC-AK-131` обещал вторую запись на прогон, статья о
  проверке на адрес дерева отбивала бы каждую отправку разбора, токены выдавала админка.
  `Q-14` помечен закрытым.
- Закон о наблюдаемости снят с отписки и разложен: четыре файла, правило и паттерн при нём.
- Этап 2: типы груза в общей либе, схема Postgres на пять сущностей, первая миграция, домен
  хранилища четырьмя слоями, состав контейнеров, проба живости. `docker compose up -d --build`
  поднимает базу и приёмник; проба отвечает `200`, при остановленной базе — `503`, имя каркаса
  из заголовков ответа снято. Прогон: 24 проекта зелёные, пять сценариев покрыты.
- Гейт правил звал на серверную сторону правила фронта, витрины и кита — карта разведена
  ветками по путям приёмника, сгенерированный клиент выведен из-под правил вовсе.
- Компаньон правила наблюдаемости заполнен по факту: две статьи исполнены, четыре не применимы
  к приёмнику, остальные помечены «не исполняется» с причиной. `sync --check` зелёный.
- `npm run check:docs` — 196 документов, расхождений нет. `npm run check:specs` краснеет на 228
  строках, все прежние: столкновение номера `SC-AK-131` с тестом пакета о другом и пустые
  привязки в компаньонах правил. Ни одна строка не этой работы.
- Каталог границ заведён: рёбра переехали в `eslint/boundaries/domains`, свод — `index.mjs`,
  конфиги линтера переведены в модульные. Границы приняты на внесённом нарушении. Долг этапа 1
  закрыт; проверка при этом стала требовать тег `scope:common-util`, которого в дереве нет вовсе.
- Правила каркаса фронта сняты с серверной стороны отдельным конфигом `eslint/backend.config.mjs`:
  шаблон путей в корневом конфиге не работает — цель линта у каждого проекта зовётся из его
  каталога.
- Этап 3: домен деревьев четырьмя слоями, проверка токена на всё приложение, три операции приёма,
  разбор отказов с журналом, миграция под необязательную сводку. Прогон `lint test build` — 22
  проекта зелёные, 59 тестов.
- Сборку пришлось чинить дважды: `express` не был объявлен зависимостью, а его типы в дереве
  описывают прошлую редакцию — операции объявили узкий тип ответа сами.
- Диск виртуальной машины контейнеров был полон, и миграция отказывала на теневой базе. Снят
  кэш сборки — 13 ГБ; образы и тома не тронуты.
- `npm run check:docs` — 200 документов, расхождений нет. `sync --check` зелёный.
- Этап 4: четыре команды деревьев — разбор доводов и печать в слое утилит, записи в
  `data-access`, служба и свой модуль в `feature`, разветвление точки входа в приложении.
  Прогон `lint test build` — 22 проекта зелёные, 83 теста.
- Образ приёмника не собирался с этапа 3: `Dockerfile` копировал `eslint.config.cjs`, а
  конфиги линтера стали модульными, и каталога границ он не копировал вовсе. Починено той же
  правкой — сборка образа проверена.
- Команды прогнаны на поднятой базе обоими путями запуска: собранным `dist` и образом через
  `docker compose exec`. Заведение печатает токен один раз, в хранилище лежат только хеши по
  64 знака; выданный командой токен приёмник принимает, отозванный отбивает `401`; дерево с
  отозванным токеном остаётся в списке вместе с днём последнего прогона, и его запись месяца
  цела.
- `npm run check:specs` о приёмнике молчит: все тридцать две метки `SC-MB` покрыты, пометок
  «Не покрыто» в спеке не осталось. Красные 226 строк — прежний долг, ни одна не этой работы.
- Этап 5: форма груза переехала в пакет, приёмник берёт её подпутём; в пакете заведены снимок
  надстроек, невыбранное, признак дерева, сборка трёх родов груза и запрос в приём; ключи
  `intake`, `token`, `tree` и `postmortems` в конфиге дерева; `propose` больше не заводит записей
  в очереди работ. Прогон `lint test build` — 23 проекта зелёные, 244 теста пакета.
- Образ собран заново, дерево заведено командой приёмника под своим признаком, токен положен вне
  дерева. Живой прогон: первый увёз сводку и разборы и завёл запись месяца, второй дописал её;
  третий увёз все три рода, и предложение пометилось. Отправка без токена отбита кодом 1,
  отправка отозванным — ответом 401; записи месяца при этом целы.
- Тексты пакета приведены к сделанному: README, скил пакета, команды разбора и сведения,
  сценарии домена `SC-AK-78`, `SC-AK-79`, `SC-AK-80`, `SC-AK-81`, компаньон и линия работ.
  Одиннадцать сценариев `SC-AK` договорённости отправителя сняты с пометки «Не покрыто»;
  оставшиеся пять — про строку наблюдения и гард происшествия, это первая задача линии.
- `check:specs` — 218 красных строк, было 226; `check:docs` — 200 документов, расхождений нет;
  `check:layers` — 5 прежних, ни одной новой; `sync --check` зелёный.
- Этап 6: компаньоны `testing`, `lib-layers`, `task-flow`, `angular-patterns` и `dependencies`
  перестали утверждать, что приложений здесь нет; указатель спеков и правило ведения работы
  зовут доменом и приложение; словарь дерева получил раздел о приёме груза; `Q-2` линии работ
  помечен закрытым, а её списки «что стоит» и «чего нет» приведены к сделанному.
- Проверки этапа 6: `check:docs` — 200 документов без расхождений; `agent-kit:check` зелёный;
  `check-file-size` — 10 прежних строк, ни одной новой.
- Главная ветка влита: она ушла на 34 коммита вперёд. Конфликтом встали пять файлов — четыре
  разложенных и настройка проверок; разложенное взято стороной главной и разложено заново с
  локального пакета, настройка собрана из обеих сторон. `sync --check` зелёный на v0.8.1.
- Столкновение номеров сценариев мерж не показал вовсе: обе договорённости дописывали в разные
  файлы. Нашла его сверка спеков — «помечен „Не покрыто“, но тест на него есть».
- Прогон после мержа: `lint typecheck test build` на всём дереве — 28 проектов зелёные;
  `lint:styles` зелёный.
- Шаг 9: договорённость о приёмнике влита — домена не было вовсе, каталог переехал на уровень
  выше, к нему написан `implementation.md` на тридцать семь правил, указатель спеков перестал
  звать её предложенной. Договорённость отправителя не вливается: линия работ не закрыта.
- Шаг 10: спек домена `agent-kit` перестал утверждать, что груз уезжает в очередь работ, —
  правлены терминология, контракт, коды отказов, решение и две привязки на снятый `submit.ts`;
  `Q-3` линии помечен закрытым, а её четвёртая задача знает, что отправка уже заведена.
- Проверки закрытия: `check:specs` — 191 красная строка, было 218 до мержа и 193 после него;
  `check:docs` — 200 документов без расхождений; `check:layers` — 5 прежних; `sync --check`
  зелёный.
- Задачи, под номером которой шла работа, в очереди работ не нашлось: хостинг отвечает на неё
  «нет такой». Разбор показал, что помечена сама машинная учётная запись — её задачи, отчёты и
  профиль не видны никому, включая владельца репозитория, а пределы обращений у её токена
  анонимные. Заведён разбор происшествия о подмене названного инструмента, отправлено обращение
  о снятии пометки.
- Задача заведена заново под номером 573 учётной записью владельца, положена в очередь работ и
  переведена в колонку работы; ветка и папка задачи переименованы под её номер.
