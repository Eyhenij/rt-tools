# Plan

**Task:** RT-2206 · **Branch:** RT-2206-board-audit-demands-epic-folder
**Epic:** RT-2195 · `docs/plans/tree-tells-truth-about-itself.md`
**Behaviour:** unchanged — правится проверка очереди работ; приложений и их экранов работа не
касается

## Task footprint

| What  | Where                                                |
| ----- | ---------------------------------------------------- |
| Specs | `docs/specs/agent-kit/board/`                        |
| Code  | `projects/agent-kit/assets/checks/board-folders.mjs` |
| Tests | `projects/agent-kit/tests/checks-board.test.sh`      |

## What counts as done

- Аудит очереди не требует папку задачи от ветки эпика.
- От ветки задачи требует по-прежнему: это проверяется внесённым нарушением.
- Ветка эпика узнаётся из плана, без сети: проверка остаётся единственной в аудите, работающей
  от местного репозитория.

## Stages

### 1. Сценарий проверки отбивает сегодняшнее поведение

- **What is done:** в набор проверки добавляются два случая — ветка, названная планом эпика, папки
  не требует; ветка задачи требует, как прежде.
- **Readiness sign:** набор красный до правки кода и зелёный после; счёт растёт с 86.
- **Verified by:** `bash projects/agent-kit/tests/checks-board.test.sh` — «N ok, 0 провалов» при N
  больше 86.

### 2. Проверка читает ветки эпиков из планов

- **What is done:** `checkBranchFolders` собирает имена веток эпиков из шапок планов тем же
  образцом, каким их пишет создающая команда, и такие ветки пропускает.
- **Readiness sign:** три строки «the task folder never travelled» сходят на одну — про эту самую
  ветку задачи, пока её папка не закоммичена, и на ноль после коммита папки.
- **Verified by:** `npm run check:board 2>&1 | grep -c 'never travelled'` — сейчас 3, после правки
  и коммита папки 0.

### 3. Описание доски называет новое правило

- **What is done:** статья о том, что ветка эпика папки задачи не носит, встаёт в описание доски с
  привязкой и сценарием.
- **Readiness sign:** сверка описаний зелёная, новый сценарий закрыт набором.
- **Verified by:** `node tools/check-specs.mjs` — код возврата 0.

## What this work does not do

- Остальные восемнадцать расхождений аудита не чинятся: часть из них верна и ждёт слова владельца,
  часть — работа соседней сессии.
- Метка эпика у хоста не спрашивается: проверка остаётся без сети.
