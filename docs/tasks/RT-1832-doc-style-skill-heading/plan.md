# Замысел

**Задача:** RT-1832 · **Ветка:** RT-1832-doc-style-skill-heading
**Поведение:** не меняется — правка заголовка раздела правила и надстройки, кода не задевает.

После написания этот файл не правится. Пересмотр этапа идёт в `progress.md` решением по ходу.

## След задачи

| Что        | Где                                            |
| ---------- | ---------------------------------------------- |
| Правило    | `projects/agent-kit/assets/rules/doc-style.md` |
| Надстройка | `.claude/rt-kit/overrides/rules/doc-style.md`  |
| Копия      | `.claude/skills/doc-style/SKILL.md`            |

## Что считается сделанным

- Раздел правила и надстройки называется «Скил без закона», список под ним прежний.
- Раскладка сходится, проверка слов зелёная.

## Этапы

### 1. Заголовок

- **Что делается:** заголовок раздела переименован в правиле пакета и в надстройке дерева,
  копия обновлена раскладкой.
- **Признак готовности:** слова «Скилы дерева» в дереве нет, раздел надстройки по-прежнему
  замещает пакетный.
- **Чем проверяется:** `pnpm run agent-kit:check` — «разложенное сходится»;
  `node tools/check-glossary.mjs` — «расхождений нет».

### 2. Отдача

- **Что делается:** папка задачи разбирается, заявка открывается черновиком чередой за
  RT-1830.
- **Признак готовности:** заявка открыта, прогон на вершине идёт.
- **Чем проверяется:** `gh pr view` — номер и состояние.

## Чего эта работа не делает

- Не правит словарь: слово в единственном числе он не снимал.
- Не снимает запись долга у дерева-потребителя: она у него своя и снимется его раскладкой.
