# Progress

## Where we stand

Rewritten by every session, not appended to.

- **State:** `этап-идёт`
- **Stage:** 3 of 5 — Усечение подписи с подсказкой
- **Done:** этап 1 закрыт — у метки есть описание домена: четырнадцать статей, тринадцать
  сценариев, привязки. Заголовки проб помечены номерами той же правкой. Этап 2 закрыт — у
  метки три ступени размера.
- **Next step:** усечение подписи многоточием и подсказка с целым значением.
- **Uncommitted:** папка задачи, описание домена метки, заголовки проб.
- **Waiting for the owner:** нет.
- **PR:** not open yet

## Decisions along the way

- **Семейство не пишется заново: дополняется готовая метка `rt-tag`** — измерено, что она закрывает
  подпись, значок с любой стороны, палитру, форму и заливку, а не закрывает ровно двух вещей значка
  первого кита: размеров и усечения подписи с подсказкой. Тело карточки писалось в расчёте на то,
  что семейства во втором ките нет вовсе. Affected stage of the plan: 2, 3.
- **Описание у метки заводится этой же работой** — `tools/specs-for.mjs` отвечает, что о файле
  метки не говорит ни одно описание. Договорённость поэтому пишется прямо в него, черновика не
  заводится. Affected stage of the plan: 1.

## Sessions

### 2026-09-17

- Отправные точки сняты: проверки метки 30 из 30; `npm run check:specs` — код выхода 0; кадры
  второго кита 589 из 589; аудит входов кита — 76 страниц-обзоров без расхождений.
- **Первый прогон кадров был красным и красным не был.** Восемь падений из 568, все по истечении
  15 секунд, ни одного расхождения кадра: рядом шли прогон кадров первого кита и `check:affected`.
  На тихой машине — 589 из 589. Набор кадров второго кита чувствителен к занятости машины, и его
  красноту надо читать по причине падения, а не по цвету.
- **Этап 1 сделан.** Заведено `docs/specs/ui-kit-v2/tag/`: четырнадцать статей на то, что метка
  обещает сегодня, и на два новых обещания — размеры и усечение. Тринадцать сценариев, номера
  проставлены в заголовки пятнадцати проб той же правкой. Две статьи о новом стоят с приговором
  «не исполнено» и называют этап, которым придут: договорённость идёт до кода.
  Две привязки пришлось переписать — форма адреса не берёт имя с двумя дефисами впереди и имя с
  точкой внутри, так что `--rt-tag-radius` и `rt-kit.components` названы иначе.
  Проверено: `npm run check:specs` — код выхода 0; проверки метки 30 из 30; предел длины файлов
  расхождений не называет.
- **Этап 2 сделан.** У метки появился вход ступени с тремя значениями. Отступы, кегль и просвет
  берутся шкалой; средняя ступень повторяет то, чем метка рисовалась до неё, так что вид без входа
  не двигается ни на пиксель. Своего входа у значка нет — его ступень ведёт ступень пилюли: 12, 16
  и 20 пикселей. Названы ось на странице-обзоре и вход в обеих таблицах.
  Шкала отступов второго кита идёт не подряд — в ней нет ни `1-5`, ни `3`, — и ступени взяты по
  тому, что в ней есть.
  Проверено: проверки метки 33 из 33 (было 30), `npm run check:specs` — код выхода 0, линтер
  второго кита чист, `verify-ui-kit-v2-docs` — 76 страниц без расхождений.
