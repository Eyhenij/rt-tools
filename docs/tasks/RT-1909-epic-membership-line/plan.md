# Замысел

**Task:** RT-1909 · **Branch:** RT-1909-epic-membership-line
**Behaviour:** unchanged — работа правит сверку очереди работ; кода приложения не касается

После написания этот файл не правится. Пересмотр этапа идёт в `progress.md` решением по ходу.

## След задачи

| Что       | Где                                 |
| --------- | ----------------------------------- |
| Проверки  | `projects/agent-kit/assets/checks/` |
| Наборы    | `projects/agent-kit/tests/`         |
| Спек      | `docs/specs/agent-kit/checks/`      |
| Раскладка | `tools/`                            |

## Что считается сделанным

- Задача, называющая номер эпика в объяснении, ложной строки не получает.
- Задача, объявившая принадлежность так, как предписывает отказ сверки, судится с обеих сторон
  по-прежнему: и «замысел называет, а тело — нет», и «тело называет, а замысел — нет».
- Статья спека со строкой компаньона и сценарием на месте; набор сверки очереди работ зелёный.
- `npm run check:board` не даёт ложных строк о принадлежности эпику.

## Этапы

### 1. Принадлежность объявляется словом о задаче, а не упоминанием номера

- **Что делается:** выражение, которым читается тело задачи, требует слова о задаче прямо перед
  словом об эпике. Так объявлены все живые задачи эпиков, и так же звучит строка, которую сверка
  предписывает в отказе; упоминание номера в объяснении под это не подходит.
- **Признак готовности:** новые пробы зелёные — тело с упоминанием номера в объяснении молчит,
  тело с объявленной принадлежностью судится как прежде.
- **Чем проверяется:** `bash projects/agent-kit/tests/checks-board.test.sh` — `0 провалов`.

### 2. Спек, раскладка и сверка вживую

- **Что делается:** статья о том, чем объявляется принадлежность эпику, встаёт в спек со строкой
  компаньона и сценарием; пакет собирается и раскладывается.
- **Признак готовности:** сценарий привязан, раскладка сходится, ложных строк о принадлежности нет.
- **Чем проверяется:** `npm run check:board` — ни одной ложной строки о принадлежности эпику.

## Чего эта работа не делает

- Не трогает сторону замысла: она закрыта задачей RT-1906.
- Не заводит замысел эпика #1896: он лежит в невлитой ветке.
