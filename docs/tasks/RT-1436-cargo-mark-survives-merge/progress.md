# Ход работы

## Где стоим

- **Состояние:** `этапы-кончились`
- **Этап:** 3 из 3 — признак отставшей отметки заведён
- **Сделано:** все три этапа закрыты. Подраздел с вызовом отметки в паттерне `task-flow-archive`,
  две статьи в правиле `cargo-triage` с привязками, строка о полном ключе в образце папки
  задачи, второй отбор в команде `cargo:fixed` с четырьмя сценариями пробы. `check-file-size` —
  длиннее предела 0, `check:specs` — код 0, `check-doc-paths` — код 0, `agent-kit:check` —
  разложенное сходится, проба отбора — 14 ok, 0 провалов
- **Следующий шаг:** привести тексты к сделанному, прогнать набор, разобрать папку
- **Незакоммиченное:** правки всех трёх этапов
- **Ждём владельца:** нет
- **PR:** ещё не открыт

## Решения по ходу

- **Шаг встаёт в состояние `влито`, а не в закрытие работы** — к закрытию слияния ещё не было.
  Затронутый этап замысла: 1.
- **Правило `cargo-triage` правится в дереве, а не в пакете** — оно своё: пакет везёт общее, а
  разбор груза исполняет это дерево. Замысел называл пакетный путь, дерево держит правило
  своим. Затронутый этап замысла: 1.
- **Привязка статьи вердиктом, а не местом в коде** — исполнение шага машине не видно, и
  проверка на него не заводится. Затронутый этап замысла: 1.
- **Признак живёт в команде `cargo:fixed`, а не в сверке очереди** — сверка очереди приезжает
  раскладкой из пакета, а приём груза свой: пакетная проверка не может звать чтение этого
  дерева. Команда отбора своя и уже делает то же самое для новых записей.
  Затронутый этап замысла: 3.

## Заходы

### 2026-08-27

- Разобраны 19 записей груза, стоявших в «в работе»: все закрыты, отмечены починенными
  шестнадцатью вызовами, чтение приёма отвечает «всего 0».
- Заведена задача RT-1436 на три разрыва, из-за которых отметка терялась.
