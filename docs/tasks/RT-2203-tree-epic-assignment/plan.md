# Plan

**Task:** RT-2203 · **Branch:** RT-2203-tree-epic-assignment
**Behaviour:** unchanged — правка трогает пакет правил и объявления дерева; кода приложения нет

## Task footprint

| What          | Where                                                                   |
| ------------- | ----------------------------------------------------------------------- |
| Specs         | `docs/specs/agent-kit/delivery-gate/`                                   |
| Laws          | `docs/constitution/work-conduct.md`                                     |
| Rules         | `.claude/skills/task-flow/`, `projects/agent-kit/assets/rules/`         |
| Code          | `projects/agent-kit/assets/hooks/`, `projects/agent-kit/assets/checks/` |
| Данные дерева | `.claude/rt-kit/assignments.md`, `.claude/rt-kit/tree-name`             |
| Tests         | `projects/agent-kit/tests/`                                             |

## What counts as done

- Назначения всех рабочих копий лежат в одном файле в главной ветке: строка на копию — имя копии,
  эпик, план работ, дата назначения.
- Рабочая копия называет себя коротким именем из местного файла вне истории; путей машины в
  репозитории нет.
- Заведение задачи, создание ветки и перенос карточки запрещены, если эпик не назначен этой копии,
  строки для копии нет или назначенный эпик кончился.
- Отказ называет, что делать: спросить владельца о назначении, а не искать работу самому.

## Stages

### 1. Файл назначений и имя копии

- **What is done:** формат файла назначений объявлен и заполнен по живым копиям; местный файл имени
  копии заведён и назван в `.gitignore`; чтение того и другого вынесено в один вызов.
- **Readiness sign:** вызов печатает назначение этой копии и молчит про чужие; для копии без строки
  говорит, что назначения нет.
- **Verified by:** `node tools/tree-assignment.mjs` — печатает эпик этой копии; та же команда во
  второй рабочей копии — «назначения нет».

### 2. Запрет чужого эпика и просрочки

- **What is done:** проверка поставки запрещает заведение задачи, создание ветки и перенос карточки
  при чужом эпике, отсутствии строки и кончившемся назначении.
- **Readiness sign:** набор сценариев проверки зелёный, включая три новых случая.
- **Verified by:** `bash projects/agent-kit/tests/guard-tree-assignment.test.sh` — 0 провалов.

### 3. Тексты слоя правил

- **What is done:** статья правила о назначении рабочей копии и её привязка; сценарии в описании
  проверок поставки; строка в спутнике правила вместо нынешней «не проверяется ничем».
- **Readiness sign:** описания и адреса сходятся, раскладка пакета чистая.
- **Verified by:** `npm run check:specs` — новые сценарии не в долгах; `pnpm run agent-kit:check` —
  расхождений нет; `npm run check:docs` — расхождений нет.
