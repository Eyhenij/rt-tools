# Замысел

**Задача:** RT-1463 · **Ветка:** RT-1463-integration-value-follows-its-neighbour
**Поведение:** не меняется — работа правит слой правил; кода приложения не касается

После написания этот файл не правится. Пересмотр этапа идёт в `progress.md` решением по ходу.

## След задачи

| Что     | Где                                              |
| ------- | ------------------------------------------------ |
| Правило | `projects/agent-kit/assets/rules/reuse-first.md` |
| Спутник | `.claude/skills/reuse-first/implementation.md`   |

## Что считается сделанным

- Правило говорит: значение интеграции берётся тем же путём, что и соседнее значение той же
  интеграции; расхождение соседей — вопрос «почему один сделан, а второй нет».
- Привязка статьи стоит в спутнике, сверка спеков зелёная.

## Этапы

### 1. Статья о соседнем значении интеграции

- **Что делается:** статья в раздел применения правила единообразия, строка привязки в спутнике.
- **Признак готовности:** статья и привязка стоят, длина в пределе.
- **Чем проверяется:** `npm run check:specs`, `node tools/check-file-size.mjs`,
  `pnpm run agent-kit:check`.

## Чего эта работа не делает

- Не заводит проверки: значение, зашитое константой, синтаксически исправно, и сверка повторов
  пару констант в двух приложениях под свои признаки не берёт.
