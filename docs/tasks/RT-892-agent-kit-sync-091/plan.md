# Замысел

**Задача:** RT-892 · **Ветка:** RT-892-agent-kit-sync-091
**Поведение:** не меняется — правка задевает только разложенный слой правил, кода приложения
в ней нет ни строки.

## След задачи

| Что     | Где                                                    |
| ------- | ------------------------------------------------------ |
| Законы  | `docs/constitution/` — все, шапкой                     |
| Правила | `.claude/skills/` — все разложенные, шапкой            |
| Код     | нет; правка кладётся командой раскладки, а не руками   |
| Прочее  | `.claude/hooks/`, `.claude/agents/`, `tools/`, словарь |

## Что считается сделанным

- Шапка `rt-kit v0.9.1` стоит во всех разложенных файлах, а `v0.9.0` не осталось ни в одном.
- `pnpm run agent-kit:check` зелёная — расхождений ноль.
- Кроме номера в шапках, не изменилось ничего: построчный отбор разницы мимо шапок пуст.

## Этапы

### 1. Раскладка версии 0.9.1

- **Что делается:** ветка от вершины главной ветки, где лежат исходники `0.9.1`; сборка пакета
  и `pnpm run agent-kit:sync`; сверка того, что содержательных правок в разложенном нет.
- **Признак готовности:** сверка раскладки зелёная, а разница правки помимо строк шапки пуста.
- **Чем проверяется:** `pnpm run agent-kit:check`, построчный отбор разницы мимо шапок,
  набор гейта пуша.

## Чего эта работа не делает

- Правку самих ресурсов пакета: `0.9.1` не тронул ни одного из них, и заводить своё этой
  работой не за чем.
- Правку надстроек дерева в `.claude/rt-kit/overrides/`: разделов они не потеряли, раз
  разложенное совпало с прежним до символа.
- Подъём номера версии и публикацию — они уже сделаны владельцем.
