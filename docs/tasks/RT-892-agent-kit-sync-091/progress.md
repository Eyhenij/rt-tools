# Ход работы

## Где стоим

- **Этап:** 1 из 1 — раскладка версии 0.9.1
- **Сделано:** разложено 155 файлов, содержательных правок в них нет ни одной; папка задачи
  собрана, задача заведена и стоит в колонке работы
- **Следующий шаг:** набор гейта пуша, коммит, пуш, PR черновиком
- **Незакоммиченное:** раскладка и эта папка
- **Ждём владельца:** нет
- **PR:** ещё не открыт

## Решения по ходу

- **Раскладка была положена раньше, чем завелась папка задачи.** Сверка и `sync` прошли до
  того, как появились задача, ветка с её номером и замысел: команда владельца выглядела
  служебной, и порядок правила был вывернут. Отбил это гард хода работы на первой же попытке
  написать сам разбор просьбы, а не сама правка: `sync` пишет мимо путей, за которыми гард
  смотрит. Разбор —
  `docs/postmortems/2026-08-19-layout-laid-before-the-work-was-framed.md`. Затронутый этап: 1.
- **Ветка заведена от вершины главной ветки, а не от той, на которой шёл заход.** Прежняя
  работа влита, а исходники `0.9.1` лежат вершиной главной ветки — раскладка читает их, а не
  реестр пакетов. Затронутый этап: 1.

## Заходы

### 2026-08-19

- Сверка раскладки до правки: 155 расхождений, все одного рода — «отстал от пакета»; правленых
  на месте файлов нет ни одного.
- `pnpm run agent-kit:sync` переписал те же 155 файлов. Отбор разницы мимо строк шапки пуст:
  версия `0.9.1` не тронула ни одного разложенного ресурса, суммы совпали до символа.
- Заведена задача RT-892, ветка по её номеру, папка задачи.
- Открыт PR #893 черновиком; ревьювер — владелец, исполнитель — учётная запись машинной работы,
  задача переставлена в колонку разбора.
- Написан разбор происшествия о вывернутом порядке и предложение слою правил при нём — двумя
  пунктами: раздел о порядке правки пакета и признак гарда хода работы.
