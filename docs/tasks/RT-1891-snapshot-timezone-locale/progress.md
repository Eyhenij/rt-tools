# Progress

## Where we stand

- **State:** `этап-идёт`
- **Stage:** 5 из 5 — работа отдана, и стопка разблокирована
- **Done:** этапы 1–4 закрыты. Файл настроек прогона задаёт язык запуска браузера и пояс с
  языком контекста; набор снимков зелёный целиком — 469 кадров, ни одного расхождения, ни одной
  сироты. Переснято 22 эталона: 6 у выбора даты и 16 у переписки. Правило о снимках знает про
  пояс и язык, и знает про сирот как следствие упавшего первого кадра.
- **Next step:** набор проверок целиком, пуш, заявка черновиком; затем вливание в обе ветки
  стопки и пуш их
- **Uncommitted:** нет
- **Waiting for the owner:** нет
- **PR:** ещё не открыт

## Decisions along the way

- **Ветка идёт от главной и становится новым низом стопки** — работа не принадлежит эпику
  «Один кит». Обе ветки стопки получают её слиянием и после этого пушатся. Задет этап 5 замысла.
- **Привязки в спутнике не появилось: у образца спутника нет.** Замысел обещал её по невнимности —
  `ui-component-tests-visual` объявлен образцом, а не правилом, и привязки статей живут у правила
  над ним, `ui-component-tests`. Новая статья ничего у того правила не меняет: она о готовом
  приёме, а не о том, что должно быть истинным. Задет этап 4 замысла.

## Sessions

### 2026-09-07

- Расхождение воспроизведено по кадрам различий: 15 файлов — 6 у выбора даты, 1 у поля ввода,
  8 у переписки. Восемь сирот оказались следствием: у упавшей истории переписки второй кадр, на
  ширине 1441, не снимается вовсе.
- Причина прочитана прямо с кадров: у выбора даты сменился порядок дня и месяца и появился
  двенадцатичасовой формат, у переписки время разъехалось на пять часов при одинаковой дате.
- Найдена дверь для настроек: прогон снимков ищет свой файл настроек в каталоге витрины и,
  найдя, отдаёт его сборщику тестов вместо своего. Файл при витрине второго кита первого кита
  не касается вовсе.
- Замером на пяти заходах выяснено, что настроек нужно две, а не одна: язык контекста меняет
  `navigator.language`, но формат родного поля даты не трогает — его задаёт язык запуска самого
  браузера. Пояс, наоборот, слушается настройки контекста. Кадры замера: `03/15/2026` без
  настроек и `15.03.2026` с языком запуска.
- Этапы 1–3. Прогон целиком после прибивания: 14 расхождений вместо прежних 15 — поле ввода
  сошлось само, у него разошёлся только двенадцатичасовой формат времени. Прочие 447 кадров
  совпали: русский язык и пояс `UTC` не задели ничего, кроме даты и времени.
- Все 14 кадров различий прочитаны до съёмки. У выбора даты разошлись только разделители —
  `15/03/2026` против `15.03.2026`, ширина полей та же; у переписки — только время, `23:02`
  против `16:02`. Ни одной уехавшей вёрстки.
- Переснято 22 эталона: 6 у выбора даты и 16 у переписки. Шестнадцать вместо восьми потому, что
  у переписки снова снимаются кадры на ширине 1441 — те самые восемь сирот. Их обрамление
  сверено со старым файлом из истории: рамка та же, разошлось только время.
- Прогон целиком после съёмки: 469 кадров, все сошлись, сирот нет.
- Этап 4. Правило о снимках получило строку в перечне недетерминированного, замечание о том, что
  настроек нужно две, и ловушку про сирот как следствие упавшего первого кадра. Сверка спеков
  зелёная: домены 6, сценарии 1303, покрыто 1179.
