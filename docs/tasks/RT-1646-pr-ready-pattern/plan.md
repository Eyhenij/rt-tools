# Замысел

**Задача:** RT-1646 · **Ветка:** RT-1646-pr-ready-pattern
**Поведение:** не меняется — работа правит слой правил, кода приложения не задевает

## След задачи

| Что     | Где                                                                    |
| ------- | ---------------------------------------------------------------------- |
| Спеки   | нет — договорённости о продукте у слоя правил не бывает                |
| Законы  | `docs/constitution/delivery.md` — под ним живёт правило поставки       |
| Правила | `git-workflow` и его паттерны: `-pr`, `-stack`, `-freshness`, `-merge` |
| Код     | нет — пакет правил, ресурсы `projects/agent-kit/assets`                |

## Что считается сделанным

- Паттерн `git-workflow-pr-ready` лежит в пакете и разложен в дерево.
- Правило поставки перечисляет его среди своих паттернов.
- Паттерн говорит четыре вещи: когда снимается черновик, что раньше — доведение отданного или
  новая задача, чем разбирается красный прогон, куда идёт правка общей причины у череды.
- Чтение состояния заявок в паттерне не повторяется — стоит ссылка на `git-workflow-freshness`.
- Правило поставки остаётся в пределе веса текста.

## Этапы

### 1. Паттерн написан и вписан в правило

- **Что делается:** заводится `projects/agent-kit/assets/patterns/git-workflow-pr-ready.md`,
  его имя дописывается в раздел «Паттерны» правила поставки, пакет раскладывается.
- **Признак готовности:** файл разложен в `.claude/skills/git-workflow-pr-ready/SKILL.md`, имя
  видно в списке паттернов правила.
- **Чем проверяется:** `pnpm run agent-kit:check` — расхождений нет.

### 2. Пределы и привязки сошлись

- **Что делается:** правило укладывается в предел веса текста, привязки статей в компаньоне
  правятся под изменённые формулировки.
- **Признак готовности:** обе проверки зелёные.
- **Чем проверяется:** `node tools/check-file-size.mjs` — долг 0; `node tools/check-specs.mjs`
  — расхождений нет.

### 3. Наборы проб пакета зелёные

- **Что делается:** прогон наборов пакета целиком.
- **Признак готовности:** ни одного провала.
- **Чем проверяется:** `bash projects/agent-kit/tests/run.sh` — «ВСЕ НАБОРЫ ЗЕЛЁНЫЕ».

## Чего эта работа не делает

- Не заводит стража под снятие черновика: гард брошенного черновика уже стоит и работает.
- Не трогает признак ложного слияния после перебазирования отданной ветки — соседний промах
  того же захода, задача под него не заведена.
- Не переписывает чтение состояния заявок: оно живёт в `git-workflow-freshness`.
