<!-- rt-kit v0.10.0 · samples/tasks/_template/progress.md · fc2dd20e34b0 · правится надстройкой, не здесь -->

# Ход работы

## Где стоим

Перезаписывается каждым заходом, а не дописывается.

- **Состояние:** `работа-отдана`
- **Этап:** 3 из 3 — все отмечены, работа выложена
- **Сделано:** числа объявлены в настройке агента, разбор состояния печатает пороги и называет
  расхождение, 13 сценариев зелёные, правило и оба спека говорят то же
- **Следующий шаг:** взята следующая задача эпика; сюда возвращаться после разбора владельцем
- **Незакоммиченное:** нет
- **Ждём владельца:** разбора заявки
- **PR:** #983, черновик, ждёт прогона и разбора · не сделано: разбор папки задачи

## Решения по ходу

- **Ветка отведена от главной, а не от прошлой задачи эпика.** Порядок эпика назначен, работы
  не пересекаются: там хук передачи, здесь числа порогов.

## Заходы

### 21 августа 2026

- Заход начат с передачи прошлого. Прочитан замысел эпика, прочитано тело задачи.
- Разведка по порогам: размер окна дерево задаёт, страж считает от него, хук передачи стоит
  на событии перед сжатием. У инструмента нашлось два рычага порога сжатия — настройка размера
  окна автосжатия и переменная окружения с долей в процентах.
- Расхождение измерено чтением кода инструмента: страж останавливает заход на половине окна,
  сжатие приходит почти у предела.
- Владелец ответил: сводим на половине окна, задаём обоими рычагами сразу. Замысел написан на
  три этапа.
- **Первый этап.** В настройке агента объявлены четыре числа: размер окна, оба порога стража и
  пара сжатия — окно автосжатия и доля. Профиль пакета называет, что порог сжатия задаётся этой
  парой, и почему своих значений у пакета тут нет.
- **Второй этап.** Модуль порогов читает настройку разбором, сводит пару стража с парой сжатия
  и отдаёт раздел разбору состояния. Не разобравшаяся настройка и дерево без размера окна
  проходят молча. 13 сценариев зелёные, набор гардов зелёный, разбор состояния на этом дереве
  печатает сведённые пороги.
- **Третий этап.** Правило ведения работы говорит, что порог сжатия задаёт дерево и сводит с
  порогом остановки; спек гардов хода обещает это тремя утверждениями с привязками, спек
  раскладки — утверждением о печати порогов. Три сценария заведены, сверки текстов зелёные.
- Формулировка о хуке передачи сначала стояла безусловной. Правило о переносимом тексте требует
  условной: хук приходит второй задачей эпика, и в дереве, где его не разложили, безусловная
  фраза врёт. Поправлено в правиле и в спеке.
- **Гард длины отбил пуш.** Правило ведения работы выросло до 505 строк при пределе в 500:
  запаса в файле оставалось четыре строки, а статья заняла девять. Ужата до четырёх — обещание
  держится привязкой и сценариями, а не длиной формулировки. Правило пора делить, и это
  отдельная задача; названа владельцу в теле заявки.
- **Заявка #983 открыта черновиком**, колонка переведена в разбор, ревьювер и метка на месте.
  Проверки перед пушем: `nx affected -t lint typecheck test build` — 344 теста зелёные, набор
  гардов зелёный, пять сверок текстов с нулевым кодом, `agent-kit:check` сходится.
