<!-- rt-kit v0.8.2 · samples/tasks/_template/plan.md · c862bbaf5407 · правится надстройкой, не здесь -->

# Замысел

**Задача:** RT-734 · **Ветка:** RT-734-node-spec-merge
**Поведение:** не меняется — договорённость описывает уже выкаченное, ни одного нового
обещания работа не даёт.

Первая задача эпика RT-733. Замысел эпика — `docs/plans/message-bus.md`.

После написания этот файл не правится. Пересмотр этапа идёт в `progress.md` решением по ходу.

## След задачи

| Что     | Где                                                         |
| ------- | ----------------------------------------------------------- |
| Спеки   | `docs/specs/message-bus/`, `proposed/rented-node/`          |
| Замыслы | `docs/plans/message-bus.md`                                 |
| Правила | `.claude/skills/task-flow/`, `.claude/skills/git-workflow/` |
| Код     | `apps/message-bus/`, `deploy/`                              |

## Что считается сделанным

- Правила договорённости стоят в `docs/specs/message-bus/spec.md`, сценарии — в `scenarios.md`
  с прежними номерами, привязки — в `implementation.md` и ведут в живой код.
- Каталога `docs/specs/message-bus/proposed/` на диске нет.
- `npm run check:specs` не даёт новых расхождений, `check:docs` зелёный.
- Замысел эпика лежит в дереве, и очередь работ на него ссылается.
- Папка задачи разобрана, чужая папка `RT-727-rules-review-cleanup` снята тем же коммитом.

## Этапы

### 1. Эпик объявлен

- **Что делается:** карточка RT-733 с меткой эпика заведена, замысел `docs/plans/message-bus.md`
  написан: состав, порядок задач, признак закрытия, открытый вопрос о дампе.
- **Признак готовности:** замысел лежит в дереве, карточка стоит в очереди работ.
- **Чем проверяется:** `npm run check:board`, `npm run check:docs`.

### 2. Договорённость влита

- **Что делается:** правила, сценарии и привязки переезжают в спек домена; каталог `proposed/`
  снимается; раздел «Что не входит» перечитывается целиком.
- **Признак готовности:** привязки ведут в живой код, номера сценариев прежние.
- **Чем проверяется:** `npm run check:specs`, `npm run check:docs`.

### 3. Тексты приведены к сделанному

- **Что делается:** перечитываются `docs/PROD.md`, `docs/specs/README.md` и разделы «Чего из
  закона здесь нет» у правил по следу задачи.
- **Признак готовности:** ни один из них не описывает приёмник таким, каким он был до выкатки.
- **Чем проверяется:** чтение глазами, `check:docs`.

## Чего эта работа не делает

- Не правит приёмник: код этой задачей не трогается вовсе.
- Не правит запись архива о RT-588, сказавшую о вливании раньше времени, — это решение владельца.
- Не берёт задач вне эпика RT-733: границу назвал владелец.
