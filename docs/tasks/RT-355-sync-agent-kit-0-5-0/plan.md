# Замысел

Задача: #355. Ветка: `RT-355-sync-agent-kit-0-5-0`.

**Задача:** RT-355 · **Ветка:** RT-355-sync-agent-kit-0-5-0
**Драфт:** не требуется — работа не заводит поведения
**Поведение:** не меняется — раскладка обновляет номер редакции в шапках; тела ресурсов те же.

После написания этот файл не правится. Пересмотр этапа идёт в `progress.md` решением по ходу.

## След задачи

| Что     | Где                               |
| ------- | --------------------------------- |
| Спеки   | не задеты                         |
| Законы  | не задеты                         |
| Правила | разложенные файлы с шапкой пакета |
| Код     | не задет                          |

## Что считается сделанным

- `pnpm run agent-kit:check` выходит нулём и называет редакцию `0.5.0`.
- Правка не задевает ничего, кроме строки редакции в шапках разложенных файлов.

## Этапы

### 1. Дерево разложено выпущенной редакцией

- **Что делается:** `pnpm run agent-kit:sync` и коммит того, что он поправил.
- **Признак готовности:** сверка раскладки сходится с пакетом `0.5.0`.
- **Чем проверяется:** `pnpm run agent-kit:check`, `git diff` — только строки шапок.

## Чего эта работа не делает

- Не правит тексты ресурсов: их правка идёт в пакете, а не в разложенном.
- Не разбирает папку задачи #351, лежащую в главной ветке: это отдельный долг.
