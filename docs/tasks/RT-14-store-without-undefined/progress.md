# Ход работы

## Где стоим

- **Состояние:** `этапы-кончились`
- **Этап:** 2 из 2 сделаны — пустоты в селекторах, отказах, типах и начальном состоянии
- **Сделано:** селекторы отвечают объявленным типом на состоянии без полей, разбор отказа судит
  пустоту, общий тип списка называет отсутствие одним способом, начальное состояние приведено к
  своему типу
- **Следующий шаг:** отдача ждёт слова владельца — ветка держится локально
- **Незакоммиченное:** нет
- **Ждём владельца:** нет
- **PR:** не открывается — владелец назначил держать ветки локально

## Решения по ходу

- **2026-08-20. Ветка остаётся локальной, отчёт не открывается.** Владелец сказал прямо: «Не пуш
  ветки, храни их только локально».

## Заходы

### 2026-08-20

- Задача взята из очереди работ, разведка по пакету стора идёт.

- **2026-08-20. Тело задачи пустое, объём взят из кода.** Заголовок называет и типы, и рантайм;
  разведка нашла четыре места, где стор отдаёт пустоту, и все четыре закрыты. Пятое — тип
  полезной нагрузки сообщения — вынесено из работы: он требует связи сообщения с его нагрузкой.
- **2026-08-20. Поле состояния подробностей снято из начального состояния.** Его не было ни в
  типе, ни у одного читателя во всём дереве: оно лежало там, где наследники его не видят.
- Этап 1 и 2 сделаны: `pnpm exec nx run-many -t lint typecheck build test -p @rt-tools/store` —
  27 спек зелёных, линт, типы и сборка прошли. Соседние пакеты: 272 и 32 спеки зелёных.
- `npm run check:specs` — четыре домена, 538 сценариев; `npm run check:docs` — 361 документ без
  расхождений.
