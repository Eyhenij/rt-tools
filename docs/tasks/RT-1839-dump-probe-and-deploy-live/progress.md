# Ход работы

## Где стоим

Перезаписывается каждым заходом, а не дописывается.

- **Состояние:** `этап-идёт`
- **Этап:** 2 из 4 — выкатка конвейером из главной ветки
- **Сделано:** этап 1 — команда `probe` в `deploy/dump.sh`, прогнана на местной копии
  состава прода: совпадение даёт код 0, расхождение называет таблицу и даёт код 1, контейнер
  пробы снесён в обоих случаях
- **Следующий шаг:** запустить рабочий поток выкатки с главной ветки и дождаться конца
- **Незакоммиченное:** чужая правка манифеста дерева — в коммит не идёт
- **Ждём владельца:** нет
- **PR:** ещё не открыт

## Решения по ходу

Пока нет.

## Заходы

### 2026-09-06

- Разведка: описание прода уже несёт порядок пробы на одноразовой базе; на узле нет каталога
  дампов; выкатка конвейером проходила восемь раз до 29 августа; главная ветка с тех пор ушла
  вперёд примерно на 1200 коммитов; новых миграций после выкаченной версии нет.
