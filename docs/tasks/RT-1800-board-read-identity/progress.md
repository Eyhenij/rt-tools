# Ход работы

## Где стоим

Перезаписывается каждым заходом, а не дописывается.

- **Состояние:** `этапы-кончились`
- **Этап:** оба сделаны
- **Сделано:** ответы `task` и `pr` несут поле `viewer`; пояснение о токене в помощнике
  клиента называет, какие команды его подставляют; правило поставки и спек проверок получили
  по фразе, сценарий SC-AK-873; набор борды разделён — состояние заявки уехало в свой файл,
  4 новых пробы, без поля краснеют все 4
- **Следующий шаг:** дождаться полного набора проб, закоммитить, разобрать папку, открыть
  заявку
- **Незакоммиченное:** помощник, раскладка, пробы, спек, правило и его компаньон
- **Ждём владельца:** нет
- **PR:** ещё не открыт

## Решения по ходу

- **Поле `viewer` со значениями `machine` и `client`** — логин потребовал бы сети, а гарды
  читают ответ без неё. Затронутый этап: 1.
- **Набор борды разделён по предмету** — он перерос предел длины файла; состояние заявки —
  свой набор. Затронутый этап: 1.

## Заходы

### 2026-09-05

- Заведены ветка, папка задачи и разбор просьбы.
- Поле `viewer`, пробы, спек и правило.
