# Замысел

Задача: #685. Ветка: `RT-685-merge-button-past-guard`.

**Задача:** RT-685 · **Ветка:** RT-685-merge-button-past-guard
**Поведение:** не меняется — правятся закон, правило и паттерны слоя правил; ни `apps/`, ни
`libs/` в этом дереве нет. Подтверждено владельцем.

После написания этот файл не правится. Пересмотр этапа идёт в `progress.md` решением по ходу.

## След задачи

| Что         | Где                                                                   |
| ----------- | --------------------------------------------------------------------- |
| Закон       | `projects/agent-kit/assets/laws/work-conduct.md`                      |
| Правило     | `projects/agent-kit/assets/rules/task-flow.md`                        |
| Паттерны    | `projects/agent-kit/assets/patterns/task-flow-close.md`, `-resume.md` |
| Чужая папка | `docs/tasks/RT-682-task-folder-always/`                               |

## Что считается сделанным

- Закон о ведении работы несёт четыре статьи: исполнитель называет, чего ждёт и что сделает
  следом; работа не готова, пока он не сказал этого прямо; уборка идёт до просьбы о включении;
  ожидание чужого шага исполнителя не останавливает — последняя в законе уже есть.
- Правило `task-flow` называет, чем это исполняется здесь: заявка, прогон, просьба о слиянии.
- Паттерн `task-flow-close` даёт готовые формулировки обоих сообщений владельцу — после
  открытия заявки и перед просьбой о слиянии.
- Папка задачи RT-682 разобрана в описание прошлого, своя папка — тем же коммитом.
- Раскладка сходится с пакетом, проверки зелёные.

## Этапы

### 1. Закон

- **Что делается:** три новые статьи в раздел о ведении работы рядом со статьёй об отданном на
  разбор.
- **Признак готовности:** закон говорит, что исполнитель называет ожидание, объявляет
  готовность отдельной просьбой и убирает за собой до неё.
- **Чем проверяется:** чтение закона целиком.

### 2. Правило и паттерны

- **Что делается:** в `rules/task-flow.md` — как это исполняется здесь; в
  `patterns/task-flow-close.md` — готовые формулировки двух сообщений и порядок между ними; в
  `patterns/task-flow-resume.md` — что следующая задача берётся, пока прежняя ждёт разбора.
- **Признак готовности:** из паттерна видно, что именно сказать владельцу и когда.
- **Чем проверяется:** чтение паттерна целиком.

### 3. Чужая папка и своя

- **Что делается:** папка RT-682 разбирается по трём исходам, своя — тем же коммитом.
- **Признак готовности:** сверка очереди работ не называет ни одной папки.
- **Чем проверяется:** `npm run check:board`.

### 4. Раскладка и проверки

- **Что делается:** форматтер, `agent-kit:sync`, весь набор гейта.
- **Признак готовности:** раскладка сходится с пакетом, проверки зелёные.
- **Чем проверяется:** `agent-kit:check`, `check:docs`, сценарии гардов, юниты пакета.

## Чего эта работа не делает

- Не заводит проверку в конвейер: владелец выбрал сохранить порядок и закрыть окно словами, а
  не красным прогоном.
- Не трогает гард слияния на своей машине: он остаётся как был.
