# Progress

## Where we stand

Rewritten by every session, not appended to.

- **State:** `этапы-кончились`
- **Stage:** 3 of 3 — Каталог предложений отмечает разобранное
- **Done:** все три этапа закрыты. Привязка о базе PR в стопке называет `rt_epic_pull_base` вместо
  вердикта «не проверяется ничем»; статья о своде вписана в правило о проверках компонентов с
  привязкой, ради неё правило сжато с 24 451 до 23 947 знаков при пределе 24 000; три блока в
  каталоге предложений помечены — два применёнными, третий отбитым с причиной
- **Next step:** свериться полным набором проверок и отдать работу
- **Uncommitted:** правки двух правил
- **Waiting for the owner:** нет
- **PR:** not open yet

## Decisions along the way

- **Цитаты двух блоков в сегодняшних файлах не находятся.** Слой за пять дней перешёл на издание
  0.28.0, и статьи сжимались. Блоки сверены с сегодняшним текстом, а не применены вслепую.
- **Второй блок применён: повод держится.** Статья о своде показов говорит, что между «прогон
  прошёл» и «показанное показано» в дереве нет ничего, кроме свода. О том, что сам свод меряет
  площадь корня показа и зелен при обеих пустых половинах, не сказано нигде. Статья вписана, рядом
  встала привязка с вердиктом «не проверяется ничем» и причиной.
- **Ради второго блока сжаты доводы шести статей.** Правило стояло у предела длины, и добавленная
  статья его перешла. Сжимался довод, не утверждение: убраны повторы истории — «правка была
  написана для одного кадра из двух», перечень условий запуска, объяснение про растровые ореолы.
  Решение по каждой статье осталось прежним.
- **Проверка третьего этапа сухой отправкой перенесена.** План брал признаком число предложений в
  сухой отправке, а она в этой ветке по-прежнему останавливается на именах своих правил: починка
  лежит в задаче RT-2196 и в главную не влита. Отметки проверены разбором самого каталога —
  `parseProposals` читает обе применённые; третья стоит отбитой, и такую поле отправки не
  показывает по устройству. Блоки с адресом «дерево» наружу не уезжают вовсе, так что для отправки
  их отметки ничего не решают.
- **Третий блок не применяется: его повод в этой ветке отсутствует.** Обе статьи о ширине
  половины — та, что даёт ширину ячейке строки, и та, что спрашивает ширину у пары, — пришли
  коммитом `bcfaf20cc` от 13 сентября, и он лежит в ветке эпика `RT-1870-one-kit`, а в главную не
  влит. Спорить в этой ветке нечему: статей здесь нет ни одной. Блок ждёт слияния эпика «Один кит»
  и применяется там.

## Session entries

- **17 сентября 2026 года.** Третья задача эпика взята после того, как первые две ушли заявками
  #2198 и #2199, а заявка эпика RT-2161 — #2201 — вышла из черновика с зелёным запуском.
