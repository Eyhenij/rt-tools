# Plan

**Task:** RT-2202 · **Branch:** RT-2202-own-rules-pending-edits
**Epic:** RT-2195 · `docs/plans/tree-tells-truth-about-itself.md`
**Behaviour:** unchanged — правятся тексты правил этого дерева; приложений и их экранов работа не
касается

## Task footprint

| What  | Where                                                                                                                                       |
| ----- | ------------------------------------------------------------------------------------------------------------------------------------------- |
| Rules | `.claude/skills/git-workflow/implementation.md`, `.claude/skills/ui-component-tests/SKILL.md`, `.claude/skills/rt-tools-storybook/SKILL.md` |

## What counts as done

- Привязка о базе PR в стопке называет проверку, которая её судит, вместо вердикта «не
  проверяется ничем».
- Два других блока либо применены, либо закрыты с названной причиной — молчания о них не остаётся.
- Сверка описаний расхождений не называет.

## Stages

### 1. Привязка о базе называет свою проверку

- **What is done:** строка 69 привязок правила о сдаче работы получает адрес
  `git-guard-delivery-epic.sh:rt_epic_pull_base` вместо вердикта «не проверяется ничем»; текст
  говорит, что внутри эпика база судится, а вне его — нет.
- **Readiness sign:** в файле не остаётся вердикта «Not checked by anything» у этой статьи, и
  сверка описаний молчит.
- **Verified by:** `node tools/check-specs.mjs` — код возврата 0.

### 2. Два других блока сверены с сегодняшним текстом

- **What is done:** правила о проверках компонентов и о витрине читаются на предмет повода из
  блоков; повод держится — статья вписывается, отпал — блок закрывается причиной в записи хода.
- **Readiness sign:** про каждый из двух блоков в записи хода сказано, что с ним сделано и почему.
- **Verified by:** `grep -c 'wave\|обёртк' .claude/skills/ui-component-tests/SKILL.md` — число
  названо в записи хода вместе с решением.

### 3. Каталог предложений отмечает разобранное

- **What is done:** три блока с адресом «дерево» помечаются разобранными, чтобы следующая отправка
  не предлагала их заново.
- **Readiness sign:** сухая отправка не называет эти три блока среди уезжающих.
- **Verified by:** `node dist/agent-kit/bin/agent-kit.js propose --dry-run` — число предложений
  меньше одиннадцати.

## What this work does not do

- Пакетные блоки того же файла не трогаются: их адрес — приём, и уедут они отправкой.
- Устаревшие цитаты в пакетных блоках здесь не чинятся: это отдельная работа при отправке.
