# Ход работы

## Где стоим

- **Состояние:** `этапы-кончились`
- **Следующий шаг:** разобрать папку задачи и открыть PR черновиком

## Сессии

### 2026-09-11

- Ветка отведена от ветки эпика `RT-2028-cargo-intake`, задача переведена в работу.
- Запись груза сверена с кодом отправки: поле `sent` читается по наличию, значение ей безразлично.
- **Этап 1 сделан.** Отбор груза разделён надвое; пропущенные как отправленные печатаются строкой со
  счётом и перечнем «файл:строка — пометка», и ноль печатается тоже. Оба прогона — сухой и
  настоящий — говорят одинаково.
- **Этап 2 сделан.** Форма пометки объявлена один раз рядом с её читателем: пишет её простановка,
  читает вывод о пропущенном. Пометка не своей формы названа поставленной рукой.
- **Этап 3 сделан.** Сценарий SC-AK-1090 записан, пакет собран и разложен.
  `pnpm exec nx test @rt-tools/agent-kit` — 28 наборов, 442 пробы, провалов нет.
  `npm run check:specs` — 6 областей, 1547 сценариев, расхождений нет.
  `pnpm run agent-kit:check` — разложенное сходится с пакетом v0.27.0.
  `node tools/check-file-size.mjs` — длиннее предела 0.
- Отправка перестала помещаться в предел длины: `markProposals` и вывод о пропущенном переехали к
  разбору блоков, где живёт само поле пометки. Пробы о пропущенном лежат своей спекой рядом.
