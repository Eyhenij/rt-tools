# Ход работы

## Где стоим

- **Этап:** все три закрыты — осталось запушить и открыть PR
- **Сделано:** каталог чужого пакета ищется разрешением модуля от подпроекта, который его
  объявил; отсутствие пакета проверку не роняет; накопленные 26 повторов записаны долгом;
  проверка встала первой строкой в набор гейта пуша, и отказ подтверждён прогоном гарда
- **Следующий шаг:** пуш, открытие PR черновиком, разбор папки задачи последним коммитом
- **Незакоммиченное:** нет
- **Ждём владельца:** нет
- **PR:** ещё не открыт

## Решения по ходу

- **Слитая задача закрыта, а не стёрта.** Правило велит стирать вторую задачу вместе с номером,
  но стирание необратимо, а закрытие с пометкой дубля даёт то же и откатывается. Недостающее из
  тела #693 дописано в #681 комментарием. Затронутый этап замысла: 1.

## Заходы

### 2026-08-16

- Замер на вершине главной ветки: `node tools/check-dupes.mjs` кончается отказом чтения каталога
  и до сверки не доходит.
- Разведка раскладки: пакет объявлен зависимостью `projects/ui-kit` и `projects/core`, в корневом
  `node_modules` его нет вовсе, а в хранилище лежат две версии сразу — `0.3.1` и `0.3.2`.
- Этап 1: зашитый путь снят, внешние пакеты объявляет дерево ключом `externalEnums`; каталог
  ищется разрешением модуля от корня и от каждого подпроекта, объявившего пакет. Проверка дошла
  до сверки впервые: 26 расхождений там, где раньше был отказ чтения каталога.
- Этап 2: отсутствие пакета проверку не роняет. Сценарии `SC-AK-244` и `SC-AK-245` в наборе
  пакета — четыре утверждения, все зелёные; в наборе проверок стало 109.
- Накопленные 26 повторов сняты базовым списком: гейт падает на новом повторе, старое остаётся
  видимым числом в сводке. Разбор этого долга — отдельная работа, и её объём заранее неизвестен.
- Этап 3: строка в наборе гейта пуша. Приёмка: одна запись убрана из списка долга — гард отбил
  пуш и назвал упавшую команду; список возвращён, проверка зелёная.
