# Ход работы

## Где стоим

- **Этап:** 5 из 8 — гард хранилища и набор сценариев проверок делятся по разделам
- **Сделано:** этапы 1, 2, 3 и 4 закрыты — оба спека поделены на поддомены, сверка спеков и
  сверка раскладки разошлись по модулям; долг в перечне длины упал с одиннадцати строк до четырёх
- **Следующий шаг:** поделить `projects/agent-kit/assets/hooks/sql-guard.sh` по его разделам —
  нормализация, разбор команды, прод, разрушительное, адресация — и
  `projects/agent-kit/tests/checks.test.sh` по проверкам, которые он гоняет
- **Незакоммиченное:** нет
- **Ждём владельца:** нет
- **PR:** ещё не открыт

## Решения по ходу

- **Числа одиннадцати файлов пересняты до первой правки** — разведка предыдущего захода снята на
  вершине главной ветки, и они сошлись. Затронутый этап замысла: нет, доэтапное.
- **Поддоменов шесть, а не пять.** Первая раскладка сложила гарды и проверки дерева в один
  поддомен, и его сценарии дали 703 строки — сам поддомен вышел бы длиннее предела. Гарды хода
  агента и проверки дерева разошлись по двум поддоменам: у первых предмет — событие агента, у
  вторых — состояние дерева. Затронутый этап замысла: 1.
- **Открытые вопросы остались в корневом спеке целиком**, как в образце RT-750; поддомены на них
  ссылаются строкой. Решения, наоборот, разошлись по поддоменам: их 216 строк, и корневой спек с
  ними не влез бы в предел. Затронутый этап замысла: 1.
- **Номера открытых вопросов оказались продублированы вливаниями** — с первого по четырнадцатый
  заводились дважды, каждая договорённость нумеровала свои заново. Пересчёт не делается: на номер
  ссылаются задачи и коммиты. Заведён `Q-24` о том, что с этим делать. Затронутый этап замысла: 1.
- **Помощник сверки назван `spec-*`, а не `check-*`.** Умолчание пакета собирает набор гейта
  пуша перебором имён `check-<что>.mjs` в каталоге проверок: помощник с таким именем гейт стал бы
  гонять как отдельную проверку, и она отказала бы на первом же вызове. Затронутый этап
  замысла: 3.
- **Стенд набора сценариев везёт модули рядом со сверкой.** Он копирует проверку в свой
  временный репозиторий, и без соседних модулей она не поднимается вовсе — восемь сценариев
  разом отдали ноль совпадений вместо ожидаемых. Заодно фикстуры привязок переведены с
  `sectionOf` на символ, оставшийся в самой сверке. Затронутый этап замысла: 3.
- **Строка «Нет.» посреди открытых вопросов убрана.** Осталась от вливания договорённости, у
  которой открытых вопросов не было, и ни к одному номеру не относилась. Затронутый этап
  замысла: 1.

- **Красное от этапа 3 починено правкой, а не строкой в списке принятого.** Линтер пакета
  отбивал три неиспользуемых импорта, оставшихся в сверке спеков и её помощнике после деления:
  прогон `pnpm run lint` этап 3 не гонял, и промах вылез на этапе 4. Чинится в этой же ветке —
  слово владельца. Затронутый этап замысла: 3.
- **Модули сверки раскладки названы `lib-*`, а не `check-*`** — по тому же основанию, что и
  помощники сверки спеков: перебором имён `check-<что>.mjs` умолчание пакета собирает набор
  гейта пуша. Затронутый этап замысла: 4.

## Заходы

### 17 августа 2026

- Передача предыдущего захода прочитана, эпик RT-806 и его находки подняты, задача RT-807
  прочитана из очереди работ.
- Числа одиннадцати файлов пересняты: сходятся с разведкой, счёт проверки на строку меньше
  `wc -l`.
- Шесть обязательных вопросов заданы, ответы владельца записаны дословно. Владелец выбрал одну
  ветку на все одиннадцать файлов — рекомендованное деление на четыре отвергнуто.
- Ветка `RT-807-split-oversized-files` заведена, задача переведена в колонку работы, папка задачи
  собрана с образца и заполнена.
- **Этап 1.** Образец прочитан целиком обходом каталога — `docs/specs/message-bus/`, пятнадцать
  файлов: корневой спек держит общее, при нём четыре поддомена тройками, корневые сценарии и
  компаньон — указатели.
- Спек пакета правил поделён на шесть поддоменов: раскладка, тексты, гарды, проверки, ведение
  работы, наблюдения. Переехали 218 правил, 267 сценариев и 218 привязок — раскладку считал
  скрипт, и он же показал, что ни одна привязка не осталась без правила и ни одно правило без
  привязки.
- Длиннее предела не осталось ни одного файла спека: самый длинный поддомен — 428 строк,
  корневой спек — 236.
- Зелёное: `npm run check:specs` (435 сценариев, покрыто 353 — как до деления),
  `node tools/check-doc-paths.mjs` (306 документов), `node tools/check-dupes.mjs` (26 повторов,
  новых нет), `node tools/check-file-size.mjs` (долг 9, новых нет).
- **Этап 2.** Спек второго кита поделён на три поддомена — снимки витрины, уход с route-панели,
  оформление: границы взяты те, что уже были проведены тремя договорённостями, из которых спек
  сложился. Переехали 62 правила, 52 сценария и 62 привязки, раскладку считал тот же приём.
- Самый длинный файл второго кита теперь 330 строк, корневой спек — 165. Долг перечня длины упал
  до восьми строк.
- Зелёное после этапа 2: `npm run check:specs` (те же 435 сценариев и 353 покрытых),
  `node tools/check-doc-paths.mjs` (315 документов), `node tools/check-dupes.mjs` (26, новых
  нет), `node tools/check-file-size.mjs` (долг 8, новых нет).
- **Этап 3.** Сверка спеков поделена по тем границам, что файл провёл сам: пять механизмов
  разошлись по четырём модулям рядом — `spec-common`, `spec-anchors`, `spec-contract`,
  `spec-scenarios`, — в самой сверке остался прогон. Самый длинный кусок 447 строк.
- Копии в `tools/` приехали раскладкой: правился только исходник в пакете.
- Зелёное после этапа 3: `npm run check:specs`, `pnpm run agent-kit:check` (разложенное сходится
  с пакетом), `bash projects/agent-kit/tests/checks.test.sh` (122 сценария, 0 провалов),
  `npm run check:hooks` (10 сценариев), `node tools/check-file-size.mjs` (долг 6, новых нет),
  `node tools/check-doc-paths.mjs`, `node tools/check-dupes.mjs`.
- **Этап 4.** Сверка раскладки поделена по предметам сверки: доменная сетка — `lib-domains`,
  манифест либы с её тегом и алиасом — `lib-manifests`, границы и списки зависимостей —
  `lib-boundaries`, барели и реэкспорты — `lib-reexports`, общее чтение дерева — `lib-common`;
  в самой сверке остался прогон в 62 строки. Самый длинный кусок — 205 строк.
- Приёмка шла на внесённых нарушениях, а не на зелёном прогоне: пустой каталог домена, реэкспорт
  чужого символа, чужой тег в `project.json`, снятое описание тега в границах — каждое из
  четырёх покраснело своей строкой, и вывод зелёного прогона тот же, что до деления.
- Копии в `tools/` приехали раскладкой; строки обеих копий сверки убраны из перечня длины.
- Зелёное после этапа 4: `node tools/check-lib-layers.mjs`, `npm run check:specs` (те же 435
  сценариев и 353 покрытых), `pnpm run agent-kit:check`, `bash projects/agent-kit/tests/checks.test.sh`
  (127 сценариев, 0 провалов), `npm run check:hooks` (10 сценариев), `pnpm run lint` (79
  проектов), `node tools/check-file-size.mjs` (долг 4, новых нет), `node tools/check-doc-paths.mjs`,
  `node tools/check-dupes.mjs`.
