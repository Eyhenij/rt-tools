# Замысел

**Task:** RT-1858 · **Branch:** RT-1858-tree-docs-english
**Behaviour:** unchanged — владелец: «Переводить всё, как задумано»; код приложения не правится

После написания этот файл не правится. Пересмотр этапа идёт в `progress.md` решением по ходу.

## След задачи

| Что            | Где                                  |
| -------------- | ------------------------------------ |
| Спеки          | `docs/specs/`                        |
| Замыслы        | `docs/plans/`                        |
| Решения        | `docs/adr/`                          |
| Ключи разделов | `projects/agent-kit/assets/checks/`  |
| Образец домена | `docs/specs/_template`               |
| Замысел эпика  | `docs/plans/rules-layer-language.md` |

## Что считается сделанным

- Ключи разделов спека читаются под двумя именами: спеки, заведённые до перевода, работают без
  правки, и `npm run check:specs` сходится на смеси английских и русских заголовков.
- Двести три спека, двадцать пять замыслов и одно решение написаны по-английски; компаньоны
  правлены той же правкой, что и цитируемые статьи.
- Описание прошлого и папки задач остаются русскими: там слова владельца дословно.
- `npm run check:specs`, `npm run check:docs` и `pnpm run check:all` зелены; замер записан
  девятым разделом в замысел эпика.

## Этапы

### 1. Ключи разделов спека под двумя именами

- **Что делается:** `spec-common.mjs`, `spec-anchors.mjs`, `spec-scenarios.mjs`,
  `spec-contract.mjs` и `check-specs.mjs` учатся английским именам разделов, отметок и вердиктов
  наравне с русскими; образец домена переводится.
- **Признак готовности:** проверка спеков сходится и на русском спеке, и на английском.
- **Чем проверяется:** `bash projects/agent-kit/tests/run.sh` — набор спеков зелёный;
  `npm run check:specs` — расхождений нет.

### 2. Спеки пакета правил

- **Что делается:** сто пять файлов `docs/specs/agent-kit/` вместе с компаньонами.
- **Признак готовности:** кириллицы в домене нет, кроме цитат чужих строк.
- **Чем проверяется:** `npm run check:specs`; `node tools/check-doc-paths.mjs`.

### 3. Спеки приёмника

- **Что делается:** тридцать девять файлов `docs/specs/message-bus/` вместе с компаньонами.
- **Признак готовности:** тот же, что у этапа 2.
- **Чем проверяется:** `npm run check:specs`; `node tools/check-doc-paths.mjs`.

### 4. Спеки китов, ядра и хранилища

- **Что делается:** пятьдесят семь файлов `docs/specs/ui-kit-v2`, `ui-kit`, `core`, `store`.
- **Признак готовности:** тот же, что у этапа 2.
- **Чем проверяется:** `npm run check:specs`; `node tools/check-doc-paths.mjs`.

### 5. Замыслы и решения

- **Что делается:** двадцать пять замыслов `docs/plans/` и решение `docs/adr/`.
- **Признак готовности:** кириллицы в замыслах нет, кроме цитат слов владельца.
- **Чем проверяется:** `node tools/check-doc-paths.mjs`; `node tools/check-prose-style.mjs`.

### 6. Замер и разбор

- **Что делается:** замер записывается в замысел эпика; исключения перечисляются поимённо; эпик
  закрывается словом владельцу.
- **Признак готовности:** гейт пуша зелёный.
- **Чем проверяется:** `pnpm run check:all` — зелено.

## Чего эта работа не делает

- Описание прошлого `docs/archive/` и папки задач `docs/tasks/` — там слова владельца дословно.
- Русский текст в задачах, заявках, коммитах и ответах владельцу.
