# Ход работы

## Где стоим

- **Этап:** 8 из 8 — договорённость влита, тексты приведены, папка задачи разбирается
- **Сделано:** законы и закон о наблюдаемости (этап 1); правила работы, текстов, спеков и
  проверок с паттерном `doc-style-trace` (этап 2); поставка — девять статей и четыре ловушки в
  каждый из трёх видов, паттерны `git-workflow-docker` и `git-workflow-secrets` (этап 3);
  оформление, списки, права, цены, единообразие, переводы, классы Angular, компонент, язык и
  общий код (этап 4); наблюдаемость, сущность захода, переводы содержимого, шторка и
  оповещение поисковика (этап 5); слой гейта `skill-gate-layers.sh`, проверка `check-file-size`,
  сценарии SC-AK-99…108, карта гейта на вызовах и набор гейта пуша (этап 7). Отставание
  223 → 105 дословных совпадений, и остаток разобран решением по ходу
- **Следующий шаг:** отчёт открывается после разбора папки
- **Незакоммиченное:** нет
- **Ждём владельца:** решения о меню на разборе — ниже, в решениях по ходу
- **Отчёт:** не открыт

## Решения по ходу

- **Три вопроса владельцу не задавались: ответы нашлись в правилах.** Гард разговора отбил меню
  до отправки и потребовал поиска по теме. Поиск дал границу переносимого (спек домена пакета,
  терминология), место предметного слоя (там же, «Что не входит») и объём прошлого переноса
  (запись RT-362 в архиве). Затронутый этап замысла: до этапа 1.
- **Договорённость написана раньше своего этапа.** Замысел называл её директорию, и гард хода
  работы отбил первую же правку закона: путь в шапке замысла обязан существовать. Этап 6
  исполнен вперёд этапа 1; порядок остальных этапов не менялся. Затронутый этап замысла: 6.
- **Две редакции разошлись о меню на разборе, и это к владельцу.** Дерево-источник утверждает,
  что меню годится для любого вопроса, потому что строка ответа своими словами есть всегда;
  пакет говорит обратное — «выбор слова, имени и термина узким вопросом не является никогда», и
  за этим стоит его собственный случай с ложной посылкой. Статья не перенесена: правка правила
  здесь меняет договорённость с владельцем, а не приём. Затронутый этап замысла: 2.
- **Отставание меряется сравнением утверждений, а не датами.** Скрипт сравнения вынимает жирные
  зачины пунктов из законов и правил дерева-источника и ищет их дословно в текстах пакета: 223
  утверждения без пары, 43 ресурса. Дата не годится — часть найденного по дате уже перенесена
  прошлой работой.
- **Сравнение считает дословные совпадения, и остаток по этапу 2 — не отставание.** Зачин, из
  которого убрана предметность источника, перестаёт находиться дословно, поэтому по закрытому
  этапу в выводе остаются восемнадцать строк, и семнадцать из них пакет несёт своими словами:
  сводная цель вместо имени команды, сводка покрытия вместо имени сверки, порты стенда вместо
  их числа, признак служебного посещения вместо имени параметра, слияние вместо мержа, второй
  порог вместо доли окна, указатель доменов вместо пути, подстановка порта вместо номера.
  Восемнадцатая — статья о меню на разборе, и она ждёт владельца. Затронутый этап замысла: 2.

- **Отставание гарда меряется его поведением, а не совпадением строк.** Сравнение утверждений
  по гардам не работает: там сверяется, что гард делает, а не что о нём написано. Из пяти
  названных замыслом отстали двое. Гард документов, гард разговора и гард второго сервера пакет
  несёт впереди дерева-источника: пары документов у него называет профиль дерева, вопрос
  владельцу судится на двух событиях вместо одного, а порты стенда вынесены в настройку.
  Затронутый этап замысла: 7.
- **Признак вызова живёт в карте гейта, а не в его оболочке.** Карта судит текст команды, и
  разбор границ вызова принадлежит тому, кто по этому тексту решает. Разбор в оболочке гейта
  расходился бы с картой дерева молча: дерево объявляет свою `skill_for` и о чужих границах не
  знает. Затронутый этап замысла: 7.
- **Гейт пуша зовёт то, что лежит на диске, а не то, что пакет везёт.** Список печатается по
  наличию файла: отказ «нет такой проверки» читается как поломка машины, а не как правило.
  Затронутый этап замысла: 7.
- **Скрипт сравнения написан заново, и его счёт — 105, а не 104.** Прежний пропал вместе с
  заходом, который его писал. Метод тот же — жирные зачины пунктов ищутся в текстах пакета
  дословно, — а расхождение в одну строку стоит того, чтобы мерило снова было в руках.
  Затронутый этап замысла: 7.
- **Слой законов приложения дерева-источника в счёт отставания не идёт.** Организации, роли,
  платформа, доступ, деньги и мультиобъектность — 103 утверждения — это законы, верные только
  для того приложения; предметность в них законна, потому что она их предмет. Пакет везёт свой
  слой законов приложения и берёт из чужого только то, что верно любому дереву класса.
  Затронутый этап замысла: 1.
- **Остаток сравнения в 105 строк отставанием не является, и вот почему.** Восемьдесят семь
  строк — предметность дерева-источника: его объекты, брони, цены в местной валюте, локали,
  приёмники оповещения поисковика, имена его компонентов и портов. Восемнадцать пакет несёт
  своими словами, и дословно они не находятся: хранилище вместо базы, все линтеры вместо обоих,
  предел длины вместо числа 500, поиск по дереву вместо имени команды, сводная цель вместо
  имени скрипта. Проверено поимённо чтением текстов пакета. Одна статья — меню на разборе —
  ждёт владельца и записана выше. Затронутый этап замысла: 8.

## Заходы

### 2026-08-13

- Дерево-источник подтянуто до вершины его главной ветки токеном владельца: помощник связки
  ключей отдаёт машинную запись, у которой туда доступа нет.
- Разведка: пакет в том дереве не установлен вовсе — оно источник подходов, а не потребитель;
  сравнение по утверждениям дало 223 строки и двенадцать ресурсов, которых в пакете нет.
- Заведены задача #419, ветка, папка задачи; замысел разложен на восемь этапов.
- Этап 2 закрыт: четыре статьи о проверках, три о стенде и признаке служебного захода, шесть о
  закрытии работы, возвращении и устройстве спеков; заведён паттерн `doc-style-trace` —
  обратный проход по закрытым задачам против текстов.
- Остаток вывода сравнения по этому этапу разобран поимённо: отставанием он не является,
  разбор записан решением по ходу.
- Этап 3 закрыт: девять статей поставки и четыре ловушки легли в три вида правила сразу, каждая
  словами своего хостинга — колонка борды, список доски и состояние рабочего элемента; ручной
  запуск, событие отчёта и признак невалидного файла конвейера у каждого свои.
- Этап 4 закрыт: оформление получило слои, указатель, длину файла и четыре ловушки о том, чего
  не видит ни сборка, ни линт; права — роль во владении, чтение прав при каждом вызове,
  ограничитель частоты и согласие гостя; списки — общую страницу, общую основу экрана и
  сортировку по полю сервера; переводы, единообразие, общий код, классы Angular, компонент и
  язык — по одной-две статьи.
- Этап 5 закрыт. Правило наблюдаемости — двадцать шесть статей — и паттерн строки лога встали
  под закон, заведённый первым этапом. Заведены правило `ownership-session` с паттерном
  процедуры, паттерн `translations-content` и паттерн `styling-bem-sheet`; правило `seo`
  получило пятнадцать статей об оповещении поисковика и блоках разметки.
- **Охват объекта отдельным ресурсом не заводится.** Правило `ownership-scope` пакета — это он
  и есть: все шесть его статей совпали дословно по смыслу, а разошёлся один пункт — у источника
  переключатель рисует экран, у пакета стоял в шапке. Дописан пункт, называющий оба случая.
  Затронутый этап замысла: 5.
- **Сущность захода — своё правило, а не дописка к охвату.** Охват разрешает идентификатор,
  названный в запросе; сущность захода отвечает, в какой сущности работает вошедший, и держит
  границу в выборках. Признак деления из паттерна `spec-driven-rule` сработал: у двух половин
  разные поводы взять правило в руки. Затронутый этап замысла: 5.
- Набор локалей дерева-источника вычищен из четырёх мест пакета, а имена его домена — из кода
  нового паттерна: и то и другое читалось как обязательное для любого дерева.
- Этап 7 сделан наполовину. Слои гейта вынесены отдельным файлом и зовутся из гейта в его же
  оболочке; карта гейта теперь судит только путь — признак по тексту правки уехал в слои вместе
  со своими сценариями. Заведена проверка длины файла с разделением принятого и долга.
  Сценарии договорённости SC-AK-99…108 покрыты, пометки «Не покрыто» сняты.
- **Признак по тексту правки жил в карте гейта и переехал в слои.** Договорённость называет
  слои отдельным файлом, а карта несла два таких признака сама — и снять их деревом было нечем.
  Перенос задел два сценария наборов: карта теперь судит путь, слой судит текст. Затронутый
  этап замысла: 7.
- Приставка дерева-источника вычищена из шести мест пакета: она приехала прошлым переносом и
  читалась как имя, обязательное для любого дерева.
- Заведены два паттерна поставки: `git-workflow-docker` — демон, реестр из службы, сборка под
  платформу сервера; `git-workflow-secrets` — ключи внешних служб, пять состояний строки
  интеграции и обещание зелёной пробы.

### 2026-08-13, второй заход

- Скрипт сравнения написан заново; отставание переносимого — 105 утверждений, и все они
  разобраны решением по ходу.
- Пять гардов сверены с дерева-источника по поведению: отстали гейт правил и гейт пуша.
- Этап 7 закрыт. Карта гейта судит вызов, а не упоминание, и знает то, чего не знала:
  заведение ветки и задачи, открытие и правку отчёта, работу с образами, схему хранилища,
  конвейер, состав образа, свои проверки дерева и сквозные спеки. Слияние отчёта требует двух
  правил. Проверка через браузер требует правило под инструмент. Гейт пуша зовёт сборку,
  сценарии гардов и разложенные проверки; отказ гейта правил называет запасной ход.
- Наборы: `bash projects/agent-kit/tests/run.sh` — тринадцать наборов, ноль провалов; умолчания
  67 сценариев, гейт правил 52. Проверено, что новые сценарии краснеют на прежней редакции
  карты: браузер и проверки дерева не давали правила вовсе, упоминание команды считалось
  вызовом, слияние отчёта тянуло одно правило из двух.
- Этап 8. Договорённость `gate-layers` влита в спек домена: четырнадцать правил, сценарии
  SC-AK-99…113, привязки к коду; директория «предложено» снята. Из правил вычищена адресность
  дерева-источника: порты и путь прокси в правиле проверки через браузер, имена переменных
  входа и счёт либ в правиле проверяемости.
- `check:docs` — 167 документов, расхождений нет. `check:specs` — 139 расхождений, все на
  привязках разложенных правил к спутникам этого дерева; на домен пакета сверка молчит.
  Доэтапное, не этой работой сделанное: число то же и до вливания.
- Главная ветка влита в ветку задачи без конфликтов. С ней приехали папки закрытых задач
  RT-272, RT-386 и RT-393 — чужой хвост, на котором краснеет `check:board`; в эту работу он не
  входит.
