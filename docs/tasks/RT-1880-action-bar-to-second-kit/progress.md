# Progress

## Where we stand

Rewritten by every session, not appended to.

- **State:** `этап-идёт`
- **Stage:** 3 of 5 — Пробы семейства
- **Done:** этап 1 закрыт — у семейства есть описание домена. Этап 2 закрыт — полоса, держатель,
  договор и служба написаны на готовых частях второго кита, `@angular/material` в них нет ни
  строкой.
- **Next step:** написать пробы на каждый сценарий описания и снять пометки «ещё не закрыт».
- **Uncommitted:** нет.
- **Waiting for the owner:** нет.
- **PR:** not open yet

## Decisions along the way

- **Номера сценариев начинаются с SC-UKV-199, а не со 181** — 181…198 выданы работой RT-1879,
  которая стоит в PR #2224 и в ветку эпика не влита. На этой ветке их не видно, и взять их второй
  раз значило бы оставить два сценария под одним номером после слияния. Affected stage of the
  plan: 1.
- **Подпись действия не урезается, а ряд действий переносится** — измерено при разборе кнопки
  кита: подпись рисует она сама, внутри себя, директивой через `Renderer2`. Резать её из полосы
  значило бы писать правило по внутренностям чужого семейства, а дополнять готовое там, где оно
  живёт, — работа своя, не эта. Обещание усечения из договорённости убрано, вместо него — предел
  ширины и перенос ряда; SC-UKV-211 вычеркнут, и номер за ним остаётся пустым. Affected stage of
  the plan: 1, 2, 4.
- **Признак значка стоит модификатором, а подпись снимает правило стиля** — сперва было написано
  условие в шаблоне, как в первом ките. Правило оформления называет это прямо: условие в шаблоне
  ради других отступов есть оформление, уехавшее в код. Сценарии SC-UKV-207 и SC-UKV-208
  переписаны на модификатор — его проба и увидит, а раскладки у неё нет всё равно. Affected stage
  of the plan: 1, 2, 3.
- **В словаре кита заведён ключ счёта с обоими числами** — `uiSelectedCount` говорит только
  «Выбрано: N», а полоса первого кита показывала «N из M» склейкой английских слов прямо в
  шаблоне. Заведён `uiSelectedOf`; русский набор витрины дополнен той же правкой — ключ, забытый
  в нём, рисует пустую строку. Affected stage of the plan: 2.
- **Сценарии на этапе 1 стоят с пометкой «ещё не закрыт», а не с именем теста** — договорённость
  идёт до кода, и тестов на этапе 1 нет ни одного: семейства самого ещё нет. Аудит описаний без
  пометки красный, и признак готовности этапа, как он записан в замысле, выполняется именно ею.
  Каждая пометка уходит вместе с тестом, который сценарий закроет, — этапом 3. Один сценарий, о
  слое каскада, останется с пометкой навсегда: тесту там смотреть не на что, его закрывает
  проверка слоя. Affected stage of the plan: 1, 3.

## Sessions

### 2026-09-17

- Отправные точки сняты: `npm run check:specs` — код выхода 0; аудит входов кита — 76
  страниц-обзоров без расхождений; счёт семейств витрины — 124 компонента и директивы, 94 предмет
  истории, 4 показаны внутри соседней, 26 названы причиной; кадры второго кита 589 из 589.
- **Пары у полосы во втором ките нет, и это уже записано в дереве:** страница-обзор `rt-toolbar`
  прямо говорит, что полоса массовых действий переезжает задачей RT-1880. Сам `rt-toolbar` о
  другом — три слота над содержимым, своих входов у него нет вовсе. Так что здесь, в отличие от
  RT-1879, работа и правда на перенос, а не на дополнение готового.
- **Всё, из чего полоса собирается, во втором ките уже есть:** всплывающий слой, меню с пунктами,
  кнопка, кнопка со значком, значок, словарь подписей. Своей разметки для них писать не надо.
- **Этап 1 сделан.** Заведено `docs/specs/ui-kit-v2/action-bar/`: четырнадцать статей на то, что
  семейство обещает, четырнадцать сценариев с SC-UKV-199 и таблица привязок. Все привязки стоят с
  приговором «не исполняется» и называют этап, которым придут: договорённость идёт до кода.
  Две вещи взяты из первого кита не как есть, а решением: произвольные стили кнопки действия не
  переносятся вовсе, а подпись счётчика перестаёт склеиваться из английских слов прямо в шаблоне —
  её даёт словарь кита.
  Статья о пределе ширины сразу разделена надвое: одним предложением она выходила за предел длины
  в сорок слов, и проверка слога её отбила.
  Проверено: `npm run check:specs` — код выхода 0; предел длины файлов и аудит адресов расхождений
  не называют.
- **Этап 2 сделан.** Написаны полоса, держатель, договор и служба. Ничего своего в разметке нет:
  значок, кнопка со значком, всплывающий слой и пункты меню взяты готовыми, подписи — из словаря
  кита. Держатель прикалывает полосу над страницей и держит её в разметке ровно на длительность
  ухода — число одно на разметку и правило стиля и лежит в договоре.
  Обводка фокуса сперва была написана своей парой свойств и отбита проверкой токенов: у кита есть
  готовая тень фокуса, ею и взято.
  Проверено: типы второго кита сходятся, линтер и линтер стилей чисты, проверка токенов и проверка
  слоя каскада расхождений не называют, сборка пакета зелёная, прежние пробы кита зелёные.
  `@angular/material` в каталоге семейства — ноль упоминаний.
