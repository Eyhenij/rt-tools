<!-- rt-kit v0.10.0 · samples/tasks/_template/progress.md · fc2dd20e34b0 · правится надстройкой, не здесь -->

# Ход работы

## Где стоим

Перезаписывается каждым заходом, а не дописывается.

- **Состояние:** `этап-идёт`
- **Этап:** 5 из 6 — засев стенда и сквозная спека
- **Сделано:** этапы 1–4 закрыты и закоммичены: разборщик разметки, компонент кита, шесть
  историй с эталонами, три вида груза в админке
- **Следующий шаг:** править случай с разметкой в засеве стенда под новое решение и завести
  сквозную спеку показа с номерами `SC-MB-208`…`SC-MB-221`, потом переснять кадры панели
- **Незакоммиченное:** нет
- **Ждём владельца:** нет
- **PR:** ещё не открыт

## Решения по ходу

- **Показ разметкой ставится и сводке месяца, не только разборам и предложениям** — замысел
  эпика называл два рода груза, но текст сводки приезжает такой же разметкой и показан тем же
  блоком; отдельная задача на одну строку шаблона стоила бы дороже правки. Слово владельца.
  Затронутый этап замысла: 4.
- **Компонент и разборщик живут в ките, а не в общем слое админки** — слово владельца. Цена:
  задача трогает пакет, у которого своя витрина и свои снимки. Затронутые этапы замысла: 1, 2, 3.
- **Уровень заголовка и вложенность пункта приходят в разметку строкой, а не числом** — не
  спрашивалось. Директива модификатора сводит нестроковое значение к признаку и даёт класс без
  значения: все уровни получали один и тот же класс. Отсюда поле `levelName` в узле разбора.
  Затронутый этап замысла: 2.
- **Шаблон кусков строки пишется без переносов, форматтер на нём выключен** — не спрашивалось.
  Перенос строки в разметке становится пробелом, и размеченный кусок отъезжал от соседнего
  знака: «жирно , а рядом». Затронутый этап замысла: 2.
- **Правило оформления текста ушло из общего слоя панели админки** — не спрашивалось. Класса,
  которому оно отвечало, в разметке больше нет: текст рисует компонент кита, и оформление
  приходит оттуда. Затронутый этап замысла: 4.

## Заходы

### 2026-08-21

- Разведка командами по дереву: три вида груза, спек админки с отменяемым правилом, засев
  стенда с готовым случаем разметки, зависимости — библиотеки разбора в рантайме нет.
- Разбор просьбы с владельцем: одиннадцать вопросов, все ответы легли в `grill.md`.
- Записана договорённость `docs/specs/message-bus/proposed/cargo-text-markup/` — спек, 14
  сценариев с `SC-MB-208` по `SC-MB-221`, привязка. Номера выбраны с оглядкой на невлитые
  ветки: наибольший занятый — `SC-MB-207` в ветке RT-911.
- Этап 1: разборщик разметки в слое `util` кита — модель узлов, разбор строки, разбор блоков;
  25 тестов спеки зелёные.
- Этап 2: компонент `rt-markdown-text` — шаблон, оформление, объявление наружу, спутник;
  18 тестов спеки зелёные, линт и сборка пакета зелёные.
- Этап 3: шесть историй на витрине и страница обзора; обход всех 450 историй пустых показов не
  нашёл, 6 эталонов сняты после осмотра кадров глазами.
- Этап 4: три вида груза перешли на компонент кита, спеки трёх видов зелёные (7 + 7 + 7),
  сборка админки зелёная.
- Осмотр витрины нашёл три дефекта, которых спека не видела: модификатор со значением, пробел
  вокруг размеченного куска и блок кода, растягивающий хозяина. Все три починены и закрыты
  спекой либо замером.
- **Чужое красное:** `pnpm run check:tokens-graph` падает на `--rt-space-2-5` в
  `projects/ui-kit-v2/src/showcase/templates/styles/_profile-menu.scss`. Расхождение лежит в
  главной ветке и этой работой не заведено; задачи на него нет.
