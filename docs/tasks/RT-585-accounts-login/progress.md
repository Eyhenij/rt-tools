# Ход работы

## Где стоим

Перезаписывается каждым заходом, а не дописывается.

- **Этап:** 5 из 7 — операции чтения груза; этапы 1–4 закрыты
- **Сделано:** хранилище (учётная запись и вход, миграция накатана), четыре команды учётных
  записей, операции входа с кукой и сроком, объявление доступа на каждой операции и единственная
  проверка вместо прежней проверки токена
- **Следующий шаг:** этап 5 — операции чтения груза: `GET /api/trees`, `/api/postmortems`,
  `/api/proposals`, `/api/summaries` со страницей, размером, порядком и отбором по дереву
- **Незакоммиченное:** нет
- **Ждём владельца:** нет
- **Отчёт:** ещё не открыт

## Решения по ходу

- **Договорённость покрывает и вход, и админку, а задачи под неё две.** Работа делится по тому,
  что откатывается порознь, а описание продукта — по предмету: вход без экранов и экраны без
  входа — половина одного и того же обещания. Затронутый этап замысла: 7 — вливается она один
  раз, последним коммитом отчёта задачи #587.
- **Три расхождения ролей со словом владельца вынесены ему и решены им.** Роли урезали работу до
  одного списочного экрана, трёх законов и отказа от сквозного набора; владелец вернул три
  экрана, пять законов и сквозной набор. Затронутый этап замысла: нет — решение принято до
  написания замысла.
- **Срок входа взят двенадцатью часами допущением.** Число называет владелец, вопрос `Q-21`
  открыт; меняется настройкой `SESSION_TTL_MS` и работу не держит. Затронутый этап замысла: 3.
- **Объявление доступа вынесено отдельным доменом `access`, а прежняя проверка токена снята.**
  Читают объявление обе стороны — и проверка токена дерева, и проверка входа; положенное к одной
  из них, оно сделало бы вторую её потребителем. Две глобальные проверки подряд означали бы, что
  запрос с токеном доходит до чтения груза, если вторая забыла отказать. Затронутый этап
  замысла: 4 — он вырос с «гард входа рядом» до «единственная проверка».
- **Куку разбирает своя чистая функция, а не пакет-разборщик.** Приёмнику нужна ровно одна кука,
  а новая зависимость — это ещё пакет в образе и ещё строка, которую поднимать при каждом
  обновлении. Затронутый этап замысла: 3.
- **Эхо ввода пароля не гасится.** Терминал контейнера отдаётся не всегда, а команда, молча не
  принимающая ввод, выглядит зависшей; пароль виден в окне того, кто его вводит, и больше нигде.
  Затронутый этап замысла: 2.
- **Признак единообразия `foreign-runner` сужен до `projects/`.** Под `libs/` прогонщик Vitest,
  и признак отбивал у приёмника ровно тот импорт, которого требует его собственный паттерн спек.
  Затронутый этап замысла: нет — правка настройки дерева.

## Долги этапов, закрытых не до конца

- **Неудачные попытки входа подряд ответ пока не удлиняют.** Правило в договорённости есть,
  кода нет: этап 3 закрыт без него. Строка в компаньоне договорённости стоит «ждёт кода».
- **Строки при старте о том, что учётных записей нет ни одной, пока нет.** Это этап 6, он
  впереди; команда `account:list` об этом уже говорит.

## Заходы

### 2026-08-15

- Закрыты хвосты прошлой работы: отчёты #575 и #577 слиты, задачи #563 и #564 закрыты.
- Заведены задачи #580, #581, #582 по находкам, замеченным при слиянии, и #585–#588 по этой
  работе.
- Разбор просьбы записан, конвейер ролей прогнан: 4 роли, 18 находок состязательного разбора.
- Договорённость доведена: спек 336 строк, 47 сценариев `SC-MB-33`…`79`, компаньон на 48 правил,
  16 из них уже с привязками.
- Код: два домена (`accounts`, `access`) по четыре слоя, миграция хранилища, четыре команды,
  три операции входа, единая проверка доступа; прежняя проверка токена и метка открытости сняты.
- Проверки: `lint`, `test`, `build` тринадцати проектов приёмника зелёные, 19 сценариев проверки
  доступа; `check:specs` 201 расхождение — столько же, сколько на главной; `check:docs`,
  `check:layers`, `check:reuse` без своих расхождений.
- Живой прогон на поднятом приёмнике (порт 3100, остановлен по идентификатору процесса):
  заведение записи, занятое имя в другом регистре, вход годной и негодной парой, неизвестное имя,
  чтение без входа, выход, приём груза годным токеном дерева после смены объявления доступа и обе
  подмены способа представиться — все ответы те, что обещает договорённость.
- Споткнулись трижды: раскладка отказала на несобранном пакете (лечится `--skip-nx-cache`);
  сборка падала на клиенте хранилища, не пересобранном после правки схемы (`prisma generate`);
  права границ пришлось дописывать дважды — сначала своим доменам, потом трём доменам груза,
  которые стали объявлять доступ.
