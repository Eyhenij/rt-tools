<!-- rt-kit v0.8.2 · samples/tasks/_template/progress.md · baf326afb38f · правится надстройкой, не здесь -->

# Ход работы

## Где стоим

- **Этап:** этапы кончились, работа лежит в ветке `RT-749-dump-and-deploy-live`
- **Сделано:** загрузка дампа проверена на одноразовой базе рядом с боевой — свод по семи
  таблицам и четыре отпечатка сошлись один в один; выкатка запущена из главной ветки рабочим
  потоком, дошла до узла и поднялась образом вершины `5dcc7da6`, проба живости снаружи отвечает;
  описание прода получило рецепт этой пробы
- **Следующий шаг:** прогнать набор гейта, открыть PR черновиком
- **Незакоммиченное:** нет
- **Ждём владельца:** нет
- **PR:** ещё не открыт

## ГРАНИЦА РАБОТЫ ЭТОГО ДЕРЕВА

Владелец назвал её 16 августа 2026 года: **в этом рабочем дереве делается только эпик RT-733
«Приёмник груза»**. Брать в работу задачи вне эпика запрещено.

## Решения по ходу

- **Проба загрузки шла на одноразовом контейнере базы, боевая не тронута.** Выбор владельца.
  Контейнер поднят тем же образом базы и тем же окружением прода, дамп снят свежий, после
  сверки контейнер снесён вместе с томом.
- **Годность восстановленного проверена отпечатками, а не глазами.** Считались в обеих базах и
  сравнивались: хеши токенов, идентификаторы предложений, их тексты, имена разборов. Все четыре
  сошлись; своды по семи таблицам совпали тоже.

## Заходы

### 16 августа 2026

- Заход на узел открыт владельцем; узел жив семь часов, три контейнера работают.
- Снят свежий дамп — 32 КБ; в каталоге дампов до него лежал один, ночной.
- Поднята одноразовая база, дамп загружен в неё без единой ошибки.
- Свод сошёлся: одно дерево, один годный токен, одна запись месяца, 17 предложений, 2 разбора,
  ноль учётных записей — и в пробе, и в боевой базе поровну.
- Четыре отпечатка сошлись: токены, идентификаторы предложений, тексты предложений, имена
  разборов. Проба снесена, место на узле — 31 %.
- Выкатка запущена из главной ветки рабочим потоком: два образа собраны, узел поднял
  `5dcc7da6`, прогон объявил себя удавшимся после ответа пробы живости.
- На узле лежат образы двух sha — нового и прежнего; чистка чужого не тронула.
- **Находка: учётных записей в хранилище ноль.** Вход в админку сейчас невозможен ни для кого;
  сценарий `SC-MB-61` говорит, что служба сообщает об этом при старте. Владельцу это называется
  отдельной строкой — заводить запись или нет, решает он.
- Задача заведена — RT-749, на борде, в колонке работы. Ветка отведена от главной, папка
  переименована под номер, правка `docs/PROD.md` перенесена сюда из общей ветки двух работ.
