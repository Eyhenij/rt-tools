# Progress

## Где мы стоим

- **State:** `этап-идёт`
- **Этап:** 1 из 4 — идёт
- **Сделано:** ветка отведена от ветки эпика, карточка перенесена в «🏗 In progress», папка задачи
  заведена, разбор просьбы записан, измерение снято, план записан
- **Следующий шаг:** первый этап — дать ячейкам трёх рядов ширину
- **Не в индексе:** папка задачи
- **Ждём владельца:** нет
- **PR:** ещё не открыт

## Решения по ходу

- **Столбец переносится подстановкой верных значений** — в дереве опознаватели вариантов столбца
  устарели, и команда переноса отказывает. Чинит это правка #2088, ещё не влитая; дублировать её
  работу здесь не стал. Затронут этап плана: ни один.

## Заседания

### 2026-09-14

- Просьба владельца принята дословно, разбор записан в `grill.md`.
- Найдено чтением: части `slots`, `active` и `widths` не дают ячейке ряда ширину, а части
  `presets` и `themes` в том же файле дают `width: 30rem` и нарисованы правильно.
- Эталонные кадры и обёртка лежат в одном коммите `b0afd0fa2` — кадры сняты уже сломанными.
- Измерено на поднятой витрине, окна 1280, 1080 и 768. Ширина ячейки нигде не объявлена и гуляет:
  у показа ширин на окне 768 она выходит 150 точек при трёх панелях по 320 и 360. Высота рабочего
  стола везде 900 точек при рамке в 290 — её он берёт от окна, и рамка его обрезает; это не
  поломка, а объявленное поведение.
