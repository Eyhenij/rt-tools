# Progress

## Где мы стоим

- **State:** `этап-идёт`
- **Этап:** 3 из 4 — закрыт
- **Сделано:** ветка, карточка, папка задачи, разбор, измерение, план; ячейкам трёх рядов дана
  ширина, кадры просмотрены глазами, девять эталонов рабочего стола пересняты
- **Следующий шаг:** четвёртый этап — набор проверок дерева и след в описании
- **Не в индексе:** папка задачи
- **Ждём владельца:** нет
- **PR:** ещё не открыт

## Решения по ходу

- **Столбец переносится подстановкой верных значений** — в дереве опознаватели вариантов столбца
  устарели, и команда переноса отказывает. Чинит это правка #2088, ещё не влитая; дублировать её
  работу здесь не стал. Затронут этап плана: ни один.

## Заседания

### 2026-09-14

- Просьба владельца принята дословно, разбор записан в `grill.md`.
- Найдено чтением: части `slots`, `active` и `widths` не дают ячейке ряда ширину, а части
  `presets` и `themes` в том же файле дают `width: 30rem` и нарисованы правильно.
- Эталонные кадры и обёртка лежат в одном коммите `b0afd0fa2` — кадры сняты уже сломанными.
- Измерено на поднятой витрине, окна 1280, 1080 и 768. Ширина ячейки нигде не объявлена и гуляет:
  у показа ширин на окне 768 она выходит 150 точек при трёх панелях по 320 и 360. Высота рабочего
  стола везде 900 точек при рамке в 290 — её он берёт от окна, и рамка его обрезает; это не
  поломка, а объявленное поведение.
- Этап 1: частям `slots`, `active` и `widths` задан `slotWidth="56rem"`. Одной ширины ячейки
  оказалось мало: пунктирная рамка внутри ячейки — элемент гибкой раскладки и ужималась до
  своего содержимого, поэтому на окне 1080 ячейка мерилась 896, а рамка 557. Рамке дан
  `inline-size: 100%`. После этого на всех трёх окнах ячейка, слот и рамка мерятся 896.
- Этап 2: кадры трёх показов сняты и просмотрены. В каждой ячейке видны все три панели с
  подписями, обе половины набора оформления стоят целиком одна под другой, подписи строк
  читаются.

- Этап 3: переснято девять эталонов рабочего стола, чужих семейств в индексе нет.
  `pnpm run test:visual:v2` — 568 эталонов сошлись, 168 наборов, сирот нет.
- Витрина протухла после переключения ветки: семь показов страницы списка она объявила
  несуществующими, хотя файл историй лежит на месте. После подъёма заново их снова 7 из 625.
  Это задача RT-1970, отдельная.

## Находки за рамками работы

- **Показ «только центр» рисует три колонки вместо одной.** Объявлена одна середина, а в кадре
  стоят две пустые области по краям и разделители между ними. Так же было и до правки. Ни одно
  условие выхода этой работы такого не называет — кит здесь не правится. Заводится отдельной
  задачей.
