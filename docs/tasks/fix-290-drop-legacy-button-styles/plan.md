# Замысел

Задача: #290. Ветка: `fix/290-drop-legacy-button-styles`.

Записан задним числом: правило `task-flow` встало в дерево, когда работа была уже сделана.

## Что считается сделанным

- В `projects/ui-kit/src` нет ни одного `.c-button`.
- `styles/components/_button.scss` не существует и не подключён.
- Каждое место, где набор применялся, попадает в снимок витрины.

## Этапы

### 1. Покрытие витриной

- **Что делается:** истории на пять состояний, которых не было в кадре.
- **Признак готовности:** снимок каждого места с кнопкой старого набора.
- **Чем проверяется:** `pnpm run test:visual`.

### 2. Замена

- **Что делается:** девять шаблонов кита и две обёртки витрины.
- **Признак готовности:** набор снят, прогон зелёный.
- **Чем проверяется:** `pnpm exec nx run-many -t lint typecheck test build --all`, `pnpm run lint:styles`, `pnpm run test:visual`.

## Чего эта работа не делает

- Не поднимает точность версий зависимостей — заведено задачей #303.
- Не снимает вход `isMobile` — заведено задачей #302.
