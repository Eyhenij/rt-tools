# Ход работы

## Где стоим

- **Этап:** 2 из 3 — сценарии на обход у каждого гарда
- **Сделано:** этап 1 закрыт коммитом `1191c2c8` — `Bash` и формы терминала среды стоят в
  строке `# rt-hook:` у `task-flow-guard.sh` и `reuse-first-guard.sh`; главная ветка влита
  без конфликтов
- **Следующий шаг:** сценарии на вторую дверь в `reuse-guard.test.sh` и `skill-gate.test.sh`,
  снятые с тринадцати у `task-flow-guard.test.sh`, и их номера в спеке домена
- **Незакоммиченное:** правка этого файла
- **Ждём владельца:** нет
- **PR:** ещё не открыт

## Решения по ходу

- **Шесть вопросов владельцу не задавались** — ответ на каждый записан в теле задачи #754 и в
  его реплике «примени все решения и инструменты из предложенных». Откуда взят каждый — таблицей
  в разборе просьбы. Затронутый этап замысла: ни один.
- **В работу вошла правка объявления события, а не только сценарии** — задача просит сценарии,
  но сценарий гоняет гард напрямую с подставленным вводом и объявления не читает: он был бы
  зелёным при гарде, которого в жизни никто не зовёт. Затронутый этап замысла: 1.
- **16 августа 2026 года. В этап 2 вошла правка тела двух гардов, а не только сценарии.**
  Этап 1 дописал в объявление и формы терминала среды — те же, что стоят у гарда документов, —
  а тело обоих гардов разбирает только `Bash` и на остальные имена выходит молча. Объявленное
  событие, которого тело не разбирает, хуже необъявленного: снаружи оно выглядит закрытым.
  Замерено вызовом гарда с подставленным вводом: `Bash` — отказ, обе формы терминала — пропуск.
  Затронутый этап замысла: 2.
- **16 августа 2026 года. Объявление гейта правил приведено к тому, что разбирает его тело** —
  дописаны заведение файла средой и обе формы терминала. Ветка браузера в объявление намеренно
  не вошла: она судит инструмент, а не запись файла, и замысел прямо выводит браузерные гарды
  из работы. Она названа владельцу отдельно — под неё нужна своя задача.
  Затронутый этап замысла: 2.

## Заходы

### 16 августа 2026 года

- Задача взята сразу после того, как PR #765 ушёл на разбор; ожидания обоих прогонов висят
  фоновыми командами.
- Разведка по четырём гардам, отбивающим правку файла. Найдено: `task-flow-guard` и
  `reuse-first-guard` разбирают команду оболочки в теле, но подписаны только на инструменты
  правки — на команду они не зовутся вовсе, и код разбора не исполняется никогда. Сценарии на
  обход есть только у `task-flow-guard` — тринадцать; у `reuse-first-guard` и `skill-gate` ни
  одного ввода с командой оболочки.
- Главная ветка влита без конфликтов; этап 1 закрыт коммитом `1191c2c8`.
- Этап 2: тело `task-flow-guard` и `reuse-first-guard` разбирает обе формы терминала среды
  вместе с `Bash`, вложенная команда универсального исполнителя вынимается из строки при
  `--command`. Объявление гейта правил приведено к тому, что разбирает его тело.
- Сценарии второй двери: 11 у гарда единообразия, 11 у гейта правил, 2 у гарда замысла;
  спек домена получил `SC-AK-249`–`SC-AK-255`. `nx run @rt-tools/agent-kit:test-hooks` —
  17 наборов зелёные, `npm run check:specs` — нулевой.
- Доэтапное, не этой работы: гейт правил разбирает вызовы браузера и требует под них правило,
  но в объявлении и в настройках дерева их нет — `SC-AK-111` зелёный только потому, что набор
  зовёт гейт напрямую. В живом дереве это правило не требуется ни разу.
