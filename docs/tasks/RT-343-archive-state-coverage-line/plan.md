# Замысел

Задача: #343. Ветка: `RT-343-archive-state-coverage-line`.

**Задача:** RT-343 · **Ветка:** RT-343-archive-state-coverage-line
**Драфт:** не требуется — работа не заводит поведения
**Поведение:** не меняется — правка задевает только тексты, папки задач и архив

После написания этот файл не правится. Пересмотр этапа идёт в `progress.md` решением по ходу.

## Что считается сделанным

- Папок закрытых задач в `docs/tasks/` нет: RT-339 и RT-341 разобраны, слова владельца и
  решения по ходу сохранены в `docs/archive/` по одному файлу на задачу, замыслы удалены.
- Линия работ покрытия витрины закрыта: волна 3 отмечена влитой, у волн 4, 5 и долга по спекам
  стоит отчёт #342, замеры пересчитаны на день закрытия, файл переехал в `docs/archive/`.
- Открытые вопросы линии разобраны на действующие и снятые; каждый действующий заведён задачей
  и назван в архивной записи номером.
- Ссылки на линию из правил, спеков и соседней линии работ ведут по новому пути.
- `npm run check:board` про папки задач молчит, `npm run check:docs` не находит новых мёртвых
  путей.

## Этапы

### 1. Разбор папки RT-341

- **Что делается:** `grill.md`, `progress.md` и `curation.md` складываются одним файлом
  `docs/archive/RT-341-ui-kit-v2-composite-coverage.md`, `plan.md` удаляется, папка сносится.
- **Признак готовности:** в архиве лежит запись с просьбой владельца, решениями по ходу и
  разбором задачи глазами правил; папки в `docs/tasks/` нет.
- **Чем проверяется:** `npm run check:board` — строки про RT-341 нет.

### 2. Разбор папки RT-339

- **Что делается:** то же для чужой закрытой задачи: разбор просьбы и решения по ходу — в
  `docs/archive/RT-339-archive-closed-task-folders.md`, замысел удаляется.
- **Признак готовности:** папки в `docs/tasks/` нет, запись в архиве есть.
- **Чем проверяется:** `npm run check:board` — строки про RT-339 нет.

### 3. Открытые вопросы линии разобраны и заведены задачами

- **Что делается:** три вопроса, вынесенные линией из объёма, сверяются с деревом: закрытое
  отмечается закрытым с указанием работы, действующее заводится задачей `npm run task:new`.
- **Признак готовности:** у каждого действующего вопроса есть номер задачи на борде.
- **Чем проверяется:** `npm run check:board` — заведённые задачи стоят в очереди с исполнителем.

### 4. Линия работ закрыта и переехала

- **Что делается:** таблица «Ход» приводится к действительности, замеры пересчитываются
  командами, дописывается отметка о закрытии со ссылками на заведённые задачи, файл переезжает
  в `docs/archive/`; ссылки на него правятся во всех документах, которые его называют.
- **Признак готовности:** `docs/plans/` линии не содержит, архив содержит, ни одна ссылка не
  ведёт в никуда.
- **Чем проверяется:** `npm run check:docs` и `grep` по прежнему пути.

## Чего эта работа не делает

- Не трогает кит и его витрину: правок кода в ветке нет вовсе.
- Не разбирает `UI-KIT-V2-ISSUES.md` — это отдельный документ со своим списком работ, и его
  разбор заводится своей задачей, если понадобится.
- Не чинит остальные 30 расхождений сверки очереди работ: они про чужие задачи без исполнителя
  и без борды, и правятся не текстами, а самой очередью.
