# Ход работы

## Где стоим

- **Состояние:** `этап-идёт`
- **Этап:** 4 из 4 — пакет собран и разложен

## Решения по ходу

- **Порядок переставляется, а не смягчается.** Прогон остаётся обязательным и остаётся до
  разбора папки: двигается он на один шаг, а не отменяется.

## Записи заходов

### 26 августа 2026 — заход первый

Взята задача 5 эпика. Прочитаны оба паттерна: условие взятия закрытия требует прогнанного
набора, ловушка разбора папки перечисляет порядок целиком.

Найден довод сильнее записанного в находках: прогон, стоящий после вливания, проверяет и само
вливание. Сегодня он проверяет то состояние дерева, которое в главную ветку не поедет.

Все четыре этапа закрыты. Порядок переставлен в трёх текстах разом: правило ведения работы
(таблица состояний, граф хода, новая статья), паттерн закрытия работы (условие взятия и абзац
довода), паттерн разбора папки (ловушка с порядком). Новая статья получила привязку в
компаньоне с вердиктом «Не проверяется»: порядок шагов внутри ветки машине не виден.

Пакет собран и разложен, сверка сходится. Весь набор сценариев пакета зелёный.
