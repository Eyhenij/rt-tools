# Progress

## Where we stand

- **State:** `этапы-кончились`
- **Stage:** 2 of 2 — Прогон и проверка в браузере
- **Done:** перебивка снята, правило и сценарий SC-UK-60 записаны, показ `SubMenuKeepsItsPlace` заведён; сборка, линт и `check:specs` зелёные
- **Next step:** прогнать показ на витрине и убедиться, что без правки он красный
- **Uncommitted:** папка задачи, правка стилей, показ, спека
- **Waiting for the owner:** нет
- **PR:** ещё не открыт

## Decisions along the way

- **Причина найдена замером, а не чтением** — `scrollIntoView` пойман патчем прототипа, прокрутка контейнера снята слушателем. Затронут этап плана: 1.
- **Сценарий закрыт показом, а не юнитом** — стили компонента в спеке не применяются, контейнер там без своей высоты, и мерить нечего. Так же закрыт соседний SC-UK-49. Затронут этап плана: 2.

## Sessions

### 2026-09-09

- Разобрана причина: `position: relative` возвращает шторку в поток, контейнер получает `scrollHeight` 2466 при `clientHeight` 1233, и `scrollIntoView` из `RtScrollToElementDirective` прокручивает его на 96px.
- Снято `position: relative` с `.rtui-sub-side-menu-content`, причина записана рядом. Сборка пакета зелёная, `nx lint @rt-tools/ui-kit` зелёный, `npm run check:specs` без расхождений.
- Заведены `side-menu.drift-asserts.ts` и показ `SubMenuKeepsItsPlace`; сценарий SC-UK-60 записан по форме соседа — «Not covered» с причиной и строкой о показе, иначе аудит спек его не принимает.
