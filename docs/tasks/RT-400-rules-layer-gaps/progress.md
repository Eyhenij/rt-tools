# Ход работы

## Где стоим

- **Этап:** 12 из 12 — все этапы замысла сделаны
- **Сделано:** десять пунктов задачи закрыты в исходниках пакета, договорённость влита в спек
  домена, оба файла предложений сняты, механизм проверен на дереве-потребителе
- **Следующий шаг:** разбор отчёта; после слияния — разбор папки задачи и сверка очереди
- **Незакоммиченное:** нет
- **Ждём владельца:** разбора отчёта
- **Отчёт:** #403, ждёт разбора · замечаний нет · не сделано: разбор папки задачи

## Решения по ходу

- **Конвейер ролей не запускается** — формулировки при каждом из десяти пунктов уже написаны и
  проверены по файлам пакета, разбор `skill-curator` сведён туда же. Затронутый этап замысла: до
  этапа 1.
- **2026-08-13. Гард замысла в этом дереве жив, а разведка захода утверждала обратное.**
  `rt_is_app_code` ищется не только в `.claude/rt-kit/project.sh`: хук читает четыре пути, и
  функция приезжает разложенным умолчанием пакета — `.claude/rt-kit/defaults/project.sh`. Из
  десяти функций профиля, которых ждут разложенные хуки, здесь не определена ни одна. Утверждение
  исправлено в договорённости; замысел не правится, признак готовности этапа 7 читается с этой
  поправкой: `doctor` называет недостающие функции, а в этом дереве список выходит пустым.
  Затронутый этап замысла: 7.
- **2026-08-13. `rules-feedback` в спек домена не вливается этой веткой.** Директория
  «предложено» пережила мерж RT-396, и вливание её потянуло бы сверку каждого правила той работы
  с кодом — это её долг, а не этой. Заведено отдельной задачей. Затронутый этап замысла: 11.
- **2026-08-13. Проверка на дереве-потребителе идёт чтением, а не установкой.** У команд есть
  флаг корня, и `sync --check` с `doctor` ничего не пишут: собранный пакет проверен против того
  дерева, не тронув в нём ни файла. Затронутый этап замысла: 12.

## Заходы

### 2026-08-13

- Разведка по дереву командами до первого вопроса: исходники пакета — `projects/agent-kit`;
  разложенное отстало от пакета на 15 файлов.
- Четыре вопроса владельцу меню со свободным вариантом; ответы записаны в разбор.
- Заведена ветка `RT-400-rules-layer-gaps`, задача #400 переведена в колонку работы, папка
  задачи написана целиком — разбор, замысел на 12 этапов, этот файл.
- Двенадцать коммитов. Прогоны: `projects/agent-kit/tests/run.sh` — все наборы зелёные (два
  новых, 14 сценариев); `nx test @rt-tools/agent-kit` — 197 спек; `pnpm run agent-kit:check` —
  разложенное сходится с пакетом; `pnpm run check:specs` — 139 расхождений, все до этой ветки и
  ни одного про домен пакета.
- Проверка на дереве-потребителе: `doctor` и `sync --check` собранным пакетом через флаг корня.
  Механизм называет там четыре разорванные связи — правило работы взято без трёх своих гардов,
  хук запуска сессии без помощника о нехватке профиля. Ровно то, ради чего П-7 и заводился.
- Споткнулись на собственном утверждении о дереве: «гард замысла мёртв» вывели из одного
  греповского вывода, а хук читает четыре пути. Поймали чтением самого хука перед правкой кода.
- Второе: счёт функций профиля дважды считал не то — нашла проверка на живом дереве, не свой
  прогон.
- Ветка запушена, отчёт #403 открыт от учётной записи машинной работы, ревьювер — владелец,
  метка та же, что у задачи. Задача переставлена в колонку разбора; `pnpm run check:board` —
  расхождений нет.
