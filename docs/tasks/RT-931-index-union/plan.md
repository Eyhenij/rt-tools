# Замысел

**Задача:** RT-931 · **Ветка:** `RT-931-index-union`
**Поведение:** не меняется — правка в настройке слияния, продукта она не задевает.

После написания этот файл не правится. Пересмотр этапа идёт в `progress.md` решением по ходу.

## След задачи

| Что    | Где                             |
| ------ | ------------------------------- |
| Код    | настройка слияния дерева        |
| Тексты | довод записан в самой настройке |
| Дерево | `.gitattributes`                |

## Что считается сделанным

- Две ветки, дописавшие по строке в один указатель, сливаются без участия человека.
- Обе строки остаются на месте.
- Тексты, которые переписывают, под правило не подпадают.

## Этапы

### 1. Указателям объявлен union

- **Что делается:** в настройке слияния объявляется `merge=union` двум указателям, с доводом
  рядом.
- **Признак готовности:** слияние двух веток, дописавших по строке, проходит само.
- **Чем проверяется:** замер на копии дерева — слияние проходит, обе строки в хвосте таблицы.

## Чего эта работа не делает

- Не трогает прозу указателей и прочие тексты: сложение обеих сторон верно только для дописи.
- Не выносит таблицу в отдельный файл: это правит проверку указателя и стоит дороже.
