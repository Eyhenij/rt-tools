# Ход работы

## Где стоим

- **Состояние:** `разбор-кончился`
- **Этап:** 4 из 4 — закрыт: набор гейта прогнан целиком
- **Сделано:** все четыре этапа закрыты; наборы пакета зелёные, affected-набор зелёный, раскладка сходится
- **Следующий шаг:** разобрать папку последним коммитом и открыть заявку черновиком
- **Незакоммиченное:** нет
- **Ждём владельца:** нет
- **PR:** ещё не открыт

## Решения по ходу

- **Договорённости о продукте нет** — работа правит тексты слоя правил и надстройку дерева,
  кода приложения не касается. Затронутый этап замысла: все.

## Заходы

### 2026-08-23

- Дефект пришёл от соседнего захода и проверен здесь: в `deploy.yml` единственный триггер —
  `workflow_dispatch`, а правило выкатки утверждает, что прод едет от мержа.
- Замысел записан четырьмя этапами; править решено надстройкой дерева, не источником пакета.
- **Пересмотр этапов 1 и 2.** Одной надстройки мало: правило пакета утверждало про дерево
  безусловно, а по правилу `spec-driven` переносимый текст так говорить не вправе. Статья
  «Мерж в главную ветку выкатывает прод» разделена надвое во всех трёх видах хостинга: чем
  запускается выкатка, называет теперь дерево. Надстройка дерева осталась — она везёт цену
  промаха и команды. Этап 2 закрыт проверкой: в правиле поставки этой строки не оказалось
  вовсе — она стояла только в правиле выкатки.
- **Этап 3.** Сверка очереди работ судит прод по последней успешной выкатке, а не по прогону
  главной ветки: `deployLag` в `tools/board-runs.mjs`, поток и ветка — ключ `deploy` в
  настройке дерева. Два новых сценария: SC-AK-531, SC-AK-532. Проверено на живом: по
  `deploy.yml` отставание 0, по чужому потоку — 7 коммитов.
- Модуль работы с очередью перерос предел длины от этой правки и поделён по предмету: прогоны
  и выкатка уехали в `checks/board-runs.github.mjs`.
- Наборы: пакет — все зелёные; `nx affected -t lint typecheck test build` — 8 проектов;
  `test-hooks` — зелёный; `check-file-size` — длиннее предела 0; раскладка сходится с v0.12.0.
