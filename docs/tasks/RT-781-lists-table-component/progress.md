# Ход работы

## Где стоим

- **Этап:** 1, 2 и 3 закрыты; следующий — 4, вливание договорённости и тексты домена
- **Сделано:** задача переведена в работу, ветка `RT-781-lists-table-component` заведена, папка
  задачи собрана, разбор просьбы записан. Разведка прочитала списочный экран сводок здесь и
  списочный экран брони у дерева-образца: расхождение — элемент `<rt-table>` против
  `<table rt-table>`, признак таблицы строкой вместо префикса раздела; строки объявлены с двух
  сторон одинаково, а слот тулбара под отбор у общей страницы уже есть
- **Сделано:** этап 1 — четыре раздела объявляют таблицу элементом кита; кит ставит на своём теге
  роль таблицы, потому что у элемента её нет, а роли строк и ячеек CDK ставит сам; префикс раздел
  называет одним полем, а якоря таблицы и строки собирает общая основа; готовый код паттерна
  переписан надстройкой и разложен. `SC-MB-132` и `SC-MB-135` закрыты спеками, `SC-MB-136` помечен
  непокрытым с причиной. Сборка админки, линт, типы и спеки четырёх экранов и таблицы кита зелёные
- **Сделано, этап 2:** пустое состояние стало видом со значком, заголовком и словом о том, откуда
  записи приходят: подпись, стоявшая одной фразой через двоеточие, разделена на две, вторую даёт
  основа, а у приглашений её перебивает свой раздел — их приносит команда владельца, а не дерево.
  `SC-MB-129`, `SC-MB-130` и `SC-MB-131` закрыты сквозными спеками; весь набор 31 спека зелёная.
  Замер на стенде: таблица — `RT-TABLE` с `role="table"`, значок 64×64 отцентрован по таблице
  (отклонение 0), заголовок «Записей нет» 16px, описание приглушённым цветом, скелетонов нет
- **Сделано, этап 3:** правки кода не понадобилось — отбор лежит в слоте с первой задачи эпика, а
  входа под отбор у общей страницы нет ни одного. Закрыто спеками: `SC-MB-133` — отбор стоит
  именно в левой части тулбара, `SC-MB-134` — у раздела без отбора левой части нет вовсе, и
  пустой половины тулбара на экране не бывает. Сквозной набор — 32 спеки зелёные
- **Сделано, этап 4:** договорённость влита в спек поддомена чтения коммитом `20de3e02` — четыре
  правила в разделы «Списки груза» и «Чем раздел говорит с общей страницей списка», сценарии
  `SC-MB-129`…`SC-MB-136` прежними номерами, четыре привязки. Два правила договорённости не
  переехали: об отборе в слоте и о незанятом слоте — они стоят в спеке с RT-780 и ведут в те же
  символы. Правилу `lists`, его спутнику и паттерну `admin-lists-screen` дописаны утверждения о
  языке разметки и пустом состоянии; раздел «Где эпик стоит» в замысле эпика правлен. `check:specs`
  и `check:docs` зелёные, драфта `proposed/lists-on-table/` на диске нет
- **Следующий шаг:** пуш ветки с прогоном гейта, PR черновиком, разбор папки задачи последним
  коммитом, `npm run check:board`, снятие черновика
- **Незакоммиченное:** правка этого файла
- **Ждём владельца:** ничего
- **PR:** не открыт

## Решения по ходу

- **17 августа 2026. Экран приглашений входит в задачу наравне с тремя названными.** Решение
  владельца при разборе: он собран тем же приёмом, а заведён после того, как эпик был расписан.
  Оставленный в стороне, он через задачу читается как принятый приём. Затронутый этап замысла:
  нет — объём назван замыслом с самого начала.
- **17 августа 2026. Паттерн дерева о списочном экране правится этой же работой.** Решение
  владельца при разборе: его готовый код пишет таблицу атрибутом, и следующий раздел собрали бы по
  нему. Затронутый этап замысла: нет — правка стоит в этапе 1.
- **17 августа 2026. Пустое состояние отдельным видом даёт готовый компонент кита.** Разведка
  нашла его у образца: экран отказов ставит вид пустого состояния вместо таблицы, а вход
  `[emptyMessage]` остаётся короткой формой внутри таблицы. В ките этого дерева такой компонент
  тоже есть, заводить свой не надо. Затронутый этап замысла: нет.

## Заходы

### 17 августа 2026

Заход закрыл RT-801 целиком: три расхождения спеков починены правкой текста, два зависших коммита
запушены, папка задачи разобрана записью архива, тело PR переписано, черновик снят, PR #802 влит
владельцем коммитом `df7da529`.

Найдено по ходу и заведено задачей RT-813: конвейер не завёл прогона ни на один из трёх коммитов
ветки, правящих только текст, — очередь прогонов пуста, наборы проверок на вершине есть лишь от
сторонних приложений, а на коммитах с кодом прогоны шли. Все три въехали в главную ветку
подтверждёнными только гейтом пуша на машине исполнителя.

Разбор закрытой работы правилами по RT-801 не запускался: у сессии стоит запрет звать роли без
слова владельца. Сводка для него — в записи архива `docs/archive/RT-801-tree-invite.md`.

Этап 4: правки форматтера в семи чужих `.md`, лежавшие с прошлого захода, сняты — правило
`dependencies` отвечает прямо: переформатируется только то, чьё форматирование проверяет линтер,
а `.md` здесь не проверяет никто. Договорённость влита, тексты приведены к сделанному.

Найдено по ходу и не этой работой сделано: `node tools/check-file-size.mjs` падает необработанным
`ENOENT`, когда в дереве есть удалённый, но не закоммиченный файл — список берётся из индекса, а
файла на диске уже нет. Проверка обязана отчитаться, а не упасть. Заведено задачей не будет до
слова владельца.
