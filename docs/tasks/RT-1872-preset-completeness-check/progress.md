# Progress

## Where we stand

- **State:** `этапы-кончились`
- **Stage:** все четыре закрыты
- **Done:** этап 1 закрыт — двум янтарным именам рельсы шагов проставлена причина общего цвета,
  остальные семь молчащих имён следуют за набором через ссылку. Этап 2 закрыт — проверка полноты
  написана и замерена на трёх случаях. Этап 3 закрыт — контраст считается в четырёх видах, 48 пар
  материального набора записаны в список принятого поимённо. Этап 4 закрыт — проверка стоит в
  наборе перед пушем и в шаге конвейера, правило получило две статьи с привязками
- **Next step:** слово владельца — пуш упирается в две стены сразу, и обе не в работе
- **Uncommitted:** нет
- **Waiting for the owner:** да — три вещи. Слить #1893, #1894, #1895 снизу вверх: прогоны у всех
  трёх зелёные, но черновик не снять, пока рецензент не оставил отзыв. Отдать #1888: конфликт
  разрешён и закоммичен, а пуш ждёт, когда правка RT-1891 дойдёт до главной. Открыть заявку этой
  работы — гард отдачи требует служебную учётку, токена от неё нет
- **PR:** ещё не открыт

## Decisions along the way

- **Ветка стоит на RT-1871, а не на главной** — материальный набор лежит там и в главную не влит.
  Второе записанное исключение из «ветки идут от главной». Задет след задачи.
- **Замысел в спеку домена не сводится этой работой.** У договорённости о наборе четырнадцать
  сценариев, и мои из них три; остальные — про значки, витрину и ступени бренда, то есть про
  следующие задачи эпика. Договорённость вливается, когда закроется последний, а не первый.
- **Порог читаемости считает прежняя проверка, а не новая.** Замысел третьего этапа называл
  признаком вывод `check-preset-complete`, но контраст пар «подпись и подложка» уже считает
  `check-tokens-theme` — со своим списком принятого, своим разбором цвета и сверкой перечня пар
  со страницей замера. Написать второй такой разбор значило бы завести два места, которые
  разъедутся; готовое расширено двумя видами. Замысел после написания не правится, потому
  решение стоит здесь.
- **Видов четыре, а не два.** Тёмная тема сильнее набора: имя, на которое она отвечает, держит в
  материальном наборе тёмный цвет, а имя, о котором она молчит, берёт цвет набора. Поэтому
  «набор под тёмной темой» — не повтор тёмной темы, и замер это подтвердил: числа в этих двух
  видах у половины пар разные.

## Sessions

### 2026-09-07

- Замер по источникам набора: 214 назначений базового набора, 61 переписано материальным, 153 нет.
  Цветовых среди непереписанных девять — пять подписей роли на поверхности, цвет отключённой
  подписи и ещё три.
- Сборщик слоя оформления уже роняет сборку на имени, которого нет в базовом наборе. Обратной
  проверки — «в базовом есть, в материальном молчание» — у него нет, и заводить её сюда.

- Проверка полноты `tools/check-preset-complete.mjs` написана. Пометки она не требует от имени,
  чьё значение целиком есть ссылка на имя, которое набор переписывает: такое имя следует за
  набором само, и проверка выводит это сама, проходя цепочку ссылок до конца. Пометка, которой
  больше нечему отвечать, роняет проверку по имени — иначе она переживает правку и начинает врать.
- Замер трёх признаков готовности этапа. Снятая причина у `--rt-color-stepper-current-subtle` —
  одно расхождение, код 1. Пометка на `--rt-color-text-muted`, который набор переписывает, — одно
  расхождение по имени. Пометка на `--rt-color-action-primary-on-surface`, который следует через
  ссылку, — одно расхождение, и отказ называет саму ссылку. Дерево возвращено, проверка зелёная:
  69 цветовых назначений, 60 переписано, 7 следуют через ссылку, 2 общие с названной причиной.
- Причины у двух янтарных имён переписаны на язык их файла: соседние записки в нём русские, а
  отказ проверки, который читает сессия, остался английским.
- Порог читаемости заводить с нуля не придётся: `tools/check-tokens-theme.mjs` уже считает
  контраст пар «подпись и подложка» по списку `tools/tokens-contrast-pairs.json` в светлой и
  тёмной темах. Третий этап расширяет её на материальный набор, а не пишет вторую проверку.
- Контраст: `check-tokens-theme` считает четыре вида — светлая, тёмная, материальный набор и он
  же под тёмной темой. Замер: 48 пар ниже порога 4.5:1, все до одной в материальном наборе; в
  базовом ни одной новой. Наследство вида первого кита — белая подпись на его акценте, цвет роли
  как текст на белом, плашки сообщений. Все 48 записаны в список принятого поимённо, с числом и
  причиной, задача RT-1872.
- Замер двух признаков готовности этапа 3. Пара, снятая из списка, роняет прогон и называет своё
  число. Запись, которой больше нечему отвечать, роняет прогон и требует убрать строку.
- Страница замера `Colors.mdx` получила два столбца — «набор» и «набор, тёмная»; её вступление
  говорило «в обеих темах», теперь говорит про четыре вида. Сборка витрины второго кита зелёная,
  код 0: страница правится в `docs/`, а линтер и проверка типов туда не смотрят.
- Проверка вписана в три места: имя `check:preset-complete` в списке команд, строка в наборе
  гарда перед пушем и строка в шаге «Style layer» конвейера — рядом с четырьмя её соседками по
  слою оформления. Гард разбирается: `bash -n` зелёный.
- Правило `rt-tools-styling` получило две статьи — о том, что молчание набора называется
  причиной и что молчание, которому причина не нужна, выводится, и о том, что порог читаемости
  считается во всех видах, в каких кит рисуется. Обе с привязками в спутнике; сверка спеков
  зелёная.
- **Файл цвета пришлось делить.** Две строки причин перевели `tokens.light-color.mjs` за предел
  длины: 499 строк было, 504 стало при пределе 500. Проверка длины говорит прямо — делить, а не
  дописывать строку в список принятого. Граница проведена по предмету, который в файле и так
  был: цвет ролей палитры остался на месте, цвет названных частей кита — рельса шагов с двумя
  ореолами, переписка и поверхность входа — уехал в `tokens.light-parts.mjs`. Стало 418 и 98.
- Собранный слой оформления от этого поменялся только порядком объявлений внутри одной примеси:
  блок рельсы шагов встал после блока состояний. Значения не тронуты, ссылки в пользовательских
  свойствах разрешаются в месте применения, а не объявления. Сборка сходится с источником, пять
  файлов.
- Написаны два набора сценариев над проверками — `tools/tests/check-preset-complete.test.sh` на
  пятнадцать случаев и `tools/tests/check-tokens-theme.test.sh` на восемь. Общий прогон зелёный.
- **Находка, которая в задачу не входит.** Сценарии SC-UKV-104, SC-UKV-105 и SC-UKV-107 покрытыми
  не считаются, хотя наборы над ними написаны и зелёные: сборщик покрытия обходит оболочечные
  наборы только под корнями исходников приложения, а проверки этого дерева лежат в
  `tools/tests/`. Записано честной причиной в каждом сценарии. Заявку на это завести не удалось —
  гард отказал, пока стоит конфликтной своя прошлая заявка.
- **Конфликт своей прошлой заявки разобран.** #1888, ветка RT-1875: спорила спека второго кита —
  главная перевела её на английский, ветка убрала счёт предметов и добавила седьмой поддомен.
  Разведено в пользу обоих. Список принятых адресов взят из RT-1891 слово в слово. Пуш при этом
  не прошёл: витринный гард второго кита падает на пятнадцати кадрах — это та самая беда, которую
  чинит RT-1891, а до главной та правка ещё не дошла. Слияние лежит закоммиченным в ветке и уедет,
  как только #1893 вольют.
- **Пуш упёрся в стенд, и причина не в работе.** Правка трогает `package.json` — имя
  `check:preset-complete` рядом с четырьмя соседками по слою оформления. Разборщик состава правки
  относит корневые настройки к «everything»: неизвестное читается как «могло задеть что угодно», и
  это верно — в корневых настройках меняют и версии зависимостей. От этого поднимаются все пять
  тяжёлых шагов гейта, а не один.
- **Из пяти тяжёлых шагов на этой учётной записи машины исполнимы два.** Замер: снимки первого
  кита — ветка его не трогает вовсе, ноль файлов; снимки второго кита идут; сквозная спека админки
  и две сборки образов требуют движка контейнеров, а его здесь нет ни одного — ни `docker`, ни
  `colima`, ни `podman`, ни `nerdctl`, ни своего постгреса. В конвейере те же шаги зелёные: раннер
  стоит под другой учётной записью той же машины. Три прошлые ветки прошли гейт потому, что
  корневых настроек не трогали.
- **Обойти это составом правки нельзя, и это замерено, а не прикинуто.** Разборщик прогнан по
  трём задетым файлам вне второго кита: `.github/workflows/ci.yml` — «задето всё»,
  `package.json` — «задето всё», `.claude/rt-kit/project.sh` — без тяжёлого шага. Строку в шаге
  конвейера требует сам замысел четвёртого этапа, то есть правка `ci.yml` неизбежна и одна уже
  поднимает весь набор. Снять имя команды из корневых настроек было бы бесполезно: помеха осталась
  бы та же.
- **Цена владельцу, одно решение из двух.** Поднять движок контейнеров под этой учётной записью —
  тогда гейт проходит как есть. Либо отправить ветку своей рукой, приняв, что сквозную спеку и две
  сборки образов проверит конвейер, а не машина.
- **Вторая стена: снимки второго кита на этой машине красные, и причину я не назвал.** Замер по
  четырём прогонам. В 20:00 при одной поднятой витрине — 469 кадров, все зелёные; это было уже
  после деления файла цвета. После полуночи: витринный гард на той же витрине уронил четыре кадра
  CollapsibleText; свежая витрина на другом порту — восемь кадров переписки; та же витрина после
  снятия второй — те же восемь. Последние два прогона сходятся кадр в кадр, то есть расхождение
  устойчивое, а не шум.
- Что на картинке различий: весь столбец переписки сдвинут по вертикали на несколько пикселей —
  пузыри, поле ввода, кнопка отправки и заглушки загрузки. Значения собранного слоя оформления при
  этом те же: набор имён и значений в `_semantic.scss` до деления и после совпадает построчно,
  215 строк. Эталоны в рабочей копии — те, что лежат в истории. Метка времени в показе задана
  числом, а не текущим временем. Конвейер тот же набор кадров прошёл зелёным на #1895 часом
  раньше.
- **Промах мой, и правило называет его прямо.** «Череда прогонов не терпит правок дерева под
  собой»: витрина держится горячей заменой, и переход на другую ветку со слиянием главной считается
  правкой наравне с правкой файла — прежний счёт после такого никуда не годится, дерево
  перезапускается начисто. Я сделал ровно это: ушёл на RT-1875, влил туда главную, вернулся — под
  живой витриной, — и принялся разбирать расхождение как дефект. Правило я загрузил уже после того,
  как потратил на разбор три прогона.
- Второй промах поменьше: поднял вторую витрину на соседнем порту, чего делать не следовало — две
  на одной машине делят процессор. Гипотезу это не подтвердило, свою витрину снял, чужую на 6007 не
  трогал.
- **Перезапуск начисто расхождение не снял, то есть и это объяснение неверно.** Сняты кэш сборщика
  и кэш витрины, поднята чистая витрина, прогон на ней: те же восемь кадров, те же истории.
  Правило само называет признак своего случая — «расхождение не повторяется на чистом
  перезапуске», — а здесь оно повторяется.
- **Собранный слой оформления от деления файла не менялся вовсе.** Сверены все четыре собранных
  файла до правки и после — объявления совпадают в каждом; поменялся только порядок строк внутри
  одной примеси.
- **Доля расхождения велика для шума:** от 0.31% до 1.52%, до 12 137 пикселей на кадр. Это сдвиг
  разметки, а не сглаживание.
- **Две версии проверены и отпали.** Первая — что порядок строк в собранном файле всё-таки решает:
  в нём есть имя, объявленное дважды, `--rt-input-font-size`, и второе объявление сидит под
  условием грубого указателя. Проверено: до разреза и после оба объявления стоят на тех же
  строках, разрез их не трогал. Вторая — что съёмка идёт с грубым указателем, и поле ввода растёт
  с 14 на 16 пикселей: тогда красными были бы все истории с полями, а зелёными прошли все —
  поле, выбор, автодополнение, выбор даты, выбор файла, многострочное поле. Красное только
  семейство переписки.
- **Что при этом стоит запомнить помимо разбора:** пояс и язык съёмки закреплены настройками
  прогона, а вид указателя — нет, и он остаётся зависимостью от машины. Сегодня она себя не
  проявила, но условие в слое оформления на неё есть.
- **Итог разбора: причину я не нашёл.** Что известно точно: в 00:19 на том же дереве прогон дал
  469 кадров из 469 зелёными; в 00:36, 00:51 и 00:58 — восемь красных, устойчиво, на двух разных
  витринах, включая поднятую начисто. Правок разметки и стилей между этими прогонами не было —
  только тексты, наборы сценариев и переход на соседнюю ветку и обратно. Конвейер тот же набор
  кадров прошёл зелёным на #1895. Дальше гадать дороже, чем передать: разбор продолжать с этого
  места, а не начинать заново.

## Handover of the session

### Where the work stands

- **State:** `этапы-кончились` — все четыре этапа закрыты и закоммичены.
- **Что осталось:** отправить ветку, разобрать папку задачи, открыть заявку черновиком.
- **Что этому мешает:** одна стена из двух — вторая ушла. Гейт пуша зовёт сквозную спеку админки, её стенд поднимает базу в
  контейнере, а движка контейнеров на этой учётной записи машины нет ни одного. Обойти составом
  правки нельзя — это замерено: правку описания конвейера требует сам замысел четвёртого этапа, а
  она одна уже поднимает весь набор тяжёлых шагов.
- **Решение владельца, одно из двух:** поднять движок контейнеров под этой учётной записью — или
  отправить ветку своей рукой, приняв, что сквозную спеку и две сборки образов проверит конвейер.
- Заявку всё равно откроет владелец: гард отдачи требует служебную учётную запись, токена от неё
  на машине нет. Три прошлые заявки открывались так же.

### Что случилось за ночь помимо задачи

- **Все четыре заявки влиты владельцем** — #1893, #1894, #1895 и #1888, около 04:20. Ветки
  RT-1891, RT-1871 и RT-1876 сняты как влитые. Ветка RT-1875 осталась: её содержимое в главной,
  но лежит незапушенный местный коммит слияния, и правило не даёт снимать невлитую ветку.
- **Главная влита в эту ветку.** Спорил один файл — спутник договорённости о наборе: главная
  перенумеровала все четырнадцать сценариев, SC-UKV-99…112 стали SC-UKV-115…128. Три привязки
  этой ветки переехали вместе со своими сценариями: 104 → 120, 105 → 121, 107 → 123.

### Красные кадры переписки — сошлись после слияния главной

Восемь кадров семейства переписки не сходятся с эталонами: доля от 0.31% до 1.52%, до 12 137
пикселей на кадр — это сдвиг разметки, а не сглаживание. Что известно точно и проверено:

- в 00:19 на том же дереве прогон дал 469 кадров из 469 зелёными; в 00:36, 00:51 и 00:58 — восемь
  красных, устойчиво, на двух разных витринах, включая поднятую начисто со снятыми кэшами;
- правок разметки и стилей между этими прогонами не было;
- деление файла цвета ни при чём: объявления во всех четырёх собранных файлах до правки и после
  совпадают, поменялся только порядок строк;
- версия про грубый указатель отпала: тогда красными были бы все истории с полями ввода, а
  зелёными прошли все шесть.

**После слияния главной прогон зелёный целиком: 472 кадра из 472, 162 набора, 461 проверка.**
Слияние принесло в кит восемь переснятых эталонов и правки самой витрины — среди них ни одного
эталона переписки, то есть вылечило не подменой эталона, а тем, что ветка перестала отставать.
Правило о проверке в браузере говорит об этом прямо: рабочая ветка держит вершину главной влитой
всё время, а не только перед отдачей, — и я его загрузил, но к этому случаю не приложил.

Что осталось необъяснённым: почему те же кадры были зелёными в 00:19 и красными в 00:36, когда
главная ещё не двигалась. Если покраснеют снова — начинать отсюда, а не с нуля.

### Свои промахи этой сессии

- Разбирал красные кадры как дефект, не загрузив правило о проверке компонентов. Оно называет
  случай прямо: череда прогонов не терпит правок дерева под собой, и переход на соседнюю ветку я
  сделал ровно под живой витриной. Объяснение потом не подтвердилось, но три прогона на него ушло.
- Поднял вторую витрину на соседнем порту, чего делать не следовало: две на одной машине делят
  процессор. Свою снял, чужую на 6007 не трогал.

### Находка, которая в задачу не вошла

Сценарий о механике проверки нельзя отметить покрытым, если набор над ней лежит не под корнем
исходников: сборщик покрытия обходит оболочечные наборы только там, а проверки этого дерева лежат
рядом с остальным инструментарием. Набор написан, зелёный, зовётся общим прогоном — и сценарий всё
равно стоит «Не покрыто». Отличить это от ненаписанного набора нельзя ничем. Заявку завести не
удалось: гард отказывал, пока стояла конфликтной своя прошлая заявка. Теперь она влита, и заявку
можно завести.

### Незакоммиченное

```
нет
```

### Коммиты над главной

```
0e9b82348 Merge branch 'main' into RT-1872-preset-completeness-check
e27c23146 docs(rt:ui-kit-v2): две версии про красные кадры проверены и отпали
4cfbfd9e8 docs(rt:ui-kit-v2): разбор красных кадров доведён до честного «причину не нашёл»
a013fcad2 docs(rt:ui-kit-v2): обойти помеху составом правки нельзя — замерено
2f529635b docs(rt:ui-kit-v2): разбор красных кадров записан вместе со своим промахом
6691722c2 docs(rt:ui-kit-v2): помеха с пушем названа тремя решениями владельцу
e549b8a4b docs(rt:ui-kit-v2): цена помехи с пушем замерена и названа тремя решениями
02b8d7a18 docs(rt:ui-kit-v2): ход RT-1872 — пуш упёрся в стенд сквозной спеки
b50492430 docs(rt:ui-kit-v2): ход RT-1872 — все четыре этапа закрыты, дальше сдача
f2e3ff462 test(rt:ui-kit-v2): механика двух проверок набора закрыта наборами сценариев
1d8107a9d feat(rt:ui-kit-v2): проверка полноты набора стоит в гарде и в конвейере
0f66134ab feat(rt:ui-kit-v2): порог читаемости считается и в материальном наборе
c6be55d73 feat(rt:ui-kit-v2): молчание материального набора о цвете отбивается проверкой
d28e1a15f feat(rt:ui-kit-v2): молчание набора у двух янтарных имён названо причиной
aae875664 docs(rt:ui-kit-v2): папка задачи RT-1872 заведена, замысел записан
```
