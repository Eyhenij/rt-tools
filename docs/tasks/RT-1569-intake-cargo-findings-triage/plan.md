# Замысел

**Задача:** RT-1569 · **Ветка:** RT-1569-intake-cargo-findings-triage
**Поведение:** не меняется — разбор находок и правка текстов слоя правил

## След задачи

| Что      | Где                                                        |
| -------- | ---------------------------------------------------------- |
| Находки  | `docs/plans/intake-cargo-rules-findings.md`                |
| Ловушки  | `pitfalls/task-flow.md`, `pitfalls/git-workflow.github.md` |
| Паттерны | `patterns/spec-driven-rule.md`, `patterns/testing-e2e.md`  |
| Правила  | `rules/typescript-conventions.md`                          |
| Эпик     | #1198, замысел `docs/plans/findings-triage.md`             |

## Что считается сделанным

- В файле находок не осталось ни одного неразобранного пункта.
- По каждому закрытому видно, чем он закрыт; живые внесены правкой ресурсов.

## Этапы

### 1. Двадцать пунктов сверены с деревом

- **Что делается:** каждый пункт проверяется по нынешним ресурсам.
- **Признак готовности:** у каждого назван исход.
- **Чем проверяется:** `grep -c '^- \*\*' docs/plans/intake-cargo-rules-findings.md` — 0 после разбора.

### 2. Живое внесено правкой

- **Что делается:** живые пункты ложатся статьями и ловушками.
- **Признак готовности:** каждый живой пункт назван текстом ресурса.
- **Чем проверяется:** `pnpm run agent-kit:sync` — раскладка кладёт файлы без отказа.

### 3. Пределы и пути сходятся

- **Что делается:** прогон проверок текстов.
- **Признак готовности:** расхождений нет.
- **Чем проверяется:** `node tools/check-file-size.mjs` — долг 0, новых нет.

## Чего эта работа не делает

- Не трогает два оставшихся файла находок.
