# Progress

## Where we stand

- **State:** `этап-идёт`
- **Stage:** 4 of 6 — осмотр кадров и снятие эталонов
- **Done:** шесть из семи компонентов получили свой показ, эталоны сняты; седьмой оказался
  сломанным и заведён отдельной задачей
- **Next step:** перечень непоказуемого и ответ про счёт по компонентам
- **Uncommitted:** нет
- **Waiting for the owner:** нет
- **PR:** ещё не открыт

## Decisions along the way

- **Показ пишется разметкой, а не обёрткой с осями входа** — у этих компонентов осей входа нет.
  Affected stage of the plan: 3.
- **Считаем не по вхождению имени в разметку, а по тому, что называет заголовок истории** — по
  первому счёту семь из карточки выпадали из списка: их имена стоят в обёртке родителя, а своей
  истории у них нет. Affected stage of the plan: 1.
- **Семь получают свою историю, 23 — запись с причиной** — у семи есть свой вид: отступы, рамка,
  раскладка, кнопки. У 23 своей разметки нет вовсе: это маркеры проекции, и виден не маркер, а то,
  как родитель разложил содержимое. Affected stage of the plan: 3 и 5.
- **Перечень непоказуемого получает второй ключ — по компонентам, а не по семействам** — нынешний
  ключ заводится на семейство, а все 23 лежат внутри семейств, которые до витрины доходят. Это же
  отвечает на третий пункт карточки. Affected stage of the plan: 5.
- **Проецируемой подсказке поля свой показ не нужен: он уже есть** — история `Hint` семейства поля
  показывает обе стороны пары, строковую подсказку и проекцию с разметкой, в одном кадре. Карточка
  назвала её ошибочно, и первый счёт повторил ошибку. Affected stage of the plan: 3.
- **Панель настройки колонок показа не получила: она сломана** — открывается и через несколько
  секунд закрывает себя сама, кадр застаёт пустую страницу. Заведена задача RT-2049. Affected stage
  of the plan: 3 и 5.
- **Экран-образец называет активную таблицу до перехода** — кит требует этого от потребителя, а
  экран здесь и есть образец потребителя. Самозакрытия это не вылечило, но без этого показ не
  открылся бы и после починки. Affected stage of the plan: 3.

## Решение по каждому

Свой показ разметкой с проекцией содержимого — шесть заведено:

- `rt-aside-footer` — четыре набора зон, включая содержимое без атрибута, которое не рисуется вовсе
- `rt-dialog-footer` — четыре набора содержимого, включая статусную строку слева от кнопок
- `rt-aside-unsaved-dialog` — три исхода, отказ от правок подан красной рамкой
- `rt-menu-confirm-dialog` — три тона кнопки и два вида заголовка, каждая ячейка со своим инжектором
- `[rtTableRow]` — четыре состояния строки сразу: обычная, наведение, нажатие, фокус с клавиши
- `rt-table-settings-aside` — **не заведён**, задача RT-2049

Показ уже есть, запись в перечень не нужна — один:

- `[rtFieldHint]` — история `Hint` семейства поля показывает строковую подсказку и проекцию с
  разметкой рядом

Запись в перечне с причиной — 23 маркера проекции содержимого:

- `container` — 10, `toolbar` — 3, `workspace` — 4, `thread-list` — 3, `table` — 2, `chat` — 1

## Sessions

### 2026-09-10

- Заведены задача RT-2021, ветка от ветки эпика, папка задачи; колонка передвинута в работу
- Этап 1: объявлено 116 компонентов и директив кита, заголовков историй 91, своей истории нет у 30
- `node tools/check-kit-coverage.mjs` — семейств 74, доходят 73: счёт по семействам этих 30 не видит
- Этап 2: решение по каждому из 30
- Этап 3: заведено пять новых файлов историй, 16 показов; шестой компонент оказался уже показан,
  седьмой сломан
- Этап 4: кадры осмотрены поштучно, снято 16 эталонов. По осмотру исправлены сообщения окна
  подтверждения — второе и третье противоречили своим заголовкам
- Сосчитаны кадры: на диске было 549, историй со снимком 468, кадров без своей истории нет ни
  одного, одинаковых по байтам нет ни пары
