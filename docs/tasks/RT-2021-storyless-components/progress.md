# Progress

## Where we stand

- **State:** `этап-идёт`
- **Stage:** 2 of 6 — решение по каждому компоненту без своей истории
- **Done:** список из 30 собран; по каждому написано, что делается
- **Next step:** истории с разметкой и проекцией содержимого семи компонентам
- **Uncommitted:** нет
- **Waiting for the owner:** нет
- **PR:** ещё не открыт

## Decisions along the way

- **Показ пишется разметкой, а не обёрткой с осями входа** — у этих компонентов осей входа нет.
  Affected stage of the plan: 3.
- **Считаем не по вхождению имени в разметку, а по тому, что называет заголовок истории** — по
  первому счёту семь из карточки выпадали из списка: их имена стоят в обёртке родителя, а своей
  истории у них нет. Affected stage of the plan: 1.
- **Семь получают свою историю, 23 — запись с причиной** — у семи есть свой вид: отступы, рамка,
  раскладка, кнопки. У 23 своей разметки нет вовсе: это маркеры проекции, и виден не маркер, а то,
  как родитель разложил содержимое. Affected stage of the plan: 3 и 5.
- **Перечень непоказуемого получает второй ключ — по компонентам, а не по семействам** — нынешний
  ключ заводится на семейство, а все 23 лежат внутри семейств, которые до витрины доходят. Это же
  отвечает на третий пункт карточки. Affected stage of the plan: 5.

## Решение по каждому

Своя история разметкой с проекцией содержимого — семь:

- `rt-aside-footer` — две именованные зоны и раскладка по краям; безатрибутное содержимое не
  проецируется вовсе, и это видно только своим кадром
- `rt-dialog-footer` — верхняя граница и прижатие к правому краю
- `rt-aside-unsaved-dialog` — три исхода, отказ от правок подан красной кнопкой; открывается
  службой, истории нужен готовый `RtDialogRef`
- `rt-menu-confirm-dialog` — окно подтверждения деструктивного пункта; истории нужны `RT_DIALOG_DATA`
  и `RtDialogRef`
- `rt-table-settings-aside` — панель по маршруту; истории нужны запись в реестре таблиц и выход
  маршрута
- `[rtFieldHint]` — проецируемая подсказка вместо строковой; разница видна рядом со строковой
- `[rtTableRow]` — строка становится активируемой; вид даёт `[clickable]` на таблице, и в кадр
  попадает только состояние наведения и фокуса

Запись в перечне с причиной — 23 маркера проекции содержимого:

- `container` — 10: `[rtContainerHeader]`, `[rtContainerContent]`, `[rtContainerContentPanel]`,
  `[rtContainerLeftSidenav]`, `[rtContainerLeftSidenavPanel]`, `[rtContainerRightSidenav]`,
  `[rtContainerRightSidenavPanel]`, `[rtContainerToolbarLeft]`, `[rtContainerToolbarCenter]`,
  `[rtContainerToolbarRight]`
- `toolbar` — 3: `[rtToolbarLeft]`, `[rtToolbarCenter]`, `[rtToolbarRight]`
- `workspace` — 4: `[rtWorkspaceList]`, `[rtWorkspaceCenter]`, `[rtWorkspaceAside]`,
  `[rtWorkspaceDesktopOnly]`
- `thread-list` — 3: `ng-template[rtThreadListRow]`, `ng-template[rtThreadListFilters]`,
  `ng-template[rtThreadListSearch]`
- `table` — 2: `ng-template[rtTableCard]`, `ng-template[rtTableRowActions]`
- `chat` — 1: `ng-template[rtChatMessageActions]`

## Sessions

### 2026-09-10

- Заведены задача RT-2021, ветка от ветки эпика, папка задачи; колонка передвинута в работу
- Записаны разбор и замысел на шесть этапов
- Этап 1: собран перечень. Объявлено 116 компонентов и директив кита, заголовков историй 91,
  своей истории нет у 30. Семь из карточки в списке есть; остальные 23 — маркеры проекции
  содержимого семейств `container`, `toolbar`, `workspace`, `thread-list`, `chat`, `table`
- `node tools/check-kit-coverage.mjs` — семейств 74, доходят 73: счёт по семействам этих 30 не видит
- Этап 2: решение по каждому из 30 — семь получают свою историю, 23 запись с причиной
- Заодно сосчитаны кадры: на диске 549, историй со снимком 468, ширинных вариантов 81, кадров без
  своей истории нет ни одного, одинаковых по байтам нет ни пары
