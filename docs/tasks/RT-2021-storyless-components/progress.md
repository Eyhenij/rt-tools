# Progress

## Where we stand

- **State:** `этап-идёт`
- **Stage:** 1 of 6 — перечень компонентов без своей истории
- **Done:** список собран разовым скриптом: своей истории нет у 30 компонентов, а не у семи
- **Next step:** решение по каждому — своя история или запись в перечне с причиной
- **Uncommitted:** нет
- **Waiting for the owner:** нет
- **PR:** ещё не открыт

## Decisions along the way

- **Показ пишется разметкой, а не обёрткой с осями входа** — у этих компонентов осей входа нет.
  Affected stage of the plan: 3.
- **Считаем не по вхождению имени в разметку, а по тому, что называет заголовок истории** — по
  первому счёту семь из карточки выпадали из списка: их имена стоят в обёртке родителя, а своей
  истории у них нет. Affected stage of the plan: 1.

## Sessions

### 2026-09-10

- Заведены задача RT-2021, ветка от ветки эпика, папка задачи; колонка передвинута в работу
- Записаны разбор и замысел на шесть этапов
- Этап 1: собран перечень. Объявлено 116 компонентов и директив кита, заголовков историй 91,
  своей истории нет у 30. Семь из карточки в списке есть; остальные 23 — маркеры проекции
  содержимого семейств `container`, `toolbar`, `workspace`, `thread-list`, `chat`, `table`
- `node tools/check-kit-coverage.mjs` — семейств 74, доходят 73: счёт по семействам этих 30 не видит
