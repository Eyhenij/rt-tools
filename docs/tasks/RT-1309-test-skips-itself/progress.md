# Где стоим

- **Состояние:** `этапы-кончились`
- **Задача:** RT-1309 — тест, снимающий себя по состоянию экрана

## Сделано

- Разбор груза закрыт: одна запись в работу.
- В закон встали две статьи: снятие по тому, что тест застал на экране, и требование к тесту
  приводить экран сам, а общее состояние возвращать таким, каким застал.
- Правило проверяемости согласовано с новой статьёй: сказано, что считается состоянием стенда, а
  что — чужим следом.
- Прогоны зелёные: раскладка, сверка спеков, наборы оболочки, длина файлов, адреса документов.
  Находки слога в обоих файлах прежние, к этой правке отношения не имеющие.
