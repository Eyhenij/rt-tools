# Замысел

Задача: #671. Ветка: `RT-671-override-heading-in-sample`.

**Задача:** RT-671 · **Ветка:** RT-671-override-heading-in-sample
**Поведение:** не меняется — правится текст скила пакета, кода приложения работа не задевает.

После написания этот файл не правится. Пересмотр этапа идёт в `progress.md` решением по ходу.

## След задачи

| Что            | Где                                                                         |
| -------------- | --------------------------------------------------------------------------- |
| Ресурсы пакета | `projects/agent-kit/assets/skills/agent-kit-extend.md`                      |
| Правила        | `.claude/skills/agent-kit-extend/` — разложенная копия, правится раскладкой |
| Спек домена    | `docs/specs/agent-kit/` — описывает сам пакет; смотрится на закрытии        |
| Код            | не задевается: разбор разделов верен и покрыт спекой                        |

## Что считается сделанным

- Паттерн надстроек называет четвёртый случай: заголовок, стоящий внутри примера, разделом не
  является, и надстройка с таким именем уезжает новым разделом в конец.
- Назван признак промаха: два одинаковых заголовка в разложенном файле при зелёной раскладке.
- Сказано, чем такая правка делается вместо этого — заголовком того раздела, внутри которого
  лежит нужный абзац.
- Ловушка стоит в списке ловушек, а не только в теле раздела.
- Правки разложены в дерево тем же коммитом, `pnpm run agent-kit:check` зелёный, набор пакета
  зелёный.
- Предложение #626 закрыто со ссылкой на этот отчёт.

## Этапы

### 1. Четвёртый случай в таблице исходов

- **Что делается:** раздел о слиянии надстройки в паттерне надстроек получает абзац о
  заголовке из примера и способ дотянуться до нужного абзаца.
- **Признак готовности:** по разделу видно, почему надстройка не сработала и что писать вместо
  неё.
- **Чем проверяется:** чтение раздела целиком, `pnpm run check:docs`.

### 2. Ловушка

- **Что делается:** в список ловушек паттерна дописывается признак промаха.
- **Признак готовности:** ловушка называет то, что видно глазами в разложенном файле.
- **Чем проверяется:** чтение списка.

### 3. Раскладка, проверки, отчёт

- **Что делается:** правка раскладывается, гоняется набор пакета, открывается отчёт,
  предложение #626 закрывается.
- **Признак готовности:** `pnpm run agent-kit:check` зелёный, набор зелёный, отчёт открыт.
- **Чем проверяется:** `pnpm run agent-kit:sync`, `pnpm run agent-kit:check`,
  `pnpm exec nx run-many -t lint typecheck test build -p @rt-tools/agent-kit`,
  `pnpm run check:docs`, `pnpm run check:board`.

## Чего эта работа не делает

- Не правит разбор разделов: ограда блока кода учтена намеренно и покрыта спекой.
- Не переименовывает образцы внутри ресурсов пакета.
- Не трогает требование о строке про отчёт — оно в пакете уже стоит.
