# Замысел

**Задача:** RT-1534 · **Ветка:** RT-1534-tree-allowance-outside-index
**Поведение:** не меняется — работа трогает слой правил и его привязку, кода приложения нет.

После написания этот файл не правится. Пересмотр этапа идёт в `progress.md` решением по ходу.

## След задачи

| Что       | Где                                            |
| --------- | ---------------------------------------------- |
| Правила   | `projects/agent-kit/assets/rules/task-flow.md` |
| Компаньон | `.claude/skills/task-flow/implementation.md`   |
| Спеки     | `docs/specs/agent-kit/work/`                   |

## Что считается сделанным

- Правило ведения работы несёт статью о том, что действующее на дерево лежит вне индекса, и
  прежняя статья об образце вобрана в неё, а не стоит рядом.
- Привязка компаньона названа заново: старой формулировки в правиле больше нет.
- Предел веса правила выдержан.

## Этапы

### 1. Статья правила

- **Что делается:** статья об образце переписывается в общую — о том, что действует на дерево, а
  не на правку; в неё входит и разрешение владельца работать вне эпика.
- **Признак готовности:** статья одна, обе прежние вещи под неё попадают, предел веса выдержан.
- **Чем проверяется:** `node tools/check-file-size.mjs` — «длиннее предела 0».

### 2. Привязка и раскладка

- **Что делается:** привязка компаньона переименована под новую формулировку, пакет собран,
  раскладка сверена.
- **Признак готовности:** сверка спеков расхождений по этой правке не находит.
- **Чем проверяется:** `npm run check:specs` и `pnpm run agent-kit:check` — без строк
  расхождения.

## Чего эта работа не делает

- Не заводит в пакет гарда эпика и файла разрешений: их у пакета нет вовсе.
- Не выбирает за владельца между двумя выходами разбора — закрепляет действующее умолчание.
