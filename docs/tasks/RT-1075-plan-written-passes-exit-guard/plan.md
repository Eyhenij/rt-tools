# Замысел

**Задача:** RT-1075 · **Ветка:** RT-1075-plan-written-passes-exit-guard
**Поведение:** не меняется — работа правит обвязку и тексты пакета правил, кода приложения не
трогает; так сказал владелец, поручив брать задачи с приёмника по одной

После написания этот файл не правится. Пересмотр этапа идёт в `progress.md` решением по ходу.

## След задачи

| Что     | Где                                |
| ------- | ---------------------------------- |
| Спеки   | `docs/specs/agent-kit/work/`       |
| Законы  | не задеваются                      |
| Правила | `.claude/skills/task-flow/`        |
| Код     | `projects/agent-kit/assets/hooks/` |

## Что считается сделанным

- Страж выхода хода не выпускает ход, объявивший состояние записанного замысла, и называет в
  отказе первый этап замысла.
- Статья правила ведения работы говорит: взятие задачи переводит работу в состояние, у которого
  обязательное действие уже другое.
- Сценарии обвязки закрывают обе ветки: отказ в состоянии записанного замысла и прежнее
  поведение в прочих состояниях.
- Набор обвязки зелёный, раскладка сходится.

## Этапы

### 1. Правка стража

- **Что делается:** ветка разбора состояния записанного замысла с отказом, называющим первый этап
- **Признак готовности:** страж отбивает такой ход и пропускает прочие
- **Чем проверяется:** `bash projects/agent-kit/tests/turn-exit-guard.test.sh` — сценарии зелёные

### 2. Сценарии и спек

- **Что делается:** сценарии на обе ветки и их привязка в спеке работы пакета
- **Признак готовности:** сценарии стоят в спеке, заголовки тестов их называют
- **Чем проверяется:** `npm run check:specs` — молчание сверки

### 3. Статья правила и закрытие

- **Что делается:** статья о взятии задачи, раскладка, набор обвязки, разбор папки задачи
- **Признак готовности:** папка разобрана, набор зелёный; заявка ждёт утра — владелец просил
  ветки наружу не отправлять
- **Чем проверяется:** `bash projects/agent-kit/tests/run.sh` — строка о зелёных наборах

## Чего эта работа не делает

- Не судит так же состояния разбора и договорённости: их обязательное действие — писать текст,
  и он же и есть та правка, которую страж видит.
