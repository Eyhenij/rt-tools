# Замысел

**Задача:** RT-848 · **Ветка:** RT-848-table-sort-checked
**Поведение:** меняется — несовпавшее имя колонки наружу больше не уходит
**Спек:** `docs/specs/ui-kit/` — договорённости о таблице первого кита в дереве нет, поэтому
поведение описано здесь и в теле заявки

## След задачи

| Что    | Где                                                                             |
| ------ | ------------------------------------------------------------------------------- |
| Код    | `projects/ui-kit/src/lib/ui-kit/table/components/table/rtui-table.component.ts` |
| Спека  | рядом с компонентом, если она есть                                              |
| Задача | #848                                                                            |

## Что считается сделанным

- Приведения `as ISortModel<SORT_PROPERTY>` в методе нет.
- Имя колонки сверяется с набором колонок таблицы, и несовпавшее не отдаётся.
- Сборка кита и его спеки зелёные.

## Этапы

### 1. Проверка вместо приведения

- **Что делается:** метод сверяет `propertyName` с `columns()` и молчит на несовпадении.
- **Признак готовности:** приведения в файле нет, типы сходятся.
- **Чем проверяется:** `pnpm exec nx build @rt-tools/ui-kit` — сборка проходит.

### 2. Поведение закрыто пробой

- **Что делается:** спека на оба исхода — совпавшее имя уходит, чужое нет.
- **Признак готовности:** обе пробы зелёные.
- **Чем проверяется:** `pnpm exec nx test @rt-tools/ui-kit --testFile=<путь спеки>` — 0 провалов.

## Чего эта работа не делает

- Не заводит сообщения об ошибке: кит нигде в консоль не пишет.
- Не делит крупные файлы второго кита — это задача #849.
