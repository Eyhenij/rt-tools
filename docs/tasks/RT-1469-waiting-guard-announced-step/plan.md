# Замысел

**Задача:** RT-1469 · **Ветка:** RT-1469-waiting-guard-announced-step
**Поведение:** не меняется — работа правит обвязку слоя правил; кода приложения не касается

После написания этот файл не правится. Пересмотр этапа идёт в `progress.md` решением по ходу.

## След задачи

| Что      | Где                                                     |
| -------- | ------------------------------------------------------- |
| Гард     | `projects/agent-kit/assets/hooks/waiting-turn-guard.sh` |
| Проба    | `projects/agent-kit/tests/waiting-turn-guard.test.sh`   |
| Сценарий | `docs/specs/agent-kit/guards/scenarios.md`              |

## Что считается сделанным

- Ход, объявивший своё следующее действие и не сделавший по нему ни правки, ни вызова, гардом
  отбивается, и отказ называет объявленное.
- Ход, где после объявления работа шла, проходит по-прежнему.
- Сценарий с номером стоит в спеке, пробы его закрывают, набор проб пакета зелёный.

## Этапы

### 1. Признак объявленного своего шага

- **Что делается:** набор образцов будущего времени в последней части хода и ветка вердикта.
- **Признак готовности:** `bash -n` молчит, прежние сценарии не краснеют.
- **Чем проверяется:** `bash projects/agent-kit/tests/waiting-turn-guard.test.sh`.

### 2. Сценарий и пробы

- **Что делается:** сценарий в спеке гардов, пробы на отказ и на проход.
- **Признак готовности:** проба падает без правки и проходит с ней.
- **Чем проверяется:** `npm run check:specs`, набор проб пакета.

## Чего эта работа не делает

- Не судит смысл объявления: набор образцов ловит форму, а не намерение.
