# Замысел

**Задача:** RT-546 · **Ветка:** RT-546-kit-directives
**Поведение:** не меняется — проверка слоя правил и её набор признаков

После написания этот файл не правится. Пересмотр этапа идёт в `progress.md` решением по ходу.

## След задачи

| Что      | Где                                              |
| -------- | ------------------------------------------------ |
| Проверки | `projects/agent-kit/assets/checks/signals.mjs`   |
| Правила  | `projects/agent-kit/assets/rules/reuse-first.md` |
| Спеки    | `docs/specs/agent-kit/checks/reuse/`             |
| Пробы    | `projects/agent-kit/tests/checks-reuse.test.sh`  |

## Что считается сделанным

- Нативный тег с объявленной директивой дерева признаком не считается.
- Дерево без объявленных директив ведёт себя как прежде.
- Своё вырезание набора остаётся в силе.
- Сценарий покрыт пробой, правило и спека называют настройку.

## Этапы

### 1. Вырезание директив в загрузке признаков

- **Что делается:** `withKitDirectives` дописывает вырезание к признакам нативных тегов
- **Признак готовности:** четыре пробы сценария зелёные
- **Чем проверяется:** `bash projects/agent-kit/tests/checks-reuse.test.sh` — 12 ok, 0 провалов

### 2. Правило, спека, раскладка

- **Что делается:** строка в правило, статья и сценарий в спеку, привязка, раскладка
- **Признак готовности:** сверка спеков и раскладки молчит
- **Чем проверяется:** `npm run check:specs` — расхождений нет

## Чего эта работа не делает

- Своих директив этому дереву не объявляет: второй дизайн-системы здесь нет.
