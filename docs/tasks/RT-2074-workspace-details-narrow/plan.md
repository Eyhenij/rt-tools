# Plan

**Task:** RT-2074 · **Branch:** RT-2074-workspace-details-narrow
**Behaviour:** unchanged — правка трогает показы витрины, а не кит. Что видит потребитель пакета,
не меняется.

## Task footprint

| What  | Where                                                                |
| ----- | -------------------------------------------------------------------- |
| Code  | `projects/ui-kit-v2/src/lib/components/workspace/stories/component/` |
| Rules | `.claude/skills/rt-tools-storybook/`                                 |

## What counts as done

- Кадр показа рабочего места кончается вместе с ящиком показа, а не тянется на тысячи точек вниз.
- Эталоны показов рабочего места и их кадры порогов пересняты после осмотра.
- Причина записана там, где её прочитают: правило показа получает статью о компоненте, который
  берёт высоту от окна.

## Stages

### 1. Ящик показа становится границей кадра

- **What is done:** ящику показа рабочего места задан обрез переполнения, и рабочее место больше
  не выходит за него в кадр.
- **Readiness sign:** нарисованное у показа `Slots` на пороге 768 не выше рамки корня показа.
- **Verified by:** замер своим скриптом до правки и после — высота нарисованного в точках.

### 2. Осмотр и пересъёмка

- **What is done:** кадры показов рабочего места осмотрены, эталоны пересняты.
- **Readiness sign:** съёмка зелёная, кадров без своего показа нет.
- **Verified by:** `node tools/visual-snapshots-v2.mjs` — расхождений нет, сирот нет.

### 3. Причина записана

- **What is done:** правило показа получило статью о компоненте, который берёт высоту от окна, и
  привязку к коду рядом.
- **Readiness sign:** статья стоит в правиле, привязка — в файле рядом с ним.
- **Verified by:** `node tools/check-specs.mjs` — расхождений нет.

## What this work does not do

- Не трогает правило кита `block-size: calc(100dvh - …)`: для приложения оно верно.
- Не чинит показы вне матриц — это задача RT-2075.
- Не трогает первый кит.
