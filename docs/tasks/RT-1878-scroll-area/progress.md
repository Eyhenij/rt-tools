# Progress

## Where we stand

- **State:** `замысел-записан`
- **Stage:** 1 of 6 — Договорённость записана
- **Done:** ветка отведена от ветки эпика, карточка в работе, разбор и план записаны
- **Next step:** написать поддомен `docs/specs/ui-kit-v2/scroll-area/`
- **Uncommitted:** папка задачи
- **Waiting for the owner:** нет
- **PR:** не открыта

## Decisions along the way

- **Разбор написан до заведения ветки, черновиком вне истории.** Ветку было не отвести, пока ветка
  эпика не ушла на хост, а она ждала слияния правки #2131. Затронут этап: 1.

## Sessions

### 2026-09-15

- Прочитан образец: 757 строк в 11 файлах, из них 185 — компонент с тремя директивами слотов.
- Замерено, что второй кит уже даёт: `rtBlock`/`rtElem` из `@rt-tools/core` (83 файла второго
  кита), `TNullable` из `@rt-tools/utils` (14), семьи `icon` и `tooltip`.
- Сняты исходные числа для признаков готовности: `check:specs` — сценариев 1600, покрыто 1447;
  `check-kit-coverage` — семей 74, на витрине 73, со страницей входов 71.
