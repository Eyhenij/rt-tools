# Progress

## Where we stand

- **State:** `этапы-кончились`
- **Stage:** 6 of 6 — закрыт
- **Done:** все шесть этапов закрыты
- **Next step:** разобрать папку задачи и открыть заявку в ветку эпика
- **Uncommitted:** нет
- **Waiting for the owner:** нет. Перенос этой папки согласован 15 сентября 2026 года: «доделывай
  текущую задачу». Перенос таблицы обсуждается отдельно, и сам я его не начинаю
- **PR:** не открыта

## Decisions along the way

- **У перенесённой семьи есть инструкция по переходу, и этого шага в плане эпика не было.** Слово
  владельца 15 сентября 2026 года: при переносе каждого компонента дописывать инструкцию по
  миграции с первого кита, которую читает приложение. Инструкция стоит последним разделом
  страницы-обзора семьи — там её и найдёт тот, кто переписывает экран; шаг 7 дописан в ритм
  переноса плана эпика, и семья без такой инструкции перенесённой не считается. Затронут этап: 6.

- **Работа начата без согласования, и это промах исполнителя.** Владелец просил обсудить перенос
  первого кита до начала; отчёт о состоянии за обсуждение не считается, и отказ стража вопроса
  согласованием не был. Дальше каждая папка модуля согласовывается до начала — слово владельца
  15 сентября 2026 года. Затронут этап: 4.

- **Кегль значка приведён к первому киту.** При сличении оказалось: в первом ките значок 1.25rem
  — 20 точек, а у меня стоял шаг `sm` — 16. Расхождение кегля план считает дефектом переноса,
  поэтому взят шаг `md` (те же 20), и четыре эталона пересняты. Затронут этап: 6.
- **Признак непоказанного снизу сличить не с чем, и это сказано словами.** Все пять кадров
  прокрутки первого кита сняты с выключенным признаком: ни полосы растушёвки, ни значка на них
  нет. Сличены три части и их отступы; сам признак сверен с кодом первого кита, а не с кадром.
  Затронут этап: 6.
- **Вход о признаке берёт приведение `booleanAttribute`.** Проверка перед отправкой нашла: без
  него голый атрибут `isScrollHintShown` дал бы пустую строку, а это ложь. В первом ките вход
  объявлен без приведения; во втором это требование договорённости. Затронут этап: 6.
- **Папку задачи я разобрал до зелёной проверки, и пришлось вернуть.** Разбор снимает план, а без
  плана правку кода уже не сделать — а проверка нашла дефект входа. Разбор идёт после зелёной
  проверки, не до. Затронут этап: 6.
- **Восьмое правило семьи пробой не закрывается, и сценарий несёт пометку с причиной.** Правило
  про то, что полоса пропускает указатель, а значок ловит, проверяется вычисленными стилями, а
  среда пробы стилей компонента не применяет вовсе — преобразование спеки их снимает, и
  `getComputedStyle` там отвечает про узел без правил. `SC-UKV-155` помечен «Не покрыто» с этой
  причиной; пара подтверждается измерением на поднятой витрине, этап 6. Затронут этап: 3.
- **Признак готовности этапа 1 читается его пояснением, а не первой строкой.** В плане строка
  признака и пояснение под ней разошлись: сценарии без пробы стоят в отказе до этапа 3, и это
  законно — пояснение так и говорит. Сейчас `check:specs` даёт 18 расхождений, и все восемнадцать
  — это девять новых сценариев без пробы и девять привязок в ещё не написанный каталог. Затронут
  этап: 1.
- **Разбор написан до заведения ветки, черновиком вне истории.** Ветку было не отвести, пока ветка
  эпика не ушла на хост, а она ждала слияния правки #2131. Затронут этап: 1.

## Sessions

### 2026-09-15

- Прочитан образец: 757 строк в 11 файлах, из них 185 — компонент с тремя директивами слотов.
- Замерено, что второй кит уже даёт: `rtBlock`/`rtElem` из `@rt-tools/core` (83 файла второго
  кита), `TNullable` из `@rt-tools/utils` (14), семьи `icon` и `tooltip`.
- Сняты исходные числа для признаков готовности: `check:specs` — сценариев 1600, покрыто 1447;
  `check-kit-coverage` — семей 74, на витрине 73, со страницей входов 71.
- Этап 1 закрыт: `docs/specs/ui-kit-v2/scroll-area/` — три файла, девять правил, девять сценариев.
- Этап 2 закрыт: `pnpm exec nx run-many -t lint build -p @rt-tools/ui-kit-v2` — «Successfully ran
  targets lint, build», 2 минуты 3 секунды. Линтер второго кита отбивает `@angular/material`, и
  ни одного такого импорта в семье нет: значок взят `rt-icon`, подсказка — `rtTooltip`.
- Этап 6 закрыт. Сличено с `components-scrollable--all-parts.png` первого кита: отступы совпали
  до точки — шапка 8 на 16, тело 16, подвал 16. Расхождение нашлось одно, кегль значка 16 против
  20; исправлено, кадры пересняты, сдвиг замерен — 170 точек на кадре 1248 на 370, это 0.037 %.
  `node tools/visual-gate.mjs ui-kit-v2` — 572 снимка из 572.
- Этап 5 закрыт: показы просмотрены глазами на поднятой витрине, потом сняты четыре эталона —
  `parts`, `hint`, `presets`, `themes`. `node tools/visual-gate.mjs ui-kit-v2` — 170 наборов, 551
  проверка, 572 снимка, осиротевших эталонов нет; обе пробы обвязки отвечают.
- Этап 4 закрыт: `node tools/check-kit-coverage.mjs` — «families of the second kit 75, reaching
  the showcase 74», «families that have one 72». До работы было 74, 73 и 71. Три маркера частей
  названы в списке с причиной: своей разметки у них нет, в кадре видно, как область разложила
  содержимое. `pnpm exec nx run-many -t lint typecheck` — «Successfully ran targets lint,
  typecheck».
- Этап 3 закрыт: `npm run check:specs` — «domains 6, scenarios 1609 — covered 1455, partial 16,
  without tests 138». До работы было 1600 и 1447: девять сценариев прибавилось, восемь закрыто
  пробой, девятый несёт пометку. Расхождений ноль — привязки читаются. Весь кит:
  `pnpm exec nx test @rt-tools/ui-kit-v2` — 1543 пробы из 1543.

## Handover of the session

Put together by a hook before the compaction of the context (auto).

**Working tree:** /Users/sviatoslavkhutornoy/WebstormProjects/rt-tools
**Branch:** RT-1878-scroll-area

### Where we stand at the minute of the compaction

- **State:** `этап-идёт`
- **Stage:** 6 of 6 — закрыт
- **Next step:** разобрать папку задачи и открыть заявку в ветку эпика
- **PR:** не открыта

The progress in full — `docs/tasks/RT-1878-scroll-area/progress.md`; the plan lies next to it.

### Uncommitted

```
none
```

### Commits over the main branch

```
a2513b321 fix(rt:ui-kit-v2): кегль значка прокрутки приведён к первому киту, вход берёт приведение
17a5432df Merge branch 'RT-1870-one-kit' into RT-1878-scroll-area
3fb8abe50 Merge remote-tracking branch 'origin/main' into RT-1870-one-kit
5ee513967 test(rt:ui-kit-v2): эталоны семьи прокрутки сняты
1150e9a2a feat(rt:ui-kit-v2): витрина показывает семью прокрутки
308e4103e test(rt:ui-kit-v2): пробы семьи прокрутки, работа остановлена до согласования
89bb3ee73 feat(rt:ui-kit-v2): семья прокрутки перенесена на примитивы второго кита
f948f7cc0 docs(rt:ui-kit-v2): договорённость о прокрутке записана до кода
c6382878e docs(rt:ui-kit-v2): заведена папка задачи RT-1878, разбор и план записаны
a8f9c5309 Merge remote-tracking branch 'origin/main' into RT-1870-one-kit
cb14c63b7 docs(rt:ui-kit-v2): в состоянии эпика записана передача сессии
f5ef6ff36 Merge remote-tracking branch 'origin/main' into RT-1870-one-kit
d373b433c [RT-2102] Рабочий стол не рисует панель, для которой не объявлен шаблон (#2128)
5d4db1852 docs(rt:ui-kit-v2): папка задачи RT-2102 разобрана
21fe46f7f docs(rt:ui-kit-v2): край без слота списка написан на странице обзора, эталоны сходятся
701a06ec7 fix(rt:ui-kit-v2): рабочий стол не рисует панель, для которой не объявлен шаблон
6c3d741e7 docs(rt:ui-kit-v2): папка задачи RT-2102 заведена, разбор и план записаны
af04b5342 Merge remote-tracking branch 'origin/main' into RT-1870-one-kit
f64ef92a7 [RT-1951] Страница с входами во всех семействах зовётся одним именем (#2126)
87bd0b9bf Merge branch 'RT-1870-one-kit' into RT-1951-playground-one-name
```

Written by a hook before the compaction of the context. Everything standing here is checked
against the tree: a handover retells what was written and describes the minute it was put together.
