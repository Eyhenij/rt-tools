# Замысел

**Задача:** RT-1788 · **Ветка:** RT-1788-guard-deadlocks
**Поведение:** не меняется — работа правит гарды слоя правил, кода приложения не касается.

## След задачи

| Что     | Где                                 |
| ------- | ----------------------------------- |
| Спеки   | `docs/specs/agent-kit/work-guard/`  |
| Законы  | `docs/constitution/work-conduct.md` |
| Правила | `.claude/skills/task-flow/`         |
| Код     | `projects/agent-kit/assets/hooks/`  |

## Что считается сделанным

- Снятие просроченных записей описания прошлого проходит гард хода работы в ветке без папки
  задачи.
- Правка живой записи того же каталога гард по-прежнему судит.

## Этапы

### 1. Команда снятия выводится из-под гарда

- **Что делается:** имя команды снятия объявляется профилем, разбор вызова её не судит.
- **Признак готовности:** вызов проходит, а правка файла того же каталога отбивается.
- **Чем проверяется:** `bash projects/agent-kit/tests/task-flow-guard.test.sh` — «0 провалов».

### 2. Отметка груза и разбор папки

- **Что делается:** три записи приёма переводятся в починку, папка разбирается.
- **Признак готовности:** записей темы в новых нет, запись архива лежит.
- **Чем проверяется:** `node tools/cargo-pull.mjs --kind postmortem --state new --size 20` —
  записей темы нет.

## Чего эта работа не делает

- Разборов о ходе работы не касается: они заведены задачей RT-1789.
