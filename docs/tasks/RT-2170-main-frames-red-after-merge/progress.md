# Progress

## Where we stand

Rewritten by every session, not appended to.

- **State:** `этап-идёт`
- **Stage:** 1 of 4 — Найти величину, по которой расходится растр знаков
- **Done:** главная краснеет на собственных эталонах — 24 падения из 123 на ветке от её вершины.
  Три подозреваемых отпали: аргументы отрисовки знаков, сборка браузера, неустойчивость поднятий
- **Next step:** проверить смешивание: снять одну и ту же страницу с полупрозрачным слоем над
  текстом и без него и посмотреть, в каком из двух случаев растр держится
- **Uncommitted:** папка задачи
- **Waiting for the owner:** нет
- **PR:** not open yet

## Decisions along the way

- **Ветка отведена от главной, не от ветки эпика** — чинится состояние самой главной, и эпик до
  неё дойдёт нескоро. Affected stage of the plan: все.

## Sessions

### 16 сентября 2026

- Расхождение измерено на ветке RT-2165: со стороны главной сквозной набор админки падает на 21
  кадре, витрина первого кита на 5 из 88; со стороны ветки 123 из 123 и 88 из 88
- Пересъёмка кадров админки на дереве с влитой главной не переписала ни одного файла
- Кадр `shell.png` главной и кадр ветки: размер один, 1440 x 1739, вес 185 954 против 184 660
  байт, подсвечены все знаки подряд — расходится растр букв, а не разметка
- Обвязку набора и стили админки ветка против главной не меняла; браузеру набор называет три
  величины растра, отрисовку знаков не называет ни один из трёх наборов кадров дерева
- Все 22 кадра админки и 5 кадров первого кита сравнены по точкам через перегон в TIFF: сторона
  главной и сторона ветки различаются у всех 27
- Аргументы `--font-render-hinting=none`, `--disable-font-subpixel-positioning`,
  `--disable-lcd-text` и все три сразу дают один растр: `32c16b3e846d` во всех пяти случаях
- Две сборки браузера дерева — chromium-1234 (151.0.7922.34) и chromium-1243 (153.0.8010.12) —
  тоже дают один растр
- Шесть поднятий браузера подряд: растр `55e113c2b855`, текст 396,23, поле 168 — величина стоит
  намертво. Неустойчивость, которую ловила RT-2165, сейчас не воспроизводится
- **Расхождение садится под полупрозрачным слоем.** На разностном кадре `columns-panel` верх
  страницы — тот, что накрыт затемнением открытой панели, — засыпан красным по каждому знаку,
  а низ, где затемнения нет, дал лишь бледные контуры. Похоже на округление при смешивании, а
  не на отрисовку знаков как таковую
- **Главная краснеет на себе самой.** Ветка от вершины главной, её же эталоны, эта машина:
  `pnpm exec nx run message-bus-admin-e2e:e2e` — 24 падения, 99 прошли. Расхождения от 1254 до
  14 084 точек, доля 0,01 у всех. Значит дело не в слиянии и не в ветке эпика
