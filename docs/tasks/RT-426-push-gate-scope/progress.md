# Ход работы

## Где стоим

Перезаписывается каждым заходом, а не дописывается.

- **Этап:** 5 из 5 — раскладка и сведение, сделан
- **Сделано:** все пять этапов. Драфт спека, три статьи в три вида правила, два пункта и абзац
  в три вида паттерна отчёта, проверка `check-push-gate` со своим местом в наборе, профиль
  дерева и объявления одиннадцати шагов конвейера; набор перед отчётом прогнан
- **Следующий шаг:** решение владельца по двум вопросам — открывать ли отчёт и что делать с
  чужим коммитом `458e5dc7`, приехавшим в ветку от ветки-черновика
- **Незакоммиченное:** правка владельца в `docs/plans/agent-kit-observations.md` — в коммиты
  ветки не бралась; черновик соседней линии работ `docs/tasks/_draft-feedback-loop/`
- **Ждём владельца:** нет
- **Отчёт:** ещё не открыт

## Решения по ходу

- **Закон в ветке не правится, статья о пустом выводе несётся владельцу текстом.** Правило
  ведения работы говорит прямо: правило и спек правятся в ветке, закон — нет. Затронутый этап
  замысла: 2.

## Заходы

### 14 августа 2026

- Разобраны 6 записей предложений и 3 разбора происшествия; заведены 4 задачи — RT-426, RT-427,
  RT-428, RT-429.
- RT-426 взята в работу: ветка `RT-426-push-gate-scope`, колонка «В работе», папка задачи
  заведена.
- Разведка дала число, с которого начинается работа: в конвейере пять шагов, в гейте пуша этого
  дерева — две строки.
- Владелец выбрал механизм: сверка по объявленному списку, а не разбор файла конвейера машиной.
  Снимки витрин встают в гейт после RT-427.
- Драфт спека написан и проверен: `pnpm run check:specs` на нём молчит. Сама проверка при этом
  красна на 169 строках — это долг компаньонов правил, накопленный до ветки; предмет RT-428.
- Проверка `check-push-gate` заведена, покрыта двенадцатью утверждениями сценариев и стоит
  строкой в наборе гейта. На этом дереве: одиннадцать шагов конвейера, три закрыты набором,
  восемь объявлены исключениями с причиной.
- Сверка живая — проверено снятием: убрал сборку из набора, проверка тут же назвала шаг
  «Test, Lint, Typecheck, Build» и отдала единицу.
- Долг компаньона после ветки — 170 строк вместо 169. Прибавила одна статья, у которой якоря
  нет по существу: «после вливания набор пересматривается» держится чеклистом, и гарду не видно,
  что именно принесло вливание.
- Набор перед отчётом прогнан по файлу конвейера. Зелены: линт с типами, тестами и сборкой,
  линтер стилей, сверка собранных пакетов (задач нет), все наборы сценариев пакета,
  `check-doc-paths`, `check-push-gate`. Не гонялись снимки обеих витрин — объявлены исключением
  до RT-427. Красны `check-file-size` (те же десять файлов, что и до ветки), `check-board`
  (пять расхождений, все до ветки) и `check-specs` (170).
