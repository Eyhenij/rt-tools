# Замысел

**Задача:** RT-1196 · **Ветка:** RT-1196-app-config-icons-comment
**Поведение:** не меняется — правится комментарий, ни одной строки исполняемого кода работа не трогает

После написания этот файл не правится. Пересмотр этапа идёт в `progress.md` решением по ходу.

## След задачи

| Что     | Где                                                            |
| ------- | -------------------------------------------------------------- |
| Спеки   | `docs/specs/ui-kit-v2/icon-on-demand/` — читается, не правится |
| Законы  | не задеты                                                      |
| Правила | `.claude/skills/doc-style/`                                    |
| Код     | `apps/message-bus-admin/src/app/app.config.ts` — комментарий   |

## Что считается сделанным

- Комментарий описывает то, как кит работает сегодня: значок едет по запросу разметки.
- Верные части комментария — про адрес набора, подписи, службу темы — остались нетронутыми.
- Других текстов с тем же расхождением в дереве нет.

## Этапы

### 1. Комментарий приведён к киту

- **Что делается:** первый абзац переписывается по действующему поведению; остальные не трогаются.
- **Признак готовности:** в комментарии нет обещания загрузки набора вперёд.
- **Чем проверяется:** `grep -n "одним проходом" apps/message-bus-admin/src/app/app.config.ts` —
  пусто; `git diff --stat` — один файл.

## Чего эта работа не делает

- **Не трогает вызов `provideRtIcons('/icons')`** — он законен: сигнатуру при переводе кита
  сохранили, а адрес набора приложение называет само.
- **Не чинит мелкие значки у стороннего потребителя** — это другое дерево и другая причина.
