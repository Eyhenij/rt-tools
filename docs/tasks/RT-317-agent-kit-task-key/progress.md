# Ход работы

## Где стоим

Этап 1 закрыт: дерево знает свой ключ, гард ветки читает пакетную форму, все 89 задач
репозитория несут номер в заголовке. Следующее — этап 2, ключ задач как статья закона в пакете.

## Решения по ходу

- **Ветка заведена прежней формой и переименована после правки профиля.** Гард имени ветки — то
  самое, что чинится этой работой, и новую форму он отбивал; а править профиль на главной ветке
  не даёт гард замысла. Круг разомкнут заходом через законную на тот момент форму
  `chore/317-…` с немедленным `git branch -m`. Затронутый этап замысла: 1.
- **Заголовки отчётов не трогались.** Владелец выбрал охват «все issue репозитория»; отчёты в
  него не входят. Сверка очереди судит заголовки только у открытых отчётов, а таких сейчас нет.
  Затронутый этап замысла: 1.

## Заходы

### 2026-08-09

- Удалены три вмерженные локальные ветки; `origin` при этом держит четыре вмерженные — не тронуты,
  это правка удалённого дерева.
- Задача RT-315 закрыта: машинной учётной записи выдана роль `WRITER` на доску мутацией
  `updateProjectV2Collaborators` от токена владельца. `check:board` перестал отказывать и впервые
  напечатал состояние очереди — 64 расхождения.
- Из них 32 были одним дефектом: при пустом ключе проверка требовала `[-<номер>]`. Настройка
  дерева получила `taskKey: "RT"`, и эти строки ушли все.
- `rt_task_branch_ok` переписан на `RT-<номер>-<slug>` — ту же форму, что строит `task-new.mjs` и
  читает `numberFromBranch`. Три места сошлись, до этого расходились молча.
- Переименованы 89 задач репозитория, прогон идемпотентный: повторный проход печатает
  «уже с номером: 89».
- RT-313 и RT-314 поглощены RT-317 и закрыты.
- На заведении RT-317 всплыл четвёртый дефект пакета: `task-new` падает после того, как задача
  уже заведена, — `resolve`, `dirname` и `fileURLToPath` зовутся, но не импортированы. Дефект в
  самом ресурсе, а не в разложенной копии; дописан в тело задачи.
