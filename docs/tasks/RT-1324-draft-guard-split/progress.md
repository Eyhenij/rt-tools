# Где стоим

- **Состояние:** `этапы-кончились`
- **Задача:** RT-1324 — требование договорённости отделяется от требования папки

## Сделано

- Разбор груза закрыт: одна запись в работу.
- Гард договорённости заведён своим ресурсом на те же события; ярус договорённости из гарда
  хода работы снят целиком.
- Общий разбор вызова — путь правки, ветка, корень, папка задачи, замысел — вынесен помощником
  `hooks/task-flow-context.sh`: разложенный вторым разом, он расходился бы молча.
- Сценарии поделены: договорённость — в наборе нового гарда, остальное на месте. Заведены
  SC-AK-742, SC-AK-743, SC-AK-744.
- Правило и компаньоны названы: обе привязки договорённости ведут в новый гард.

## Найдено по ходу

- Гард спрашивал историю ветки относительным путём, а рабочий каталог вызова бывает
  подкаталогом дерева: `git ls-tree -- docs/tasks/<ветка>` из `projects/agent-kit/assets/hooks`
  не находил папку, и законная правка отбивалась требованием её закоммитить. Все обращения к
  истории переведены на `git -C "$root"`.

## Чем проверено

- `bash projects/agent-kit/tests/run.sh` — все наборы зелёные, из них гард замысла 54 ok, гард
  договорённости 18 ok.
- `pnpm exec jest --config projects/agent-kit/jest.config.ts --testPathPatterns assets` — 21 ok.
- `npm run agent-kit:sync`, `npm run agent-kit:check` — разложенное сходится с пакетом.
- `npm run check:specs` — расхождений нет, три новых сценария покрыты.
- `node tools/check-file-size.mjs`, `npm run check:docs`, `npm run check:boundary` — чисто.
