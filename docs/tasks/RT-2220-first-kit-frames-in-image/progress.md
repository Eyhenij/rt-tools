# Progress

## Where we stand

Rewritten by every session, not appended to.

- **State:** `этап-идёт`
- **Stage:** 4 of 4 — Конвейер и тексты дерева
- **Done:** этапы 1 и 2 закрыты. `KITS['ui-kit']` переведён на собранную витрину и браузер
  образа; заведён `projects/ui-kit/.storybook/test-runner-jest.config.js`; отсечка чужой сети в
  `projects/ui-kit/.storybook/test-runner.ts` научена своему же адресу витрины. Этап 3: 84
  эталона сняты заново образом, второй подъём дал 88 из 88. Этап 4: шаг конвейера зовёт ту же
  команду, тексты двух правил выправлены.
- **Next step:** отдать работу: разобрать папку задачи и открыть PR в ветку эпика.
- **Uncommitted:** правки оснастки, 84 эталона, шаг конвейера, два текста правил.
- **Waiting for the owner:** нет. Слово о снятии запрета дано: «делать чтобы избежать постоянного
  рассинхрона».
- **PR:** not open yet

## Decisions along the way

- **Довод у `judgesFrames` переписан, а не удалён** — витрина без образа остаётся возможной, и
  проверка о ней говорит вслух. Affected stage of the plan: 1.
- **Отсечка чужой сети берёт своё имя витрины из `STORYBOOK_URL`, а не из списка** — адрес витрины
  каждый запуск свой: порт свободный, имя машины сетевое. Список, знавший одну петлю, обрывал у
  себя же каждый отложенный кусок. Affected stage of the plan: 2.

## Sessions

### 2026-09-17

- Замер до правки: `node tools/visual-gate.mjs ui-kit` поднимал витрину и печатал «The frames of
  ui-kit are matched in the pipeline only», до сравнения не доходя.
- Этап 1: правка `tools/visual-gate.mjs`, синтаксис цел.
- Этап 2: первый запуск по новой дороге дал 88 отказов из 88, все `ChunkLoadError`. Причина —
  отсечка чужой сети в оснастке первого кита: она знала только петлю, а витрина раздаётся по
  сетевому имени машины, потому что `localhost` внутри образа — свой. Линтер по тронутому файлу
  чистый.
- Этап 3: съёмка до пересъёмки — 84 кадра из 88 разошлись; после — 88 из 88 зелёные, и второй
  подъём подряд дал те же 88 из 88.
- Этап 4: шаг `Visual tests` в конвейере зовёт `node tools/visual-gate.mjs ui-kit` вместо своего
  подъёма на порту 6106. Проверки: адреса документов 648 без расхождений, описания зелёные,
  длина файлов — 0 сверх предела.
