# Разбор просьбы

## Просьба владельца

> бери следующие задачи из бэклога по порядку начиная с самой свежей

Очередь работ отдала следующей задачу RT-837. Тело её приехало из дерева-потребителя на
редакции пакета 0.8.3 и называет два случая.

## Что уже есть в дереве

- Признак записи и разбор путей — `projects/agent-kit/assets/defaults/shell.sh`, функции
  `rt_shell_writes_default` и `rt_shell_paths_default`; профиль зовёт их из `defaults/project.sh`.
- Паттерн слияния — `projects/agent-kit/assets/patterns/git-workflow-merge.md`, блок команд и
  раздел «Частые промахи».
- Ловушка правила ведения работы: задача, отобранная по своему телу, бывает закрыта до начала
  работы — между заведением и взятием дерево правится соседними работами.

## Что уже сказано в правилах

Отбор задачи начинается с чтения кода, а разбор просьбы называет, что из тела задачи в дереве
ещё верно. Нынешняя редакция пакета — 0.20.0, тело задачи описывает 0.8.3.

## Вопросы и ответы

Вопросов владельцу нет: обе половины проверяются в дереве вызовом.

## Решения

- **Сперва проверяется, что из тела задачи ещё верно** — правка того, что уже починено, стоила
  бы второй статьи о том же.

## Что осталось невыясненным

- Нет.
