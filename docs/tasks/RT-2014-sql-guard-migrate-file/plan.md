# Plan

**Task:** RT-2014 · **Branch:** RT-2014-sql-guard-migrate-file
**Behaviour:** unchanged — работа правит слой правил и его проверки, приложения не касается

## Task footprint

| What  | Where                                                |
| ----- | ---------------------------------------------------- |
| Страж | `projects/agent-kit/assets/hooks/sql-guard-write.sh` |
| Пробы | `tools/tests/`                                       |
| Rules | `.claude/skills/agent-kit-source/`                   |

## What counts as done

- Цепочка «миграция на местный адрес и доставка файла флагом» получает вопрос, а не молчание.
- Одиночная миграция на местный адрес по-прежнему проходит молча.
- Проба на обе стороны стоит в наборе стража и краснеет на снятом условии.

## Stages

### 1. Признак второй записи считает и доставку файла

- **Steps:**
    1. досрочный выход берёт тот же признак записи файлом, каким его считает основная ветка
    2. проба на обе стороны: цепочка с доставкой файла спрашивает, одиночная миграция молчит
- **What is done:** страж перестал молчать на паре «миграция и доставка файла».
- **Readiness sign:** проба краснеет на снятом условии.
- **Verified by:** `bash tools/tests/run.sh` — 0 провалов.

## What this work does not do

- Не трогает разбор запроса и адресацию строк: там дефекта не найдено.
- Не заводит вопрос на одиночную миграцию — ради её молчания досрочный выход и существует.
