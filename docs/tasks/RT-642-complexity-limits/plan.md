# Замысел

Задача: #642. Ветка: `RT-642-complexity-limits`.

**Поведение:** не меняется — правится текст правила, кода в правке нет.

После написания этот файл не правится. Пересмотр этапа идёт в `progress.md` решением по ходу.

## След задачи

| Что     | Где                                                         |
| ------- | ----------------------------------------------------------- |
| Законы  | `docs/constitution/code-structure.md`                       |
| Правила | `projects/agent-kit/assets/rules/typescript-conventions.md` |

## Что считается сделанным

- Правило устройства кода называет пределы сложности функции своей строкой, рядом с пределом
  длины файла.
- Там же сказано, чем спрашивают действующие значения, и почему их не вспоминают.
- Утверждение написано условно: дерево, где плагин не поднят, оно не обманывает.

## Этапы

### 1. Пределы сложности записаны своей строкой

- **Что делается:** утверждение дописывается в раздел «Как закон применяется здесь», после
  пункта о длине файла.
- **Признак готовности:** правило называет предел и способ спросить действующие числа.
- **Чем проверяется:** `pnpm run agent-kit:check`, `node tools/check-doc-paths.mjs`.

### 2. Запись приёма закрывается ссылкой на задачу

- **Что делается:** #595 закрывается с указанием номера задачи.
- **Признак готовности:** записи нет в открытых.
- **Чем проверяется:** `gh issue list --label agent-kit-feedback --state open`.

## Чего эта работа не делает

- Не поднимает плагин в этом дереве и не трогает его конфиг линтера.
- Не переносит числа в текст: они меняются вместе с версией плагина.
