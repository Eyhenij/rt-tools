# Замысел

**Задача:** RT-1764 · **Ветка:** RT-1764-ui-kit-v2-utils-spec
**Поведение:** не меняется — правятся манифест пакета, скрипт публикации и лок-файл. Подтверждено владельцем: «да, публикуй новую версию».

После написания этот файл не правится.

## След задачи

| Что     | Где                                                                                                    |
| ------- | ------------------------------------------------------------------------------------------------------ |
| Правила | `.claude/skills/dependencies/`, `.claude/skills/git-workflow/`                                         |
| Код     | `projects/ui-kit-v2/package.json`, `update-version-utils.cjs`, `pnpm-lock.yaml`, `pnpm-workspace.yaml` |

## Что считается сделанным

- В реестре есть `@rt-tools/utils@0.4.0` с `EListSortOrder`.
- В реестре есть `@rt-tools/ui-kit-v2@0.8.1` с зависимостью `@rt-tools/utils ^0.4.0`.
- `update-version-utils.cjs` переписывает зависимость и у ui-kit-v2.
- `pnpm install --frozen-lockfile` на `main` проходит.

## Этапы

### 1. Публикация utils 0.4.0

- **Что делается:** ручной запуск `publish-utils.yml` на `main` с версией 0.4.0.
- **Признак готовности:** версия в реестре, коммит подъёма версии на `main`.
- **Чем проверяется:** `npm view @rt-tools/utils version` — печатает `0.4.0`.

### 2. Зависимость ui-kit-v2 и каскад

- **Что делается:** `^0.4.0` в `dependencies` и `peerDependencies` ui-kit-v2; ui-kit-v2 добавлен в
  `update-version-utils.cjs`; лок-файл и список исключений возраста пакетов синхронизированы.
- **Признак готовности:** установка с замороженным лок-файлом проходит, PR влит.
- **Чем проверяется:** `pnpm install --frozen-lockfile` — код возврата 0.

### 3. Публикация ui-kit-v2 0.8.1

- **Что делается:** ручной запуск `publish-ui-kit-v2.yml` на `main` с версией 0.8.1.
- **Признак готовности:** версия в реестре с нужной зависимостью.
- **Чем проверяется:** `npm view @rt-tools/ui-kit-v2@0.8.1 peerDependencies` — содержит `@rt-tools/utils: ^0.4.0`.

## Чего эта работа не делает

- Не добавляет проверку импортов собранного пакета против опубликованных соседей — отдельная задача.
- Не публикует core, store и ui-kit.
