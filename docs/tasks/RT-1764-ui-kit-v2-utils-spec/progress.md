# Ход работы

## Где стоим

- **Состояние:** `этапы-кончились`
- **Этап:** 2 из 3 — Зависимость ui-kit-v2 и каскад — закрыт; этап 3 идёт после слияния PR
- **Сделано:** этап 1 — `npm view @rt-tools/utils version` печатает `0.4.0`, коммит подъёма `e941fd9e1` в `main`; этап 2 — `^0.4.0` в манифесте ui-kit-v2, ui-kit-v2 в каскаде `update-version-utils.cjs`, замок и список исключений синхронизированы, `pnpm install --frozen-lockfile` — код 0
- **Следующий шаг:** разобрать папку, открыть PR черновиком; после слияния — `publish-ui-kit-v2.yml` с версией 0.8.1
- **Незакоммиченное:** папка задачи и правка
- **Ждём владельца:** слияние PR
- **PR:** ещё не открыт

## Решения по ходу

- **Папка переименована и номер проставлен вручную** — `task:new` завёл задачу, но остановился на добавлении на доску («Content already exists in this project») и папку не переименовал; `task:move` прошёл, задача на доске. Затронутый этап: подготовка.
- **Этап 3 выполняется после слияния** — публикация идёт с `main`, а не с ветки; в ветке этап закрыть нельзя. Затронутый этап: 3.

## Заходы

### 2026-09-03

- Разобрана жалоба потребителя: сверены опубликованные tarball utils 0.3.2 и ui-kit-v2 0.7.0/0.8.0.
- Публикация utils 0.4.0 запущена по слову владельца и прошла (прогон 33726970638); в tarball 0.4.0 `EListSortOrder` есть.
- Каскад в `main` поднял зависимость на utils у core, store и ui-kit; ui-kit-v2 не тронут — его в скрипте нет.
- Замок: сменились только записи `@rt-tools/*`, 59 строк. `pnpm install --frozen-lockfile` с первого раза упал на `rimraf .nx` (ENOTEMPTY — демон nx писал в каталог); после остановки демона прошёл.
