<!-- rt-kit v0.10.0 · samples/tasks/_template/progress.md · fc2dd20e34b0 · правится надстройкой, не здесь -->

# Ход работы

## Где стоим

Перезаписывается каждым заходом, а не дописывается.

- **Состояние:** `этапы-кончились`
- **Этап:** все четыре закрыты; восемнадцать сценариев покрыты, пометок в договорённости не
  осталось
- **Сделано:** индекс миграцией, приёмник с отбором и порядком, админка со столбцом и третьим
  отбором, сквозные спеки с замером узкого экрана
- **Следующий шаг:** прогнать гейт пуша целиком, запушить ветку и открыть PR черновиком
- **Незакоммиченное:** этап 4 — сквозные спеки, засев версий, перенос в тулбаре кита и
  пересня́тые эталоны
- **Ждём владельца:** ничего
- **PR:** нет

## Решения по ходу

- **Задача взята, пока #988 не влита** — разбор и договорённость от вливания не зависят, а
  правка кода зависит: столбцы, набор сортируемых полей и выборка адреса лежат в той ветке.
  Ветка отведена от главной; #988 вольётся в неё мержем, когда владелец её вольёт.
  Затронутый этап замысла: нет, замысел ещё не написан.

- **2026-08-21. Порядок по версии считает приёмник, а не хранилище.** Колонка строковая, и
  `orderBy` поставил бы `0.10.0` перед `0.9.0`. Страница при порядке по версии берётся двумя
  запросами: первый приносит признак и версию отобранных записей, второй — поля самой страницы
  по отобранным признакам. Отвергнут сырой SQL с `array_position` по упорядоченному массиву
  версий: порядок остался бы за хранилищем, но это был бы первый сырой запрос в дереве, а
  проверить его рукописным двойником базы нечем — сценарии порядка ушли бы в долг до сквозной
  спеки. Цена выбранного названа прямо в коде: при порядке по версии читаются два поля каждой
  отобранной записи, а не одной страницы. Затронутый этап замысла: второй — препятствия он не
  называл.
- **2026-08-21. Род груза переехал в общую либу.** Операция версий принимает его параметром, и
  слова стали договором двух сторон: своя копия набора у админки разошлась бы с приёмником
  молча. Перечисление домена груза снято, четыре его потребителя переведены на общее.
  Затронутый этап замысла: второй.

## Заходы

### 2026-08-21

- Ветка `RT-987-release-version-filter` заведена от свежей главной, задача переведена
  в `🏗 In progress`, папка задачи собрана.
- Разведка: поле `releaseVersion` уже стоит у обоих родов груза и прямо помечено в схеме
  как «отбор и порядок — работа своей задачи, индекса нет»; модель, маппер и панель записи
  версию уже несут; правила о законности версии живут в общей либе, приёмник её принимает и
  проверяет. Образец хода — работа RT-913 о состоянии, отданная заявкой #988.
- Четыре вопроса отвечены: порядок по номерам версии, в отборе стоит «Без версии», столбец
  виден сразу за состоянием, операция отдаёт все встретившиеся версии без предела.
- Из первого ответа вышло следствие: строка схемы хранилища «форма принадлежит дереву:
  приёмник её не разбирает» этой работой отменяется, и работе нужно правило для версии, не
  ложащейся в форму.
- Два оставшихся вопроса владельцу не задавались: гард разбора отбил их, и он прав — ответы
  лежат в спеках. Форма версии решена правилом «Формы версии приёмник не разбирает» спека
  правки состояния; признак закрытия и ненужность правки правил выведены из RT-913.
- По ходу нашлась неправда: спек правки состояния писал, что отбор и порядок по версии — работа
  RT-913. Задачу разделили словом владельца позже, чем эта строка была написана; строка
  исправлена на RT-987.
- Заявка #988 влита владельцем, главная подтянута и влита в эту ветку без конфликтов: правка
  кода больше ничем не заблокирована.
- Написана договорённость `docs/specs/message-bus/proposed/cargo-release-version-filter/` —
  восемнадцать сценариев `SC-MB-237`…`SC-MB-254`, все с пометкой «Не покрыто»: код ещё не
  написан. `node tools/check-specs.mjs` — код возврата 0.
- Написан замысел: четыре этапа — индекс миграцией, приёмник с версиями и порядком, админка со
  столбцом и третьим отбором, сквозные спеки с замером узкого экрана.
- Этап 1 закрыт: обеим таблицам груза заведён индекс по колонке версии выпуска миграцией
  `20260821223150_cargo_release_version_index`. Цепочка накатана с нуля на одноразовый
  контейнер (порт 55439 — рабочая база занимает 55432): все миграции применились подряд,
  `prisma migrate diff --exit-code` ответил «No difference detected», код возврата 0.
  Контейнер снят, клиент хранилища пересобран, местная база догнала ветку.
- Комментарий схемы больше не обещает, что индекса нет, а заметка спека правки состояния о том,
  что индексов колонки не несут, приведена к сделанному: у версии выпуска он есть, у текста
  починки — нет.
- Этап 2 начат. Готова общая либа: `release-version-order.ts` — чистые функции разбора и
  сравнения версий по номерам частей; выборка груза приняла версию и признак «без версии»
  (`CARGO_VERSION_NONE`), отбив не-строку и строку длиннее 64 знаков; версия встала в набор
  сортируемых полей обеих операций. `pnpx nx test message-bus-common` — 79 спек зелёных,
  красных нет; `pnpx nx lint message-bus-common` зелёный.
- **Решение по ходу принято не будет, пока не проверено:** порядок по версии Prisma обычным
  `orderBy` не даст — колонка строковая, и `0.10.0` встанет перед `0.9.0`. Замысел этого не
  называл. Разбирать это следующему заходу: либо запрос списка при порядке по версии идёт сырым
  SQL с `array_position` по упорядоченному массиву версий, либо порядок собирается иначе.
  Договорённость при этом не двигается: номера читаются при чтении списка, а не при приёме.

### 2026-08-21 (продолжение)

- Этап 2 закрыт. Общая либа: род груза `ECargoKind` с разбором параметра и отказом; порядок
  страницы по версии `releaseVersionPageIds` — записи без версии последними при возрастании и
  первыми при убывании, второй ключ прежний.
- Запросы обоих родов груза приняли версию: отбор сужает версией и «без версии», строка списка
  несёт версию, страница при порядке по версии собирается двумя запросами, заведён сбор
  встретившихся версий — пустые отсеивает и повторы сводит хранилище.
- Заведена операция `GET /api/cargo/versions` контроллером домена груза: род обязателен, отказ
  называет параметр и набор слов.
- Обеим либам запросов заведена первая спека: до этой правки ни одной в них не было, и прогон
  проходил зелёным ни на чём.
- Проверка этапа: `pnpx nx run-many -t test` по семи проектам — все зелёные, красных нет ни
  одной: 88 спек общей либы, 39 у разборов, 27 у предложений, 35 у домена груза, 13 у разбора
  тела, по 2 у обеих либ запросов. Линт по тем же проектам зелёный, `pnpx nx build message-bus`
  собирается.
- Договорённость получила строки привязки по приёмнику и пометки покрытия: пять сценариев
  перешли из «Не покрыто» в частичное, у оставшихся причина названа этапом работы.
  `node tools/check-specs.mjs` — код возврата 0.
- В коды отказов договорённости дописан отбой запроса версий без рода груза: контракт род
  называл, а отказ по нему — нет.

### 2026-08-21 (этап 3)

- Админка знает версию: она встала в выборку адреса и в сборку адреса обратно, третьим условием
  вошла в признак «сужен ли список», а смена версии сбрасывает страницу, не трогая двух других
  отборов.
- Заведены обращение за встретившимися версиями и стор при нём: набор у каждого рода груза свой,
  и запросы двух разделов друг друга не отменяют. Род груза раздел называет одним полем; раздел
  сводок не называет ничего, и версий не спрашивает.
- Заведён отбор `admin-version-filter` рядом с отбором по состоянию: первым пунктом «все версии»,
  вторым «без версии», дальше версии в порядке приёмника.
- Оба раздела груза получили столбец «В какой версии» сортируемым заголовком; у невыпущенной
  записи ячейка пуста. Версия переехала в строку списка обеих моделей и обоих мапперов.
- Подписи ушли в словарь приложения: один ключ на все показы версии — столбец, отбор и строка
  панели; прежний ключ панели переименован, чтобы объявление осталось одно.
- Проверка этапа: `pnpx nx run-many -t lint test` по девяти проектам админки — всё зелёное,
  красных спек нет; `pnpx nx build message-bus-admin` собирается. `node tools/check-specs.mjs` —
  код возврата 0: непокрытыми остались три сценария четвёртого этапа.

### 2026-08-21 (этап 4)

- Засев стенда получил версии: две числовые у разборов, своя у предложений; нечисловая там
  стояла и раньше. Сквозные спеки заведены двумя файлами — обычный экран и узкий.
- Замер узкого экрана нашёл дефект, которого не было видно глазами: третий отбор уезжал за
  правый край окна на 88 пикселей (499,8 при ширине 412) и обрезался прокруткой. Зона тулбара
  на узком экране стекала в колонку, а её содержимое переноса не имело — перенос объявлен в
  ките, у самой зоны: узкий экран — забота тулбара, а не потребителя.
- Спека `SC-MB-229` о пустом списке взяла другое состояние: выпущенные предложения на стенде
  теперь есть — ими проверяется отбор по версии, а готовых нет.
- Девять эталонов пересняты: расхождение разобрано по картинке — новый отбор в полосе и новый
  столбец со значениями, сдвинувший соседние столбцы. Строки и высоты не двигались.
- `npx playwright test` — 92 спеки зелёные, красных нет ни одной. `node tools/check-specs.mjs` —
  код возврата 0, все восемнадцать сценариев работы закрыты.
