# Замысел

**Задача:** RT-1830 · **Ветка:** RT-1830-spec-bullets-past-subheading
**Спек:** `docs/specs/agent-kit/texts/spec-checks/spec.md`
**Поведение:** меняется

После написания этот файл не правится. Пересмотр этапа идёт в `progress.md` решением по ходу.

## След задачи

| Что     | Где                                                 |
| ------- | --------------------------------------------------- |
| Спек    | `docs/specs/agent-kit/texts/spec-checks/`           |
| Код     | `projects/agent-kit/assets/checks/spec-common.mjs`  |
| Код     | `projects/agent-kit/assets/checks/spec-anchors.mjs` |
| Проба   | `projects/agent-kit/tests/checks-specs.test.sh`     |
| Правила | `.claude/skills/spec-driven/`                       |

## Что считается сделанным

- Подзаголовок внутри раздела список пунктов не кончает; таблица по-прежнему кончает.
- Отказ о пустом разделе называет, что в нём стоит вместо пунктов.
- Спек поддомена несёт правило, сценарии и привязки; проба зелёная; раскладка сходится.

## Этапы

### 1. Разбор пунктов

- **Что делается:** в источнике пакета строка заголовка внутри раздела пропускается, а не
  кончает список; отказ о пустом разделе цитирует первую непустую строку раздела.
- **Признак готовности:** сверка на дереве зелёная, на фикстуре с подзаголовками — тоже.
- **Чем проверяется:** `bash projects/agent-kit/tests/checks-specs.test.sh` — «разошлось: 0».

### 2. Спек и раскладка

- **Что делается:** правило, два сценария и привязки в поддомене сверки спеков; копия в
  дереве обновляется раскладкой.
- **Признак готовности:** сверка спеков код 0, раскладка сходится.
- **Чем проверяется:** `pnpm run agent-kit:check` — «разложенное сходится».

### 3. Отдача

- **Что делается:** папка задачи разбирается, заявка открывается черновиком чередой за
  RT-1828.
- **Признак готовности:** заявка открыта, прогон на вершине идёт.
- **Чем проверяется:** `gh pr view` — номер и состояние.

## Чего эта работа не делает

- Не меняет судьбу таблицы внутри раздела: она по-прежнему кончает список.
- Не отмечает запись груза «в работе»: запись у приёма числится за другим признаком дерева, и
  своим токеном её не двинуть; закрывается она издателем после слияния.
