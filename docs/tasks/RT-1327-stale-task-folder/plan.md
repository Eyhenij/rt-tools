# Замысел

**Задача:** RT-1327 · **Ветка:** RT-1327-stale-task-folder
**Поведение:** не меняется — из главной ветки уходит папка закрытой задачи, оставшаяся там по
пропущенному шагу закрытия. Ни правил, ни кода работа не трогает.

После написания этот файл не правится. Пересмотр этапа идёт в `progress.md` решением по ходу.

## След задачи

| Что         | Где                     |
| ----------- | ----------------------- |
| Папки задач | `docs/tasks/`           |
| Сверка      | `tools/check-board.mjs` |

## Что считается сделанным

- `npm run check:board` не называет папку закрытой задачи среди текущих.

## Этапы

### 1. Довезти уборку до главной ветки

- **Что делается:** коммит разбора папки, оставшийся в местной ветке слитой работы, переносится
  черри-пиком со ссылкой на исходный.
- **Признак готовности:** папки закрытой задачи в ветке нет, сверка очереди о ней молчит.
- **Чем проверяется:** `npm run check:board` — строки о папке закрытой задачи в выводе нет.

## Чего эта работа не делает

- Причину пропуска не чинит: шаг закрытия стережёт гард поставки, и он на месте.
- Прочих расхождений очереди работ не трогает: у каждого своя задача.
