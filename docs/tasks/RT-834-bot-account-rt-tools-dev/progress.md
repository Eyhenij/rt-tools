<!-- rt-kit v0.8.3 · samples/tasks/_template/progress.md · baf326afb38f · правится надстройкой, не здесь -->

# Ход работы

## Где стоим

Перезаписывается каждым заходом, а не дописывается.

- **Этап:** 1 из 1 сделан в рабочем дереве; осталось закоммитить, запушить и открыть PR
- **Сделано:** имена дерева переведены на `rt-tools-dev` и число `317887029` в трёх местах —
  `.claude/rt-kit/project.sh`, `.claude/rt-kit/checks.json`, компаньон правила поставки.
  Утверждение об ограничении прежней записи переписано в прошедшем времени: обход с чужим
  исполнителем снят, задачи заводит и ведёт новая запись. Проверено делом: задача RT-834
  заведена командой уже новой записью, и она же стоит её исполнителем
- **Что назначил владелец:** подтянуть главную ветку и работать из учётной записи `rt-tools-dev`
- **Следующий шаг:** коммит правки, пуш, PR черновиком, `npm run check:board`
- **Незакоммиченное:** вся правка имён дерева и папка задачи
- **Ждём владельца:** ничего
- **PR:** ещё не открыт

## Решения по ходу

- **18 августа 2026. Имя бота в настройке проверок поправлено до заведения задачи.** Команда
  `npm run task:new` читает логин оттуда и отказывала на скрытой прежней записи: завести задачу,
  не тронув имя, было нечем. Альтернатива — завести задачу через веб — отвергнута: заведённая
  мимо команды, она не попадает на борду и гардом не отбивается.

## Заходы

### 18 августа 2026

- Главная ветка подтянута: PR #832 влит коммитом `00eb37a5`, пять влитых веток сняты.
- PR #820 (задача RT-782, предыдущая в эпике) стоял конфликтующим после вливания #832: главная
  влита в его ветку, конфликт разобран в двух указателях — в таблице архива сохранены обе
  дописанные строки, в состоянии задач эпика оставлена свежая сторона. На ветке лежали два
  коммита мержа — свой и приехавший с хостинга; сведены в один. После пуша PR стал `MERGEABLE`,
  прогон пошёл.
- Проверки в ветке RT-782: `nx affected -t lint typecheck test build` зелёный на 11 проектах,
  сквозной набор — 49 тестов зелёных, `agent-kit:check` сходится, `check:docs` и `check:specs`
  без расхождений.
- Заход закрыт заполнением окна на 50%.
