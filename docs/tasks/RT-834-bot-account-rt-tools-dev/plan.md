<!-- rt-kit v0.8.3 · samples/tasks/_template/plan.md · c862bbaf5407 · правится надстройкой, не здесь -->

# Замысел

**Задача:** RT-834 · **Ветка:** RT-834-bot-account-rt-tools-dev
**Поведение:** не меняется — правятся имена дерева при правиле поставки; кода приложения работа
не касается, и договариваться о продукте не о чем

После написания этот файл не правится. Пересмотр этапа идёт в `progress.md` решением по ходу.

## След задачи

| Что                | Где                                                                                 |
| ------------------ | ----------------------------------------------------------------------------------- |
| Профиль поставки   | `.claude/rt-kit/project.sh` — `RT_COMMIT_EMAIL`                                     |
| Настройка проверок | `.claude/rt-kit/checks.json` — ключ `board.bot`                                     |
| Имена при правиле  | `.claude/skills/git-workflow/implementation.md`                                     |
| Разбор промаха     | `docs/postmortems/handled/2026-08-15-commit-signed-with-strangers-id.md` — читается |

## Что считается сделанным

- Имена дерева называют `rt-tools-dev` и её число `317887029`; прежней записи в них не осталось.
- Гард поставки принимает подпись новой записи, а команда заведения задачи ставит её исполнителем.
- Утверждение об ограничении прежней записи переписано в прошедшем времени и называет, что
  обход с чужим исполнителем снят.

## Этапы

### 1. Имена дерева называют новую запись

- **Что делается:** правятся профиль поставки, настройка проверок и компаньон правила поставки.
- **Признак готовности:** прежнего логина и прежнего числа в них нет; задача и ветка этой работы
  заведены уже новой записью.
- **Чем проверяется:** `npm run check:board`, гард поставки на пуше, `pnpm run agent-kit:check`.

## Чего эта работа не делает

- Перенос прежних задач и PR на новую запись: заведённое прежней записью остаётся как есть.
- Правка разборов происшествий: они описывают день, когда их писали.
