# План

**Task:** RT-2244 · **Branch:** RT-2244-waiting-guard-ci-rerun
**Spec:** `docs/specs/agent-kit/turn-guards/spec.md`
**Behaviour:** unchanged — правится проверка пакета правил, приложений не касается. Подтверждено владельцем.

## След задачи

| Что      | Где                                                                   |
| -------- | --------------------------------------------------------------------- |
| Описания | `docs/specs/agent-kit/turn-guards/` — spec, implementation, scenarios |
| Законы   | `docs/constitution/work-conduct.md`                                   |
| Правила  | `.claude/skills/turn-conduct/implementation.md`                       |
| Код      | `projects/agent-kit/assets/hooks/waiting-turn-guard.sh`               |
| Тесты    | `projects/agent-kit/tests/waiting-turn-guard.test.sh`                 |

## Что считается сделанным

- Ход, запустивший или перезапустивший CI и не поставивший команду ожидания его конца, не
  кончается; отказ называет команду ожидания.
- Ход с командой наблюдения за CI после запуска судится как прежде.
- Описание домена получило утверждение, привязку и сценарий SC-AK-1133 с тестом.

## Этапы

### 1. Признак и ярус

- **Что делается:** в `waiting-turn-guard.sh` образцы запуска CI и ожидания его конца, приговор
  `owe:watch` и его отказ; сценарий SC-AK-1133 в наборе: отказ на перезапуске без ожидания,
  пропуск с фоновым наблюдением, пропуск с блокирующим наблюдением, пропуск без перезапуска.
- **Признак готовности:** набор зелёный с новыми строками.
- **Verified by:** `bash projects/agent-kit/tests/waiting-turn-guard.test.sh` — итог набора без провалов.

### 2. Описание домена и привязка правила

- **Что делается:** утверждение, привязка, сценарий и история в описании домена; адрес нового
  признака в строке файла привязок статьи правила о конце CI.
- **Признак готовности:** проверка описаний без расхождений.
- **Verified by:** `npm run check:specs` — без расхождений.

### 3. Установка файлов из пакета и сдача

- **Что делается:** `pnpm run agent-kit:sync`, коммит, PR в ветку эпика, слияние в браузере,
  отметка груза «исправлено», папка задачи в архив.
- **Признак готовности:** PR влит, запись груза в состоянии fixed.
- **Verified by:** `pnpm run agent-kit:check` — без расхождений; `gh pr view --json state` — MERGED.

## Чего эта работа не делает

- Не спрашивает хостинг о состоянии CI.
- Не правит текст правила `turn-conduct`.
