# Plan

**Task:** RT-2165 · **Branch:** RT-2165-showcase-font-from-machine
**Behaviour:** unchanged — работа про кадры витрины и объявление шрифта, экранов приложений она не
трогает; так сказано в самой задаче, заведённой по измерению

## Task footprint

| What  | Where                                                              |
| ----- | ------------------------------------------------------------------ |
| Specs | `docs/specs/ui-kit-v2/`                                            |
| Rules | `.claude/skills/ui-component-tests/`                               |
| Code  | `projects/ui-kit-v2/src/styles/`, `projects/ui-kit-v2/.storybook/` |

## What counts as done

- Причина скачка ширины названа числом, а не подозрением: измерение бьёт по той же величине, что
  берёт браузер.
- Кадр поля переписки даёт одно и то же число при двух разных поднятиях витрины подряд.
- Владелец выбрал дорогу закрытия, и выбранное сделано.

## Stages

### 1. Причина доказана измерением по цифре

- **What is done:** измерение ширины знакоместа цифры каждым начертанием набора по отдельности и
  набором целиком, при двух поднятиях витрины подряд, с перезапуском между ними.
- **Readiness sign:** числа двух поднятий либо разошлись на той же величине, что коробка поля, —
  причина в шрифте; либо совпали — подозрение снято и ищется другое.
- **Verified by:** вывод измерения в записи хода работ, с числами обоих поднятий.

### 2. Владельцу названы дороги и их цена

- **What is done:** обе дороги описаны числами первого этапа: что меняется у потребителя кита и что
  только в кадрах.
- **Readiness sign:** владелец назвал дорогу.
- **Verified by:** его слово в переписке, записанное в ход работ.

### 3. Выбранная дорога сделана

- **What is done:** правка по выбранной дороге и пересъёмка тех кадров, которые она двигает.
- **Readiness sign:** два поднятия витрины подряд дают зелёный обход снимков.
- **Verified by:** `node tools/visual-gate.mjs ui-kit-v2` — «579 из 579» дважды, с перезапуском
  витрины между прогонами.

### 4. Договорённость и правило дописаны

- **What is done:** в договорённость второго кита записано, чем закреплено начертание кадров; в
  правило о проверке компонентов — статья о том, что величина, зависящая от машины, ловится двумя
  поднятиями, а не одним прогоном.
- **Readiness sign:** `node tools/check-specs.mjs` без расхождений.
- **Verified by:** `node tools/check-specs.mjs` — «divergences 0».
