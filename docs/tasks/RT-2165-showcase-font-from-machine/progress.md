# Ход работ RT-2165

## Где стоим

- **State:** `этап-идёт`
- **Этап:** 2 из 4 — цена обеих дорог измерена, выбор за владельцем
- **Следующий шаг:** сделать дорогу, которую назовёт владелец (этап 3)
- **PR:** не открыт

## Что уже известно

Числа разбора лежат в `grill.md`. Коротко: величина устойчива внутри одного поднятия витрины и
скачет между поднятиями; ветка и обвязка показа ни при чём; измерение по кириллической подписи
разницы не видит, потому что подпись рисует запасное начертание.

## Этап 1: подозрение на шрифт снято, причина не найдена

Измерение по знакоместу цифры — той самой величине, по которой браузер считает свою ширину поля, —
снято при двух поднятиях витрины подряд, с перезапуском и чисткой кеша между ними.

| что мерили                        | поднятие 1 | поднятие 2 |
| --------------------------------- | ---------- | ---------- |
| знакоместо цифры, набором целиком | 8.668      | 8.668      |
| знакоместо цифры, `system-ui`     | 8.668      | 8.668      |
| знакоместо цифры, `Montserrat`    | 7          | 7          |
| коробка поля                      | 195        | 195        |
| собственная ширина textarea       | 161        | 161        |

Числа совпали до сотых, и коробка оба раза 195. **Подозрение на шрифт не подтвердилось.**

Заодно снято, что `Montserrat` и три Noto дают ровно 7 — то же, что даёт отсутствующее начертание;
в наборе их нет, и рисует всё `system-ui`. Это по-прежнему верно и по-прежнему стоит починки, но
скачок 192 и 195 объясняет не это.

### Третье поднятие, уже под нагрузкой

Состояние 192 наблюдалось, когда машина была под нехваткой памяти: набор проверок был убит ею
дважды, подкачка стояла на 14.7 из 16 гигабайт, а в дереве висели сироты — витрина второго кита с
прошлого прогона десятичасовой давности и демон nx на полтора гигабайта.

Третье поднятие сделано витриной, которую поднимает сама проверка, при подкачке 15.1 из 16
гигабайт: обход снимков зелёный, 572 из 572, и величина снова 195. **Нехватка памяти сама по себе
состояние не воспроизводит.**

## Решение по ходу: перестать гоняться, дать обвязке ловить

За сессию состояние 192 не воспроизвелось ни разу из трёх поднятий, а выпадает оно раз в несколько
дней и стоит часа разбора каждый раз. Гоняться за ним дальше — тратить время на ожидание, а не на
работу.

Поэтому обвязка снимков второго кита теперь складывает улику о разошедшемся кадре, как это давно
делает обвязка первого. Рядом с кадром расхождения ложатся условия съёмки: окно, поток, плотность
точек, размер страницы, набор начертаний и знакоместо цифры — то самое число, которого сегодня не
хватило и на измерение которого ушёл час.

Путь проверен целиком: временный отказ под признаком окружения дал по каталогу на историю, и в
каждом лежат условия с числами. Признак после проверки убран, сборка витрины зелёная.

Правило проверки компонентов больше не утверждает, что у второго кита укладки улики нет: привязка
статьи в `implementation.md` поправлена и называет оба кита.

## Этап 2: цена обеих дорог измерена

Цену задаёт одно число — сколько кадров вообще двигает смена начертания. Оно измерено, а не
прикинуто: набор витрины временно прибит к моноширинному шрифту и снят полный обход.

| что               | число |
| ----------------- | ----- |
| показов в обходе  | 551   |
| покраснело        | 488   |
| осталось зелёными | 63    |

Временная правка снята, эталоны не тронуты — их по-прежнему 572. Заодно улика, сложенная на
прошлом этапе, впервые отработала на потоке: рядом с каждым из 488 кадров легли условия съёмки.

Обе дороги двигают ровно эти 488 кадров. Разница не в кадрах, а в том, кого ещё это касается.

### Дорога первая: кит несёт свой шрифт файлом

- Сегодня кит не несёт ни одного файла шрифта и не объявляет ни одного `@font-face`. Набор назван,
  а рисует его `system-ui` — то, что стоит на машине у потребителя.
- Значит, у всех, кто уже поставил кит, текст перерисуется. Это видимое изменение вида, а не
  починка кадров.
- Знакоместо цифры при этом меняется с 8.668 на 7 — почти пятая часть ширины. Поля станут уже.
- В наборе четыре веса и три неевропейских начертания: тайское, корейское, деванагари. Подписи кит
  отдаёт приложению, то есть текст у потребителя может быть на любом из этих письмен, и файл нужен
  на каждое. Корейское — самое тяжёлое.

### Дорога вторая: витрина снимает кадры своим начертанием

- Меняются только кадры. Вид кита у потребителя остаётся прежним.
- Начертание нужно одно и только для съёмки: письмена сверх латиницы и кириллицы не нужны.
- Расхождение 192 и 195 закрывается, но кит по-прежнему объявляет начертания, которых не несёт.
  Подпись у потребителя рисуется тем, что нашлось на его машине. Это остаётся починкой на потом.

Работа у обеих дорог стоит одинаково: пересъёмка 488 кадров и просмотр их глазами до съёмки —
эталон снимают после того, как на кадр посмотрели.

## Handover of the session

Put together by a hook before the compaction of the context (auto).

**Working tree:** /Users/sviatoslavkhutornoy/WebstormProjects/rt-tools
**Branch:** RT-2165-showcase-font-from-machine

### Where we stand at the minute of the compaction

- **State:** `этап-идёт`
- **Stage:** 1 из 4 — идёт, причина не найдена, улика поставлена на ловлю
- **Next step:** дождаться, когда расхождение выпадет само, и разобрать его по сложенной улике
- **PR:** не открыт

The progress in full — `docs/tasks/RT-2165-showcase-font-from-machine/progress.md`; the plan lies next to it.

### Uncommitted

```
none
```

### Commits over the main branch

```
e10855e1f docs(rt:ui-kit-v2): ход RT-2165 записан — гоняться за редким состоянием перестал
cc4880799 feat(rt:ui-kit-v2): обвязка снимков второго кита складывает улику о разошедшемся кадре
e15eccac8 docs(rt:ui-kit-v2): подозрение на шрифт снято измерением по знакоместу цифры
a8307dd85 docs(rt:ui-kit-v2): заведена папка задачи RT-2165, разбор и замысел записаны
2fcb70054 Merge remote-tracking branch 'origin/main' into RT-1870-one-kit
b9aad6cc7 docs(rt:ui-kit-v2): перенос таблицы вписан в эпик двумя задачами
87b77af8d docs(rt:ui-kit-v2): замысел эпика назвал потерянную задачу и догнал доску
1ef964350 Merge remote-tracking branch 'origin/main' into RT-1870-one-kit
3a92344a1 docs(rt:ui-kit-v2): в состоянии эпика отмечена влитая прокрутка
f55c97f87 Merge remote-tracking branch 'origin/main' into RT-1870-one-kit
d9c2213d2 Merge remote-tracking branch 'origin/RT-1878-scroll-area' into RT-1870-one-kit
612319d52 Merge remote-tracking branch 'origin/RT-1870-one-kit' into RT-1870-one-kit
c2765a434 Merge remote-tracking branch 'origin/main' into RT-1870-one-kit
0531064a1 docs(rt:ui-kit-v2): план эпика назвал задачу RT-2142 о ручке бокового меню
38f324bf6 [RT-1878] Семья прокрутки перенесена из первого кита во второй (#2141)
e9ad8133e Merge branch 'RT-1870-one-kit' into RT-1878-scroll-area
ac4b9c3df Merge remote-tracking branch 'origin/main' into RT-1870-one-kit
d4acbe6d8 docs(rt:ui-kit-v2): папка задачи RT-1878 разобрана
c9e4f3b0f chore(rt:ui-kit-v2): обёртки показов прокрутки названы в списке единообразия
ccacff5aa docs(rt:ui-kit-v2): инструкция по переходу с первого кита у семьи прокрутки
```

Written by a hook before the compaction of the context. Everything standing here is checked
against the tree: a handover retells what was written and describes the minute it was put together.
