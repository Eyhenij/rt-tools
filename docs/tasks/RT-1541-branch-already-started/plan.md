# Замысел

**Задача:** RT-1541 · **Ветка:** RT-1541-branch-already-started
**Поведение:** не меняется — правится текст паттерна слоя правил. Кода приложения работа не
задевает.

После написания этот файл не правится. Пересмотр этапа идёт в `progress.md` решением по ходу.

## След задачи

| Что      | Где                                                                |
| -------- | ------------------------------------------------------------------ |
| Паттерны | `projects/agent-kit/assets/patterns/git-workflow-commit.github.md` |

## Что считается сделанным

- Паттерн говорит: перед заведением ветки читаются локальные ветки с номером этой задачи, и
  одна строка ответа означает, что работа уже начата.
- Запись груза, вызвавшая правку, отмечена в приёме после слияния.

## Этапы

### 1. Абзац о локальных ветках задачи

- **Что делается:** в раздел «Сначала задача на борде, потом ветка» дописывается абзац с
  готовым вызовом чтения локальных веток и разбором ответа.
- **Признак готовности:** абзац стоит в паттерне, предел веса выдержан.
- **Чем проверяется:** `node tools/check-file-size.mjs` — файл паттерна не назван.

### 2. Раскладка и сверка

- **Что делается:** пакет собирается, ресурсы раскладываются в дерево.
- **Признак готовности:** раскладка совпадает с источниками.
- **Чем проверяется:** `pnpm run agent-kit:check` — расхождений нет.

## Чего эта работа не делает

- Не заводит гарда на заведение ветки: вызов без чтения ответа неотличим от заведения вслепую.
- Не трогает раздел «Ветка заводится отдельным вызовом»: он про имя ветки и гард поставки.
