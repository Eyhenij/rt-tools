# Разбор просьбы

## Просьба владельца

Ночное распоряжение: брать в работу весь груз приёма, править самостоятельно, ветки держать
локально и ставить каскадом друг за другом.

## Записи груза

- `7e498810c0b0ac46e745f8269d6c42c40b87466366457583640bbee88ec27efa` — диспетчер молчит о
  ветке, которая упала с ненулевым кодом и пустым выводом.
- `e2425bb61c1adc55c618fd4210563966bec6b4fb2c8503a6a4dd248eb068d91e` — гард источника правил
  отбивает файл в конфликте, хотя разрешение конфликта содержания копии не меняет.
- `2c5f44fcba3d837786defc0eead197fb5b4a9112ddfd87d8fb1f09a075737474` — гард прогона перед пушем
  молчит о своём исходе, и по логу не отличить зелёный набор от ненайденного.
- `09ffbcbc54b498a2f0280ff92b849ff610baaa587036f3bfd712c52c47c3f5a8` — гард главной ветки судит
  голым поиском глагола: `-c ключ=значение` между `git` и глаголом уводит команду мимо отбора, а
  `git log --grep commit` отбивается зря.

## Шесть вопросов

1. **Что меняется для владельца.** Ничего в приложении: правятся четыре гарда слоя правил.
2. **Где это живёт.** `projects/agent-kit/assets/hooks/` — источник пакета правил; раскладка
   кладёт копии в `.claude/hooks/`.
3. **Чем это подтверждается.** Прямой вызов каждого гарда на подготовленном вводе, сверка
   раскладки, тесты пакета правил.
4. **Что из этого правило, а что код.** Всё четыре — код гардов; статей правил работа не заводит.
5. **Чего работа не делает.** Не трогает карту гейта и не заводит новых гардов.
6. **Где ветка.** `RT-1690-guard-fixes`, каскадом от `RT-1689-task-flow-articles`.
