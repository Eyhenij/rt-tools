# Замысел

**Задача:** RT-2026 · **Ветка:** RT-2026-pr-author-not-reviewer
**Поведение:** не меняется — правится блокирующая проверка слоя правил и её описание; кода
приложений и китов работа не трогает.

После записи этот файл не правится. Пересмотр этапа уходит в ход работ решением по пути.

## След задачи

| Что      | Где                                                           |
| -------- | ------------------------------------------------------------- |
| Описание | `docs/specs/agent-kit/delivery-identity/`                     |
| Правила  | `.claude/skills/git-workflow/`                                |
| Код      | `projects/agent-kit/assets/hooks/git-guard-delivery.sh`       |
| Код      | `projects/agent-kit/assets/hooks/git-guard-delivery-draft.sh` |
| Тесты    | `projects/agent-kit/tests/`                                   |

## Что считается сделанным

- Вызов, где открывающий и названный рецензент — разные лица, проходит независимо от того,
  машинная это учётка или нет.
- Вызов, где они совпали, не проходит, и отказ говорит именно об этом.
- Открытие не машинной учёткой названо вслух в выводе.
- Снятие черновика судится тем же признаком.
- Тесты покрывают оба исхода, описание сходится с поведением.

## Этапы

### 1. Описание переписано на новый признак

- **Что делается:** правила подраздела `delivery-identity` говорят про совпадение открывающего с
  рецензентом, а не про машинную учётку; сценарии заведены и привязки поправлены.
- **Признак готовности:** аудит описаний не находит расхождений по этому подразделу.
- **Проверено:** `npm run check:specs` — код выхода 0.

### 2. Проверка судит открывающего против рецензента

- **Что делается:** правится исходник в пакете и снимается разложенная копия; тот же признак
  ставится на снятие черновика.
- **Признак готовности:** вызов с рецензентом `Eyhenij` от `treble3d` проходит; вызов, где они
  совпали, не проходит.
- **Проверено:** `bash projects/agent-kit/tests/run.sh` — все наборы зелёные.

### 3. Правило поставки называет новый признак

- **Что делается:** статья правила `git-workflow` про личность вызова переписывается, привязка в
  соседнем файле поправляется.
- **Признак готовности:** статья и привязка сходятся, аудит молчит.
- **Проверено:** `npm run check:specs` — код выхода 0.

## Чего эта работа не делает

- Не выясняет, каким путём пятнадцать PR прошли мимо проверки 7–9 сентября.
- Не меняет, кто ставится исполнителем задачи: это по-прежнему машинная учётка.
- Не трогает подпись коммитов: она судится отдельной проверкой.
