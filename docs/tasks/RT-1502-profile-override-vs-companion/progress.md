# Ход работы

## Где стоим

- **Состояние:** `этап-идёт`
- **Этап:** все три сделаны
- **Сделано:** задача заведена, ветка отведена от RT-1500, папка собрана; долг ветки RT-1498 закрыт
- **Следующий шаг:** прогнать набор целиком, привести тексты, разобрать папку, открыть заявку
- **Незакоммиченное:** проверка, её набор проб, места вызова, спека, надстройки в компаньонах
- **Ждём владельца:** нет
- **PR:** ещё не открыт

## Заходы

### 2026-08-28

- Задача заведена по пятому пункту отчёта дерева-потребителя о редакции 0.16.1.
- Доэтапное, не этой работы: ветка `RT-1498-spec-proposals` отдана заявкой #1499 с неразобранной
  папкой задачи и без записи в архиве. Починено в той ветке отдельным коммитом, запись легла в
  `docs/archive/RT-1498-spec-proposals.md`, тело заявки поправлено, починка перевлита вверх по
  череде. Визуальный набор второго кита при этом упал на `collapsible-text` и прошёл повтором —
  флак: 160 наборов, 450 тестов, 458 снимков зелёные.
- Этап 1: `checks/check-profile-drift.mjs` сверяет замещённые переменные профиля с текстами
  компаньонов; набор `tests/checks-profile-drift.test.sh` — 10 ok.
- Этап 2: проверка встала в набор гейта пуша умолчанием пакета, в `package.json` командой
  `check:profile-drift` и шагом «Profile drift» конвейера; сценарии SC-AK-765…768 в спеке
  проверок, утверждения и привязки при них.
- Этап 3: своё дерево зелёное — три надстройки о заявке названы компаньоном поставки, стенды
  дерева названы компаньоном проверки через браузер. Замещено 7, все названы.
- Находка по дороге, не этой работы: набор `cargo-mark.test.sh` ждёт двойника приёма отсчётом
  (`sleep 1`) и под нагрузкой краснеет четырьмя проверками — на спокойной машине те же четыре
  зелёные. Чинится в семье обвязки пакета: ждать записи файла, а не времени.
