# Progress

## Where we stand

- **State:** `этапы-кончились`
- **Stage:** 2 из 2 — этапы кончились
- **Done:** оба этапа закрыты. Дерево называет корнем `tools`, разложенные копии обновлены,
  статьи и сценарии SC-AK-921 и SC-AK-922 стоят в подобласти про проверки дерева.
- **Next step:** разобрать папку задачи и открыть черновик заявки.
- **Uncommitted:** нет.
- **Waiting for the owner:** нет.
- **PR:** ещё не открыт.

## Decisions along the way

- **Ветка снята со стопки, а не с главной** — след правки задевает `docs/specs/agent-kit/checks/`,
  который RT-1909 переписал и частью перенёс. Затронутый этап замысла: 2.

## Sessions

### 2026-09-08

- Заведена папка задачи, ветка `RT-1910-suite-roots` от `RT-1909-epic-membership-line`.
- Проба: номер сценария, дописанный в `tools/tests/check-cascade-layer.test.sh`, сборщику
  покрытия невидим.
- Этап 1: ключ `testRoots`, сборка `TEST_ROOTS` из двух списков, отбрасывание вложенных корней;
  новый набор из 6 проб. На прежнем сборщике краснеют обе пробы SC-AK-921, без отбрасывания
  вложенных — обе пробы SC-AK-922.
- Этап 2: дерево называет корнем `tools`, раскладка сходится с пакетом, статьи и строки
  компаньона встали в `docs/specs/agent-kit/checks/`. Проба на живом дереве: набор
  `tools/tests/check-cascade-layer.test.sh` теперь виден сборщику одним местом.
- `bash projects/agent-kit/tests/run.sh` — «ВСЕ НАБОРЫ ЗЕЛЁНЫЕ»; `node tools/check-specs.mjs` —
  1354 сценария, расхождений нет.
