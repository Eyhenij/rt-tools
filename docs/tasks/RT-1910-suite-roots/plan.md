# Plan

**Task:** RT-1910 · **Branch:** RT-1910-suite-roots
**Behaviour:** unchanged — правка живёт в слое правил и в настройке проверок; приложения она не
касается.

## Task footprint

| What  | Where                                                                               |
| ----- | ----------------------------------------------------------------------------------- |
| Specs | `docs/specs/agent-kit/checks/`                                                      |
| Laws  | `docs/constitution/verifiability.md`                                                |
| Rules | `.claude/skills/agent-kit-extend/`                                                  |
| Code  | `projects/agent-kit/assets/checks/`, `projects/agent-kit/tests/`, `.claude/rt-kit/` |

## What counts as done

- Набор, лежащий под корнем, который дерево назвало ключом `testRoots`, виден сборщику покрытия.
- Дерево, не назвавшее этот ключ, судится ровно как прежде.
- Корень, вложенный в другой названный корень, обходится один раз: ссылка не удваивается.
- Это дерево называет корнем `tools`, и `tools/tests/check-cascade-layer.test.sh` попадает в
  сборщик.

## Stages

### 1. Корни наборов объявляются своим ключом

- **What is done:** ключ `testRoots` заводится в настройках пакета, `TEST_ROOTS` собирается из
  корней исходников и из него, вложенные корни отбрасываются; проба закрывает оба случая.
- **Readiness sign:** новый набор красен на прежнем коде и зелен на правленом.
- **Verified by:** `bash projects/agent-kit/tests/checks-specs-roots.test.sh` — строка
  «0 провалов».

### 2. Дерево называет свой корень и текст догоняет код

- **What is done:** `.claude/rt-kit/checks.json` называет `tools`, разложенные копии обновляются,
  статья и строка компаньона встают в подобласть про проверки дерева.
- **Readiness sign:** сборщик видит набор рядом с инструментами, сверка спеков зелена.
- **Verified by:** `npm run check:specs` — код 0.

## What this work does not do

- Не правит пометку «Не покрыто» у сценариев второго кита: это работа того, кто ведёт его
  договорённость.
- Не заводит наборы под сценарии, которые их не имеют: правка возвращает видимость, а не покрытие.
