# Замысел

**Задача:** RT-1766 · **Ветка:** RT-1766-published-neighbour-imports
**Поведение:** не меняется — проверка дерева и её спек, кода приложения не задевает.

После написания этот файл не правится. Пересмотр этапа идёт в `progress.md` решением по ходу.

## След задачи

| Что      | Где                                                       |
| -------- | --------------------------------------------------------- |
| Проверка | `tools/check-package-imports.mjs`                         |
| Проба    | `projects/agent-kit/tests/checks-package-imports.test.sh` |
| Спек     | `docs/specs/agent-kit/checks/`                            |
| Гейт     | `.claude/rt-kit/project.sh`, `package.json`               |
| Конвейер | `.github/workflows/ci.yml`                                |

## Что считается сделанным

- Проверка называет пакет, символ и версию соседа, когда символ импортирован из соседа, а в
  опубликованной версии под диапазон манифеста его нет; на текущем дереве она зелёная.
- Проверка стоит в наборе гейта пуша и шагом конвейера.
- Правила спека с привязками, сценарии, проба на фикстуре без сети.

## Этапы

### 1. Проверка

- **Что делается:** чтение импортов из исходников пакетов, забор типов опубликованного соседа
  из реестра с кэшем, сверка имён; проба на фикстуре.
- **Признак готовности:** проба покрывает сошедшийся, разошедшийся, многострочный импорт,
  цепочку `export *`, пропуск проб и историй, отсутствие версии под диапазон и неответ реестра.
- **Чем проверяется:** `bash projects/agent-kit/tests/checks-package-imports.test.sh` —
  «разошлось: 0»; `node tools/check-package-imports.mjs` — код 0.

### 2. Подключение и тексты

- **Что делается:** строка в наборе гейта пуша и скрипт в манифесте, шаг конвейера; правила,
  сценарии и привязки в спеке проверок.
- **Признак готовности:** проверка набора гейта и сверка спеков зелёные.
- **Чем проверяется:** `node tools/check-push-gate.mjs` — код 0; `node tools/check-specs.mjs` —
  код 0.

### 3. Отдача

- **Что делается:** папка задачи разбирается, заявка открывается черновиком чередой за
  RT-1836.
- **Признак готовности:** заявка открыта, прогон на вершине идёт.
- **Чем проверяется:** `gh pr view` — номер и состояние.

## Чего эта работа не делает

- Не сверяет импорты из чужих пакетов вне `@rt-tools/*`: их версии держит замок.
- Не читает сборку пакета: исходники читаются без неё.
- Не публикует пакеты и не правит манифесты.
