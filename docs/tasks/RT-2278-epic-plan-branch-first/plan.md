# План работы

**Задача:** RT-2278 · **Ветка:** RT-2278-epic-plan-branch-first
**Поведение:** не меняется — правка лежит в проверках дерева, не в приложении.

## След задачи

| Что            | Где                                                           |
| -------------- | ------------------------------------------------------------- |
| Чтение плана   | `projects/agent-kit/assets/checks/board-epic-plan.github.mjs` |
| Описание доски | `docs/specs/agent-kit/board/`                                 |
| Пробы          | `projects/agent-kit/tests/checks-board-epic-plan.test.sh`     |

## Что считается сделанным

`node tools/check-board.mjs` не говорит про задачи #2256, #2257 и #2269, что план эпика #1870 их
не несёт. Сценарий описания закрыт пробой, набор перед отправкой зелёный, PR в ветку эпика открыт
с рецензентом.

## Этапы

- [ ] 1.1 Сценарий описания доски: ветка эпика старше диска не бывает
- [ ] 1.2 Чтение плана берёт ветку эпика, пока текущая ветка её вершину не вобрала
- [ ] 1.3 Пробы на новый порядок и на прежний
- [ ] 1.4 Набор проверок перед отправкой и PR в ветку эпика

### Этап 1 — план берётся оттуда, где он новее

Условие одно и проверяется одной командой: вершина ветки эпика содержится в текущей ветке или нет.
Содержится — диск не старее, читается диск. Не содержится — читается ветка эпика.

Признак готовности: `node tools/check-board.mjs` без трёх строк про план эпика #1870,
`bash projects/agent-kit/tests/run.sh` зелёный, `node tools/check-specs.mjs` без расхождений.
