# Замысел

**Задача:** RT-1626 · **Ветка:** RT-1626-autonomous-work
**Поведение:** не меняется — ресурсы слоя правил, кода приложения работа не задевает

После написания этот файл не правится. Пересмотр этапа идёт в `progress.md` решением по ходу.

## След задачи

| Что      | Где                                                         |
| -------- | ----------------------------------------------------------- |
| Законы   | `projects/agent-kit/assets/laws/autonomous-work.md`         |
| Правила  | `projects/agent-kit/assets/rules/autonomous-work.md`        |
| Паттерны | `projects/agent-kit/assets/patterns/autonomous-work-run.md` |

## Что считается сделанным

- Пакет несёт закон об автономном заходе, правило под ним и паттерн с готовым циклом ночи.
- Раскладка кладёт их в дерево: закон в свод, правило и паттерн — скилами.
- Пределы длины и веса целы, наборы пакета зелёные.

## Этапы

### 1. Три ресурса в пакете

- **Что делается:** закон, правило и паттерн ложатся в источники пакета
- **Признак готовности:** все три на месте, фронтматтер правила и паттерна разобран сборкой
- **Чем проверяется:** `bash projects/agent-kit/tests/run.sh` — все наборы зелёные

### 2. Раскладка и сверка

- **Что делается:** ресурсы раскладываются в дерево
- **Признак готовности:** свод несёт закон, `.claude/skills` — правило и паттерн
- **Чем проверяется:** `pnpm run agent-kit:check` — разложенное сходится с пакетом

## Чего эта работа не делает

- Стража под закон не заводит: запрет на видимое снаружи держится правилом, а команда пуша
  ночью ничем не отличается от дневной.
- Прочих ночных задач не трогает — они идут своими ветками чередой от этой.
