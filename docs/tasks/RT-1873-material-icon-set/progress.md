# Progress

## Where we stand

Rewritten by every session, not appended to.

- **State:** `этапы-кончились`
- **Stage:** 1, 2, 3 и 4 закрыты
- **Done:** перечень заведён — 31 запись, 28 с парой и 3 с причиной; проверка стоит в наборе гейта
  и шагом конвейера, отбита с обеих сторон; история пар собрана и эталон снят, кадр посмотрен;
  спека линии знает адрес перечня
- **Next step:** разобрать папку задачи и открыть заявку черновиком
- **Uncommitted:** папка задачи, перечень, проверка, история, эталон, правки спек
- **Waiting for the owner:** нет
- **PR:** ещё не открыта

## Decisions along the way

- **Пара ведёт на имя из фрейма, а не на своё с приставкой** — у 22 смыслов в наборе лежат два
  рисунка. Имён из фрейма 229 против 118, и они одной рисовки: смешанный набор дал бы разнобой
  на экране. Владельцу восемь таких пар названы отдельно, чтобы он мог переложить их на свои.
  Affected stage of the plan: 1.
- **`backup` вышел из списка недостающих** — у Material это облако со стрелкой вверх, то есть
  `cloud-upload`. В недостающие он попал по ошибке обхода. Affected stage of the plan: 1.
- **Снята чужая метка непокрытия у `SC-UKV-129`** — после слияния главной проверка спек стала
  отказывать: сценарий помечен непокрытым, а набор сценариев к нему уже виден сборщику покрытия.
  Метка — мой же остаток от RT-1916, и главная с ним красная. Правка на одну строку сделана
  здесь, а не отложена: иначе гейт пуша не пропустит ни одну ветку дерева. Affected stage of the
  plan: ни один — находка вне замысла.

## Sessions

### 2026-09-08

- Разобраны все области значков первого кита: 31 литеральное имя в 73 местах, 13 выражений в 19
  местах вынесены владельцем за границу.
- Замерен набор второго кита: 347 файлов, союз имён сходится с ними один в один, 22 смысла имеют
  по рисунку в каждом из двух языков имён.
- Сопоставлено 28 пар из 31; проверка отбита с двух сторон — на несуществующем имени и на имени,
  записанном дважды.
- Эталон истории снят на собранной витрине, поднятой отдельным процессом: снимающий вызов
  блокирует цикл событий, и сервер в том же процессе не отвечает.
- Найдено, что главная ветка красная на проверке спек; причина названа и снята одной строкой.
