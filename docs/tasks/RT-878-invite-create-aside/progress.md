# Ход работы

## Где стоим

- **Этап:** 5 из 5 — PR открыт черновиком, ветка запушена; ждёт зелёного прогона и уборки
- **Сделано:** этапы 1–5. Семь коммитов, последний `c6801296`. PR #879 черновиком, задача в
  колонке разбора. По слову владельца слово «дерево» убрано со всех экранов админки — подписи,
  тексты отказов приёмника, ожидания спек и семнадцать эталонов кадров в двух профилях съёмки.
  Разбор происшествия записан в `docs/postmortems/2026-08-19-tree-word-shown-to-users.md`
- **Следующий шаг:** дочитать прогон CI на вершине `c6801296`; при зелёном — последний коммит
  PR: вливание договорённости в спеки поддоменов, разбор папки задачи, снятие черновика
- **Незакоммиченное:** нет
- **Ждём владельца:** входа в поднятую локально админку — пароль исполнитель не вводит
- **PR:** #879, черновик, прогон на последней вершине не дочитан

## Решения по ходу

- **Шесть обязательных вопросов разбора не задавались** — владелец прервал разбор прямой
  командой начинать работу. Границы взяты исполнителем и записаны решениями в разборе просьбы.
  Затронутый этап замысла: все.
- **Конвейер ролей не гонялся** — по той же причине: работа начата прямой командой владельца,
  договорённость написана исполнителем. Затронутый этап замысла: все.

- **Кнопка выдачи не прячется после удачи, а отключается** — футер панели проецирует кнопки по
  атрибутам, и блок `@if` вокруг кнопки рвёт проекцию: узел до слота не доходит вовсе, и в
  футере оставалась одна «Закрыть». Нашёл прогон сквозного набора. Договорённость поправлена той
  же правкой. Затронутый этап замысла: 3.

## Заходы

### 2026-08-19

- Владелец прочитал на кнопке «Пригласить дерево» и остановил работу: слово словаря проекта
  оказалось на экране. Заменено на «проект» везде, где его видит человек. Правка пошла тремя
  кругами — подписи, ожидания спек, эталоны кадров, — и каждый следующий круг был виден только
  по красному прогону предыдущего. Первая пересъёмка кадров обновила один профиль из двух;
  второй поймал гейт пуша.
- Заведена следующая работа — RT-880 о лишнем в устройстве приёма: признак отправителя
  дублирует токен, приглашение — обряд поверх него. В работу не взята.
- Локально подняты приёмник (3000) и админка (4200), открыт экран входа. Дальше не пошло:
  войти исполнитель не может — ввод пароля в форму ему запрещён, а учётной записи в местной
  базе он не нашёл.

- Полный сквозной набор зелёный: 52 спеки. `check:all` упал на одной спеке экрана — она искала
  в пустом состоянии команду узла, которую эта же работа оттуда убрала; поправлена, повторный
  набор зелёный.
- Первый прогон сквозного набора нашёл три падения: кадр списка разошёлся с эталоном из-за новой
  кнопки в тулбаре, а кнопка выдачи не рисовалась вовсе — блок `@if` рвал проекцию в футер. Обе
  причины сняты, спеки раздела зелёные (8 из 8).

- Разобрано устройство выдачи токена: два пути, оба мимо админки; находки записаны в разборе
  просьбы.
- Владелец назвал устройство приёма неверным, но работу назначил другую — панель создания
  приглашения. Разбор устройства остался незаведённой работой.
- Заведена задача RT-878, ветка от свежей главной, колонка переведена в работу.
- Записана договорённость: спек и девять сценариев.
- Этап 1: `POST /api/invites` в контроллере приглашений, общая с командой строки запуска выдача
  (`invite-issue.ts`), восемь новых спек — 50 зелёных в домене деревьев.
- Этап 2: запрос выдачи со своим разбором отказа, действие стора потоком, уровень выданного
  приглашения в модели и маппер к нему, строки панели в словаре админки.
- Этап 3: заведена либа панели создания — теги, алиас, границы; панель на основе кита с показом
  кода один раз, кнопка в правом слоте тулбара, маршрут в аутлете. Сборка админки зелёная,
  линтеры затронутых либ зелёные, сверка раскладки зелёная.
- Три сценария из девяти закрыты не полностью, и это записано пометками в самой договорённости:
  занятое имя заведённым деревом, объявление доступа у операции и пустой раздел — стенд
  сквозного набора пустым его не показывает.
