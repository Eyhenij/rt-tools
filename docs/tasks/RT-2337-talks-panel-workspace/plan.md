# Замысел

**Задача:** RT-2337 · **Ветка:** RT-2337-talks-panel-workspace
**Draft:** `docs/specs/chat/proposed/talks-workspace/`
**Behaviour:** changes

## След задачи

| Что     | Где                                                          |
| ------- | ------------------------------------------------------------ |
| Спеки   | `docs/specs/chat/proposed/talks-workspace/`                  |
| Спеки   | `docs/specs/chat/operator-panel/`                            |
| Правила | `.claude/skills/reuse-first/`, `.claude/skills/styling-bem/` |
| Код     | `libs/message-bus-admin/chat/feature/panel/`                 |
| Код     | `apps/message-bus-admin/src/styles/_chat.scss`               |
| Код     | `apps/message-bus-admin-e2e/src/chat-section.spec.ts`        |
| Код     | `apps/message-bus-admin-e2e/src/chat-webhook.spec.ts`        |

## Что считается сделанным

- Раздел чата в панели оператора стоит на рабочем столе кита: список, лента, подробности.
- Действия над разговором стоят в подробностях, в ленте их нет.
- Ширины колонок помнятся ключом панели.
- Сквозной набор ходит по новому виду и зелен, эталон экрана снят заново.

## Этапы

### 1. Панель собрана рабочим столом

- **Шаги:**
    1. Поставить `rt-workspace` вместо самодельной сетки: список слева, лента в середине,
       подробности справа.
    2. Взять свойства, заголовок и действия из общих частей раздела; убрать кнопку состояния
       из ленты.
    3. Убрать из слоя раскладки правила сетки раздела — колонки теперь рисует стол.
- **Признак готовности:** сборка админки проходит, линтеры кода и стилей зелены.
- **Проверено:** `npx nx run message-bus-admin:build` — «Successfully ran target build».

### 2. Сквозной набор ходит по новому виду

- **Шаги:**
    1. Привести спеки `chat-section` и `chat-webhook` к новым местам: действие в подробностях.
    2. Закрыть сцену `SC-CH-97` проверкой рабочего стола и снять с неё пометку о непокрытии.
    3. Снять эталон экрана раздела заново.
- **Признак готовности:** сквозной набор раздела чата зелен, эталон обновлён.
- **Проверено:** `npx nx run message-bus-admin-e2e:e2e` — «Successfully ran target e2e».

### 3. Работа отдана

- **Шаги:**
    1. Разобрать папку задачи последним коммитом.
    2. Прогнать набор перед отправкой и открыть заявку на ветку предыдущей задачи.
- **Признак готовности:** заявка открыта, рецензент назначен, столбец задачи переехал.
- **Проверено:** `gh api repos/Eyhenij/rt-tools/pulls/<номер>` — `draft: false`, база названа.

## Чего эта работа не делает

- Не трогает встраиваемую страницу: это задача RT-2338, и она же сольёт черновик договорённости.
- Не меняет операций чтения и записи: стол собирается из того, что уже отдаётся.
