# План

**Задача:** RT-2362 · **Ветка:** RT-2362-archive-prune-sep26
**Behaviour:** unchanged — владелец велел почистить описание прошлого задачей, код не меняется

## След задачи

| Что     | Где                             |
| ------- | ------------------------------- |
| Тексты  | `docs/archive/`                 |
| Правила | `.claude/rt-kit/assignments.md` |

## Что считается сделанным

- Записей описания прошлого старше срока нет, проверка возраста зелёная.
- Ветки `RT-1993-kit-two-font-not-shipped` на сервере нет.
- PR открыт в главную ветку.

## Этапы

### 1. Описание прошлого очищено

- **Шаги:**
    1. Убрать записи старше срока командой чистки.
- **Признак готовности:** проверка возраста записей не находит расхождений.
- **Проверено:** `node tools/check-archive-age.mjs` — расхождений нет.

### 2. Ветка RT-1993 удалена

- **Шаги:**
    1. Удалить ветку `RT-1993-kit-two-font-not-shipped` на сервере.
- **Признак готовности:** сервер не знает этой ветки.
- **Проверено:** `git ls-remote --heads origin RT-1993-kit-two-font-not-shipped` — пустой вывод.

### 3. Работа отдана

- **Шаги:**
    1. Разобрать папку задачи последним коммитом.
    2. Прогнать набор перед отправкой и открыть PR в главную ветку.
- **Признак готовности:** PR открыт, рецензент назначен.
- **Проверено:** `gh api repos/Eyhenij/rt-tools/pulls/<номер>` — `state: open`, база `main`.

## Чего эта работа не делает

- Не трогает задачи вне эпика и не начатые эпики — владелец велел их оставить.
