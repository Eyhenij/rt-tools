# Ход работы

## Где стоим

- **Состояние:** `этап-идёт`
- **Этап:** 2 из 4 — кадр перестаёт сниматься без шрифта; сделана первая его половина
- **Сделано:** три гипотезы отброшены замерами, причина названа чтением обвязки, замысел
  записан, ожидание шрифта перестало гасить свой отказ — прогон снимков после правки дал 47 из 47
- **Следующий шаг:** ответ владельца о том, чем снимать зависимость от чужой сети (вопрос задан
  не был — заход упёрся в порог окна), затем вторая половина этапа 2
- **Незакоммиченное:** нет
- **Ждём владельца:** ответа про шрифт — копия в репозитории, внешний адрес как есть или
  скачивание в кэш перед прогоном
- **PR:** ещё не открыт

## Решения по ходу

- **Задача взята сразу после конца эпика границы пакета, а не наугад.** За тот заход этот
  снимок дважды отбил гейт пуша на ветках, не тронувших ни строки показа, и оба раза прошёл со
  второго вызова той же команды. Замер сошёлся с телом задачи до последнего знака.

- **Разовый сценарий замера брошен после четырёх срывов.** Все четыре — на своей обвязке, а не
  на существе: витрина умирала вместе с поднявшим её вызовом; сценарий не находил драйвер
  браузера, потому что пакеты в этом дереве лежат по адресам pnpm; страница истории не успевала
  собраться за тридцать секунд, отведённых драйверу по умолчанию; четвёртый запуск убит по
  времени. Цена разового сценария рядом с готовой обвязкой оказалась больше самой правки —
  причина подтверждена правкой обвязки и её же командой. Затронутый этап замысла: 1 — его
  признак закрыт иначе, чем обещал замысел.

## Заходы

### 2026-08-22 … 2026-08-23

- задача RT-950 взята: ветка от свежей главной, колонка в работе, папка, разбор просьбы
- **замер первый:** десять съёмок из десяти на одной поднятой витрине сошлись — внутри прогона
  расхождения нет вовсе
- **замер второй:** пять подъёмов из пяти под нагрузкой в шесть занятых ядер сошлись — голая
  занятость машины расхождения не даёт
- **замер третий:** два прогона снимков одновременно, каждый со своей витриной, — первый кит
  сошёлся, `47 passed`
- **причина названа чтением обвязки:** ожидание шрифта значков ждало его не дольше десяти
  секунд и гасило свой отказ. Не дождавшись, обвязка всё равно снимала кадр: значки невидимы,
  подписи кнопок встают на их место, и кадр расходится ровно там, где расхождение и видели.
  Число одно и то же во всех встречах потому, что состояние «шрифт не приехал» устойчиво
- **задача #986 слита сюда** по слову владельца: тот же замер до последнего знака. В тело #950
  дописано её содержимое; стереть её вместе с номером не вышло — удаление требует прав
  владельца, запись закрыта ссылкой сюда
- **этап 2, первая половина:** ожидание перестало гасить отказ и роняет прогон с внятной
  причиной. Прогон снимков после правки — 47 сошлись из 47
