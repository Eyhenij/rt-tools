# Ход работы

## Где стоим

- **Состояние:** `этап-идёт`
- **Этап:** 3 из 5 — Оформление панели в двух модах
- **Сделано:** этапы 1 и 2 закрыты — мода панели приходит входом, нажатие уходит выходом,
  панель рисуется одним шаблоном в оверлее и в потоке, отбор по подстроке подписи считается
  в `logic.ts`; спеки шапки и логики зелёные, 55 тестов
- **Следующий шаг:** тень уходит под модификатор всплывающей панели, а не снимается у
  закреплённой
- **Незакоммиченное:** нет
- **Ждём владельца:** нет
- **PR:** ещё не открыт

## Решения по ходу

- **Работа взята вне эпика этого дерева** — по прямому слову владельца. Затронутый этап
  замысла: нет.
- **Подписи переключателя дописаны на первом этапе, а не на четвёртом** — они нужны разметке
  того же этапа, а без них кнопка осталась бы без доступного имени. Затронутый этап замысла: 1
  и 4; четвёртому остаются тексты рядом с кодом.
- **Оформление переключателя и закреплённой панели сделано на первом этапе** — без него
  кнопка встала бы флекс-ребёнком и сдвинула первую колонку, то есть панель была бы сломана
  между этапами. Полное разделение блока на всплывающую и закреплённую остаётся третьему.
  Затронутый этап замысла: 1 и 3.
- **Поле поиска — китовый `rt-input`, а не свой `<input>`** — у кита есть готовое поле с
  крестиком очистки, слотом значка и своими токенами. Ценой стал `FormControl` в компоненте:
  поле кита — контрол формы, и другого способа читать его значение нет. Затронутый этап
  замысла: 2.
- **Панель раскладывается сверху вниз: шапка, сообщение, ряд колонок** — поле поиска и
  переключатель флекс-детьми сдвинули бы первую колонку. Раскладка ряда переехала с блока на
  `__body`. Затронутый этап замысла: 2 и 3.
- **Панель получает раздел контекстом попапа, а не замыканием цикла** — иначе шаблонов стало
  бы два. Попап кладёт контекст в `$implicit`, поэтому шаблон разбирает его двумя `@let`.
  Затронутый этап замысла: 1.

## Заходы

### 2026-08-31

- Разведка по дереву: сверены все числа и имена задания с исходниками, найдены три расхождения
  — они записаны решениями разбора.
- Заведена договорённость `docs/specs/ui-kit-v2/proposed/nav-panel/`: 11 сценариев
  `SC-UKV-96` — `SC-UKV-106`, `npm run check:specs` зелёная.
- Заведены задача RT-1648, ветка и папка; колонка переведена в работу.
- Этап 1: тип моды, вход `panelMode`, выход `panelModeChange`, чистая функция
  `pinnedSectionOf`, переключатель в панели, пара значков `pin` / `pin-off`, две подписи.
  Спеки — 50 тестов зелёных, типы сходятся, `npm run check:styles` — классов без правила 0.
- `npm run check:icons` не прогнан: проба ходит в поднятую витрину на порт 6007, а её тут нет.
  Осмотр значков глазами идёт этапом 5.
- Этап 2: `filterSection` в `logic.ts`, поле поиска китовым `rt-input` в шапке панели, строка
  `uiNothingFound` при пустой выдаче, запрос стирается закрытием всплывающей панели.
  Спеки — 55 тестов зелёных, типы сходятся, `npm run check:styles` и `npm run check:reuse`
  без новых долгов.
