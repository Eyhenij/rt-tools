# Замысел

**Задача:** RT-1650 · **Ветка:** RT-1650-orphan-task-folder
**Поведение:** не меняется — уборка папки закрытой задачи, кода приложения работа не трогает

## След задачи

| Что      | Где                                           |
| -------- | --------------------------------------------- |
| Папки    | `docs/tasks/RT-1617-kit-fields-signal-forms/` |
| Папки    | `docs/tasks/RT-1650-orphan-task-folder/`      |
| Описание | `docs/archive/`                               |
| Правила  | `.claude/skills/task-flow-archive/`           |

## Что считается сделанным

- В `docs/tasks/` не лежит ни одной папки закрытой задачи.
- Слова владельца из обеих папок читаются в описании прошлого.
- `npm run check:board` не называет ни одной папки.

## Этапы

### 1. Чужая папка разбирается

- **Что делается:** `grill.md` и решение по ходу из `progress.md` складываются одним файлом в
  описание прошлого, папка снимается
- **Признак готовности:** `docs/archive/RT-1617-kit-fields-signal-forms.md` есть, папки нет
- **Чем проверяется:** `npm run check:board` — в выводе не остаётся строки про эту папку

### 2. Своя папка разбирается

- **Что делается:** `grill.md` этой работы уезжает в описание прошлого, папка снимается
- **Признак готовности:** в `docs/tasks/` не лежит ни одной папки закрытой задачи
- **Чем проверяется:** `npm run check:docs` — код 0, пути в текстах существуют

## Чего эта работа не делает

- Отставание прода от главной ветки на 386 коммитов сверка называет тем же выводом. Это выкатка,
  а не уборка папок, и решает её владелец.
- Печать `task-new: в очереди работ: НЕТ` при задаче, которая на борде есть, — находка о команде
  заведения, а не предмет этой работы.
