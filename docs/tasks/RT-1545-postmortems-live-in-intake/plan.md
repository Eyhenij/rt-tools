# Замысел

**Задача:** RT-1545 · **Ветка:** RT-1545-postmortems-live-in-intake
**Поведение:** не меняется — правится хранение разборов, тексты правил и гард слоя правил. Кода
приложения работа не задевает.

После написания этот файл не правится. Пересмотр этапа идёт в `progress.md` решением по ходу.

## След задачи

| Что       | Где                                                        |
| --------- | ---------------------------------------------------------- |
| Настройка | `.claude/rt-kit.json`, `.gitignore`                        |
| Хуки      | `projects/agent-kit/assets/hooks/postmortem-guard.sh`      |
| Правила   | `.claude/skills/cargo-triage/`, компаньоны соседних правил |
| Спеки     | `docs/specs/agent-kit/turn-guards/`                        |

## Что считается сделанным

- Все локальные разборы лежат в приёме, сверено чтением по именам записей.
- `docs/postmortems/` в дереве нет; черновик пишется в каталог вне истории.
- Гард происшествия судит каталог, названный настройкой дерева.
- Ни один текст не ведёт по мёртвому адресу разбора.

## Этапы

### 1. Отправка недостающего

- **Что делается:** 18 старых разборов переносятся в отправляемый каталог и уезжают вместе с
  остальными.
- **Признак готовности:** у каждого локального файла есть запись в приёме.
- **Чем проверяется:** `node tools/cargo-pull.mjs --kind postmortem --brief --size 20` — списки
  имён сходятся с каталогом.

### 2. Переезд каталога и гард

- **Что делается:** каталог называется ключом настройки и уходит в `.gitignore`; гард читает его
  оттуда же.
- **Признак готовности:** гард отбивает ход в дереве с новым каталогом и молчит при пустом ключе.
- **Чем проверяется:** `bash projects/agent-kit/tests/postmortem-guard.test.sh` — провалов нет.

### 3. Тексты

- **Что делается:** форма разбора переезжает в правило, адреса разборов заменяются именами
  записей.
- **Признак готовности:** мёртвых адресов нет, раскладка сходится.
- **Чем проверяется:** `node tools/check-doc-paths.mjs` — расхождений нет; `pnpm run
agent-kit:check` — разложенное сходится.

## Чего эта работа не делает

- Не правит записи архива: они описывают прошлое, и проверка путей их не судит.
- Не заводит команду прямой записи разбора в приём — черновик по-прежнему файл.
