# Ход работы

## Где стоим

- **Этап:** все пять закрыты; осталось открыть PR и дождаться прогона CI
- **Сделано:** 255 эталонов на 4.2 МБ, средний кадр 17 КБ, из них 29 — кадры порогов ширины.
  Два прогона подряд зелёные, намеренно внесённое расхождение роняет прогон на 14 кадрах и
  оставляет 14 картинок различий, ни одного файла на коммит после него в дереве не появляется.
  Исключений 62 из 288 историй. Полный заход — 86 с
- **Следующий шаг:** пуш ветки и PR; шаг CI проверяется его же прогоном
- **Незакоммиченное:** ничего своего; в дереве лежит чужое — `nx.json` с `analytics: false` и
  `RUNNER-MONITOR.md`, оба от прошлых заходов и не этой работой
- **Ждём владельца:** разбора PR #329. Оба вопроса закрыты: `Q-2` — 86 с приемлемы, долги
  заведены задачами #330–#333

Ветка отходит от `RT-326-ui-kit-v2-visual-snapshots`, которая ждёт разбора в PR #327: без её
обвязки этой работе не с чем работать.

## Решения по ходу

- **Работа заведена отдельной задачей, а не продолжена в ветке #326** — откатываются они порознь:
  обвязка остаётся годной, даже если весь каталог эталонов придётся снять заново, а PR с сотней
  PNG и одновременной правкой механики не читается.

- **2026-08-09. Область приёма файла получила свой признак `data-story-drag`.** Обвязка снимков
  требует открытой панели CDK везде, где на странице есть признак триггера перекрытия, а
  перетаскивание панели не открывает вовсе — область подсвечивает себя сама. На общем признаке
  каждая история области приёма падала бы требованием панели, которой там нет. Альтернатива —
  ослабить проверку — отвергнута: она затем и заведена, чтобы кадр с неоткрывшейся панелью не
  становился эталоном.

- **2026-08-09. Порогов ширины проставлено девять наборов, а не одиннадцать.** Из одиннадцати
  папок, называющих ширину, две кадром порога не проверяются: у `photo-viewer` нет ни одной
  истории (волна 5 плана покрытия), а единственная история `toolbar` рисует пустой `div` с
  директивой `rtToolbarLeft` — сам `rt-toolbar`, в чьих стилях лежит `@media (width <= 768px)`,
  витрина не показывает нигде. Оба — долг покрытия, а не размётки; заводятся задачами очереди.

- **2026-08-09. Истории без матрицы снимаются кадром целой страницы, а не помечаются
  исключением.** 35 файлов рисуют компонент напрямую, без сетки витрины, и корня показа на
  странице нет. Исключение сняло бы их с проверки целиком — при том, что это как раз те
  компоненты, у которых матриц ещё нет. Цена названа: порог расхождения считается от площади всей
  страницы, и мелкая правка в таком кадре проходит молча. Снимается это их волной покрытия, когда
  у них появятся матрицы.

- **2026-08-09. Наведение больше не переводит фокус на хост.** Обвязка ставила фокус любому
  жесту, а подсказка прячется по `focusout`: на ряде из трёх подсказок открытой оставалась
  последняя, и кадр показывал одну панель там, где история обещает все. Отловлено прогоном —
  история `Hosts` отказала требованием открытой панели, потому что последним в ряду стоит хост
  с пустым текстом, у которого панели нет вовсе.

- **2026-08-09. Четырнадцать эталонов, приехавших веткой #326, сняты поверх наложения с ошибкой
  сборки.** В кадре — не компонент, а красная плашка `TS4111` из `main.ts` витрины. Сама ошибка
  в дереве уже исправлена, эталоны — нет. Отдельного действия не требуют: этап 2 переснимает
  каталог целиком.

- **2026-08-09. Прогон открывает по две истории разом, а не по потоку на ядро.** Витрина по
  адресу одна, и умолчание её обгоняет: приложение истории не успевает встать, ожидание внутри
  сборщика витрины завершается пустым, кадр расходится либо история падает. Замерено: шесть
  потоков — до восьми разошедшихся кадров за заход, четыре — один отказ, два — ни одного за
  четыре захода. Цена названа числом: 86 с против 43 с. Повтор упавшей истории как средство
  отвергнут договорённостью — он делает правило о детерминированности непроверяемым.

- **2026-08-09. Восемнадцать историй, рисующих пустой набор, сняты со съёмки поимённо.** От
  таблицы без колонок до полосы вкладок без вкладок: обёртка отдаёт пустой массив, и в кадре
  либо чистый фон, либо одни разделители. Эталон поверх такого кадра объявил бы пустоту нормой.
  Наполнение — волна покрытия составных компонентов, она вне этой работы; здесь у каждой
  истории названа своя причина, и число исключений названо.

- **2026-08-09. Три истории рисовали страницу отказа `NG0201`, и две из них починены.** Вкладка
  и дополнение полосы показаны внутри `rt-tabs`: обе директивы просят `TemplateRef` и в одиночку
  не рисуются вовсе. Таблице поставлена служба хранилища настроек — `provideRtStorage()` её не
  отдаёт, и всякий потребитель таблицы обязан знать это сам; это заводится задачей. Заголовок
  сортировки получил двойника таблицы, но подпись он берёт из проекции, а обёртка её не отдаёт —
  кадр остался пустым, и история снята со съёмки вместе с остальными пустыми.

- **2026-08-09. Пороги ширины сняты у пяти папок из одиннадцати.** У `photo-viewer` историй нет
  вовсе; `toolbar` показан пустым `div` с директивой, а сам `rt-toolbar` витриной не рисуется
  нигде; `calendar`, `toast`, `workspace` и `table` сняты со съёмки как пустые. Кадры порогов
  остались у `aside`, `container`, `filter-control`, `page-header` и `chat` — 29 кадров.

## Заходы

### 2026-08-09

- Заведены задача #328, ветка `RT-328-ui-kit-v2-snapshot-baselines` и папка задачи; задача
  переставлена в колонку работы.

- Этап 1 закрыт. Размётка: 40 исключений с причиной (плюс четыре, поставленные веткой #326),
  36 файлов и две истории кадром целой страницы, 13 панельных историй, девять наборов порогов
  ширины. Три правки обвязки показа: свой признак у области приёма файла, фокус не при жесте
  наведения, разделение признаков в обёртках `file-drop`.
- Подтверждение — прогон `pnpm run test:visual:v2` на поднятой витрине: 288 историй, 48 зелёных
  (44 исключения и 4 совпавших эталона), 240 отказов, и все они — «эталона нет» либо расхождение
  с испорченным эталоном #326. `nx run @rt-tools/ui-kit-v2:typecheck` и `:lint` зелёные.
- Замер: полный заход по витрине — 40 с (41.9 с первым прогоном, 39.6 с вторым).
- Разбор с владельцем не повторялся: он записан целиком в папке задачи #326, и решения оттуда
  действуют здесь. В `grill.md` перенесено только то, что определяет эту работу.

- Этапы 2 и 3 закрыты. Каталог снят начисто: 255 эталонов, 4.2 МБ, средний кадр 17 КБ, кадров
  порогов 29. Два прогона подряд зелёные — 86.1 с и 91.0 с. Намеренное расхождение (отступ метки
  плюс пиксель) уронило 14 кадров в четырёх компонентах — сама метка, чипы множественного
  выбора, значки шапки панели и строка сведений, — выложило 14 картинок различий и не оставило
  в дереве ни одного файла на коммит; после отката прогон снова зелёный.
- Вскрытое съёмкой: испорченные эталоны прошлой ветки, ожидание значка по несуществующему
  признаку, необработанный отказ загрузки значков в ките, три истории со страницей отказа,
  восемнадцать пустых историй, свой признак у области приёма файла, число потоков прогона.
  Правка кита уехала отдельным коммитом, как требует договорённость.
- Проверено: `nx run @rt-tools/ui-kit-v2:typecheck`, `:lint`, `:test` — зелёные.

Заводится задачами очереди, не этой работой:

1. #330 — наполнить восемнадцать историй, рисующих пустой набор, и снять с них исключение
   снимков; заодно вернутся кадры порогов у четырёх папок.
2. #331 — показать `rt-toolbar` витриной: сегодня рисуется пустой `div` с директивой, и порог
   ширины этого компонента не проверяется ничем.
3. #332 — `IDBStorageService` не отдаётся `provideRtStorage()`: всякий потребитель `rt-table`
   обязан поставить её сам, иначе таблица падает `NG0201`. Ни в README, ни в спеке этого нет.
4. #333 — заголовок сортировки не показывает подписи вне таблицы: она приходит проекцией.

- Этап 4: свой шаг «Visual tests (ui-kit-v2)» в `.github/workflows/ci.yml` — витрина на 6107,
  прогон по `STORYBOOK_URL`, своё вложение `visual-diffs-ui-kit-v2`, блокирующий. Шаг первой
  витрины не тронут. Проверяется прогоном CI на PR.
- Этап 5: договорённость влита в спек пакета — `docs/specs/ui-kit-v2/{spec,scenarios,implementation}.md`,
  директории `proposed/` не осталось. Добавлены три правила, вскрытые съёмкой; язык кадра
  исправлен на русский — витрина работает на нём. Двадцать сценариев: четыре покрыты спекой
  помощников (два частично), остальные помечены отметкой с причиной — прогон снимков заводит
  тесты по историям и идентификатора сценария не несёт.
- `npm run check:specs` по этому спеку не даёт ни одного замечания; всего по дереву их 373
  против 375 до правки — эта работа не добавила ни одного.

- Владелец ответил: `Q-2` закрыт — 86 с приемлемы; долги заведены задачами #330–#333. Открытых
  вопросов у договорённости не осталось.
- Прогон CI на PR #329: все шаги зелёные, включая новый «Visual tests (ui-kit-v2)» и старый
  «Visual tests» первой витрины. Красным заход сделала послепрогонная чистка
  `Post Cache node_modules` — `Path Validation Error`, склад pnpm на раннере; к правке отношения
  не имеет, на прошлом заходе (#326, тот же файл) шаг был зелёным.
