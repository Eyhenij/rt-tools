<!-- rt-kit v0.10.0 · samples/tasks/_template/progress.md · fc2dd20e34b0 · правится надстройкой, не здесь -->

# Ход работы

## Где стоим

Перезаписывается каждым заходом, а не дописывается.

- **Состояние:** `этап-идёт`
- **Этап:** 3 из 3 — замер доли отбитий не на правке файла
- **Сделано:** первый и второй этапы закрыты и проверены набором; пустое устройство и поток
  ошибок записью не считаются, тело документа на месте путей не даёт
- **Следующий шаг:** замер доли; строка доли лежит в невлитой ветке предыдущей задачи, и чем
  закрывать этап, спрошено у владельца
- **Незакоммиченное:** второй этап
- **Ждём владельца:** чем закрывается третий этап — замером «до» сейчас или ожиданием слияния
- **PR:** ещё не открыт

## Решения по ходу

- **Задача взята, пока PR предыдущей ждёт прогона** — так требует правило: в состоянии
  «работа отдана» обязательное действие смотрит на следующую задачу, а не на открытый PR.
  Прошлый ход это требование не исполнил, и владелец назвал промах.

## Заходы

### 20 августа 2026

- Ветка заведена от вершины главной, колонка переведена, разбор просьбы и замысел написаны.
- Разведка по признаку записи: широта списка имён объявлена в комментарии при признаке и
  подкреплена разбором происшествия 15 августа, поэтому список не сужается. Два приёма с
  разбором не спорят — пустое устройство и счёт путей по заголовку команды, а не по телу
  документа на месте. Записаны открытым вопросом Q-3 в замысле эпика.
- Соседняя работа RT-962 закрыта тем же заходом: главная влита, конфликт журнала разобран,
  папка задачи уехала в описание прошлого, всё запушено. PR #970 ждёт прогона на вершине,
  черновик не снят.

### 21 августа 2026

- **Первый этап сделан.** Признак записывающей команды снимает перенаправление в пустое
  устройство и в поток ошибок до разбора: `sed` вычищает `>/dev/null`, `>/dev/stderr` и
  сведение потоков, и только остаток судится прежним образцом. Настоящая запись рядом с
  заглушённым потоком остаётся видной.
- Четыре сценария в наборе гарда замысла: поиск с заглушённым потоком, чтение в пустое
  устройство, сведение потоков и запись рядом с пустым устройством. `bash
projects/agent-kit/tests/run.sh` — все наборы зелёные, в гарде замысла 47 сценариев.
- Раскладка обновила разложенную копию профиля умолчаний одним файлом.
- **Второй этап сделан.** Пути берутся из заголовка команды: тело документа на месте
  отбрасывается разбором маркера, кроме случая, когда команду ведёт интерпретатор — там код
  приходит телом, и путь записи стоит именно в нём. Два сценария: чужой путь в теле правила не
  требует, путь в заголовке отбивается по-прежнему. Набор зелёный, в гарде замысла 49
  сценариев.
- Проверено живьём после раскладки: команда, кладущая текст с путём под каталогом кода в теле,
  прошла без отказа — прежде такая отбивалась.
- **Третий этап упёрся.** Строка доли отбитий не на правке файла лежит в ветке предыдущей
  задачи и в главную не влита: сводка на этом дереве печатает только разбивку по родам.
  Своими числами замер «до» на нынешнем отрезке — 78 отбитий на команде из 84, то есть 93
  процента; замер «после» появится только заходами на новой логике.
