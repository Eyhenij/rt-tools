# Ход работы

## Где стоим

- **Этап:** все пять сделаны, работа отдана на разбор; сверх замысла владелец дал команды
  местного запуска — четыре сделаны, пятая (прод-база) не начата
- **Сделано:** эпик объявлен карточкой RT-779 и замыслом `docs/plans/admin-ui-to-reference.md`,
  пять задач заведены и стоят в очереди работ, разбор просьбы и замысел этой задачи написаны;
  этап 1 закрыт — договорённость лежит в `docs/specs/message-bus/proposed/list-page-slots/`,
  сценарии `SC-MB-110` — `SC-MB-116`, `pnpm run check:specs` зелёная; этап 2 закрыт — токен
  хоста и модель спрошенного объявлены в слое утилит общего ядра, общая основа списочного
  экрана их исполняет, сборка админки зелёная; этапы 3 и 4 закрыты кодом — у страницы не
  осталось ни одного события, три экрана кладут отбор в левый слот и называют префикс, семь
  сценариев закрыты спеками, сборка и линтеры зелёные. Этап 4 закрыт обходом: сквозной набор — 27 сценариев зелёными на прод-сборке стенда, замер
  трёх разделов подтвердил отбор в левой части тулбара, обновление и настройку столбцов у края,
  промежуток тулбар — таблица 16 px при шаге колонки 16 px и отсутствие узла над таблицей;
  на 599 px части тулбара переносятся, не режутся. Этап 5 закрыт — статья в правиле надстройкой
  и готовый код в паттерне
- **Следующий шаг:** команда `serve:api:prod-db` — путь в базу владелец выбрал: разовый
  контейнер-ретранслятор в сети прода плюс ssh-туннель. Затем прогон конвейера; после одобрения
  — вливание договорённости в спек поддомена и разбор папки задачи, затем снятие черновика
- **Незакоммиченное:** нет
- **Ждём владельца:** разбор PR #791
- **PR:** #791, черновик, задача переставлена в `in-review`. Автор — владелец: у токена машинной
  записи кончился предел обращений к хостингу, и ревьювера у такого PR не поставить (автор не
  бывает ревьювером). Прошлые PR открывались так же

## Решения по ходу

- **Подписи в эпик не входят** — спек домена требует словарь приложения, и это выполнено;
  многоязычия владелец не просил. Затронутый этап замысла: нет.
- **Вход создания записи страница не заводит вовсе** — замысел называл в этапе 3 флаги, но
  единственным их предметом оказалось создание записи, а создавать админке нечего: заведённый
  заранее и никем не включённый вход проверяется только чтением кода. Записан открытым вопросом
  `Q-27` договорённости. Затронутый этап замысла: 3 — флагов у страницы не появится.
- **Подсказку называют все три раздела** — вход, который не включает никто, проверяется только
  чтением кода, а замысел держит подсказку в сделанном. Тексты трёх подсказок заведены в
  словаре приложения рядом с названиями разделов. Затронутый этап замысла: нет.
- **Слоты объявлены своими директивами, а не проекцией по образцу тулбара кита** — тулбар
  кита рисует левую половину только тогда, когда в ней что-то есть, и разметку раздела надо
  провести через это условие; проекция отдала бы её тулбару безусловно, и пустая половина
  занимала бы место. Затронутый этап замысла: нет.
- **Статья дописана отдельным разделом правила, а не в пакетный** — надстройка замещает раздел
  целиком, и приписка к «Как закон применяется здесь» унесла бы с собой те тринадцать пунктов
  пакета, которых дерево не переписывало. Цена: сверка привязок статьи этого раздела не видит —
  она читает пункты одного раздела, — поэтому привязки восьми статей лежат строками в
  `implementation.md`. Затронутый этап замысла: нет.

- **Команды местного запуска идут этой же веткой** — слово владельца прямое: «делай в этой
  ветке, ты обязан был завести это с самого момента инициализации». Правило поставки требует
  обратного — в ветке только та правка, за которой её заводили, — и я назвал это владельцу
  до правки. Решение его. Затронутый этап замысла: нет; в PR #791 приезжает сверх задачи
  четыре команды `serve:*` и пара документов при них.

## Заходы

### 16 августа 2026

Заход шёл с другой задачи и кончился заведением эпика. До него закрыты RT-764 и RT-771, заведена
первая учётная запись приёмника.

Расхождение показа с деревом-образцом установлено чтением обеих сторон: общая страница списка,
три списка и отсутствие эталонных снимков. Панели подробностей и вход собраны приёмами образца
и требуют доводки, а не пересборки. Первый разбор расхождения был неверен — судили по одному
файлу образца; разобрано в `docs/postmortems/2026-08-16-sample-judged-by-one-file.md`.

### 17 августа 2026

Заход прошёл все пять этапов подряд. Договорённость написана до кода, токен хоста и слоты
заведены, три экрана переставлены, статья дописана в правило и паттерн. Проверено сквозным
набором на прод-сборке стенда и замерами трёх разделов; PR #791 открыт черновиком.

Найденное по ходу: сквозной набор держался за якоря `list-*` общей страницы, и переименование
их в `<префикс>-*` уронило бы его — обвязка получила помощник `pageQa`, а не строку на месте.

Владелец дал сверх задачи команды местного запуска. Заведены четыре: `serve:db`, `serve:api`,
`serve:admin`, `serve:stand`. Проверены подъёмом: база отвечает контейнером, приёмник встал на
3000 (`/api/health` — 200, `/api/trees` без входа — 401), стенд сквозного набора поднимался
этим же заходом, цель админки — та, что держит дев-сервер на 4200. Пара документов: команды в
`CLAUDE.md`, снято утверждение «приложений здесь нет» в компаньоне правила проверки браузером.

Найденное чужое: `nx.json` объявляет плагин `@nx/eslint/plugin`, а в зависимостях стоит только
`@nx/eslint-plugin` — самого пакета в дереве нет. Холодный демон Nx на этом падает: первый
`pnpm run serve:api` кончился отказом, второй прошёл. Отдельная задача, к этой правке отношения
не имеет.

Не сделано: команда против прод-базы. Владелец выбрал путь — разовый контейнер-ретранслятор в
сети прода и ssh-туннель к нему, — но заход упёрся в порог заполнения окна раньше первой строки.
