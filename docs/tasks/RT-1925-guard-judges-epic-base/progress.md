# Ход работы

## Где стоим

Переписывается каждым заходом, а не дописывается.

- **State:** `этап-идёт`
- **Этап:** 1–4 закрыты, впереди 5 — раскладка и тексты
- **Сделано:** этапы 1 и 2. Состояние задачи несёт номер эпика; чтение объявления вынесено в свой
  модуль — его берут трое. Гард судит основание новой ветки против ветки эпика, а свежесть главной
  спрашивает у ветки эпика, где она чинится одним слиянием. Заявка задачи эпика идёт в ветку эпика:
  основание из довода команды судится, и свежесть спрашивается у эпика. Заявка самого эпика в
  главную отбивается, пока в его ветке лежит хоть одна папка задачи. Пробы — 17 ok, приняты на
  нарушении.
- **Следующий шаг:** этап 5 — раскладка, спека подобласти и её привязки, компаньон правила; сверить
  набор пакета целиком.
- **Незакоммиченное:** нет.
- **Ждём владельца:** слияния заявки #1948 в ветку эпика и #1930 — влита.
- **Заявка:** ещё не открыта.

## Решения по ходу

- **Папки задач выделены из модуля очереди в `board-task-dirs`.** Модуль очереди стоял на пределе
  длины, а эти трое ходят на диск и в сеть не ходят вовсе. Старые имена он переэкспортирует: их
  зовут три соседних модуля и команды дерева.
- **Эпик узнаётся по метке карточки, а не по виду имени ветки.** Ветка эпика и ветка задачи
  названы одинаково; метка объявлена профилем дерева ключом `RT_BOARD_EPIC_LABEL`, и дерево, не
  назвавшее её, этим условием не судится вовсе.
- **Условия об эпике вынесены в помощника `git-guard-delivery-epic.sh`.** Тем же приёмом, что папка
  задачи и подпись: гард нельзя разбить по событию — разбор команды у него общий, — и он разбивается
  по предмету.
- **Чтение объявления эпика вынесено в свой модуль `board-epic-link`.** Читателей стало трое, и
  каждый лежит в своём слое; прочитанное в двух местах объявление расходится молча. Модуль берёт
  ключ задач прямо из настроек — иначе вышло бы кольцо через модуль очереди.
- **Каждая фикстура набора, кладущая модуль очереди, кладёт и новый.** Раскол ресурса ломает
  наборы по имени файла: восемь наборов покраснели разом на отсутствующем импорте.
- **Ветка эпика вливается в ветки задач, а не обходится.** Гард требует вершину главной в
  основании новой ветки, и ветка эпика отстаёт от главной после каждого чужого слияния. Порядок:
  главная в ветку эпика, ветка эпика в ветку задачи.

## Заходы

### 09.09.2026

- Этапы 1–4 закрыты. Расколов модуль очереди дважды, обошёлся без правки соседей: старые имена
  переэкспортированы.
- Ветка эпика была отстающей на 24 коммита от главной и не несла слияния #1930. Догнана и
  отправлена, после чего от неё отведена ветка задачи.
