# Ход работ

## Где стоим

Переписывается каждой сессией, не дописывается.

- **Состояние:** `этап-идёт`
- **Этап:** 3 из 3 — Семейство без страницы входов названо причиной
- **Сделано:** этап 1 закрыт замером: 22 файла историй звали страницу `Default`, 60 —
  `Playground`; эталонов с именем `default` 19, с именем `playground` 5. Этап 2 закрыт:
  переименований `Default` не осталось, `Playground` 82, эталонов `default` 0, `playground` 24;
  `node tools/visual-gate.mjs ui-kit-v2` — 168 наборов, 546 тестов, 568 снимков, зелено.
- **Следующий шаг:** пройти `node tools/check-kit-coverage.mjs` и дописать причину тем семьям,
  у которых страницы входов нет по существу.
- **Не закоммичено:** папка задачи.
- **Ждём владельца:** ничего.
- **PR:** не открыт.

## Решения по ходу

- **Ветка отведена от ветки эпика, а не цепочкой от RT-1988.** Общих файлов с теми работами нет:
  там оснастка, тут истории и эталоны. Затронут этап плана: ни один.
- **Ветка эпика влита сюда вместе с подтянутым мейном.** До этого четыре кадра поля ввода
  сообщения краснели на любой ветке и на пустом кэше; с подтянутым мейном сличение зелёное целиком.
  Затронут этап плана: признак готовности этапа 2 закрылся только после этого.

## Находки

- **Красные кадры поля ввода дали отставшую от главной ветку, а не эталоны.** Я сначала решил
  обратное, завёл под это задачу RT-2117, переснял четыре эталона и открыл правку #2120. Числа, на
  которых строился вывод, приходили с витрины, поднятой на дереве без мейна и рядом со второй
  живой витриной того же кита: коробка поля мерилась 195. На свежей витрине без соседей — 192 три
  раза подряд, ровно как в прежних эталонах. Пересъёмка откачена, правка и задача закрыты.
- **Две живые витрины одного кита пишут в один кэш сборки.** Сам по себе признак этим не доказан,
  но оба случая, где ширина мерилась 195, были именно такими. Отдельной задачи не заведено:
  повторяемого случая нет.

## Сессии

### 2026-09-15

- Задача взята, папка заведена, счёт снят.
- Этап 2 закрыт: двадцать два `Default` переименованы в `Playground` вместе с девятнадцатью
  эталонами; ветка эпика с мейном влита, проверка снимков зелёная.
