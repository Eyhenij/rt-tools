# Замысел

Задача: #351. Ветка: `RT-351-publish-tail-cleanup`.

**Задача:** RT-351 · **Ветка:** RT-351-publish-tail-cleanup
**Драфт:** не требуется — работа не заводит поведения
**Поведение:** не меняется — уборка за публикацией: лок-файл, тексты договорённости и папки
задач. Кода приложений и китов правка не задевает.

После написания этот файл не правится. Пересмотр этапа идёт в `progress.md` решением по ходу.

## Что считается сделанным

- `pnpm install` не правит ни `pnpm-lock.yaml`, ни `pnpm-workspace.yaml`.
- Директории `docs/specs/ui-kit-v2/proposed/` в дереве нет, а её четырнадцать сценариев и
  двенадцать правил стоят в спеке домена с привязками к живым символам.
- `npm run check:specs` не показывает расхождений по домену `ui-kit-v2`.
- Папок закрытых задач #349 и #318 в `docs/tasks/` нет, а в `docs/archive/` есть записи о них.
- `npm run check:board` выходит нулём.
- `ROUTE-ASIDE-DEFECTS.md` из корня дерева убран.

## Этапы

### 1. Лок-файл согласован с деревом

- **Что делается:** правка `pnpm-lock.yaml` и `pnpm-workspace.yaml`, которую сделала установка
  после публикации, коммитится как есть.
- **Признак готовности:** повторный `pnpm install` не оставляет правок в рабочем дереве.
- **Чем проверяется:** `pnpm install --lockfile-only` и `git status`.

### 2. Договорённость влита в спек домена

- **Что делается:** правила, сценарии и привязки из `proposed/route-aside-leave/` переносятся в
  спек домена; заголовок и разделы спека обобщаются — сегодня он назван по одной своей фиче, а
  становится домом двух. Номера сценариев не меняются: на них ссылаются заголовки тестов.
- **Признак готовности:** `proposed/` удалена, ни одно правило не потеряно.
- **Чем проверяется:** `npm run check:specs`.

### 3. Папки закрытых задач разобраны

- **Что делается:** из папок #349 и #318 в `docs/archive/` уезжает то, что объясняет
  состоявшиеся решения; остальное удаляется вместе с папками.
- **Признак готовности:** папок нет, записи в архиве есть.
- **Чем проверяется:** `npm run check:board`.

### 4. Документ из корня убран

- **Что делается:** `ROUTE-ASIDE-DEFECTS.md` сносится: его слова целиком перенесены в разбор
  просьбы работы #349, а тот уезжает в архив этапом 3.
- **Признак готовности:** файла в корне нет, слова владельца не потеряны.
- **Чем проверяется:** чтение записи в архиве.

## Чего эта работа не делает

- Не публикует ничего заново: три версии уже в реестре.
- Не правит код китов и их спеки.
- Не гоняет проверку на приложении-потребителе — чужое дерево, и его в этом нет.
