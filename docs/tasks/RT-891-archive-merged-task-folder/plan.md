# Замысел

**Задача:** RT-891 · **Ветка:** RT-891-archive-merged-task-folder
**Поведение:** не меняется — работа снимает папку закрытой задачи и дописывает описание
прошлого; кода приложения она не задевает.

После написания этот файл не правится. Пересмотр этапа идёт в `progress.md` решением по ходу.

## След задачи

| Что     | Где                                                                  |
| ------- | -------------------------------------------------------------------- |
| Законы  | `docs/constitution/delivery.md`, `docs/constitution/work-conduct.md` |
| Правила | `.claude/skills/task-flow/`, `.claude/skills/task-flow-close/`       |
| Тексты  | `docs/tasks/RT-887-ci-runner-disk-cleanup/`, `docs/archive/`         |

## Что считается сделанным

- Слова владельца из разбора просьбы RT-887 лежат в `docs/archive/` вместе с решениями по ходу,
  у которых записан довод.
- Папки `docs/tasks/RT-887-ci-runner-disk-cleanup/` в дереве нет.
- Своя папка задачи снята тем же коммитом, и её разбор просьбы тоже уехал в архив.
- `pnpm run check:board` не называет среди текущих ни одной папки закрытой задачи.

## Этапы

### 1. Чужая папка разбирается

- **Что делается:** разбор просьбы и решения по ходу RT-887 сводятся одним файлом в
  `docs/archive/`, замысел удаляется, папка снимается.
- **Признак готовности:** в архиве лежит запись о том, чем кончилась RT-887 и почему она шла
  так; папки в `docs/tasks/` нет.
- **Чем проверяется:** `pnpm run check:docs`, `pnpm run check:board`.

### 2. Своя папка разбирается тем же коммитом

- **Что делается:** разбор просьбы этой задачи уезжает в архив, папка снимается — последним
  коммитом, после того как остальные проверки зелёные.
- **Признак готовности:** сверка очереди работ не называет ни одной папки.
- **Чем проверяется:** `pnpm run check:board`.

## Чего эта работа не делает

- Папку `docs/tasks/RT-880-tree-identity/` не трогает: её задача открыта и ведётся в другой
  рабочей копии.
- Гард поставки не чинит и не проверяет: пропустил ли он слияние с лежащей папкой — вопрос
  отдельной задачи, он записан в разборе просьбы.
