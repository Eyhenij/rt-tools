# Ход работы

## Где стоим

Перезаписывается каждым заходом, а не дописывается.

- **Этап:** 3 из 6 закрыты — приложение, вход, оболочка с меню; следующий этап 4, раскладка
  законов и правил
- **Сделано:** `apps/message-bus-admin` собран на ките; домен входа разложен на шесть слоёв —
  модели и род отказа, обращение к трём операциям приёмника, состояние с источником действия и
  `exhaustMap`, форма, гвард маршрута и экран; общий домен оболочки — меню объявлением, каркас
  кита, пустой раздел «Обзор»; границы семьи выписаны ребро за ребром; публикуемым пакетам
  дана метка `scope:package`
- **Следующий шаг:** этап 4 — снять пять законов из `skip` файла `.claude/rt-kit.json`,
  объявить свойство `admin` в `has`, разложить правила и паттерны
- **Незакоммиченное:** нет
- **Ждём владельца:** нет — слово на пуш и отчёт по всей линии дано
- **Отчёт:** ещё не открыт

## Долги этапов, закрытых не до конца

- **Спек нет ни у одной либы админки, кроме `auth/util`.** `passWithNoTests: true` проставлен
  всем одиннадцати конфигам — без него общий прогон краснел строкой «No test files found», то
  есть красным отвечало отсутствие спек, а не их падение. Зелёный прогон при этом ничего не
  обещает. Закрывается спеками на разбор отказа стора, на гвард и на форму — они и есть
  признаки готовности этапов 2 и 3.
- **Живого прогона против поднятого приёмника не было.** Признак готовности этапа 2 требует
  годной и негодной пары на настоящем входе; проверено пока только сборкой и линтом.
- **Замера в браузере не было.** Признак готовности этапа 3.

## Решения по ходу

- **Колонка задачи переставлена вызовами хостинга, а не командой.** Ветка отведена от главной, а
  починка очереди работ лежит в ветке задачи #627 и в главную ещё не влита. Затронутый этап
  замысла: нет.
- **Приложение зовётся `message-bus-admin`, семья либ — `libs/message-bus-admin`, префикс
  компонентов — `admin`.** Дерево уже называет серверную сторону `libs/message-bus-api`; голое
  `admin` не сказало бы, чьей админкой оно приходится. Затронутый этап замысла: 1.
- **Раздел «Обзор» лежит в общем домене оболочки, а не в своём.** Предмета за ним пока нет, и
  домен из шести слоёв под пустой экран дал бы шесть либ, которые нечем наполнить. Уходит вместе
  с первым настоящим разделом. Затронутый этап замысла: 3.
- **Публикуемым пакетам дана метка `scope:package`.** Без имени цели либа с объявленным списком
  прав не видит кит вовсе: первое подходящее правило выигрывает, и до общего разрешения дело не
  доходит. Сами пакеты правилом-источником по-прежнему не сужены. Затронутый этап замысла: 2.
- **Свойство `admin` в `has` ещё не объявлено.** Оно нужно правилу `entity-conventions`,
  помеченному `needs-admin`, и объявляется этапом 4 вместе с раскладкой — раньше него класть
  нечего. Затронутый этап замысла: нет.

## Заходы

### 2026-08-15

- Тела задач #586 и #587 приведены к слову владельца: каркас админки вернулся в #586, три списка
  и сквозной набор остались в #587.
- Заведено приложение и одиннадцать либ семьи админки. Проверено: `nx build message-bus-admin`
  зелёный, `nx run-many -t lint test build` по девяти проектам админки зелёный, 5 тестов
  исполнилось (4 на род отказа входа, 1 на корень приложения), `eslint` по всей семье без
  замечаний.
- `check:layers` заработал по семье впервые — она объявлена ключом `families`. Осталось 16
  расхождений: 11 про префикс (доэтапное, задача #641), 5 доэтапных чужих.
- Споткнулись трижды: генератор кладёт `vite.config.mts` без `passWithNoTests` — переименовано в
  `vitest.config.mts` и дописано всем одиннадцати; шина стора берёт `type`, а не `msg`; стили
  берутся исходником кита, а не собранным `tokens.css` — пакет и потребитель живут в одном
  дереве.
- Доэтапное, не этой работы: проверка раскладки требует префикс `vm` строкой в коде — заведена
  задача #641.
