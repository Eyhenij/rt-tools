# Ход работы

## Где стоим

Перезаписывается каждым заходом, а не дописывается.

- **Этап:** 3 из 6 закрыт — раздел предложений собран той же основой, покрыт спеками и проверен
  замером в браузере против поднятого приёмника
- **Сделано:** всё, что было к прошлому заходу, плюс раздел предложений семью слоями (модели,
  маппер, столбцы, обращение, оба стора, вид записи, экран списка, панель, маршруты), пункт
  меню, подписи пунктов из словаря, спеки раздела; правка кита — закрытая панель возвращает
  экран с его параметрами адреса; заведены задачи #676 и #681
- **Следующий шаг:** этап 4 — раздел сводок деревьев на той же основе; сводка показывается
  текстом, а не разметкой
- **Незакоммиченное:** нет
- **Ждём владельца:** нет — слово на работу по всей линии дано
- **Отчёт:** ещё не открыт

## Долги этапов, закрытых не до конца

- **Долги задачи #586 переходят сюда:** спек нет ни у одной либы админки, кроме `auth/util`
  (`passWithNoTests: true` стоит у всех одиннадцати конфигов, и зелёный прогон поэтому ничего не
  обещает); живого прогона входа против поднятого приёмника не было; замера в браузере не было.
  Закрываются этапами 2 и 5. Общий слой этапом 1 из долга вышел: спеки есть у всех пяти его либ.
- **Долги этапа 1:**
    - **Типы либ админки не проверяет ничто, кроме сборки приложения.** Цели `typecheck` у них
      нет, а приложение импортирует не все: два вызова отказа стора, написанные без обязательного
      довода, простояли незамеченными и нашлись только прямым вызовом компилятора. Закрывается
      этапом 6 вместе с набором гейта.
    - Кончившийся вход и замер в браузере закрыты этапом 2.
- **Долги этапа 2:**
    - **Переход по страницам нажатием не проверен:** записей на стенде девять, а переключатель
      прячет себя, пока их меньше двадцати. Адресом вторая страница проверена — запрос уходит с
      её номером и список пуст. Закрывается этапом 5 на засеянном хранилище.
    - **Пустая вторая страница объясняет себя так же, как пустой список вовсе:** «ни одно дерево
      их пока не присылало» при девяти записях в списке. Договорённость называет две пустоты,
      этой третьей у неё нет; решается на разборе спека этапом 6.
    - Задача на сломанную сборку образа заведена этапом 3 — #676.
- **Долги этапа 3:**
    - **Отказ чтения с повтором на этом разделе замером не подтверждён.** Приёмник во время
      замера отвечал исправно, а гасить его ради одного состояния значило бы ронять весь стенд.
      Состояние держит общий вид страницы, и на разделе разборов оно замерено этапом 2.
      Закрывается этапом 5 сквозной спекой: там стенд поднимается своим.
    - **Переход по страницам нажатием не проверен и здесь:** предложений на стенде шесть, а
      переключатель прячет себя, пока записей меньше двадцати. Тот же долг, что у этапа 2, и
      закрывается тем же — этапом 5 на засеянном хранилище.

## Решения по ходу

- **Словарь приложения — свой объект, а не Transloco.** Кит просит функцию-переводчик и перечень
  ключей знает сам. Затронутый этап замысла: 1.
- **Общий слой — один домен `common/core` на пять слоёв, а не домен под каждую механику.**
  Правило раскладки прямо предупреждает: домен под механику оставляет слои пустыми навсегда.
  Словарь, выборка, отказ, обращение и основа стора лежат слоями одного домена. Затронутый этап
  замысла: 1.
- **Деревья для отбора лежат в общем слое, а не своим доменом.** Своего экрана у них нет — они
  живут отбором в тулбаре каждого списка, и домен под них дал бы четыре пустых слоя из пяти.
  Затронутый этап замысла: 1.
- **Чтение списка идёт `switchMap`, деревья — `exhaustMap`.** Отбор человек меняет, не дожидаясь
  ответа, и показать хочет последний названный; деревья же во всех трёх разделах одни и те же, и
  повторный запрос поверх идущего ничего не принесёт. Затронутый этап замысла: 1.
- **Тег либы в границах пишется литералом, а не константой.** Проверка раскладки ищет строку
  `sourceTag: '<тег>'` буквально: константа в этом месте читается ею как «либа не описана».
  Списки зависимостей константами при этом законны. Затронутый этап замысла: нет.
- **Отказ чтения занимает место списка, а не подаётся тостом.** Правило списочного экрана велит
  тост, но договорённость требует состояния «отказ чтения с повтором»: тост уходит сам, и
  человеку, вернувшемуся к экрану, повторять нечем. Причина, номер обращения и повтор стоят на
  месте таблицы. Затронутый этап замысла: 1.
- **Раскладка страницы списка объявлена блоком `admin-page` в общем слое приложения.** Своего
  компонента страницы кит не везёт, а раскладка одна на три раздела: объявленная в файле стилей
  экрана, она разошлась бы между разделами молча. Затронутый этап замысла: 1.
- **Приложение видит словарь общего слоя — ребро дописано в границы.** Кит настраивается там же,
  где иконки, а подписи экранов и подписи кита лежат одним объектом: разложенные по двум местам,
  они расходятся молча. Затронутый этап замысла: 1.
- **Ширину таблице задаёт правило по селектору кита, а не директива элемента.** Таблицу
  объявляет раздел и отдаёт странице проекцией, а спроецированный узел имени блока не получает.
  Затронутый этап замысла: 1.

- **Сторона контракта раздела объявлена в его слое утилит, а не в общей либе.** У приёмника та
  же строка несёт время временем — так его отдаёт хранилище, — а по сети приезжает строка: общее
  объявление лгало бы одной из сторон. Общее у них то, что переживает передачу, — страница и
  дерево. Затронутый этап замысла: 2.
- **Основа списочного стора переводит строку сама.** Перевод обязан случиться до того, как строки
  лягут в состояние: иначе на экран попадает приехавшее по сети — со временем строкой и полями
  контракта. Затронутый этап замысла: 2.
- **Типы либы проверяются прямым вызовом компилятора.** Цели `typecheck` у либ админки нет, а
  сборка приложения видит только импортированное; вызов компилятора по конфигу либы с корнем
  дерева отвечает по одной либе за секунды. Затронутый этап замысла: нет.

- **Подписи пунктов меню переехали из литералов в словарь, и меню видит общий слой.** Тем же
  ключом называет себя заголовок экрана и заголовок вкладки браузера; написанная в декларации
  заново подпись расходилась бы с ними молча, а к третьему разделу таких мест стало бы три.
  Ребро «меню видит словарь» дописано в границы. Затронутый этап замысла: 3.
- **Перенос параметров адреса при закрытии панели починен в ките, а не обойдён в разделе.**
  Уход с адреса снимал их все, и закрытая панель возвращала первую страницу без отбора — это
  верно для всех трёх разделов сразу, и место у правки одно. Альтернатива — переопределить уход
  в каждой панели — отвергнута: три копии одного обхода. Затронутый этап замысла: 3.
- **Задача на сломанную сборку образа заведена учётной записью владельца, а не машинной.**
  Учётная запись `rt-tools-agent` помечена хостингом: её страница отвечает «не найдено» всем,
  включая владельца, GraphQL её не разрешает вовсе, а её собственный токен получает нулевой
  предел обращений. Исполнителем поэтому назван владелец — иначе задачи не завести совсем.
  Затронутый этап замысла: нет.

## Заходы

### 2026-08-15

- Заведена ветка, забран довесок задачи #586, собрана папка задачи. Договорённость и сценарии
  прочитаны целиком: серверная сторона готова задачей #585 — все десять операций на месте, и
  правится в этой работе только фронт.
- Доэтапное, не этой работы: конвейер был красным на каждой ветке — клиент хранилища на свежем
  чекауте не генерировался. Заведена задача #659, починка открыта отчётом #660.
- Этап 1, половина. Заведены пять либ общего слоя копированием образца соседней либы: генератора
  доменных либ, о котором говорит паттерн раскладки, в этом дереве нет — `tools/generate-domain-lib.mjs`
  не существует. Написаны словарь с подписями кита, выборка в адресе, род отказа чтения,
  обращение к операциям с пределом ожидания в пятнадцать секунд, основа списочного стора и стор
  деревьев. Проверено: `nx run-many -t lint test` по пяти новым либам зелёный,
  `nx build message-bus-admin` зелёный, `check:layers` даёт 21 расхождение против 16 — все пять
  прибавленных про префикс `vm`, то есть доэтапные задачи #641.
- Споткнулись дважды: линтер требует аннотацию типа у объекта словаря, а она стёрла бы
  литеральный тип, на котором стоит перечень ключей, — снято точечным отключением с причиной,
  как это сделано в самом ките; проверка раскладки не видит тег, записанный в границах
  константой.
- Этап 1 дописан и закрыт. Написаны общий вид страницы списка с заголовком, тулбаром, отбором по
  дереву, местом под таблицу, переключателем страниц и отказом с повтором; общая основа
  списочного экрана — выборка из адреса и в адрес, чтение от адреса, порядок по заголовку
  столбца, объяснение пустоты и уход в панель подробностей; раскладка `admin-page` в общем слое
  приложения; подписи кита подключены в `app.config.ts`. Спеки: разбор выборки, перевод её в
  модели кита, основа стора, вид страницы, отбор и основа экрана.
- Найдено и починено доэтапное своё: два стора звали отказ загрузки без обязательного довода —
  это не компилируется, а не ловилось ничем, потому что цели `typecheck` у либ админки нет, и
  приложение их пока не импортирует. Заменено вызовом без оповещения: отказ человек видит на
  месте списка, и строка в журнале браузера рядом с ним ничего не прибавляет.
- Этап 2, почти целиком. Домен разборов заведён семью слоями тем же копированием соседней либы; алиасы,
  границы и README на месте. Написаны модель двумя уровнями и двумя сторонами, мапперы, столбцы
  и поля порядка, обращение к чтению записи, список на общей основе стора, запись своим стором,
  вид записи с текстом и раскладка панели в общем слое приложения. Проверено: линт восьми
  проектов, спеки общего слоя, сборка приложения, прямой вызов компилятора по новым либам;
  `check:layers` прибавил семь расхождений — все того же рода про префикс, что и прежние.
- Экран списка собран общей основой и общим видом страницы, панель подробностей — основой панели
  из кита; маршруты раздела, пункт меню на месте, раздел «Обзор» снят вместе со своим маршрутом.
  Панель читает запись сама: разрешатель основы читает отсутствие записи как повод уйти с адреса,
  а договорённость требует сказать человеку, что записи нет. Проверено линтом двадцати четырёх
  проектов и сборкой приложения — она же проверка шаблонов раздела.
- Проверено: `nx run-many -t lint` по семнадцати проектам админки зелёный, `nx run-many -t test`
  по пяти либам общего слоя зелёный, `nx build message-bus-admin` зелёный, `lint:styles`,
  `check:styles`, `check:docs` и `check:reuse` без новых расхождений, `check:layers` даёт те же
  двадцать одно, что и до захода. Замера в браузере не было — показывать страницу пока негде.

### 2026-08-15, второй заход

- Спеки раздела написаны: маппер (перевод времени, чтение дерева по полям, отказ полю не из
  модели), оба стора (адрес операции, перевод строки, «записи нет» против поломки чтения, гонка
  соседних записей), вид записи (текст текстом), экран списка (выборка из адреса в запрос, строка
  таблицы, уход в панель) и панель (чтение признаком из адреса, отсутствие записи, соседняя
  запись). Выбор столбцов в спеке экрана держит рукописный двойник: настоящий лежит в базе
  браузера, которой в стенде спек нет, и её отказ ронял прогон.
- Кончившийся вход уводит на вход: перехватчик в домене входа, обращения самого входа он не
  трогает, отказ идёт дальше нетронутым. Заодно заведены первые спеки обеих либ домена входа —
  до этого там не было ни одной, и зелёный прогон ничего не обещал.
- Первый замер в браузере против поднятого приёмника показал три вещи, которых не видели ни
  спеки, ни сборка: экран из одного заголовка — приложение не ставило основание кита; панель
  блоком под таблицей вместо правой шторки; подложка закрытой шторки, перехватывающая нажатия по
  всей странице. Все три починены, панель встала справа на 540 из 1440 пикселей во всю высоту.
- Маршрут панели переехал из детей списка в соседи: рисует шторку каркас, и аутлет объявлен там
  же, где он. Адрес панели называет раздел и запись, совпадение полное — двухсегментный адрес в
  аутлете иначе не разбирается вовсе.
- Дев-сервер получил прокси к приёмнику: без него админка на своей машине не читает ничего.
- Стенд для замера: приёмник поднят из исходников на порту 3000 — образ на нём отвечал старым
  кодом без операций чтения, и собрать новый не вышло (задача заведена не была, см. долги).
  Контейнер `rt-tools-api-1` остановлен и обратно не поднят: поднятый, он снова занял бы порт
  старым кодом. Для входа заведена учётная запись «Проверка» — владельцу решать, оставить её или
  снять.

### 2026-08-15, третий заход

- Долг этапа 2 закрыт: задача о сломанной сборке образа заведена — #676, в очереди работ,
  колонка «📋 Backlog». Заводить её пришлось учётной записью владельца: машинная помечена
  хостингом и не разрешается ни GraphQL, ни поиском, а её токен получает нулевой предел
  обращений. Заодно заведена #681 — проверка повторов падает на пути, зашитом в неё строкой, и
  не стоит ни в гейте, ни в конвейере.
- Этап 3 сделан и закрыт. Раздел предложений заведён семью слоями копированием раздела
  разборов; алиасы, границы, README на месте. Своими у него оказались ровно три вещи — модели с
  маппером, столбцы с полями порядка и поля панели. Закреплён ресурс: им предложение и
  опознаётся. Месяц записи виден только в панели — в строке списка приёмник его не отдаёт.
- Подписи пунктов меню переехали в словарь, и пункт раздела заведён там же, где заведён его
  экран.
- Спеки раздела написаны: маппер (перевод времени, чтение дерева по полям, отказ полю не из
  модели, отсутствие текста и месяца в строке списка), оба стора (адрес операции, умолчание
  порядка, перевод строки, «записи нет» против поломки чтения, гонка соседних записей), вид
  записи (пять полей и текст текстом), экран списка (выборка из адреса в запрос, поле порядка не
  из набора, строка таблицы, уход в панель) и панель (чтение признаком из адреса, месяц, текст,
  отсутствие записи, соседняя запись).
- Проверено: линт и спеки 31 проекта админки зелёные, `nx build message-bus-admin` зелёный,
  прямой вызов компилятора по всем семи новым либам молчит, `check:docs` без расхождений,
  `check:styles` и `check:reuse` без прибавленных, `check:layers` даёт 35 против 28 — все семь
  прибавленных того же рода про префикс `vm`, то есть задачи #641. `check:specs` даёт те же 207,
  что и до захода.
- Замер в браузере против поднятого приёмника нашёл дефект, которого не видели ни спеки, ни
  сборка: закрытая панель снимала параметры адреса, и список возвращался первой страницей без
  отбора — шесть строк вместо двух. Уход с адреса в основе панели шёл без переноса параметров.
  Починено в ките, сценарий `SC-UKV-52`, спека на него; после правки адрес несёт те же четыре
  параметра, строк снова две. То же проверено на разделе разборов — он собран той же основой.
- Замерено на разделе предложений: раздел открывается своим адресом и по нажатию плитки меню,
  подсветка переезжает на него; таблица показывает шесть записей четырьмя столбцами; время
  показано днём и минутами; отбор по дереву нажатием сужает список до двух записей одного
  дерева и уходит в адрес; порядок по заголовку столбца уходит в адрес и меняет первую строку;
  панель встаёт справа на 540 из 1440 пикселей во всю высоту, показывает пять полей и текст
  предложения без единого узла разметки внутри; записи, которой нет, панель говорит об этом.
