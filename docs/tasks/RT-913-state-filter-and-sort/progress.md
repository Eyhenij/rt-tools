<!-- rt-kit v0.10.0 · samples/tasks/_template/progress.md · fc2dd20e34b0 · правится надстройкой, не здесь -->

# Ход работы

## Где стоим

Перезаписывается каждым заходом, а не дописывается.

- **Состояние:** `работа-отдана`
- **Этап:** 3 из 3 — закрыт
- **Сделано:** разбор, договорённость и замысел; задача RT-987 заведена и стоит в замысле эпика седьмой; все три этапа закрыты: приёмник отбирает и упорядочивает, админка показывает отбор в тулбаре, пятнадцать сценариев закрыты сквозными спеками; гоночная спека порядка починена ожиданием
- **Следующий шаг:** дождаться прогона и разбора; последним коммитом влить договорённость в спек админки и разобрать папку задачи
- **Незакоммиченное:** нет
- **Ждём владельца:** разбора заявки #988
- **PR:** #988, черновик, ревьювер запрошен · прогон на `5b4c532e` был красным одной спекой, починка запушена

## Решения по ходу

- **Версия выпуска ушла своей задачей RT-987** — слово владельца: она просит миграции с индексом
  и операции приёмника о встретившихся версиях и откатывается порознь. Замысел эпика получил её
  седьмой строкой, рабочий порядок разбора груза съехал восьмым. Затронутый этап замысла: нет,
  решение принято до его записи.
- **Конвейер ролей не запускался** — вызовы субагентов в этом заходе выключены. Договорённость
  написана главным агентом; владельцу сказано, что конвейер можно прогнать отдельно.
  Затронутый этап замысла: нет.
- **Порядок в спеке SC-MB-230 ждётся, а не читается разово** — нажатие заголовка кладёт порядок
  в адрес сразу, а строки приезжают ответом приёмника, и прочитанные тут же — ещё прежние.
  Набор эту ловушку уже знал: `expectOnlyTree` и `rowsAfterChange` описывают её словами, а
  спека порядка их обошла. Альтернатива — отсчёт времени перед чтением — отвергнута: он судит
  загрузку машины, а не экран. Затронутый этап замысла: 3, признак готовности прежний.

## Заходы

### 2026-08-21

- Заход начат с передачи по RT-911: заявка влита, задача закрыта, влитые ветки сняты.
- RT-913 взята: задача переведена в работу, ветка и папка заведены.
- Разведка по дереву: столбец состояния уже стоит с первой задачи эпика, отбор по дереву рядом
  годится образцом, выборка живёт в адресе, порядок по набору хранилища выходит даром.
- Шесть обязательных вопросов заданы и отвечены; из ответов вышла вторая задача.
- Написано: разбор, договорённость с пятнадцатью сценариями, замысел с тремя этапами.
  `node tools/check-specs.mjs` — код возврата 0.
- Этап 1 закрыт: выборка груза заведена своим модулем общей либы, состояние встало в отбор и в
  набор сортируемых полей обеих операций. Прогон `pnpx nx run-many -t test -p message-bus-common
message-bus-api-postmortems-data-access message-bus-api-proposals-data-access
message-bus-api-postmortems-feature message-bus-api-proposals-feature` — 113 спек зелёных,
  красных нет; линт пяти проектов зелёный; `pnpx nx build message-bus` собрался.
- По ходу нашлось, что сходство набора состояний со схемой хранилища ничем не проверялось, хотя
  комментарий это обещал: заведены две спеки, вторая держит порядок объявления — на нём стоит
  порядок по состоянию.
- Этап 2 закрыт: состояние заведено в выборке адреса, у общей основы завёлся признак «сужен ли»
  список, рядом с отбором по дереву встал свой отбор по состоянию, столбец состояния стал
  сортируемым в обоих разделах. Прогон `pnpx nx run-many -t test -p
message-bus-admin-common-core-util message-bus-admin-common-core-ui
message-bus-admin-common-core-feature message-bus-admin-postmortems-feature-list
message-bus-admin-proposals-feature-list` — 90 спек зелёных, красных нет; линт семи проектов
  зелёный; `pnpx nx build message-bus-admin` собрался.
- Подпись пустого списка по отбору перестала называть проект: отборов теперь два, и прежняя
  строка звала снять не тот.
- Этап 3 закрыт: пятнадцать сценариев закрыты сквозными спеками, узкий экран подтверждён
  замером двух отборов, правила получили строки привязки к коду. Первый прогон
  `pnpx nx e2e message-bus-admin-e2e` дал 12 красных: одиннадцать — расхождение эталонных
  кадров на 5716 точек (1 %) от второго отбора в полосе, одно — своё: столбец состояния не был
  помечен сортируемым в наборе столбцов, и заголовок сортировки кит не рисовал вовсе. Пометка
  поставлена, одиннадцать эталонов пересняты, кадры разделов без груза не тронуты. Повторный
  прогон — 81 зелёная, красных нет.
- Раздел приглашений один раз упал на числе строк (пять вместо четырёх) в прогоне с
  пересъёмкой. Правку не задевает — набор без пересъёмки его не повторил.
- Ветка запушена, заявка #988 открыта черновиком: автор — машинная запись, ревьювер запрошен,
  метка от задачи. Задача переведена в разбор. `npm run check:board` по этой задаче чист, кроме
  ожидаемой строки о неразобранной папке — её снимает последний коммит.
- Заход после передачи: прогон на `5b4c532e` кончился красным — одна спека из 81,
  `SC-MB-230 — заголовок столбца состояния меняет порядок списка`. Разобрано по журналу
  задания: в момент чтения столбец нёс исходный порядок, то есть список ещё не перечитан.
  Дефекта экрана нет — соседняя `SC-MB-231`, открывающая адрес сразу с `sort=state`, зелёная.
- Спека переписана на ожидание порядка через `expect.poll`. Прогон
  `pnpx nx e2e message-bus-admin-e2e --args="--project=chromium src/list-state-filter.spec.ts --repeat-each=3"`
  — 30 зелёных, красных нет; `pnpx nx lint message-bus-admin-e2e` зелёный.
