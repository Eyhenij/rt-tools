<!-- rt-kit v0.10.0 · samples/tasks/_template/progress.md · fc2dd20e34b0 · правится надстройкой, не здесь -->

# Ход работы

## Где стоим

Перезаписывается каждым заходом, а не дописывается.

- **Состояние:** `этап-идёт`
- **Этап:** 2 из 3 — Админка: отбор в тулбаре и порядок по столбцу
- **Сделано:** разбор, договорённость и замысел; задача RT-987 заведена и стоит в замысле эпика седьмой; первый этап закрыт — приёмник отбирает и упорядочивает по состоянию
- **Следующий шаг:** завести состояние в выборке адреса админки и компонент отбора рядом с отбором по дереву
- **Незакоммиченное:** нет
- **Ждём владельца:** нет
- **PR:** ещё не открыт

## Решения по ходу

- **Версия выпуска ушла своей задачей RT-987** — слово владельца: она просит миграции с индексом
  и операции приёмника о встретившихся версиях и откатывается порознь. Замысел эпика получил её
  седьмой строкой, рабочий порядок разбора груза съехал восьмым. Затронутый этап замысла: нет,
  решение принято до его записи.
- **Конвейер ролей не запускался** — вызовы субагентов в этом заходе выключены. Договорённость
  написана главным агентом; владельцу сказано, что конвейер можно прогнать отдельно.
  Затронутый этап замысла: нет.

## Заходы

### 2026-08-21

- Заход начат с передачи по RT-911: заявка влита, задача закрыта, влитые ветки сняты.
- RT-913 взята: задача переведена в работу, ветка и папка заведены.
- Разведка по дереву: столбец состояния уже стоит с первой задачи эпика, отбор по дереву рядом
  годится образцом, выборка живёт в адресе, порядок по набору хранилища выходит даром.
- Шесть обязательных вопросов заданы и отвечены; из ответов вышла вторая задача.
- Написано: разбор, договорённость с пятнадцатью сценариями, замысел с тремя этапами.
  `node tools/check-specs.mjs` — код возврата 0.
- Этап 1 закрыт: выборка груза заведена своим модулем общей либы, состояние встало в отбор и в
  набор сортируемых полей обеих операций. Прогон `pnpx nx run-many -t test -p message-bus-common
message-bus-api-postmortems-data-access message-bus-api-proposals-data-access
message-bus-api-postmortems-feature message-bus-api-proposals-feature` — 113 спек зелёных,
  красных нет; линт пяти проектов зелёный; `pnpx nx build message-bus` собрался.
- По ходу нашлось, что сходство набора состояний со схемой хранилища ничем не проверялось, хотя
  комментарий это обещал: заведены две спеки, вторая держит порядок объявления — на нём стоит
  порядок по состоянию.
