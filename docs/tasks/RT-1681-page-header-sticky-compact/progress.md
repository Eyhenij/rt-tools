# Ход работы

## Где стоим

Перезаписывается каждым заходом, а не дописывается.

- **Состояние:** `этапы-кончились`
- **Этап:** 2 из 2 — контекст, обзор, витрина
- **Сделано:** этап 1 — входы, сигнал сжатия, разметка, стили, спеки (56 тестов); этап 2 —
  контекст и обзор, история `Compact` с прокруткой в `play`, кадр осмотрен, эталоны сняты
- **Следующий шаг:** набор `nx affected`, разбор папки, заявка
- **Незакоммиченное:** чужая правка манифеста дерева — в коммит не идёт
- **Ждём владельца:** нет
- **PR:** ещё не открыт

## Решения по ходу

Пока нет.

## Заходы

### 2026-09-06

- Макет: узел 4489:3762 — полоса 46, круги 24 с шагом 12, иконки 16, крошки 16px, слоты
  центра и правого края у потребителя.
- Этап 1 сделан. Порядок кругов в стопке — свой контекст наложения и индекс из разметки: «ещё»
  без индекса лежит выше всех.
- Замер прилипания по положению хоста в витрине не сработал: у края прокручиваемого контейнера
  липкий узел стоит там же, где стоял в потоке, и сравнивать нечего. Заменён наблюдателем
  пересечения с верхом на пиксель выше края: прилипнув, шапка теряет пиксель из видимой области.
- Этап 2 сделан. Слоты в истории — крошки текстом, `rt-toggle-button-group`, две
  `rt-icon-button`; сжатие ждётся в `play` по классу хоста, а не отсчётом времени.
