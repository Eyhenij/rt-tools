# Ход работы

## Где стоим

- **Этап:** 1–5 закрыты, остался 6 — проверка отказа опытом на самой этой работе
- **Сделано:** гард отбивает слияние с лежащей папкой, требует запись в архив и принимает обход;
  сверка очереди видит вложенные папки; закон, правило и паттерн дописаны; всё разложено в
  дерево; правила спека привязаны к коду
- **Следующий шаг:** открыть отчёт, попробовать слить до разбора папки и записать сюда текст
  отказа, потом разобрать папку
- **Незакоммиченное:** чужое — `nx.json` и `RUNNER-MONITOR.md`, оба от прошлых заходов
- **Ждём владельца:** ничего; отчёт #335 по прошлой задаче ждёт его разбора

## Решения по ходу

- **Тексты этой работы переписаны на простой язык.** Владелец сказал, что читать их тяжело.
  Переписаны комментарии в гарде и проверках, тексты отказов, правила спека, статья закона,
  правило и паттерн. Стиль соседних текстов пакета не трогали — это отдельная работа.
  Затронутый этап замысла: все.

- **Сверка спеков научилась читать наборы сценариев на shell.** Замысел этого не предполагал.
  Сценарии `SC-AK-12`…`SC-AK-21` покрыты наборами на bash — иначе исполняемые файлы не
  проверить, — а сверка искала идентификаторы только в `*.spec.ts` и считала все десять
  непокрытыми. Пометить их «Не покрыто» значило бы соврать: тесты есть и гоняются.
  Затронутый этап замысла: 5.

- **Обход папок каталога задач переехал из сверки в общий модуль борды.** В самой сверке он
  лежал после запроса к очереди работ, и позвать его без сети было нельзя — то есть и сценария
  на него написать. Затронутый этап замысла: 2.

- **Условие «дерево не задало каталог задач» проверяется надстройкой профиля, а не переменной
  окружения.** Умолчание пакета подставляется через `:-`, поэтому пустое значение из окружения
  оно перебивает. Это заметил критик договорённости, а подтвердил упавший сценарий.
  Затронутый этап замысла: 1.

- **Три новые статьи правила привязаны таблицей, которой в спутнике не было.** Сверка читает
  привязки только из таблицы `| Статья | Где исполняется |`; в спутнике `task-flow` её не было
  вовсе, и все 28 статей числились непривязанными. Завели таблицу с тремя своими строками —
  остальные 25 остались долгом, лежавшим здесь до этой работы. Затронутый этап замысла: 5.

## Заходы

### 2026-08-09

- Разбор просьбы: девять вопросов в первом заходе, семь во втором — после конвейера ролей.
- Конвейер отработал четырьмя ролями. Аналитик предложил урезать работу втрое; автор
  договорённости молча сузил её до одной точки вопреки записанному слову владельца; критик это
  поймал и дал 15 находок. Расхождения отнесены владельцу, а не исполнены молча.
- Владелец развернул объём вторым заходом: сторожевая точка одна и это слияние, а не открытие
  отчёта; взятие задачи в работу выкинуто.
- Договорённость влита прямо в спек пакета: 10 правил, сценарии `SC-AK-12`…`SC-AK-21`.
- Гард получил ярус слияния: поиск папки в содержимом ветки, требование записи в архив, обход
  из текста команды и из тела отчёта, подсказка на открытии отчёта.
- Сценарии: 37 в наборе гардов поставки (было 24), 28 в наборе проверок (было 23).
- Отказ проверен опытом на наборе: в копии гарда сломана строка требования — прогон покраснел,
  15 провалов.
- Сверка очереди теперь видит вложенную папку: `docs/tasks/chore/312-sync-agent-kit/` впервые
  названа расхождением, раньше её находили только грепом.
- Прогоны зелёные: наборы пакета, линт и спеки `@rt-tools/agent-kit` (140 тестов), раскладка
  сходится с пакетом. Сверка спеков — 373 замечания, ровно столько же, сколько до работы.
