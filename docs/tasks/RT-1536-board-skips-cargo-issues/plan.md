# Замысел

**Задача:** RT-1536 · **Ветка:** RT-1536-board-skips-cargo-issues
**Поведение:** не меняется — работа трогает проверку дерева и слой правил, кода приложения нет.

После написания этот файл не правится. Пересмотр этапа идёт в `progress.md` решением по ходу.

## След задачи

| Что       | Где                                                       |
| --------- | --------------------------------------------------------- |
| Проверка  | `projects/agent-kit/assets/checks/check-board.github.mjs` |
| Настройка | `.claude/rt-kit/checks.json`, ключ `board`                |
| Правила   | `projects/agent-kit/assets/rules/git-workflow.github.md`  |
| Компаньон | `.claude/skills/git-workflow/implementation.md`           |
| Спеки     | `docs/specs/agent-kit/delivery-gate/`                     |

## Что считается сделанным

- Сверка очереди не судит как задачу запись, несущую названную деревом метку груза.
- Отсеянные записи названы числом в выводе сверки.
- Дерево, метки не назвавшее, судится как прежде.
- Восемнадцать строк о семи записях из вывода ушли, прочие расхождения остались.

## Этапы

### 1. Отсев по метке

- **Что делается:** проверка читает список меток груза из настройки дерева и отсеивает такие
  записи до всех проверок задачи; число отсеянного печатается.
- **Признак готовности:** запись с меткой не даёт ни одной строки, запись без метки судится как
  прежде.
- **Чем проверяется:** `bash projects/agent-kit/tests/checks-board.test.sh` — «0 провалов».

### 2. Настройка дерева и статья правила

- **Что делается:** дерево называет свою метку в `checks.json`; правило поставки получает
  статью о том, что груз в очереди задачей не судится; сценарии заводятся.
- **Признак готовности:** живая сверка очереди этих восемнадцати строк не печатает.
- **Чем проверяется:** `npm run check:board` — строк о `#837`, `#839`, `#850`…`#885` нет.

### 3. Раскладка и общий набор

- **Что делается:** пакет собран, раскладка сверена, набор проб прогнан.
- **Признак готовности:** разложенное сходится с пакетом, набор зелёный целиком.
- **Чем проверяется:** `pnpm run agent-kit:check` и `bash projects/agent-kit/tests/run.sh` —
  «ВСЕ НАБОРЫ ЗЕЛЁНЫЕ».

## Чего эта работа не делает

- Не закрывает и не удаляет сами записи: решение о них — владельца.
- Не трогает прочие расхождения сверки — задачи `#985` и `#1438` без исполнителя это настоящие
  задачи, и лечатся они не здесь.
