<!-- rt-kit v0.8.3 · samples/tasks/_template/progress.md · baf326afb38f · правится надстройкой, не здесь -->

# Ход работы

## Где стоим

Перезаписывается каждым заходом, а не дописывается.

- **Этап:** 3 из 6 сделаны и подтверждены замером на стенде; следующий — 4, доводка входа
- **Сделано:** договорённость `docs/specs/message-bus/proposed/shell-top-nav/` (двадцать девять
  правил, двенадцать сценариев `SC-MB-142`…`SC-MB-153`) и замысел на шесть этапов — коммит
  `be82a4e6`. Этап 1, коммит `a31228c1`: шапка `admin-header` в `common/container/ui` ставит
  верхний ряд кита, оболочка отдала ей зону шапки, левая зона каркаса и своё производное по
  адресу сняты, границы дописаны двумя строками. Этап 2, коммит `957aa3b9`: попап профиля
  шаблоном в ряд кита — имя вошедшего и выход, стили попапа в общем слое приложения. Этап 3:
  служба языка `AdminLocaleService` и провайдер подписей в `common/core/util`, переключатель
  языка `admin-locale-switch` в `common/core/ui`, переключатель темы кита и он же — в попапе
  профиля и в полосе управления экрана входа. Семнадцать тестов в трёх либах зелёные, сквозной
  набор — тридцать два, сборка и линт по задетым зелёные, `check:styles` без новых
- **Что назначил владелец:** меню переводится на верхний ряд; панели второго уровня сегодня нет —
  четыре плоских пункта; тема заводится, переключатель стоит в попапе профиля и на экране входа;
  выбор языка ставится там же; попап профиля несёт имя вошедшего, тему, язык и выход; приложение
  называет себя словом, а не знаком — начертаний логотипа у админки нет
- **Что не входит:** многоязычие подписей админки, гейтинг разделов правами, пересборка формы
  входа, колокольчик непрочитанного, смена схемы надевания оболочки, эталонные снимки
- **Следующий шаг:** этап 4 — общая часть экрана входа переезжает в его оболочку, поля получают
  иконку и плейсхолдер, форма и подача отказа остаются теми же
- **Незакоммиченное:** нет
- **Ждём владельца:** ничего
- **PR:** ещё не открыт

## Решения по ходу

- **17 августа 2026. Оболочка остаётся одним корневым маршрутом.** У образца она надевается на
  каждый домен через ленивую загрузку, потому что оболочек там несколько; здесь она одна на все
  разделы. Альтернатива — оболочка каждого домена — отвергнута: четыре объявления вместо одного
  при том же результате. Владельцу не задавалось: это устройство маршрутов, а не вид.
- **17 августа 2026. Приложение называет себя словом, а не знаком.** Решение владельца после
  находки: `rt-logo` рисует начертания только из файлов приложения, а их у админки нет — на
  экране входа знак занимал место и не рисовал ничего. Отвергнуто: нарисовать знак самим и
  оставить пустое место до появления файлов. Договорённость правилась до кода, статус у неё
  «предложено».
- **17 августа 2026. Промах захода: `git add -A` унёс в коммит этапа 2 шесть чужих файлов**,
  которые `prettier . --write` из `lint-staged` переформатировал сам по себе. Коммит снят
  `reset --soft`, файлы возвращены `git restore`, коммит собран заново — `957aa3b9`. Порядок на
  будущее: индекс набирается путями, а после каждого коммита шесть файлов возвращаются.
- **17 августа 2026. Вторая стопка тостов снимается, остаётся встроенная в каркас.** Найдено при
  написании договорённости: каркас кита рисует стопку сам внизу по центру, а оболочка админки
  заводит вторую внизу справа — шину слушает каждая, и тост об отзыве приглашения виден дважды.
  Убрать встроенную нечем: она часть каркаса.

- **18 августа 2026. Вид дат от языка не зависит, и договорённость поправлена до кода.** Правило
  обещало, что выбор языка меняет подписи кита «и формат дат», а даты в списках рисует пайп
  админки одним видом — днём, месяцем, годом и минутами цифрами; китовых дат в админке нет ни
  одной. Отвергнуто: завести пайпу второй вид по локали — чистый пайп на смену языка не
  пересчитывается, и вид дат разошёлся бы с подписями до первой перерисовки. Договорённость
  назвала вид дат отдельным правилом, сценарий `SC-MB-149` обещает теперь только подписи кита.
- **18 августа 2026. Подписи кита отдаются своим провайдером, а не готовым `provideRtKitLabels`.**
  Готовый принимает сигналы значением, а переводчик и локаль приходят из службы — до сборки
  инжектора её нет. `provideAdminKitLabels` занимает оба токена кита фабрикой; сами токены кит
  объявляет сигналами ровно ради приложения, меняющего язык на ходу. Отвергнуто: держать сигнал
  языка вне внедрения — тогда хранилище устройства читалось бы мимо службы кита.

## Заходы

### 18 августа 2026

- Этап 3 сделан: служба языка держит выбор сигналом и кладёт его в хранилище устройства ключом
  `mb.locale`, переводчик и локаль уходят киту фабрикой провайдера, выбор языка собран рядом
  кнопок кита в общем слое вида, тему рисует готовый переключатель кита. Границы дописаны двумя
  строками: экран входа и шапка видят общий слой вида.
- Спеки: пять у службы языка, две у переключателя, десять у шапки (было восемь) — все зелёные.
  `nx affected -t lint typecheck` по тридцати восьми проектам зелёный, сборка админки зелёная,
  сквозной набор — тридцать два теста зелёных, `check:styles` без новых, `check:docs` без
  расхождений.
- `npm run check:specs` после этапа: расхождений пять вместо двенадцати — остались `SC-MB-144`,
  `SC-MB-148`, `SC-MB-151`, `SC-MB-152`, `SC-MB-153`, и все они ждут этапов 4–6.
- Замер на стенде 4310 (прод-сборка, разовый сценарий драйвером снимков вне репозитория): на
  входе полоса управления стоит закреплённой у правого верхнего угла — сверху 20, справа 20,
  122×40, зазор 8; светлая тема даёт фон `rgb(248, 250, 255)`, тёмная — `rgb(27, 31, 34)`, и
  после перезагрузки остаются и тёмная тема, и выбранный английский. Попап профиля 216 шириной:
  строки «Тема» и «Язык» по 200, подпись слева, переключатель справа. В списке смена языка в
  попапе перерисовывает подписи кита без перезагрузки — «Строк на странице: 20» становится
  «Per page: 20», «Страница 1 из 2» — «Page 1 of 2», — а заголовок «Разборы происшествий»,
  названия столбцов и дата `04.08.2026 06:40` при обоих языках одни и те же.
- Замер нашёл и мелкое: строка темы в попапе на 10px выше строки языка — высоту задают сами
  переключатели кита, круглая кнопка и ряд кнопок. Задачей это не заведено.

### 17 августа 2026

- Задача 3 эпика (RT-782) закрыта целиком: PR #820 готов к слиянию — прогон зелёный,
  договорённость влита в спек поддомена, папка задачи разобрана записью архива, черновик снят.
- Разведка по этой задаче прошла обе стороны целиком; её находки лежат в разборе просьбы.
- Найдено по дороге, не этой работой: `lint-staged` гонит `prettier . --write` по всему дереву,
  и на каждом коммите переписываются шесть файлов, которых правка не касалась — выравнивание
  таблиц в `.claude/skills/git-workflow/implementation.md`, четырёх записях архива и
  `docs/plans/tree-checks-debt-findings.md`. Правило `dependencies` говорит прямо:
  переформатируется только то, чьё форматирование проверяет линтер, а `.md` не проверяет никто.
  Каждый коммит эти файлы возвращались руками. Ждёт слова владельца о том, заводить ли задачей.
- Число из передачи пересчитано: последний выданный номер сценария домена — `SC-MB-141`, и он
  занят договорённостью RT-782, которая ждёт слияния. В главной ветке максимум — `SC-MB-136`.
- Проверка обеих сторон кита перед договорённостью: верхний ряд кита отдаёт подсветку
  маршрутизатору и принимает шаблон попапа профиля; логотипа он не рисует — его ставит шапка
  приложения рядом. Подписей кита на экране входа почти нет, поэтому выбор языка меняет там текст
  ошибки поля и подсказку кнопки-глаза, а в списках — подписи страниц и формат дат.
- `npm run check:specs` после договорённости: двенадцать расхождений, все — «сценарий не упомянут
  ни в одном тесте». Так и должно быть до этапа 6: тестов ещё нет.
- Этапы 1 и 2 сделаны и закоммичены: `a31228c1`, `957aa3b9`, а правка раскладки — `02a67419`.
  Восемь тестов шапки, линт и сборка админки зелёные, `check:styles` без новых, `stylelint` молчит.
- Замер на стенде 4310 (прод-сборка, прогон драйвером снимков разовым сценарием вне репозитория,
  потому что закреплённый профиль браузера в этот заход не подключён): шапка 58px; название
  «Приёмник» 18px, `rgb(35, 41, 52)`, левый край 20px, стоит в одной строке с рядом и левее него;
  четыре пункта в одну строку (все на 8px); `rt-section-nav` в разметке нет; подсветка на
  `/postmortems` — «Разборы происшествий», после перехода пунктом на `/proposals` — «Предложения»;
  попап профиля по наведению показывает имя «Набор» и «Выйти» шириной 200 из 216, вход при этом
  остаётся; выход из попапа уводит на `/sign-in`; на 599px инлайн-ряд скрыт, кнопка разделов
  открывает те же четыре.
- Замер нашёл дефект, которого не видели ни сборка, ни линт, ни спека: правила шапки лежали в
  файле её компонента, а класс блока висит на хосте — заскоупленные атрибутом, они до хоста не
  достают, и название стояло над рядом. Раскладка переехала в общий слой приложения, как у
  остальных экранов админки; у компонента файл стилей теперь пустой.
- Замер подтвердил и дефект, который ждёт этапа 5: `rt-toaster` на странице два — один рисует
  каркас кита, второй заводит оболочка.
- Первый замер соврал на узком экране: смотрел `.rt-page-header__list`, а кит прячет
  `.rt-page-header__nav` — список внутри скрытого узла остаётся `display: flex`.
