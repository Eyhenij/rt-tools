<!-- rt-kit v0.8.3 · samples/tasks/_template/progress.md · baf326afb38f · правится надстройкой, не здесь -->

# Ход работы

## Где стоим

Перезаписывается каждым заходом, а не дописывается.

- **Этап:** замысла ещё нет; следующий шаг — договорённость о продукте, потом замысел
- **Сделано:** задача взята в работу и переведена в колонку работы, ветка
  `RT-783-sign-in-and-shell` заведена от свежей главной, папка задачи собрана, разбор просьбы
  записан целиком — разведка обеих сторон, ответ владельца и четыре решения
- **Что назначил владелец:** меню переводится на верхний ряд с панелью колонок и групп, как у
  образца. Остальное крупное в задачу не входит, и это выведено из документов, а не спрошено:
  многоязычие эпик не делает, прав внутри админки нет по спеку, вход нуждается в доводке, а не
  в пересборке, колокольчику неоткуда брать события
- **Что входит доводкой:** попап профиля вместо «нажал — вышел», переключатель темы и выбор
  языка на экране входа, иконки и плейсхолдеры полей, прослойка-оболочка входа, заголовок
  вкладки, баррель слоя стилей, структурная форма директив зон контейнера
- **Следующий шаг:** написать договорённость `docs/specs/message-bus/proposed/<фича>/` — меню
  верхним рядом и доводка входа; сценарии нумеруются дальше `SC-MB-141`. После неё — замысел
- **Незакоммиченное:** папка задачи целиком
- **Ждём владельца:** ничего
- **PR:** ещё не открыт

## Решения по ходу

Пока нет: работа только начата.

## Заходы

### 17 августа 2026

- Задача 3 эпика (RT-782) закрыта целиком: PR #820 готов к слиянию — прогон зелёный,
  договорённость влита в спек поддомена, папка задачи разобрана записью архива, черновик снят.
- Разведка по этой задаче прошла обе стороны целиком; её находки лежат в разборе просьбы.
- Найдено по дороге, не этой работой: `lint-staged` гонит `prettier . --write` по всему дереву,
  и на каждом коммите переписываются шесть файлов, которых правка не касалась — выравнивание
  таблиц в `.claude/skills/git-workflow/implementation.md`, четырёх записях архива и
  `docs/plans/tree-checks-debt-findings.md`. Правило `dependencies` говорит прямо:
  переформатируется только то, чьё форматирование проверяет линтер, а `.md` не проверяет никто.
  Каждый коммит эти файлы возвращались руками. Ждёт слова владельца о том, заводить ли задачей.
