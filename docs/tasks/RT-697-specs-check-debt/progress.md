# Ход работы

## Где стоим

- **Этап:** все пять закрыты — `node tools/check-specs.mjs` отдаёт нулевой код и печатает
  сводку вместо расхождений
- **Сделано:** 208 расхождений трёх видов сведены к нулю; сверка принимает вердикт вместо
  адреса и не требует паттерна у правила, чьи паттерны дерево пропустило; готовый код двух
  правил дерева вынесен в три паттерна; сверка встала первой строкой в набор гейта пуша, и
  отказ подтверждён прогоном гарда на заведомо красной привязке
- **Следующий шаг:** пуш, открытие PR, затем разбор папки задачи последним коммитом
- **Незакоммиченное:** нет
- **Ждём владельца:** нет
- **PR:** ещё не открыт

## Решения по ходу

- **Ветка стоит на главной ветке, а не на ветке задачи о якоре.** Правка разбора якоря лежит в
  соседней невлитой ветке, и три строки сверки о правилах роли ревью принадлежат ей. Взять её
  основанием значило бы смешать две правки в одном PR; строки уйдут вливанием главной ветки
  перед открытием PR. Затронутый этап замысла: 3.

- **Статья, у которой места в коде нет, закрывается вердиктом с причиной.** Решение владельца
  16 августа: сверка принимает ячейку, начинающуюся с «Не исполняется», «Не применимо» или
  «Не проверяется», если за ней стоит причина. Привязать такую статью можно было только к
  файлу, который её не исполняет, — сверка приняла бы, а читателю соврало. Правка легла в
  ресурс пакета, копия приехала раскладкой; закрыто ею 36 строк разом. Затронутый этап
  замысла: 3.

- **Конец слова вердикта ищется просмотром, а не `\b`.** Первый заход правки не закрыл ни
  одной строки: границей слова JavaScript знает одну латиницу, и после кириллической буквы её
  нет вовсе. Тот же капкан чинит соседняя ветка о якоре — второй случай за две задачи.
  Затронутый этап замысла: 3.

- **Семь строк спека домена вели к решениям, а не к правилам.** Раздел решений — рассказ о
  состоявшемся, места в коде он не занимает и привязки не имеет. Строки сняты, сами решения в
  спеке остались. Затронутый этап замысла: 3.

- **Слово статьи в таблицу переносится дословно, даже когда оно из левой колонки словаря.**
  Четыре строки не сходились ровно потому, что компаньон писал «слияние» и «хеш» там, где
  правило пакета пишет «мерж» и «sha». Ключ связи — сам текст статьи, и переписать его в
  таблице значит порвать связь. Расхождение словаря с текстом пакета — не эта задача.
  Затронутый этап замысла: 1.

- **Этап 5 пересмотрен: три строки о паттернах закрываются здесь.** Замысел вывел их из работы,
  но строка в гейте пуша без них не работает вовсе: сверка красная, и гейт отбивает всякий пуш,
  включая пуш самой этой ветки. Решение владельца 16 августа: закрыть их в этой задаче.
  Альтернатива — отдельная задача, а гейт до неё, — отвергнута владельцем: красное так и
  осталось бы незамеченным. Затронутый этап замысла: 5.

- **Сверка встаёт строкой в гейт пуша, а шагом конвейера — нет.** Решение владельца 16 августа.
  Гейт отбивает до пуша и стоит полсекунды; шаг конвейера потребовал бы правки `ci.yml` с
  ручным прогоном до мержа. Известная граница: гард стоит на командах агента, и пуш руками
  идёт мимо него. Затронутый этап замысла: 5.

- **Два сценария этой ветки переномеровываются: номера заняла соседняя.** `SC-AK-238` и
  `SC-AK-239` о вердикте вместо адреса заведены здесь, но те же номера заняла договорённость о
  якоре из #745, и она вливается первой. Номер выдаётся один раз, поэтому уступает тот, кто
  вливается вторым: при вливании главной ветки оба сценария получают следующие свободные
  номера, и тем же изменением правятся заголовки их тестов. Затронутый этап замысла: 3.

## Заходы

### 2026-08-16

- Замер на вершине ветки: `node tools/check-specs.mjs` — 208 строк трёх видов, из них 105
  «пустая привязка», 74 «правило без привязки», 29 «привязка без пункта».
- Строк о компаньоне `git-workflow` — 41, ровно столько же, сколько назвал замысел.
- Этап 1: таблица компаньона переписана целиком — 27 статей получили привязку `файл:символ`,
  четыре строки со сползшим словом сведены со статьями дословно. Строк о `git-workflow` стало
  11, общий счёт трёх видов упал с 208 до 178.
- Этап 2: `task-flow` — 22 строки стало 11, привязки легли на гарды, команду заведения задачи
  и сверки. `observability` дополнен недостающей статьёй о всплеске; там 21 строка, и все
  двадцать одна — «не исполняется»: у приёмника нет ни хранилища отказов, ни оповещений.
  Общий счёт — 168.
- Замерено, сколько в остатке строк того же рода: в компаньонах 36 ячеек прямо говорят «не
  исполняется» или «не применимо», а «пустых привязок» всего 96. Владелец решил: сверка
  принимает вердикт с причиной.
- Правка сверки легла в ресурс пакета и приехала раскладкой; два сценария на неё —
  `SC-AK-238` и `SC-AK-239` — стоят в наборе пакета. Первый заход не закрыл ни строки из-за
  `\b`, второй закрыл 36: счёт 168 → 132.
- Этап 3: пятнадцать компаньонов и спек домена — `spec-driven`, `testing`, `styling-bem`,
  `typescript-conventions`, `lib-layers`, `reuse-first`, `rt-tools-storybook`,
  `browser-verification`, `platform-access`, `angular-patterns`, `component-structure`,
  `doc-style`, `dependencies`, `ui-component-tests`. Счёт 132 → 4.
- Этап 4: `SC-AK-225` и `SC-AK-230` были покрыты, а связи не было — номер поставлен в
  заголовок теста раскладки и в отчёт набора ревью.
- Осталось четыре строки, и все четыре о якоре на русском слове: их закрывает правка разбора
  якоря из ветки задачи #745. Три строки «у правила нет ни одного паттерна» замысел из работы
  вывел.
- Наборы пакета зелёные целиком, `npm run check:docs` — без расхождений.
- Этап 5: три строки «нет паттерна» закрыты. Сверка перестала требовать паттерн у правила, чьи
  паттерны стоят в списке пропусков дерева, — правка в ресурсе пакета, сценарий `SC-AK-241`.
  Готовый код правил `ui-component-tests` и `rt-tools-storybook` уехал в три паттерна:
  `ui-component-tests-spec`, `ui-component-tests-visual`, `rt-tools-storybook-story`.
- Раздел паттернов сначала читался пустым: с флагом строк `$` означает конец строки, и раздел
  кончался на первом переводе. Тот же род капкана, что `\b` у вердикта, — третий за две задачи.
- Сверка встала первой строкой в `rt_push_checks` профиля дерева. Приёмка: заведомо красная
  привязка в компаньоне `doc-style`, прогон `git-guard-push-tests.sh` — отказ назвал упавшую
  команду и напечатал хвост вывода. Фикстура возвращена.
- Главная ветка влита с влитым #747. Конфликт в разборе якоря разрешён сохранением обеих
  сторон: широкий алфавит символа из главной ветки, вердикт с причиной — из ветки.
- Сценарии о вердикте уступили номера якорю: `SC-AK-238`, `SC-AK-239` стали `SC-AK-242` и
  `SC-AK-243` вместе с заголовками своих тестов.
- Доэтапное, не этой работы: правило приёмника о сборке в конвейере несло якорь с пробелами
  (`ci.yml:Test, Lint, Typecheck, Build`) — сверка читала его пустой привязкой. Якорь сведён к
  одному слову. Оттуда же запись архива о #745 не была названа в указателе — дописана.
- Итог: `node tools/check-specs.mjs` — нулевой код, доменов 3, сценариев 404.
