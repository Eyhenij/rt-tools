# Progress

## Where we stand

Rewritten by every session, not appended to.

- **State:** `этап-идёт`
- **Stage:** 2 из 4 — проверка отбивает перелив из одной краски
- **Done:** этап 1 закрыт: у волны своя пара имён, в светлом виде концы разведены, тёмный и
  материальный вид красками не изменились
- **Next step:** написать проверку, которая обходит стили кита и судит концы переливов
- **Uncommitted:** папка задачи
- **Waiting for the owner:** нет
- **PR:** ещё не открыта

## Decisions along the way

- **Договорённость пишется прямо в спеку слоя оформления, а не отдельным черновиком** — обещание
  одно, и место для него уже есть. Затронут этап 4.

## Sessions

### 2026-09-08

- Замерена причина: в базовом наборе `--rt-color-bg-subtle` и `--rt-color-border-default` ведут на
  одну краску `#e0e0e0`, и перелив из неё в неё — ровная заливка.
- Этап 1 закрыт. Заведены `--rt-color-skeleton-base` и `--rt-color-skeleton-wave`: светлый вид —
  `#e0e0e0` и `#fafafa`, тёмный — `#17181c` и `#33383d`, материальный — `#f3f3f3` и `#e0e0e0`.
  Тёмный и материальный вид повторяют краски, которые набор давал заглушке прежде через подложку и
  рамку, — их вид не поехал. Проверено: `build-tokens-v2 --check` «what is built matches the
  source — files 5», `check-preset-complete` 71 назначение, `check-tokens-graph` без новых
  расхождений.
