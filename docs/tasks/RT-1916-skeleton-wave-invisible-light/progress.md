# Progress

## Where we stand

Rewritten by every session, not appended to.

- **State:** `этапы-кончились`
- **Stage:** 4 из 4 — все закрыты
- **Done:** волна починена, проверка написана и стоит в гейте, списке команд и конвейере, набор
  сценариев зелёный, договорённость и привязки на месте
- **Next step:** прогнать набор целиком, разобрать папку задачи и открыть заявку черновиком
- **Uncommitted:** нет
- **Waiting for the owner:** нет
- **PR:** ещё не открыта; ветка стоит на RT-1872, вливать снизу вверх

## Decisions along the way

- **Договорённость пишется прямо в спеку слоя оформления, а не отдельным черновиком** — обещание
  одно, и место для него уже есть. Затронут этап 4.

## Sessions

### 2026-09-08

- Замерена причина: в базовом наборе `--rt-color-bg-subtle` и `--rt-color-border-default` ведут на
  одну краску `#e0e0e0`, и перелив из неё в неё — ровная заливка.
- Этап 1 закрыт. Заведены `--rt-color-skeleton-base` и `--rt-color-skeleton-wave`: светлый вид —
  `#e0e0e0` и `#fafafa`, тёмный — `#17181c` и `#33383d`, материальный — `#f3f3f3` и `#e0e0e0`.
  Тёмный и материальный вид повторяют краски, которые набор давал заглушке прежде через подложку и
  рамку, — их вид не поехал. Проверено: `build-tokens-v2 --check` «what is built matches the
  source — files 5», `check-preset-complete` 71 назначение, `check-tokens-graph` без новых
  расхождений.
- Этап 2 закрыт. Заведена `tools/check-gradient-stops.mjs`. Разрешение цвета по видам вынесено в
  общий `tools/tokens-looks.mjs`: два места, считающие цвет одинаково, разъезжаются молча —
  проверка тёмной темы похудела с 402 строк до 281 и даёт те же числа. Доказано наоборот: со
  старыми именами в стилях заглушки проверка отказывает и называет светлый вид, файл, строку и
  краску `rgb(224 224 224)`.
- Этап 3 закрыт. `tools/tests/check-gradient-stops.test.sh` — 22 случая: сошедшиеся и разведённые
  концы, повтор одного имени, набор, сводящий концы, четвёртый вид, цепочка ссылок, литерал,
  неразрешимый конец, направление вместо цвета, запятая внутри вложенного вызова.
- Этап 4 закрыт. Статья и сценарий `SC-UKV-129` в спеке слоя оформления, привязка к проверке,
  статья в правиле оформления; проверка стоит в `package.json`, в наборе гейта и шагом «Style
  layer» конвейера.
