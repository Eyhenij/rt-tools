# Progress

## Where we stand

- **State:** `этапы-кончились`
- **Stage:** 3 of 3 — Сценарий записан
- **Done:** обе пробы второй витрины открывают историю общим приёмом; на витрине, которая историй
  не отдаёт, они отказывают названной строкой. Набор сценариев на этот отказ написан, статья в
  правиле и в спеке стоит, привязки на месте.
- **Next step:** прогнать набор проверок и открыть заявку черновиком.
- **Uncommitted:** папка задачи, правки проб, правила и спека.
- **Waiting for the owner:** нет.
- **PR:** ещё не открыт.

## Decisions along the way

- **Название задачи оставлено как есть** — номер уже стоит в ветке, в заявке и на доске, а
  переименование ветки стоит дороже строки в разборе. Затронут этап плана: ни один.
- **Приёмка первого этапа взята не тем адресом, что записан в плане** — витрина на порту 6007
  ожила сама, пока шла работа, и отказ на ней больше не воспроизводится. Отказной путь снят на
  заведомо пустой странице, поднятой на свободном порту: адрес, ноль корней, признак подготовки и
  отказавший запрос стоят в выводе, кода выхода единица, стека нет. Затронут этап плана: первый.
- **Общий модуль не забрал загрузку драйвера браузера** — своя копия этого приёма лежит в четырёх
  пробах дерева, и пятая была бы новой копией, а не снятой. Заводится отдельной задачей. Затронут
  этап плана: первый.
- **Проба рисования первого кита не тронута** — ждёт она не корень показа, а свою проверку, и
  общий приём ей не подходит без переделки. Заводится отдельной задачей. Затронут этап плана:
  первый.

## Sessions

### 2026-09-09

- Витрина на порту 6007 не отдавала ни одной истории: страница висела на подготовке, её горячее
  обновление отвечало четырёхсотым, корней показа ноль. Пять прогонов пробы — пять отказов.
- Свежая витрина на порту 6017: корень один, ошибок в консоли нет, три прогона — три зелёных.
  Гонки нет.
- Заведён `tools/showcase-probe.mjs`, обе пробы второй витрины открывают историю им.
- Набор `tools/tests/showcase-probe.test.sh` — десять исходов без браузера. На нарочно сломанной
  копии модуля он краснеет: два исхода из десяти.
- `node tools/visual-gate.mjs ui-kit-v2` — код выхода ноль, снимков 477 из 477, обе пробы зелёные.
- `node tools/check-specs.mjs`, `node tools/check-doc-paths.mjs`, `node tools/check-file-size.mjs`,
  `bash tools/tests/run.sh` — зелёные.
- Статья в правило проверки компонентов влезала за предел веса. Три довода из соседних статей
  ушли в холодную часть, статьи сжаты до утверждения с границей.
