# Замысел

**Задача:** RT-549 · **Ветка:** RT-549-reuse-check-as-measure
**Поведение:** не меняется — правило и вывод проверки слоя правил

После написания этот файл не правится. Пересмотр этапа идёт в `progress.md` решением по ходу.

## След задачи

| Что       | Где                                                |
| --------- | -------------------------------------------------- |
| Правила   | `projects/agent-kit/assets/rules/reuse-first.md`   |
| Проверки  | `projects/agent-kit/assets/checks/check-reuse.mjs` |
| Компаньон | `.claude/skills/reuse-first/implementation.md`     |

## Что считается сделанным

- Правило называет местом показа границу статьи о шине и говорит, где инлайн законен.
- Правило требует названного инвентаря при маркере отступления и запрещает снимать проверку с
  гейта.
- Признак отказа в списке говорит, что судит тег, а решение принимается по месту показа.
- Вывод проверки читается как измерение: готовое названо оговоркой.

## Этапы

### 1. Четыре правки

- **Что делается:** три статьи и строка признака в правиле, оговорка в выводе проверки
- **Признак готовности:** тексты на месте, привязки новых статей стоят в компаньоне
- **Чем проверяется:** `npm run check:specs` — расхождений нет

### 2. Раскладка и наборы

- **Что делается:** ресурсы раскладываются, гоняются наборы пакета
- **Признак готовности:** раскладка сходится, наборы зелёные
- **Чем проверяется:** `bash projects/agent-kit/tests/run.sh` — все наборы зелёные

## Чего эта работа не делает

- Признаков в наборах не трогает: их знает задача RT-546.
