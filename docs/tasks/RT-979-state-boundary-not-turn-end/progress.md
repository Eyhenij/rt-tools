# Ход работы

## Где стоим

- **Состояние:** `этап-идёт`
- **Этап:** 1 из 5 — закон, правило и карта говорят о границе состояния
- **Задача:** RT-979, пятая эпика RT-974 · **Ветка:** `RT-979-state-boundary-not-turn-end`
- **Обязательное действие:** доделать этап и отметить его здесь.

## Решения по ходу

- **Строка следующего движения стоит в каждом разделе состояния, а не по одной на состояние.**
  Владелец сказал «в каждом разделе состояния», а разделов больше, чем состояний: у трёх
  состояний их по два и по три. Исполнитель дочитывает раздел, в котором стоит, поэтому строка
  идёт в каждый.
- **Признак строки — общий зачин, а хвост свой.** Владелец отверг единую дословную строку;
  проверять прозу без общего признака нечем. Зачин даёт проверке зацепку, повтор хвоста она
  ловит наравне с его отсутствием. Этим закрыт вопрос, оставшийся открытым после разбора.
- **Карта хода правится наравне с правилом.** Она приходит в контекст на каждом запуске и
  перечисляет выходы хода; промолчав о переходе между состояниями, она расходится с правилом
  там, где её читают чаще всего.
- **Роли конвейера не звались.** В сессии стоит запрет на субагентов без слова владельца, и
  договорённость написана исполнителем.

## Заходы

### 21 августа 2026

- Задача взята сразу за отдачей RT-978: порядок назначен эпиком, колонка переведена в работу,
  ветка заведена от свежей главной.
- Разведка прошла до первого вопроса. Найдено три вещи: правило уже называет четыре выхода хода
  и говорит «всё остальное — продолжение хода», но слова о переходе между состояниями среди
  перечисленного нет; ни один из одиннадцати разделов состояния не говорит, кончается ли на нём
  ход; сверка состояний сравнивает только имена разделов, а внутрь не смотрит.
- Разбор просьбы закрыт четырьмя ответами владельца: строка идёт в каждый раздел состояния
  своей формулировкой, проверка заводится отдельная, закон правится этой же веткой.
- Главная ветка влита в ветку задачи тем же заходом — вместе с ней приехала работа RT-978:
  правило входа в заход, карта хода и её проверка. Тексты состояний эта работа не трогала.
- Договорённость о продукте записана: `docs/specs/agent-kit/proposed/state-boundary-not-turn-end/`,
  сценарии `SC-AK-447` … `SC-AK-458`. Вольётся она в поддомен гардов завершения хода.
- Замысел записан пятью этапами: тексты, строки в разделах, проверка, её место в наборе гейта
  пуша, набор под сценарии.
