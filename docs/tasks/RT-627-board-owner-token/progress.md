# Ход работы

## Где стоим

Перезаписывается каждым заходом, а не дописывается.

- **Этап:** 3 из 3 закрыты — правка пакета, снятие ключа, тексты домена
- **Сделано:** токен машинной записи стал необязательным в режиме `move` и в команде заведения
  задачи; `board.tokenPath` снят из настройки дерева; статьи, привязки и сценарий `SC-AK-185`
  записаны; разложенное сходится с пакетом
- **Следующий шаг:** разбор отчёта владельцем; перед слиянием разобрать папку задачи
- **Незакоммиченное:** нет
- **Ждём владельца:** разбор отчёта и слияние
- **Отчёт:** #631, открыт, ждёт разбора · колонка переставлена командой в `👀 In review` —
  первый раз, когда она сработала · папка задачи ещё в ветке

## Решения по ходу

- **Задача заведена вызовами хостинга, а не командой `task:new`.** Команда упирается в тот самый
  дефект, который задача чинит: тикет она заводит, а до борды не доводит. Все четыре шага
  сделаны вручную и подтверждены ответом очереди — тикет на борде, колонка `📋 Backlog`,
  исполнитель владелец. Затронутый этап замысла: нет.

## Заходы

### 2026-08-15

- Дефект найден перед задачей #586: борда не читается и не правится машинной записью.
- Правка легла в пакет двумя местами: режим `move` файла борды и команда заведения задачи
  берут токен, если он назван, и обходятся без него, если нет. Разложено в `tools/` — два
  файла.
- Дерево сняло `board.tokenPath`. После этого `npm run check:board` доходит до конца:
  84 расхождения, все доэтапные — тринадцать задач фидбэка `#614`…`#626` вне борды, три отчёта
  на борде вместо задач, полсотни задач без исполнителя, `#75` вне очереди и неразобранная
  папка задачи `#585`.
- `npm run task:move -- 627 in-progress` переставил колонку: `📋 Backlog → 🏗 In progress`.
- Сценарий `SC-AK-185` спрашивает хостинг подставным клиентом и смотрит, пришёл ли к нему свой
  токен: `bash projects/agent-kit/tests/checks.test.sh` — 99 ok, 0 провалов, весь набор
  `projects/agent-kit/tests/run.sh` зелёный.
- `pnpm run agent-kit:check` — разложенное сходится с пакетом; `check:docs` 212 документов без
  расхождений; `check:specs` 207 против 205, и оба новых расхождения принесла главная ветка
  задачами `#605` и `#611`, а не эта работа.
