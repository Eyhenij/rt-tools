<!-- rt-kit v0.8.3 · samples/tasks/_template/progress.md · baf326afb38f · правится надстройкой, не здесь -->

# Ход работы

## Где стоим

Перезаписывается каждым заходом, а не дописывается.

- **Этап:** 3 из 6 — экраны закрыты кадрами; четыре кадра из семнадцати не сходятся сами с
  собой, и три из них — не дефект съёмки, а дефект экрана
- **Сделано:** этап 1 закрыт не кодом, а чтением: `toHaveScreenshot` ждёт вставший кадр сам.
  Объявлены каталог эталонов и порог у нуля, заведён общий приём съёмки, кадры расставлены по
  всем экранам — семнадцать штук. Эталоны сняты начисто и лежат в
  `apps/message-bus-admin-e2e/__snapshots__/<прогон>/`.
- **Следующий шаг:** завести задачу на неопределённый порядок строк (см. «Найдено кадрами»),
  разобрать четвёртый кадр — `sign-in-dark`, 26 пикселей, причина неизвестна, — и только потом
  показывать кадры владельцу.
- **Незакоммиченное:** нет — правка набора и эталоны закоммичены
- **Ждём владельца:** ничего; кадры показываются, когда сойдутся все семнадцать
- **PR:** ещё не открыт — ветка не запушена. Пушить рано: четыре кадра красные, и гейт пуша их
  гоняет

## Найдено кадрами

- **Порядок строк в списке сводок не определён.** Сортировка по умолчанию идёт по месяцу, а у
  обеих записей стенда он один — `2026-08`. Ничья, добивки второго ключа нет, и хранилище
  отдаёт строки в произвольном порядке: два прогона подряд дали разный. Это же роняет кадр
  панели подробностей раздела сводок — панель открывается по первой строке, а первая строка
  каждый раз другая. Похоже, тем же болен и список приглашений. У разборов такой добивкой
  занимается сценарий `SC-MB-65`, у сводок его нет. **Правится не здесь:** эта работа снимает
  кадры, а не чинит экраны — заводится задача.
- **`sign-in-dark` расходится на 26 пикселей**, разбросанных по кадру. Причина не найдена;
  порядок строк тут ни при чём — на входе строк нет.

## Решения по ходу

- **18 августа 2026. Снимкам не заводится своего шага в наборе гейта и в конвейере.** Они
  снимаются внутри сквозного набора админки, а его шаг «E2E admin» уже стоит и там и там.
  Отдельный шаг по образцу снимков витрин поднимал бы стенд второй раз, а правило проверяемости
  прямо говорит, что соседний стенд делает исход съёмки зависящим от того, чем занят сосед.
  Затронутый этап замысла: 3.

- **18 августа 2026. Своего ожидания вставшего кадра в наборе не заводится — этап 1 пересмотрен.**
  Замысел велел повторить приём из обвязок витрин: молчание сети и два одинаковых размера подряд.
  Чтение исходника Playwright показало, что `toHaveScreenshot` делает ровно это сам — глушит
  движение, прячет каретку, ждёт `document.fonts.ready` и снимает кадр за кадром, пока два
  подряд не совпадут («Failed to take two consecutive stable screenshots» — его же текст
  отказа). Свой такой же цикл рядом был бы вторым ответом на один вопрос и разошёлся бы с
  первым молча. Витрины ждут руками потому, что их прогонщик такого цикла не делает вовсе:
  там кадр снимается один раз и сразу сверяется. Разница в прогонщике, а не в устойчивости
  экрана. Затронутый этап замысла: 1.

- **18 августа 2026. Эталоны, снятые в столкнувшихся прогонах, сняты и переснятые начисто.**
  Два прогона подряд упали не на кадрах: в соседнем рабочем дереве шёл прогон того же набора, а
  порты стенда зашиты одним значением на всех — 3310 и 4310. Второй прогон получил
  `EADDRINUSE`, стенд не встал, и это выглядело сорока одной красной спекой про экраны. Кадрам,
  снятым в таком прогоне, верить нельзя — каталог эталонов снесён целиком и снят заново на
  свободной машине. Затронутый этап замысла: 2.

## Заходы

### 18 августа 2026 года

- Задача взята тем же движением, которым RT-857 ушла на разбор: порядок эпика назначен, пятая
  задача в нём последняя
- Разведка: `toHaveScreenshot` в дереве не встречается ни разу — эталонов экранов нет вовсе, и
  заводить их не поверх чего-то, а с нуля
- Этап 1 закрыт чтением исходника прогонщика, а не правкой кода: писать было нечего
- Первый прогон с новым кадром красный намеренно — Playwright не досъёмывает отсутствующий
  эталон молча, а пишет его и роняет тест
- Семнадцать кадров сняты начисто: 15 широких и 2 узких. Первый прогон красный ровно
  семнадцатью тестами — Playwright пишет отсутствующий эталон и роняет тест, а не проходит молча
- Второй прогон: 45 из 49 зелёные. Четыре кадра не сошлись сами с собой, и три из четырёх —
  один и тот же дефект экрана, а не съёмки
- Ловушка правила о проверяемости подтвердилась на своей шкуре: чужой прогон того же набора на
  той же машине неотличим от собственной поломки
