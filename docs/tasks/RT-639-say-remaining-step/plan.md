# Замысел

Задача: #639. Ветка: `RT-639-say-remaining-step`.

**Поведение:** не меняется — правится текст паттерна, кода в правке нет.

После написания этот файл не правится. Пересмотр этапа идёт в `progress.md` решением по ходу.

## След задачи

| Что      | Где                                                     |
| -------- | ------------------------------------------------------- |
| Законы   | `docs/constitution/work-conduct.md`                     |
| Правила  | `.claude/skills/task-flow/`                             |
| Паттерны | `projects/agent-kit/assets/patterns/task-flow-close.md` |

## Что считается сделанным

- В разделе о разборе папки сказано: заход, открывший отчёт, называет владельцу оставшийся шаг
  вслух — после одобрения ветка получает ещё один коммит, и только потом вливается.
- Сказано и почему: молчание здесь читается как «работа кончена».

## Этапы

### 1. Оставшийся шаг называется вслух

- **Что делается:** требование дописывается в раздел о разборе папки, перед таблицей файлов.
- **Признак готовности:** паттерн говорит исполнителю, что и кому он обязан сказать при
  публикации отчёта.
- **Чем проверяется:** `pnpm run agent-kit:check`, `node tools/check-doc-paths.mjs`.

### 2. Запись приёма закрывается ссылкой на задачу

- **Что делается:** #600 закрывается с указанием номера задачи.
- **Признак готовности:** записи нет в открытых.
- **Чем проверяется:** `gh issue list --label agent-kit-feedback --state open`.

## Чего эта работа не делает

- Не заводит проверки на сказанное вслух: ответ владельцу не читает ни одна проверка.
- Не трогает порядок шагов закрытия — он уже записан верно.
