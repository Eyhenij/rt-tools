# Ход работы

## Где стоим

Перезаписывается каждым заходом, а не дописывается.

- **Состояние:** `этап-идёт`
- **Этап:** 5 из 5 — Тексты приведены и работа отдана
- **Сделано:** ряд пересчитан, светлая полоса села на образец, замер обеих тем снят,
  приглушённый текст тёмной темы подведён под порог, список принятого убыл на 11 записей
- **Следующий шаг:** привести договорённость об оформлении к сделанному, разобрать папку, открыть заявку
- **Незакоммиченное:** нет; витрина второго кита поднята на 6007
- **Ждём владельца:** нет
- **PR:** ещё не открыт

## Решения по ходу

- **Дерево-образец красится этим же китом.** Своей палитры у него нет: одна строка
  `@use '@rt-tools/ui-kit-v2/src/styles/index'`, версия пакета 0.5.0. Поля ввода, строки таблицы
  и кнопки в его экранах — компоненты кита. Значит переносить цвета неоткуда, и работа не
  «взять чужую палитру», а вернуть нейтральный ряд к тому, чем он был до пересчёта.
  Затронутый этап замысла: нет — решение принято до написания замысла.

- **Светлый конец ряда с образцом не сходится, и полосу закрыло назначение.** У образца
  `#fafafa`, `#f6f7f9` и `#e6e6e6` стоят тремя ступенями подряд — полоса светлоты шириной 0.06.
  Ровный ряд из одиннадцати ступеней, доходящий до почти чёрного, столько туда не вмещает:
  шаг у него 0.0785. Поэтому приглушённая поверхность и подсветка наведения перецеплены на
  прозрачный оттенок `--rt-overlay-black-4` — над белой поверхностью он даёт `#f5f5f5`, над
  страницей `#f0f0f0`, то есть то же, что видно в образце. Ступени ряда при этом не тронуты.
  Отвергнуто: сдвинуть шаг ряда ради светлого конца — тогда тёмный конец перестаёт доставать до
  почти чёрного. Затронутый этап замысла: 2.

- **Приглушённая поверхность и подсветка наведения оставлены на одном значении.** Замысел
  требовал их развести, но в образце они совпадают намеренно, а владелец просил сделать «так
  же». Разводить их — своя правка вида, которой он не просил. Затронутый этап замысла: 2.

- **Цвет приглушённого текста оставлен своим, а не взят у образца.** У образца это `#7e7e7e`, и
  на белом он даёт контраст 4.06:1 — ниже порога 4.5:1, который держит договорённость об
  оформлении. Оставлен `#676767` (5.7:1). Затронутый этап замысла: 2.

- **Наведение у кнопки внутри поля было невидимым.** `--rt-input-color-bg-hover` стоял на тех же
  четырёх процентах чёрного, что и фон самого поля после перецепления, — подсветка не читалась
  бы вовсе. Заведены ступени `--rt-overlay-black-8` и `--rt-overlay-white-8`, наведение
  перецеплено на них. Затронутый этап замысла: 2.

- **`check:tokens-graph` красный не от этой правки.** `--rt-space-2-5` не объявлен ничем и
  зовётся из шаблона витрины; проверка была красной и до правки ряда. Заводится отдельной
  задачей. Затронутый этап замысла: нет.

- **Приглушённый текст тёмной темы не брал порога, и это чинится здесь.** Замер: `#787b7d` над
  поверхностью даёт 3.57:1, над страницей 3.66:1, над фоном поля 3.71:1 — четыре пары ниже
  порога 4.5:1, и все держал список принятого. Светлую сторону того же токена под порог уже
  подводили — комментарий рядом говорит «приглушённый текст обязан брать порог», — а тёмную
  забыли. Заведена ступень `--rt-overlay-white-50`: пятьдесят процентов — самая слабая доля,
  берущая порог над всеми тремя подложками (4.86, 5.02, 5.09). Отвергнуто: оставить как есть и
  сослаться на список — список принимает накопленное, а не то, что правится этой же правкой.
  Затронутый этап замысла: 3.

- **Список принятого убыл на одиннадцать записей.** Тем парам, что вышли из-под порога, в
  стилях больше ничего не отвечает, и проверка требует снять строку. Было 45 пар ниже порога,
  стало 34. Затронутый этап замысла: 3.

- **Волновая пересъёмка закрепила четыре кадра без значка.** Обход прошёл чисто — 450 историй,
  пустых показов нет, — а сама съёмка всех эталонов разом поймала три кадра составной кнопки и
  один кадра управления отбором в момент, когда шеврон ещё не нарисовался. Повторная сверка
  показала расхождение в 42–84 точки, то есть 0.02–0.04%: снятый эталон был без значка, живая
  страница — со значком. Кадры пересняты точечно, повторная сверка зелёная — 458 из 458.
  Признак у промаха такой же, как у мерцающей пробы отрисовки: ожидание в обвязке есть, но под
  нагрузкой волны оно отпускает съёмку раньше, чем значок доехал. Заводится отдельной задачей.
  Затронутый этап замысла: 4.

## Заходы

### 2026-08-25

- Задача взята после того, как RT-1129 отдана заявкой и влита.
- Замер «до» на витрине: `--rt-color-bg-surface-subtle`, `--rt-color-bg-hover` и
  `--rt-input-color-bg` разворачиваются в один `#d8e0f0`.
- Ряд пересчитан: цветность снята, шаг светлоты 0.0785 сохранён. Опорные ступени 50 (`#fafafa`)
  и 500 (`#7e7e7e`) сошлись с образцом точно.
- Замер после правки: поле ввода `rgb(0 0 0 / 4%)` над страницей `#fafafa`, рамка `#e0e0e0`;
  синевы не осталось ни в одной ступени.
- `check:tokens-build` и `check:tokens-styles` — зелёные.
- Замер контраста: светлая тема берёт порог целиком (от 4.96 до 14.74), тёмная после правки —
  тоже (от 4.76 до 16.99).
- `check:tokens-build`, `check:tokens-styles`, `check:tokens-theme`, `check:cascade-layer` и
  `lint:styles` — все зелёные.
- Обход историй: 450, пустых показов и ошибок отрисовки нет.
- Разошлось 382 эталона из 450 — палитра меняет почти всё.
- Пересъёмка волной: обновлено 451. Повторная сверка нашла 4 кадра без значка, они пересняты
  точечно; итоговая сверка — 458 из 458.
