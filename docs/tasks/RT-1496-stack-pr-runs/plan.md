# Замысел

**Задача:** RT-1496 · **Ветка:** RT-1496-stack-pr-runs
**Поведение:** не меняется — правка настроек конвейера и текста правила

Работа вне замысла остатка: находка захода.

После написания этот файл не правится. Пересмотр этапа идёт в `progress.md` решением по ходу.

## След задачи

| Что      | Где                                                        |
| -------- | ---------------------------------------------------------- |
| Конвейер | `.github/workflows/ci.yml` — событие и фильтр веток        |
| Правило  | `projects/agent-kit/assets/patterns/git-workflow-stack.md` |

## Что считается сделанным

- Заявка череды получает прогон: фильтр базы снят, событие приходит на любую базу.
- Паттерн череды называет это условием: без прогона на своей заявке черновик не снимается.
- Сверка очереди перестаёт печатать строку об отсутствии прогона у заявок череды.

## Этапы

1. **Фильтр базы снят, паттерн череды об этом говорит.**
