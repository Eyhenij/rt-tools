# Ход работы

## Где стоим

- **Состояние:** `этапы-кончились`
- **Этап:** 3 из 3 — откат, запись о поломке, «проверю после слияния», отметка находок
- **Сделано:** все три этапа; восемь открытых пунктов закрыты, семь были закрыты прошлыми редакциями
- **Следующий шаг:** прогнать набор, разобрать папку задачи, открыть PR черновиком
- **Незакоммиченное:** правки текстов пакета и раскладка
- **Ждём владельца:** нет
- **PR:** ещё не открыт

## Заходы

### 2026-08-28

- Задача 15 эпика RT-1198 взята следом за задачей 14; эпик на ней кончается.
- Этап 1: ключ задач назван строкой таблицы в редакции GitLab и объявленным отступлением в
  редакции Azure; там же — отступление от требования стереть поглощённую задачу (33, 40, 35, 36).
- Этап 2: в `git-workflow-merge` и `-stack` объявлено, что вызовы даны клиентом GitHub; области
  коммита в `git-workflow-commit.github.md` обезличены до `<область>` (38, 39, 42).
- Этап 3: в `git-workflow-restart` добавлен раздел об откате тем же вызовом с прежним sha; в
  `rules/deploy-flow.github.md` — статьи о разборе состоявшейся поломки записью и о правке самой
  выкатки, которую нечем прогнать иначе; обе получили строки в спутнике (43, 44, 45).
- Пункты 33–47 помечены разобранными в `docs/plans/agent-kit-rules-review-findings.md`.
- `npm run check:specs` — привязок без статей нет; `check-glossary` — расхождений нет;
  `check-file-size` — длиннее предела 0.
