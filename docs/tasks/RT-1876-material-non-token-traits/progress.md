# Ход работы

## Где стоим

- **Состояние:** `этапы-кончились`
- **Этап:** все четыре закрыты
- **Сделано:** директива волны написана, четыре пробы к ней зелёные, волна стоит на кнопке и на
  кнопке-значке; у кнопки-значка четыре формы ровным рядом и своё свойство тени; признак уровня
  атомарного дизайна записан в правило витрины, обе разъехавшиеся пары выправлены вместе с
  эталонами; свои два эталона пересняты поимённо. Пятнадцать оставшихся расхождений замерены на
  чистой главной ветке — они её, не этой работы. Пять проверок оформления зелёные. Пара половин
  «свой набор ↔ материальный» заведена обвязкой показа, обе кнопки показывают в ней палитру на
  состояние; обход всех 460 историй зелёный, оба кадра осмотрены, обрезка матрицы краем половины
  найдена и починена, два эталона сняты поимённо. Главная ветка влита в обе ветки стопки
- **Следующий шаг:** разобрать папку задачи и открыть заявку черновиком. RT-1891 сделана и влита
  снизу: набор снимков зелёный, пуш проходит. Ветка на хостинге
- **Набор проверок:** зелёный — линт, типы, тесты, сборка и сверки по 41 проекту, линтер стилей
  отдельно; тексты приведены к сделанному
- **Незакоммиченное:** нет
- **Ждём владельца:** ничего; сказать ему про красную главную ветку — пятнадцать кадров зависят
  от пояса машины и областной настройки браузера, и заводить под это карточку или нет, решает он
- **PR:** ещё не открыт

## Решения по ходу

- **Ветка стоит на `RT-1871-material-preset-tokens`, а не на главной** — тень и волна берут цвет
  из материального набора, а он лежит в той ветке. План эпика велит ветвиться от главной; здесь
  расхождение нарочное, и заявки вливаются снизу вверх. Затронутый этап замысла: нет.
- **Волна живёт в своём обрезающем слое, а не прямо в кнопке.** `overflow: hidden` на самой
  кнопке срезал бы всё, что торчит за её край: у кнопки-значка так стоит метка, вынесенная на два
  пикселя наружу. Слой ставится при первом нажатии, повторяет скругление кнопки и указателя не
  ловит. Затронутый этап замысла: 1.
- **Кнопка получает волну директивой-хозяином, кнопка-значок — в разметке.** У кнопки директива
  сама сидит на `<button>`, и хозяин ставится ей без правки чужих шаблонов; у кнопки-значка
  нажимают внутренний узел, до которого хозяин не достаёт. Затронутый этап замысла: 2.
- **Два утверждения пробы кнопки переписаны с точного списка классов на список модификаторов.**
  Директива-хозяин законно добавляет на хост свой класс блока, а утверждение сравнивало список
  целиком и краснело не на том, о чём оно. Затронутый этап замысла: 2.
- **Признак уровня записан в правило витрины, а статья о дочерней части уточнена.** Уровень
  выводится из состава: атом ни из чего кита не собран, молекула собрана из атомов, организм
  открывает перекрытие или живёт записями. Собирающий компонент законно стоит ступенью выше своей
  части, но группа у них общая — на этом обе пары и разъехались. Затронутый этап замысла: нет.
- **Промежуточные формы кнопки-значка отсчитываются от квадрата, а не своей долей стороны.**
  Доля в 22% давала на кнопке в 40 пикселей радиус в 8,8 против 8 у квадрата — ступень, которую
  не видно; у квадрата радиус постоянный, а его доля стороны гуляет от четверти до восьмой, и
  доля, взятая сама по себе, промахивается мимо него на одних размерах и сливается с ним на
  других. Затронутый этап замысла: нет — правка сделанного на этапе, поднятом владельцем.
- **Материальный набор кнопке подъёма не даёт, и этап 3 закрыт этим замером, а не тенью.**
  Признак готовности этапа обещал приподнятую кнопку в наборе, но образец её не несёт: четыре
  вида, которыми пользуется `rtui-button`, тени возвышения в Material 22 не имеют вовсе.
  Приподнять кнопку значило бы выдумать вид, которого у первого кита нет, — а ADR требует
  обратного. Кнопка-значок получила при этом свою ручку тени: её отсутствие и было настоящим
  пробелом, о котором спрашивал владелец. Затронутый этап замысла: 3.
- **Пересъёмки эталонов не делаю, пока не разобрано, чьи расхождения.** Правило говорит прямо:
  вывод «поехала вёрстка» делается замером, а не долей расхождения, а пересъёмка всего разом
  стирает и то, чего не ждали. Замер геометрии требует драйвера браузера, а он недоступен —
  закреплённого профиля в дереве нет. Затронутый этап замысла: 4.
- **Матрица внутри половины набора идёт рядами, а не сеткой.** Сетка прокручивается вбок и
  внутри половины режется её краем: нажатие и фокус с клавиши уезжали за край у обеих кнопок, а
  у кнопки-значка обрезанный столбец вылезал поверх соседней половины. У ряда значения
  независимы, и перенос по строкам ничего не теряет. Нашлось это осмотром кадра до съёмки
  эталона — ни одна проверка такого не видит. Затронутый этап замысла: 4.
- **Смотреть кадры глазами можно и без закреплённого профиля браузера.** Дверей в браузер две:
  расширение водит живую вкладку на профиле, которого на этой машине нет, а драйвер снимка
  водится сценарием и профиля не спрашивает вовсе — им сняты обход историй и эти кадры. Первый
  заход прочитал правило до половины и объявил осмотр невозможным. Затронутый этап замысла: 4.
- **Вырез рамки поля и плотность вынесены из этой задачи** — владелец просил волну и тень, и они
  делаются на кнопке; вырез рамки живёт на поле ввода, его цена выясняется только там. Держать
  волну в ветке до конца обеих работ значило бы не показать её вовсе. Затронутый этап замысла: нет.

## Заходы

### 2026-09-07 — ночь

- RT-1891 сделана отдельной веткой от главной и влита снизу в стопку: набор снимков зелёный —
  469 кадров, сирот нет, — и пуш проходит. Обе ветки стопки на хостинге.
- Гард пуша на этой ветке отбил ещё одно, своё: у обвязки показа пары наборов шаблон стоял в
  декораторе. Шаблон вынесен в свой `.html`; список принятого не тронут — правило уникальности
  разрешает ему только не расти, и три соседние обвязки в нём стоят с прежних времён, а эта туда
  не добавлена.

### 2026-09-07 — вечер

- Пятнадцать неразобранных расхождений оказались не мои: набор снимков прогнан на чистой копии
  главной ветки во втором рабочем дереве, и там падают **те же пятнадцать** — переписка (8),
  выбор даты (6), поле ввода (1), — и те же восемь сирот переписки. Перечень кадров различий
  совпал с моим до имени файла; сверх него у меня ровно два, оба `IconButton`.
- Причина пятнадцати названа по кадрам различий, а не по доле расхождения. Их две, и обе — не
  вёрстка: у переписки время сообщения `14.03.2026, 23:02` против `14.03.2026, 18:02` — эталоны
  сняты в поясе на пять часов восточнее нынешнего; у выбора даты и поля ввода `15/03/2026` против
  `03/15/2026` и `09:30` против `09:30 AM` — родные поля даты и времени рисует браузер по своей
  областной настройке. Ни пояс, ни областную настройку обвязка снимку не задаёт, и кадр от них
  зависит.
- Свои два эталона пересняты поимённо, но до пересъёмки на кадр посмотрели — и он показал дефект
  правки: `rounded-sm` в 22% стороны не отличался от квадрата. Ступени переписаны отсчётом от
  самого квадрата, замер растра по эталону: 7 / 10 / 13 / 18 пикселей на кнопке в 40 — ровный
  ряд из четырёх вместо двух неразличимых и двух.
- Заведена карточка RT-1891 на красноту снимков: пятнадцать кадров зависят от часового пояса
  машины и областной настройки браузера, а обвязка ни того, ни другого не задаёт. Доска отвечает
  о ней «📋 Backlog», проверено вызовом. Найдено мимо этой задачи и ни одним признаком её
  готовности не названо — потому карточка, а не правка здесь.
- Найдено и починено на нижней ветке: `pnpm run lint:styles` краснел шестью пустыми строками в
  собранном файле материального набора. Правка сделана на RT-1871 и влита сюда — иначе заявка о
  наборе ушла бы красной.
- Этап 3: у кнопки-значка появилось своё свойство тени и переход по нему — та ручка, что у
  обычной кнопки была, а у неё нет. Подъёма материальный набор при этом не даёт, и это замер, а
  не пропуск: `rtui-button` первого кита берёт вид `filled`, `outlined`, `tonal` и `text`, а тень
  возвышения в Material 22 несут только `protected` и круглая кнопка действия — ни один из
  четырёх её не несёт. Подъём в первом ките держат `mat-fab` и `mat-mini-fab` — закрывающая
  кнопка окна и кнопки шапки таблицы; они приедут вместе со своими семействами.

### 2026-09-07

- Этапы 1 и 2: заведена директива `rtRipple` со своим блоком, стилями и четырьмя пробами; волна
  поставлена на кнопку директивой-хозяином и на кнопку-значок разметкой. Прогон второго кита —
  1504 пробы в 121 наборе, все зелёные; пять проверок оформления зелёные.
- Найдено при этом: `jsdom` не знает `PointerEvent` вовсе — проба шлёт `MouseEvent` того же
  имени, у него те же `clientX` и `clientY`, а больше директиве от события ничего не нужно.
- Прогон снимков витрины: 17 расхождений в четырёх наборах, 444 кадра сошлись. Две поимённо мои —
  `IconButton › Shape` и `IconButton › Variant`: в матрице форм стало четыре столбца вместо двух,
  и сетка «палитра × форма» выросла вместе с ней. Остальные пятнадцать — переписка (8), выбор
  даты (6) и поле ввода (1) — не разобраны: набор снимков до начала работы не гонялся, и
  сравнить не с чем.
- Одно расхождение снято замером состава правки, а не пересъёмкой: волна клала `position:
relative` на каждую кнопку кита правилом блока. Позиция переехала в директиву — ставится тому
  элементу, который нажали, и только если он до сих пор `static`. Боковая панель после этого
  сошлась, расхождений стало 17 из 18.
- Восемь эталонов переписки с шириной `--w1441` прогон называет сиротами. Это следствие падения
  их набора, а не отдельный дефект: упавшая история второго кадра не снимает. Проверится, когда
  переписка сойдётся.
- Найдено мимо задачи: **признака уровня атомарного дизайна в дереве не записано нигде.**
  Паттерн `rt-tools-storybook-story` шлёт за уровнем нового компонента «в раскладку в правиле
  `rt-tools-storybook`», а правило называет только четыре уровня и порядок разделов — по какому
  признаку компонент атом, а не молекула, не сказано ни там, ни в спеке, ни в решении. Уровни
  при этом уже разошлись: `Toast` лежит в `Atoms/Feedback`, а его `Toaster` — в `Molecules` без
  группы; `Skeleton` в `Atoms/Feedback`, а `SkeletonWrapper` — в `Molecules` без группы. Статья
  правила о том, что дочерняя часть стоит в группе своего родителя, нарушена обеими парами.
  Замер: атомов 21, молекул 39, организмов 23.
- Замер: волны во втором ките нет ни в одном файле; `--rt-btn-shadow` объявлено трижды и трижды
  `none`; у кнопки-значка нет и механизма тени. Записан в карточку RT-1876.

## Handover of the session

Put together by a hook before the compaction of the context (manual).

**Working tree:** /Users/sviatoslavkhutornoy/WebstormProjects/rt-tools
**Branch:** RT-1876-material-non-token-traits

### Where we stand at the minute of the compaction

- **State:** `этапы-кончились`
- **Stage:** все четыре закрыты
- **Next step:** сделать RT-1891 — без неё пуш отбит гардом снимков у любой ветки дерева, и
- **PR:** ещё не открыт

The progress in full — `docs/tasks/RT-1876-material-non-token-traits/progress.md`; the plan lies next to it.

### Uncommitted

```
none
```

### Commits over the main branch

```
2abe0ab50 docs(rt:ui-kit-v2): ход RT-1876 — нижняя работа влита, дальше RT-1891
62315d6b3 Merge branch 'RT-1871-material-preset-tokens' into RT-1876-material-non-token-traits
530a2dc9f fix(rt:agent-kit): сверка адресов зелёная — три машинных файла названы принятыми
63f5d5070 docs(rt:ui-kit-v2): правило оформления знает про второй слой назначений
cba2806fe docs(rt:ui-kit-v2): ход RT-1871 — цена этапа 4 замерена по каждому пункту договорённости
49ea7339b docs(rt:ui-kit-v2): ход RT-1876 — набор проверок зелёный, заявка ждёт нижней ветки
912eaa4f2 docs(rt:ui-kit-v2): ход RT-1876 — все четыре этапа закрыты
8948bed26 fix(rt:ui-kit-v2): матрица наборов не режется краем половины, оба эталона сняты
cf51af73e docs(rt:ui-kit-v2): обзоры обеих кнопок называют материальный набор и его историю
7950d2a66 Merge branch 'RT-1871-material-preset-tokens' into RT-1876-material-non-token-traits
656328846 Merge branch 'main' into RT-1871-material-preset-tokens
d257a93ae docs(rt:ui-kit-v2): ход RT-1876 — пара наборов заведена, впереди осмотр и эталоны
1763a8033 feat(rt:ui-kit-v2): свой набор оформления и материальный стоят рядом в витрине
e309413c3 docs(rt:ui-kit-v2): таблица осей кнопки-значка сходится с компонентом
916d2866d docs(rt:ui-kit-v2): ход RT-1876 — краснота снимков заведена карточкой RT-1891
e0b2090f2 Merge branch 'RT-1871-material-preset-tokens' into RT-1876-material-non-token-traits
e25ed0b22 style(rt:ui-kit-v2): линтер стилей на собранном файле набора зелёный
9dc1863e1 feat(rt:ui-kit-v2): у кнопки-значка ровный ряд форм и своё свойство тени
fff16d994 fix(rt:ui-kit-v2): признак уровня записан в правило, обе разъехавшиеся пары выправлены
83e7bef15 docs(rt:ui-kit-v2): ход RT-1876 — признак уровня атомарного дизайна нигде не записан
```

Written by a hook before the compaction of the context. Everything standing here is checked
against the tree: a handover retells what was written and describes the minute it was put together.
