# Ход работы

## Где стоим

- **Этап:** все семь закрыты; главная ветка влита, договорённость влита в спек домена
- **Сделано:** договорённость `proposed/reuse-signals/`, наборы признаков по пакетам rt-tools,
  общий читатель наборов, гард читает те же наборы и снова отбивает, набор из двенадцати
  сценариев на гард, дерево объявило свои наборы и свои признаки, тексты правила и его спутника
  говорят о наборах, договорённость влита. Плюс хвосты RT-431 и раскладка на v0.7.0 — коммиты
  `adf784b3`, `c717c50e`, `0033bc6f`, `ad93a464`, `33867af7`, `8b52b577`, `c4bf5384`, `655d47c9`
- **Следующий шаг:** разбор отчёта владельцем; правки по замечаниям, затем разбор папки задачи
  последним коммитом — до слияния
- **Незакоммиченное:** нет
- **Ждём владельца:** разбора отчёта
- **Отчёт:** #504, открыт 14 августа, ждёт разбора · замечаний пока нет · не сделано: разбор
  папки задачи

## Решения по ходу

- **Хвосты RT-431 и раскладка на v0.7.0 ведутся этой же веткой** — решение владельца. Правила
  говорят «одна работа — одна задача»; сказано вести в 476, и это названо в теле отчёта.
  Затронутый этап замысла: до этапов.
- **Пакет выпущен как 0.7.0, а не 0.6.1** — решение владельца после того, как названо
  расхождение: восемь записей о снятых ресурсах и спека уже говорили «снят в v0.7.0», и правка
  ломающая.
- **Это дерево объявляет только набор фреймворка.** Оно киты пишет, а не потребляет: объявив их
  наборы, оно получило ложные срабатывания на собственном ките — совет из второго кита на файл
  первого. Затронутый этап замысла: 6.
- **Признак «имя принадлежит пакету» машине не даётся.** Заведён `Q-20` договорённости:
  признак читает человек, как и признак приёма в законе. Отсюда `SC-AK-146` остаётся непокрытым.
- **«Пакет называет только свои имена» правилом спека не стало.** При вливании оказалось, что
  исполнять его в коде нечем, а правило без привязки в спек домена не въезжает. Осталось
  вопросом `Q-20`, при нём — непокрытый `SC-AK-146`. Затронутый этап замысла: 7.

## Заходы

### 2026-08-14

- Промах захода: три хода подряд кончились вопросами и объяснением при готовом ответе в разведке.
  Разбор — `docs/postmortems/2026-08-14-questions-instead-of-work.md`, предложение пакету —
  `.claude/rt-kit/proposals/2026-08-14-questions-without-work.md`.
- Гард единообразия проверен на живой правке дважды: с настоящим профилем дерева он молчал на
  `@Input()`, с профилем в четыре поля отбивал. Причины две — профили печатают два поля, а гард
  читает четыре, и таб в `IFS` схлопывает пустое третье поле, унося совет в образец отмены.
- Набора сценариев у гарда не было вовсе — отсюда и жил дефект. Заведён
  `projects/agent-kit/tests/reuse-guard.test.sh`, двенадцать сценариев. Проверен на прежнем
  поведении: с отключённым чтением наборов шесть из двенадцати краснеют.
- Наборы признаков: шесть файлов — `angular`, `core`, `store`, `utils`, `ui-kit`, `ui-kit-v2`.
  Имена в них взяты из селекторов самих китов командой, а не по памяти.
- Ложные срабатывания, ради которых задача заводилась, ушли: `aside-base` и `value-accessor`
  из пакета сняты — эти основы принадлежат дереву-потребителю и объявляются им.
- Снимок накопленного долга переснят под новые признаки: 198 записей, новых расхождений нет.
- Разложенные наборы вынесены из-под форматтера: первой строкой у них шапка раскладки, а в JSON
  комментария нет — общий проход `prettier` спотыкался и отбивал коммит.
- Числа: спек пакета 229, наборов сценариев на гарды — на один больше, чем было.
- Непрояснённое: сверка спеков даёт 186 расхождений против 168 днём раньше. Прирост не от
  несохранённых правок — с отложенными он тот же. Проверить со следующим заходом.

### 2026-08-14, заход второй

- Прирост расхождений разобран: вливание договорённости о каскаде дописало восемнадцать строк
  привязки не в таблицу, а с отступом в раздел «Чем это проверяется». Сверка их не видела и
  считала все восемнадцать правил непривязанными. Строки перенесены в таблицу: 187 стало 165.
- Тексты этапа 7: правило `reuse-first` больше не перечисляет признаки списком, спутник получил
  порядок заведения своего признака, правило `agent-kit` — строку о ключах `reuse.bundles` и
  `reuse.signals`. Компаньон правила приведён к дереву: два разъехавшихся ключа привязки, два
  новых утверждения с привязками, раздел о гарде переписан — он читал «четыре образца в профиле»,
  а там сегодня один.
- Разложенные наборы в дереве лежали переформатированными: их сняли форматтером до того, как
  вывели из-под него. `sync` вернул пакетную редакцию, `sync --check` зелёный.
- Главная ветка влита: восемь конфликтов, все в разложенных файлах. Разрешены не руками —
  сборкой пакета и раскладкой поверх; исходники ресурсов слились без конфликтов.
- Договорённость влита в спек домена: семь правил с привязками, шесть терминов, четыре кода
  отказа, три решения, `Q-18`…`Q-20` и сценарии `SC-AK-142`…`SC-AK-150` с прежними номерами.
- Доэтапное, не этой работы, заведено задачами: `RT-502` — две договорённости взяли одни и те же
  номера `SC-AK-119`…`134` (тридцать две строки в сверке спеков, приехали мержем главной ветки);
  `RT-503` — спек домена `agent-kit` перерос предел длины вдвое.
- Проверки: сценарии пакета — все наборы зелёные, сверка путей — расхождений нет, сверка
  единообразия — новых расхождений нет, сверка спеков — 197, из них 32 чужие (`RT-502`) и 165
  накопленный долг привязок этого дерева.
