# План

**Task:** RT-2356 · **Branch:** RT-2356-data-table-first-kit-parity-2
**Spec:** `docs/specs/ui-kit-v2/table-material-theme/spec.md`
**Behaviour:** changes

## След задачи

| Что     | Где                                                                                     |
| ------- | --------------------------------------------------------------------------------------- |
| Спеки   | `docs/specs/ui-kit-v2/table-material-theme/`, `docs/specs/ui-kit-v2/icon-on-demand/`    |
| Правила | `.claude/skills/styling-bem/`, `.claude/skills/ui-component-tests/`                     |
| Код     | `projects/ui-kit-v2/src/lib/components/` — data-table, data-list, menu, icon, aside     |
| Значки  | `projects/ui-kit-v2/src/assets/icons/`, `projects/ui-kit-v2/src/assets/icons-material/` |
| Тексты  | `projects/ui-kit-v2/README.md`, обзоры и истории затронутых компонентов                 |

## Что считается сделанным

- Ширина колонки стоит на хосте обычной ячейки, у `th` и `td` своей ширины нет.
- Поля фильтров без заданного вида обведённые, а поиск списка остаётся в своём виде.
- Строка фильтров по умолчанию смешивает фон шапки с фоном поверхности пополам.
- `glyph="person_off"` у пункта меню рисует значок `user-off` в обоих наборах.
- Меню переносит фокус на первый пункт, ведётся стрелками и возвращает фокус на кнопку.
- README называет вторую запись публикации — `icons-material`.
- Кнопка «назад» в шапке панели круглая.
- Все проверки второго кита зелёные, каждый переснятый кадр просмотрен глазами.

## Этапы

### 1. Таблица: ширина, вид фильтров, фон строки фильтров

- **Steps:**
    1. Перенести ширину колонки с `th` и `td` на хост обычной ячейки
    2. Вернуть полям фильтров вид таблицы `outline` по умолчанию
    3. Считать фон строки фильтров от фона шапки
    4. Поправить тесты и сценарий SC-UKV-359
- **Readiness sign:** тесты таблицы и списка зелёные.
- **Verified by:** `pnpm exec nx test @rt-tools/ui-kit-v2` — в выводе `Tests:` без `failed`

### 2. Меню: значок и клавиатура

- **Steps:**
    1. Добавить значок `user-off` в оба набора и пару `person_off` в таблицу соответствий
    2. Вести меню стрелками через `FocusKeyManager`
    3. Покрыть клавиатуру меню тестами
- **Readiness sign:** проверка таблицы значков и тесты меню зелёные.
- **Verified by:** `node tools/check-icon-map.mjs` — выход 0

### 3. Панель и README

- **Steps:**
    1. Сделать кнопку «назад» в шапке панели круглой
    2. Дописать в README запись публикации `icons-material`
- **Readiness sign:** README называет обе записи, кнопка круглая в витрине.
- **Verified by:** `pnpm run check:all` — выход 0

### 4. Кадры и проверки

- **Steps:**
    1. Переснять сдвинувшиеся кадры и просмотреть каждый глазами
    2. Прогнать ворота отправки
- **Readiness sign:** визуальные ворота второго кита зелёные.
- **Verified by:** `node tools/visual-gate.mjs ui-kit-v2` — все кадры совпали

## Что эта работа не делает

- Шрифт Material в кит не везёт — договорённость о материальном наборе это запрещает.
- Первый кит не правит.
- Селект в строке фильтров отдельно не трогает: он узкий из-за ширины колонки, пункт 1.
