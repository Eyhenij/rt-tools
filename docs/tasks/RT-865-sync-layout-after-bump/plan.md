<!-- rt-kit v0.8.3 · samples/tasks/_template/plan.md · c862bbaf5407 · правится надстройкой, не здесь -->

# Замысел

**Задача:** RT-865 · **Ветка:** RT-865-sync-layout-after-bump
**Поведение:** не меняется — раскладка догоняет уже опубликованную редакцию пакета: ни одного
утверждения правил эта работа не правит и ни одной строки кода приложения не трогает.

После написания этот файл не правится. Пересмотр этапа идёт в `progress.md` решением по ходу.

## След задачи

| Что      | Где                                                     |
| -------- | ------------------------------------------------------- |
| Законы   | `docs/constitution/`                                    |
| Правила  | `.claude/skills/`                                       |
| Код      | нет                                                     |
| Проверка | `pnpm run agent-kit:check`                              |

## Что считается сделанным

- Сверка раскладки отвечает зелёным: разложенное сходится с опубликованной редакцией пакета.
- Ни одно утверждение не изменилось по существу — правка только в шапках раскладки.

## Этапы

### 1. Дерево разложено на новой редакции

- **Что делается:** `agent-kit sync` на собранном пакете; правка читается диффом — в ней не
  должно быть ничего, кроме шапок.
- **Признак готовности:** `sync --check` сходится.
- **Чем проверяется:** `pnpm run agent-kit:sync`, `pnpm run agent-kit:check`.

## Чего эта работа не делает

- **Не правит ни одного утверждения.** Всё, что меняется по существу, едет своей задачей.
- **Не чинит причину.** Почему подъём версии не тянет за собой раскладку — вопрос к рабочему
  процессу публикации, и он заводится отдельно.
