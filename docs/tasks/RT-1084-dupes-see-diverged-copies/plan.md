# Замысел

**Задача:** RT-1084 · **Ветка:** RT-1084-dupes-see-diverged-copies
**Поведение:** не меняется — работа правит проверки пакета правил, кода приложения не трогает;
так сказал владелец, поручив брать задачи с приёмника по одной

После написания этот файл не правится. Пересмотр этапа идёт в `progress.md` решением по ходу.

## След задачи

| Что     | Где                                 |
| ------- | ----------------------------------- |
| Спеки   | `docs/specs/agent-kit/checks/`      |
| Законы  | не задеваются                       |
| Правила | не задеваются                       |
| Код     | `projects/agent-kit/assets/checks/` |

## Что считается сделанным

- Проверка повторов называет повтором и ту пару таблиц, что разошлась на несколько пар: признак
  меряется долей совпавших, а не полным равенством.
- Строка находки называет число совпавших пар и число расхождений.
- Сценарии закрывают точную копию, разошедшуюся и две разные таблицы; спек проверок их называет.
- Набор обвязки зелёный, раскладка сходится.

## Этапы

### 1. Доля совпавших пар

- **Что делается:** сравнение таблиц по доле совпавших пар с высоким порогом
- **Признак готовности:** разошедшаяся копия называется, две разные таблицы — нет
- **Чем проверяется:** `bash projects/agent-kit/tests/checks-dupes.test.sh` — сценарии зелёные

### 2. Сценарии и спек

- **Что делается:** три сценария и их привязка в спеке проверок
- **Признак готовности:** сценарии стоят, сверка спеков молчит
- **Чем проверяется:** `npm run check:specs` — молчание сверки

### 3. Закрытие

- **Что делается:** набор обвязки, разбор папки задачи последним коммитом
- **Признак готовности:** папка разобрана, набор зелёный; заявка ждёт утра — владелец просил
  ветки наружу не отправлять
- **Чем проверяется:** `bash projects/agent-kit/tests/run.sh` — строка о зелёных наборах

## Чего эта работа не делает

- Не считает долю от меньшей таблицы: тогда таблица из двух пар, целиком лежащая внутри таблицы
  из двадцати, читалась бы полной копией.
