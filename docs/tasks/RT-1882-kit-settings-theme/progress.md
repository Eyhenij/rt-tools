# Progress

## Where we stand

- **State:** `этап-идёт`
- **Stage:** 4 из 5 — Витрина и кадры
- **Done:** разбор, договорённость на 20 сценариев, замысел из пяти этапов и весь первый этап:
  настройки кита объявлены, умолчания доходят до кнопки и шторы; и весь второй — у темы три
  состояния, выбор и вид разведены, слежка за машиной живая; и весь третий — признак темы
  отвечает на любом узле, метка написана, костыль витрины на 42 строки снят. Тестов 1629 при
  130 наборах против опорных 1606 при 127.
- **Next step:** завести истории семьи по договору витрины
- **Uncommitted:** нет
- **Waiting for the owner:** нет
- **PR:** ещё не открыт

## Steps

- `[x]` сделано · `[>]` идёт сейчас · `[ ]` не начато

- [x] 1.1 Объявить набор настроек и токен к нему по образцу раздачи подписей
- [x] 1.2 Написать разрешение от частного к общему в одном месте, а не в каждом узле
- [x] 1.3 Довести умолчание до кнопки через значение, с которого стартует вход
- [x] 1.4 Довести умолчание до шторы: закрытие по клавише
- [x] 1.5 Вывести раздачу наружу через точку входа пакета
- [x] 1.6 Написать тесты на шесть сценариев настроек
- [x] 2.1 Расширить набор значений темы третьим и взять умолчание из настроек
- [x] 2.2 Разрешать третье состояние в один из двух видов, не ставя третьего признака
- [x] 2.3 Хранить выбор, а не вид, в который он разрешился
- [x] 2.4 Игнорировать сохранённое значение, которого кит не знает
- [x] 2.5 Подписаться на ответ браузера о тёмном виде и снять подписку с владельцем
- [x] 2.6 Оставить прорисовку без окна немой
- [x] 2.7 Написать тесты на семь сценариев темы
- [x] 3.1 Научить порождающую программу выпускать оба набора и для узла
- [x] 3.2 Пересобрать файл свойств и убедиться, что сборка сходится сама с собой
- [x] 3.3 Написать метку, которая ставит признак темы на узел и снимает пустым значением
- [x] 3.4 Вывести метку наружу через точку входа пакета
- [x] 3.5 Снять костыль витрины, вешающий примеси руками
- [x] 3.6 Написать тесты на шесть сценариев местного куска
- [>] 4.1 Завести истории семьи по договору витрины: площадка и по истории на ось
- [ ] 4.2 Добавить пару тем и пару наборов оформления, где договор витрины требует
- [ ] 4.3 Перезапустить витрину второго кита, поднятую на прошлой ветке
- [ ] 4.4 Снять кадры, посмотреть на них глазом и только потом положить под гит
- [ ] 4.5 Прогнать обход историй
- [ ] 5.1 Влить договорённость в спеку кита подобластью и вписать строку в таблицу
- [ ] 5.2 Связать каждое обещание с местом в коде в файле привязок
- [ ] 5.3 Пройти по следу задачи и поправить устаревшее
- [ ] 5.4 Прогнать набор проверок перед отправкой целиком

## Decisions along the way

- **Ветка отведена от местной ветки эпика, не от отправленной** — во влитой главной ветка эпика
  на машине уже была, а отправить её нельзя: ворота перед отправкой держат четыре просроченные
  записи о прошлом, а своих правок ветка эпика не несёт и снимать их ей нечем. Правило прямо
  разрешает: ещё не отправленное слияние отвечает основанием.
- **RT-1881 отложена владельцем** — загрузчик изображений первого кита держится на стороннем
  пакете обрезки, и второму киту второго соседа заводить не стали.
- **Сборочная половина набора цвета не переносится** — у первого кита она примесь, которую он
  пишет руками; второй кит свои свойства оформления не пишет, а порождает из файла-источника, и
  сборочный набор там — умение порождающей программы, то есть отдельная работа.
- **Умолчание входа считается из настроек прямо при объявлении** — так публичный вид входов
  кнопки не меняется: ни тип, ни имя. Отвергнуто: убрать у входов свои значения и завести рядом
  выводимых соседей, как в первом ките, — это правка уже живущей семьи кнопки вместе с её
  тестами и описанием. Цена принятой дороги названа: настройки читаются один раз при создании
  узла, подмена на ходу на нарисованное не действует.
- **Пары кадров для сличения не будет** — у первого кита витрина тему и местный кусок не
  показывала ни одной историей, поиск шёл по всем именам службы, метки и ручки. Снимается кадр
  второго кита, и в договорённости это объявлено, а не умолчано.

## Findings

- **Набор цвета по коду не переносится — это решено трижды и до этой работы.** Действующая
  договорённость об оформлении кита держит в разделе о границах прямой отказ: настройка
  оформления кодом, то есть раздача и объект настроек над ролями в стилях, отвергнута, кит
  задаётся своими свойствами точечной правкой. Договорённость о наборе оформления, идущая в этом
  же эпике, подтверждает его во второй раз и прямо отвергает раздачу, как у первого кита. Третья
  запись — два прошлых переноса этого же эпика: у значка-бейджа произвольный цвет фона не
  перенесён, потому что палитра кита закрытая и берётся токеном, а приложению со своим цветом
  отвечает переопределение свойства блока; у полосы действий свой цвет в настройку не передать
  по той же причине. Настроек кита и темы решение не задевает: там нет ни одной роли цвета — это
  умолчания поведения и старта. Владельцу вопрос не задан: ответ лежал в дереве, и ворота
  разбора отбили попытку спросить уже отвеченное.
- Витрина второго кита скоупит тёмную половину костылём в своём глобальном стиле —
  `projects/ui-kit-v2/.storybook/storybook.scss` вешает примеси светлого и тёмного набора на
  свои же классы, потому что кит отдаёт тёмные свойства только корню страницы. Местный кусок
  темы этот костыль снимает: это не перенесённая странность первого кита, а своя нужда.
- Переключатель темы второго кита менять не придётся: он читает только признак тёмной и зовёт
  переключение. Пока третье состояние разрешается в признак тёмной само, его код остаётся как
  есть.
- Живая слежка за машиной — улучшение против образца, а не копия: первый кит спрашивает машину
  по требованию, подписки у него нет, и смена настройки машины до открытой страницы у него не
  доходит.
- Номера сценариев взяты с 330: в этой ветке последний занятый — 235, но две неслитые соседние
  ветки уже заняли до 323, и они уедут в ветку эпика раньше этой работы.

## Sessions

### 23 сентября 2026

- Снята мерка: первый кит — 5 файлов и 410 строк кода плюс 160 строк тестов; второй кит уже
  несёт половину — службу темы на 54 строки, раздачу подписей как образец формы и те же шесть
  ролей цвета под своими именами.
- Четыре дыры названы, одна из них закрыта решением: набор цвета не переносится.
- Договорённость написана и переписана: 300 строк, 20 сценариев, проверка спек и проверка
  адресов зелёные на 731 документе.
- Замысел из пяти этапов написан по опоре 127 наборов и 1606 тестов.
