<!-- rt-kit v0.8.2 · samples/tasks/_template/grill.md · bbcf7332ef07 · правится надстройкой, не здесь -->

# Разбор просьбы

## Просьба владельца

Просьбы не было: дефект замечен по ходу соседней работы и заведён задачей RT-726 тем же
движением, которым замечен. Владельцу называется вместе с отчётом о той работе.

Замер, которым дефект найден, — `npm run check:specs` на ветке RT-716 сразу после вливания
главной ветки: шесть строк, ни одна из которых не принадлежит той ветке.

## Что уже есть в дереве

- **`docs/specs/agent-kit/proposed/feedback-loop/`** лежит в главной ветке двумя файлами —
  `spec.md` и `scenarios.md`.
- **Сценарии этой договорённости уже влиты** в спек домена: `SC-AK-156`, `SC-AK-157`,
  `SC-AK-158` стоят в `docs/specs/agent-kit/scenarios.md` с пометкой о покрытии.
- **Тесты на них есть** — в наборе пакета правил, при том что в самой директории те же три
  сценария помечены «Не покрыто».
- **`docs/specs/README.md`** строки о `feedback-loop` не держит: раздел о невыкаченном о ней
  молчит, то есть половина вливания сделана.

## Что уже сказано в правилах

- **Паттерн закрытия работы** называет порядок вливания целиком, и последним пунктом в нём стоит
  удаление директории `proposed/<фича>/` вместе со строкой в указателе спеков.
- **Правило работы** говорит, что договорённость вливается последним коммитом PR и в главной
  ветке директория `proposed/` не появляется вовсе.
- **Правило спеков** говорит, что спек описывает установившееся, а единственное место, где он
  говорит о будущем, — `proposed/<фича>/`.

Отсюда: спрашивать нечего — правка описана правилами, и задача только в том, чтобы её доделать.

## Вопросы и ответы

Вопросов владельцу нет: ответ на всё, что решает эта работа, записан в правилах.

## Решения

- **Директория удаляется целиком, а не правится** — её содержимое уже стоит в спеке домена, и
  вторая копия отличается от первой только пометкой о покрытии, которая врёт.
- **Номера сценариев не трогаются** — они ключ связи с заголовками тестов.
