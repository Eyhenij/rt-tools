# Замысел

**Задача:** RT-1543 · **Ветка:** RT-1543-cargo-release-version-plain
**Поведение:** не меняется — правятся тексты правил этого дерева. Кода приложения работа не
задевает.

После написания этот файл не правится. Пересмотр этапа идёт в `progress.md` решением по ходу.

## След задачи

| Что      | Где                                 |
| -------- | ----------------------------------- |
| Правила  | `.claude/skills/cargo-triage/`      |
| Паттерны | `.claude/skills/cargo-triage-mark/` |

## Что считается сделанным

- Правило говорит, что версия выпуска называется одним номером, без имени пакета.
- Оба примера вызова в паттерне несут номер, а не `rt-agent-kit@<номер>`.
- Данные приёма приведены к этой форме — сделано до заведения задачи, сверено чтением.

## Этапы

### 1. Статья правила о форме версии

- **Что делается:** в правило `cargo-triage` дописывается статья о том, что версия — один номер.
- **Признак готовности:** статья стоит в правиле, предел веса выдержан.
- **Чем проверяется:** `node tools/check-file-size.mjs` — файл правила не назван.

### 2. Примеры в паттерне

- **Что делается:** обе строки вызова в `cargo-triage-mark` переписываются на номер, рядом
  говорится, почему форма именно такая.
- **Признак готовности:** `rt-agent-kit@` в паттерне не осталось.
- **Чем проверяется:** `grep -c 'rt-agent-kit@' .claude/skills/cargo-triage-mark/SKILL.md` — ноль.

## Чего эта работа не делает

- Не заводит проверку формы на стороне приёма: форму версии каждое дерево выбирает своё, и
  запрет отбивал бы записи соседа.
