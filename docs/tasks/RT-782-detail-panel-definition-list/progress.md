<!-- rt-kit v0.8.3 · samples/tasks/_template/progress.md · baf326afb38f · правится надстройкой, не здесь -->

# Ход работы

## Где стоим

Перезаписывается каждым заходом, а не дописывается.

- **Этап:** 1 и 2 сделаны; следующий — 3, тексты домена и вливание договорённости
- **Сделано:** договорённость написана целиком — спек, пять сценариев `SC-MB-137`…`SC-MB-141`,
  четыре привязки; замысел написан. Три панели переведены на `rt-aside-section`,
  `rt-detail-list` и `rt-detail-row`: своей разметки списка определений и своего `<h2>` в них
  не осталось, стили `fields`, `field-name`, `field-value` и `section` сняты из общего слоя.
  Признак свойства переехал с тега значения на строку кита, и на новые признаки переведены
  спеки трёх панелей, трёх асайдов и сквозного набора
- **Что нашла разведка в ките:** заводить ничего не надо, всё готовое уже есть —
  `rt-aside-section` (раздел с заголовком), `rt-detail-list` (список свойств) и
  `rt-detail-row [label] [loading]` (строка со скелетоном и своими признаками
  `detail-row-label`, `detail-row-value`)
- **Сделано этапом 2:** вид принимает вход `reading` и пустую запись, признак чтения приходит
  от асайда и уходит в каждую строку кита; при чтении показаны названия и скелетоны, а раздел
  текста скрыт — прежний текст принадлежит прежней записи. Признаки заведены на панели
  (`<раздел>-details-panel`) и её шапке (`<раздел>-details-header`); шесть сквозных тестов
  закрыли `SC-MB-139` и `SC-MB-140` на всех трёх разделах
- **Чем подтверждено:** `nx affected -t lint typecheck test` зелёный на 11 проектах,
  `nx build message-bus-admin` зелёная, `nx run message-bus-admin-e2e:e2e` — 38 тестов
  пройдено, `check:specs` говорит «пора вливать: сценариев 5», `check:docs` и `check:styles`
  без расхождений. Замер на стенде при задержанном ответе: 4 названия на месте, 5 скелетонов
  (четыре строки и заголовок шапки), скелетон строки 180×16, признаки панели и шапки по одному
- **Следующий шаг:** этап 3 — договорённость вливается в спек поддомена чтения, паттерн
  `entity-aside` правится под то, что в ките есть, тело задачи RT-782 приводится к факту
- **Незакоммиченное:** ничего сверх этапа 3
- **Ждём владельца:** ничего
- **PR:** ещё не открыт

## Решения по ходу

- **17 августа 2026. Работа идёт по тому расхождению, которое есть, а не по тексту задачи.**
  Решение владельца при разборе: список определений уже стоит, доделываются разделы, скелетоны
  и признаки. Тело задачи RT-782 переписывается под факт этапом 3 — оставленное как есть, оно
  через месяц прочитается как невыполненное. Затронутый этап замысла: нет, объём назван
  замыслом с самого начала.
- **17 августа 2026. Имена элементов берутся у паттерна дерева, а не у образца.** Владельцу не
  задавалось: ответ стоит в паттерне `entity-aside`. У образца свои имена (`list`, `row`,
  `term`, `value`) — это имена того дерева, а не общее требование. Затронутый этап замысла: нет.

- **17 августа 2026. Вместе с готовым панель приняла и его раскладку.** Готовая строка кита
  ставит название над значением, а своя разметка ставила его слева; горизонтального варианта в
  ките нет ни у неё, ни у соседней `rt-info-item`. Владельцу не задавалось: ответ стоит в
  законе `reuse-first` — отступление от общего вида здесь и есть своя разметка, а раскладка
  пар задана один раз в ките. Две описательные строки договорённости, писанные с прежнего
  вида, поправлены под факт. Затронутый этап замысла: нет.
- **17 августа 2026. Панель сняла свои отступы от края.** Зона содержимого шторки уже держит
  и отступ, и шаг между разделами, а панель объявляла их второй раз: до правки поля были
  двойными. Взят шаг шторки. Затронутый этап замысла: нет.
- **17 августа 2026. Сквозной набор переведён на новые признаки этапом 1, а не 2.** Признак
  свойства переехал на строку кита той же правкой, что и разметка, и четыре места набора
  покраснели бы сразу; замысел относил это ко второму этапу. Затронутый этап замысла: 2 — из
  него уходит перевод набора, остаётся то, ради чего он там стоял: признаки на панели и её
  шапке и скелетоны чтения.

## Заходы

### 17 августа 2026

- Заход закрыл RT-781 целиком: договорённость влита в спек поддомена чтения, правило `lists`,
  его спутник и паттерн `admin-lists-screen` дополнены, папка задачи разобрана записью архива,
  PR #817 влит владельцем.
- Найдено и починено по дороге, не этой работой: файлы `tools/spec-*.mjs` кладёт раскладка, а
  форматтер их не пропускал — разбивал длинный импорт на строки прямо в коммите, сумма тела
  переставала сходиться, `sync --check` называл файл правленым руками, и гейт пуша краснел на
  любой ветке. Одна строка `tools/spec-*.mjs` в `.prettierignore`.
- Найдено и не заведено задачей: `node tools/check-file-size.mjs` падает необработанным
  `ENOENT`, когда в дереве есть удалённый, но не закоммиченный судимый файл. Ждёт слова
  владельца.
- Учётная запись `rt-tools-agent` заблокирована GitHub: её PR не виден владельцу (`404`), а
  GraphQL ей закрыт целиком (`limit: 0`). PR #816 пришлось закрыть и открыть заново от записи
  владельца — #817. Пока запись не разблокируют, каждый следующий PR идёт тем же путём.
