<!-- rt-kit v0.8.3 · samples/tasks/_template/progress.md · baf326afb38f · правится надстройкой, не здесь -->

# Ход работы

## Где стоим

Перезаписывается каждым заходом, а не дописывается.

- **Этап:** ни один не начат; следующий — 1, панели переходят на готовое из кита
- **Сделано:** задача взята в работу, ветка заведена, папка задачи собрана, разбор просьбы
  записан, договорённость `docs/specs/message-bus/proposed/panel-sections/` написана целиком —
  спек, пять сценариев `SC-MB-137`…`SC-MB-141`, четыре привязки. Замысел написан
- **Сделано разведкой:** то, что просит тело задачи, уже выполнено — список определений стоит
  во всех трёх панелях, и признак `qa-dataid` есть на каждом значении. Расхождение с образцом
  осталось другое, и владелец решил доделать до образца
- **Что нашла разведка в ките:** заводить ничего не надо, всё готовое уже есть —
  `rt-aside-section` (раздел с заголовком), `rt-detail-list` (список свойств) и
  `rt-detail-row [label] [loading]` (строка со скелетоном и своими признаками
  `detail-row-label`, `detail-row-value`). Панели пишут это руками: `<dl>`, `<dt>`, `<dd>` и
  свой `<h2 rtElem="section">`
- **Следующий шаг:** этап 1 — перевести три панели на готовое из кита, снять свою разметку
  вместе с её стилями, поправить спеки трёх панелей и трёх асайдов
- **Незакоммиченное:** папка задачи, договорённость
- **Ждём владельца:** ничего
- **PR:** ещё не открыт

## Решения по ходу

- **17 августа 2026. Работа идёт по тому расхождению, которое есть, а не по тексту задачи.**
  Решение владельца при разборе: список определений уже стоит, доделываются разделы, скелетоны
  и признаки. Тело задачи RT-782 переписывается под факт этапом 3 — оставленное как есть, оно
  через месяц прочитается как невыполненное. Затронутый этап замысла: нет, объём назван
  замыслом с самого начала.
- **17 августа 2026. Имена элементов берутся у паттерна дерева, а не у образца.** Владельцу не
  задавалось: ответ стоит в паттерне `entity-aside`. У образца свои имена (`list`, `row`,
  `term`, `value`) — это имена того дерева, а не общее требование. Затронутый этап замысла: нет.

## Заходы

### 17 августа 2026

- Заход закрыл RT-781 целиком: договорённость влита в спек поддомена чтения, правило `lists`,
  его спутник и паттерн `admin-lists-screen` дополнены, папка задачи разобрана записью архива,
  PR #817 влит владельцем.
- Найдено и починено по дороге, не этой работой: файлы `tools/spec-*.mjs` кладёт раскладка, а
  форматтер их не пропускал — разбивал длинный импорт на строки прямо в коммите, сумма тела
  переставала сходиться, `sync --check` называл файл правленым руками, и гейт пуша краснел на
  любой ветке. Одна строка `tools/spec-*.mjs` в `.prettierignore`.
- Найдено и не заведено задачей: `node tools/check-file-size.mjs` падает необработанным
  `ENOENT`, когда в дереве есть удалённый, но не закоммиченный судимый файл. Ждёт слова
  владельца.
- Учётная запись `rt-tools-agent` заблокирована GitHub: её PR не виден владельцу (`404`), а
  GraphQL ей закрыт целиком (`limit: 0`). PR #816 пришлось закрыть и открыть заново от записи
  владельца — #817. Пока запись не разблокируют, каждый следующий PR идёт тем же путём.
