# Progress

## Where we stand

- **State:** `этап-идёт`
- **Stage:** 3 of 3 — осмотр и пересъёмка
- **Done:** этап 2 закрыт: 19 показов обёрнуты парой, проверка пары находок не дала
- **Next step:** этап 3 — осмотреть кадры и переснять эталоны
- **Uncommitted:** нет
- **Waiting for the owner:** нет
- **PR:** ещё не открыт

## Decisions along the way

- **Пару получает показ одного экземпляра у затронутого семейства.** Род показа определяется его
  разметкой, а не именем: панель лежит в слое поверх страницы, экран собран из готовых
  компонентов. Affected stage of the plan: 1.

- **Показ одного экземпляра просит у пары ширину половины входом `fill`.** Половина ужимает
  содержимое по его собственной ширине, и показ, стоявший на ширину страницы, схлопывается: хост
  полосы листания намерил восемь точек и ушёл в свёрнутую форму. Входом, а не умолчанием: половина
  ужимает нарочно — голая кнопка во всю её ширину выглядела бы не так, как та же кнопка в соседней
  матрице. Affected stage of the plan: 2.

## Sessions

### 2026-09-13

- Ветка взята от RT-2074: работа стоит четвёртой в цепочке
- Обход витрины: показов с эталоном и без корня показа — 48. Показов одного экземпляра из них 21,
  панелей 15, целых экранов 6, прочего 6
- Счёт по вычисленным значениям узлов кита: набор меняет узлы у 19 показов одного экземпляра из 21. Не меняет у строки подробностей и колокольчика — их семейства и в счёте RT-2057 стояли среди
  незатронутых
- 19 показов обёрнуты парой половин. Проверка пары своим скриптом: у каждого половин ровно две,
  обе нарисованы, половины не совпали пиксель в пиксель, наездов ячеек нет
- Замер ширины показа в половине: до входа `fill` восемнадцать показов из девятнадцати занимали
  от одного до шестидесяти пяти процентов ширины половины, после — сто. Всплывашка подтверждения
  держит свою ширину и входа не получила
