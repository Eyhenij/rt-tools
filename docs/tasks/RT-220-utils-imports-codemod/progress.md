# Ход работы

## Где стоим

- **Состояние:** `этапы-кончились`
- **Этап:** 3 из 3 — Инструмент назван деревом
- **Сделано:** инструмент, набор проб, команда дерева и записи в обоих журналах
- **Следующий шаг:** коммит и разбор папки задачи
- **Незакоммиченное:** инструмент, набор проб, команда, журналы, папка задачи
- **Ждём владельца:** нет
- **PR:** ещё не открыт — владелец велел работать локально и ветки не пушить

## Решения по ходу

- **Закрывается незакрытая половина карточки** — перенос лежит в главной ветке коммитом
  `549f393fc`, и повторять его нечем. Затронутый этап замысла: все.
- **Инструмент переименовывает перечисление положений по ходу перестановки** — карточка требует
  таблицы «имя → пакет», но `POSITION_ENUM` приехал в новый пакет под именем `EPosition`, и
  объявление, переписанное одним адресом, собиралось бы им и падало именем. Затронутый этап
  замысла: 1.
- **Имена поделённого объявления дописываются в стоящий импорт нового пакета** — второе
  объявление того же адреса собирается, но линтер потребителя считает его дублем, и правку
  инструмента пришлось бы доправлять руками. Затронутый этап замысла: 1.
- **Итог прогона печатается последней строкой** — читают его строкой снизу, а список ручного
  разбора длиннее его на каждый такой файл. Поймал набор проб: два случая читали не ту строку.
  Затронутый этап замысла: 1.

## Заходы

### 2026-08-31

- разведка: `core` держит всё переехавшее, `utils/package.json` без фреймворка, кит берёт
  переехавшее из `core` в 39 местах; инструмента переписывания импортов в `tools/` нет
- написан `tools/migrate-utils-imports.mjs`: таблица из 32 имён, обход каталога, три случая
  объявления, сухой прогон, отдельная строка про импорт пакета целиком
- заведён `projects/agent-kit/tests/migrate-utils-imports.test.sh` — 17 проверок на
  дереве-фикстуре; снятое деление смешанного объявления роняет 4 из них
- сухой прогон по `projects/` назвал 0 перестановок: дерево переставлено переносом, и инструмент
  это подтвердил
