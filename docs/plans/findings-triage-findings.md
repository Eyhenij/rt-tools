# The findings of the epic of the taking apart of the findings

## Taken apart on 29 August 2026 by the task RT-1573

Thirteen items of six tasks were reconciled with the tree. One is closed by a task of its own, one
is taken out into a task of its own, eleven were brought in by an edit of the resources here.

| Item                                                                   | Where it landed                                                                                                   |
| ---------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------- |
| an edit of a laid-out copy by an interpreter past the guard            | the task RT-1571: the guard of the place of an edit sees a write by the body and by the argument with the code    |
| a law without a single article named by a rule is not judged           | the task #1574: by a summary, not by a refusal — this finding is not closed by a text                             |
| an override of the gate map is checked by nothing                      | a section of the skill of the overrides: an edit of the map ends with a call of the gate on three paths           |
| the command of the creation of a task puts dead folders into the index | a pitfall of the rule of the delivery: the composition of the index is read before the commit, not after          |
| a sign of a stage is listed by reading, not run                        | a section of the pattern of the returning: the sign names the command and what in its output means "it coincided" |
| the "Then" of a scenario accepts two outcomes through an "or"          | a paragraph in the pattern of the spec of a domain: a fork is described by two scenarios with different "Given"   |
| an argument at an article goes stale together with the device          | a section of the pattern of the creation of a rule: the argument names the device it is true on                   |
| a requirement stands lower than the step it belongs to                 | there too: a requirement to a step stands in the section that step is led by                                      |
| an article about what is not checkable does not name the means         | there too: the article names which means exactly does not take it                                                 |
| a probe waiting for silence is green at an empty input too             | a section of the pattern of the single probes: it goes in a pair with a probe waiting for a refusal               |
| a probe on a mine that does not go off is green without a fixing       | there too: it is checked by both sides — the former state is returned and run                                     |
| a finding closed by another work looks open                            | an article of the pattern of the taking apart: the line is marked by the number of that work                      |
| a gathered finding costs as much as the exploration of its composition | there too: an item is checked for liveness before it is taken into the work                                       |

Nothing unsorted is left by this epic.
