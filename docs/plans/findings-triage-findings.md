# Находки эпика разбора находок

## Разобрано 29 августа 2026 года задачей RT-1573

Тринадцать пунктов шести задач сведены с деревом. Один закрыт своей задачей, один вынесен своей,
одиннадцать внесены правкой ресурсов здесь.

| Пункт                                                     | Куда легло                                                                                   |
| --------------------------------------------------------- | -------------------------------------------------------------------------------------------- |
| правка разложенной копии интерпретатором мимо гарда       | задача RT-1571: гард места правки видит запись телом и доводом с кодом                       |
| закон без единой статьи, названной правилом, не судится   | задача #1574: сводкой, а не отказом — текстом эта находка не закрывается                     |
| надстройку карты гейта не проверяет ничто                 | раздел скила надстроек: правка карты кончается вызовом гейта на трёх путях                   |
| команда заведения задачи кладёт в индекс мёртвые папки    | ловушка правила поставки: состав индекса читается до коммита, а не после                     |
| признак этапа перечислен чтением, а не прогнан            | раздел паттерна возвращения: признак называет команду и то, что в её выводе значит «сошлось» |
| «Тогда» сценария принимает два исхода через «либо»        | абзац в паттерне спека домена: развилка описывается двумя сценариями с разным «Дано»         |
| довод при статье стареет вместе с устройством             | раздел паттерна заведения правила: довод называет устройство, на котором он верен            |
| требование стоит ниже шага, к которому относится          | там же: требование к шагу стоит в разделе, который этот шаг ведёт                            |
| статья о непроверяемом не называет средство               | там же: статья называет, какое именно средство её не берёт                                   |
| проба, ждущая молчания, зелена и при пустом входе         | раздел паттерна единичных проб: она идёт в паре с пробой, ждущей отказа                      |
| проба на мину, которая не срабатывает, зелена без починки | там же: проверяется обеими сторонами — прежнее состояние возвращается и прогоняется          |
| находка, закрытая другой работой, выглядит открытой       | статья паттерна разбора: строка помечается номером той работы                                |
| сборная находка стоит столько, сколько разведка состава   | там же: пункт проверяется на живость до того, как берётся в работу                           |

Неразобранного по этому эпику не осталось.
